# 처음 읽는 사람을 위한 순서 안내

이 문서는 **jun-bank 설계 문서를 처음부터 읽는 사람**을 위한 길잡이다.\
무엇을 어떤 순서로 읽고, 각 단계에서 무엇을 얻어야 하는지를 정한다.

> 이 안내를 따라 읽으면 **이 시스템이 무엇을 하는지, 왜 이렇게 설계됐는지, 구체적으로 어떻게 동작하는지, 그리고 어떻게 배포되는지**를 순서대로 이해할 수 있다.

문서 지도(무엇이 어디 있나)는 [`README.md`](README.md), 지금 진행 상황은 [`HANDOFF.md`](HANDOFF.md)가 정본이다.\
이 문서는 **읽는 경로**만 맡는다.

> **한 가지 먼저** — 이 문서들은 2026-09-16에 가독성 기준으로 다시 썼다.\
> 표에 결정·예외·근거가 뭉쳐 있던 것을 풀어, **읽을 것은 산문으로, 대조할 것은 표로** 갈랐다.\
> 그러니 표의 셀을 붙들고 씨름하지 말고, 각 절의 첫 문단과 소제목을 따라 읽으면 된다.

---

## 큰 그림 — 일곱 층으로 읽는다

아래 순서는 "이 시스템이 무엇인가"에서 시작해 점점 구체적인 "어떻게"로 내려간다.\
각 층은 앞 층을 전제하므로, 앞지르지 않는 것이 요점이다.

```
1. 지도 잡기        무엇이 어디 있고 지금 어디까지 왔나
2. 무엇을 만드나    제품 정의 → 업무 규칙
3. 도메인 이해      이벤트 → 경계 → 용어 → 애그리게이트 → 상태 머신
4. 왜 이 구조인가   아키텍처 기술서 → ADR → 품질 속성 → 뷰
5. 구체적 흐름      유스케이스 20건 + 외부 인터페이스 규격
6. 왜 그렇게 됐나   설계 변경 기록(무엇이 몇 번 깨졌나)
7. 어떻게 배포되나  인프라 여정 + 배포 이슈 기록
```

---

## 1. 지도 잡기

먼저 어디에 무엇이 있고, 지금 프로젝트가 어느 단계인지부터 잡는다.

- [`README.md`](README.md) — 무엇이 어디 있나(문서 지도) · 정본이 갈릴 때 누가 이기나.
- [`HANDOFF.md`](HANDOFF.md) — 지금 어디까지 왔고, **무엇을 건드리면 사고가 재발하나**.

두 문서가 갈리면 `HANDOFF`가 이긴다.\
여기서 오래 머물 필요는 없다 — 지도만 눈에 익히고 다음으로 간다.

---

## 2. 무엇을 만드나 — product

이 시스템이 무엇을 하는 물건인지, 그리고 그 규칙이 무엇인지를 정한다.

1. [`product/00-product-definition.md`](product/00-product-definition.md) — 카드+뱅킹 시스템이 무엇을 하는가(S1~S12 시나리오).
2. [`product/01-business-rules.md`](product/01-business-rules.md) — **업무 규칙 정본** `BR-01~61` + 불일치 유형(M) + 미확정 수치 표.

`BR-nn`은 이 프로젝트 전체가 참조하는 규칙 번호다.\
다른 문서가 `BR-04`라고 하면 그 정의는 항상 여기에 있다 — 지금 전부 외울 필요는 없고, 나중에 되짚을 곳이라는 것만 기억하면 된다.

---

## 3. 도메인 이해 — domain

규칙이 어떤 개념·경계·상태로 이루어지는지를 이해한다.\
순서가 중요하다 — 이벤트에서 시작해 경계를 긋고, 그 안을 채운다.

1. [`domain/event-storming.md`](domain/event-storming.md) — 무슨 일이 일어나는가(사건의 흐름). 각 스토리라인에 **흐름도**가 있다.
2. [`domain/context-map.md`](domain/context-map.md) — 그 사건들을 **여덟 컨텍스트(C1~C8)** 로 어떻게 가르고, 관계 `R1~R20`으로 어떻게 잇나. §2에 **컨텍스트 맵 그림**.
3. [`domain/glossary.md`](domain/glossary.md) — 용어 정본. 헷갈리면 언제든 돌아올 사전.
4. [`domain/aggregates/`](domain/aggregates/) — **애그리게이트 16종**. 각 문서에 **협력·소유 구조** 그림이 있다. [`README.md`](domain/aggregates/README.md)의 **애그리게이트 지도**부터 보면 전체가 잡힌다.
5. [`domain/state-machines/`](domain/state-machines/) — **상태 머신 11종**. 허용 전이보다 **금지 전이(§4)** 가 핵심이다 — "무엇을 못 하는가"가 안전을 만든다. [`README.md`](domain/state-machines/README.md)의 **상태 머신 지도**부터.

가장 밀도 높은 것은 `aggregates/authorization.md`와 `state-machines/authorization.md`(승인)다.\
승인이 모든 판단의 중심이므로, 여기를 이해하면 나머지가 쉬워진다.

마지막으로 [`domain/handoff-to-phase3.md`](domain/handoff-to-phase3.md) — Phase 2에서 풀지 못하고 다음으로 넘긴 것(열린 의문 42건)을 훑는다.

---

## 4. 왜 이 구조인가 — architecture

같은 도메인을 왜 이런 배포·모듈·경계로 지었는지, 그 결정의 근거를 읽는다.

먼저 지도부터 선다.

- [`architecture/README.md`](architecture/README.md) — 아키텍처 기술서. **여기부터 읽으면 ADR로 가는 길이 보인다.**

그다음 **ADR 32건**([`architecture/adr/`](architecture/adr/))을 주제별로 읽는다.\
각 ADR은 「결정」 뒤에 **목표 구조 / 동작 흐름** 그림이 있다(순수 트레이드오프인 026·028 제외).

- **배포 단위의 진화** — ADR-001 → 004 → 010(최종). 한 서비스에서 코어+위성 셋으로 어떻게 갈렸나.
- **모듈·통신** — ADR-002(모듈 구조) · 005(Outbox 릴레이) · 007(읽기/쓰기 분리) · 009(파티션 키).
- **정산·배치·동시성** — ADR-006 → 011(커트오프) · 008(배치) · 012(동시성) · 013(타임아웃).
- **이벤트·스키마** — ADR-014(릴레이 운영) · 015(스키마 진화) · 021(이벤트 전송) · 022(DB 토폴로지).
- **인가·멀티테넌시** — ADR-016(인가) · 017(감사) · 018(격리 축).
- **채널·관측** — ADR-019(채널 진입) · 020(라우팅) · 023(관측).
- **배포 파이프라인** — ADR-024(릴리스) · 025(공통 라이브러리) · 026(데이터 접근) · 027(배포 오케스트레이션) · 028(언어 선택) · 029(내부 아키텍처) · 030(배포물 전달) · 031(블루-그린) · 032(배포 관측).

전부 한 번에 읽을 필요는 없다.\
번호 순서가 대체로 시간 순서이므로, **001부터 훑되 관심 주제는 깊이 읽는** 식이면 된다.

배경으로 [`architecture/quality-attributes.md`](architecture/quality-attributes.md)(채택 6·포기 6) · [`quality-scenarios.md`](architecture/quality-scenarios.md) · [`constraints.md`](architecture/constraints.md) · [`views/`](architecture/views/)(4종 뷰)를 곁들인다.

---

## 5. 구체적 흐름 — usecases · interfaces

결정이 실제 요청 하나에서 어떻게 도는지를 본다.

1. [`usecases/README.md`](usecases/README.md) — 유스케이스 대장 · 엔드포인트·이벤트 색인.
2. [`usecases/UC-01-authorize-payment.md`](usecases/UC-01-authorize-payment.md) — **여기부터 시작한다.** 카드 결제 승인 — 가장 중심 흐름이고, 다른 UC의 읽기 기준이 된다. 각 UC에 **동작 구조**와 **흐름도** 그림이 있다.
3. 나머지 **UC-02~20** — 관심 순서대로. 예외 시나리오(§4)가 성공 경로만큼 중요하다.
4. [`interfaces/`](interfaces/) — 외부 전문·파일의 **물리 규격**(승인·취소·망취소·매입 파일·입금 통지). UC의 §7 계약이 의미의 정본이고, 여기는 그 표면이다.

---

## 6. 왜 그렇게 됐나 — design-changes

설계가 처음부터 이렇지 않았다.\
무엇이 몇 번 깨졌고, 그래서 토대를 어떻게 되돌렸는지가 [`design-changes/`](design-changes/)의 **DC-001~006**에 있다.

여기는 필수는 아니지만, *"왜 이렇게까지 하나"* 가 궁금해지는 순간 답이 있는 곳이다.\
특히 **DC-001**(합계 필드를 왜 애그리게이트로 분리했나)이 이 프로젝트의 가장 큰 설계 교훈이다.

---

## 7. 어떻게 배포되나 — 인프라 여정

설계가 아니라 **실제로 돌아가는 배포·인프라**의 이야기다.\
이 부분은 코드가 있는 층이므로, 개발 기록으로 읽는다.

- [`study/tech/infra-journey/`](study/tech/infra-journey/) — 아키텍처·패턴 해설(사람 층).
- **study-note의 `project/jun-bank/`** — 배포·인프라를 이슈 단위로 기록한 개발 여정(infra 15 · core 2 · gateway 6 · settlement 1 · ledger 1). 각 이슈가 *"이 문제를 왜 이렇게 풀었나"* 를 담는다.

배포 파이프라인 전체 그림은 이렇다.

```
GitHub Actions 빌드 → GHCR digest → 서명 manifest → 오라클 엣지
    → .9 main(관문1 HMAC · 관문2 OIDC · 배포 창 락)
        → core 로컬 블루-그린 · gateway 재기동 · settlement/ledger 위성 RPC
```

정본은 ADR-027·030·031·032이고, 위 개발 기록이 그 결정이 코드가 된 과정을 서사로 풀어 준다.

---

## 목적별 지름길

전체를 순서대로 읽을 시간이 없다면, 목적에 맞는 최소 경로만 탄다.

| 목적 | 최소 경로 |
|---|---|
| **시스템이 뭔지만 빠르게** | README → product/00 → domain/context-map → UC-01 |
| **도메인을 구현하려면** | product/01(BR) → domain/aggregates → state-machines → 해당 UC |
| **인프라·배포를 이해하려면** | architecture/README → ADR-027·030·031·032 → study-note project/jun-bank |
| **왜 이렇게까지 하나** | design-changes/DC-001 → state-machines/README(설계 판단 7가지) |

---

## 읽을 때의 팁

- **소제목만 위에서 아래로 읽어 본다.** 그것만으로 그 문서의 구조와 결론이 잡히면, 세부는 필요할 때 내려가면 된다.
- **표는 대조용, 산문은 이해용.** 규칙 대장(BR·INV·규칙 ID)은 표로 훑고, "왜"는 그 아래 산문에서 읽는다.
- **`BR-nn`·`INV-n`·`R-nn` 같은 ID는 참조 좌표다.** 지금 다 외우지 말고, 정본(BR=product, 용어=glossary, 관계=context-map)으로 되짚을 수 있다는 것만 안다.
- **`[미확정]`·열린 의문은 결함이 아니라 정직이다.** 정해지지 않은 것을 추측으로 메우지 않은 표시다.

---

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v1.0 | 2026-09-16 | 신설 — 처음 읽는 사람을 위한 7층 순서 안내 + 목적별 지름길. `README.md`의 짧은 「읽는 순서」를 상세화한 별도 문서(현행 반영: ADR 32·애그리게이트 16·관계 R1~R20·가독성 재작성 완료) |
