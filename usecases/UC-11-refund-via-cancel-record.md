# UC-11. 취소 레코드로 환불을 확정한다

- 작성일: 2026-08-05
- 상태: **초안 (계약 블록 후속)**
- 액터: **매입사/VAN** (시스템 주체 — R1 파일 경로. 카드소지자에게 돌아갈 반환을 **대리** 요청한다: 목표의 주인은 소지자, 레코드의 주체는 매입사 — UC-01과 같은 대리 구도)
- 권한 요건: 시스템 주체 — **BR-54 예외 목록 ②**(매입 배치 — 취소 레코드도 같은 파일·같은 배치로 들어온다). 운영자 단계 무관 · BR-56 승인 대상 아님
- 트랜잭션 경계: **E4** (승인 + **그 승인의 미수** + 계좌 + 반환액이 회수하는 **다른 미수들** + **매입 배치** — 한 커밋)
- 관련 규칙: BR-11·12·14·17·21·23·26·33·34·43·47·51·54·57 · 승인 T10·T11·T16 · INV-7·8·9 · RC-1·RC-2 · 미수 V4·V5 (참조만 — F-24)

## 1. 목표 (한 문장)

매입사가 매입 파일의 **취소 레코드**로 이미 매입된 거래를 **얼마로 줄일지** 통보하면, 고객에게 돌려줄 **반환액과 소멸할 채권이 산식대로** 확정된다.

## 2. 사전 조건 / 사후 조건

- 사전: 원거래가 `CAPTURED` 또는 `SETTLED`다 · `(fileId, recordId)`가 아직 처리되지 않았다(BR-23) · ★ **레코드가 실은 값은 "반환액"이 아니라 거래 축소액이며 매입액 기준**이다(BR-43 ①).
- 사후(부분 — 잔여 채무 > 0): `refundedTotal` 증가 → 소멸 → 계좌 입금 → `returnedTotal` 증가. **상태는 `CAPTURED` 유지**(T11).
- 사후(전액 — 잔여 채무 0): 상태 `REFUNDED`(T10). 같은 3단계이며 **결과 상태만 다르다**.
- 사후(`SETTLED` 원거래): ★ **상태와 `settledBusinessDate`는 불변**이고 `refundedTotal`만 오른다(T16). 축소액은 그날 `refundTotal`에 집계되어 **다음 영업일 순액의 차감분**이 된다(BR-43 ② · BR-47).
- 사후(격리): 상한 초과면 **M5**로 격리되고 자금은 움직이지 않는다.
- 사후(강등): 원거래가 미매입이면 반환할 자금이 없으므로 **`void`로 강등**된다(§4.3) → UC-08의 조작.
- 사후(커밋 실패): E4 전체 롤백 — `markProcessed`도 같은 커밋이므로 **재처리가 안전**하다.
- 사후(공통): 한도는 **복원하지 않는다** — `limitContribution`은 매입 시 확정됐고 매입 완료 후 환불은 복원 대상이 아니다(BR-24 예외 ①).

## 3. 주요 성공 시나리오

1. 매입 파일에서 **취소 레코드**를 판별한다 [BR-33 — 매입·취소 레코드 혼재]
2. `(fileId, recordId)` 멱등 확인 [BR-23 · HS8]
3. 원거래 지목·상태 판정 [BR-12 · BR-17 — 매입·취소는 상관 식별자와 승인번호 모두로 식별]
4. 유형 분기 — 매입 후면 `refund`, 미매입이면 **`void` 강등** [BR-26 · 승인 SM §4.3]
5. 상한 판정 `refundedTotal + 요청액 ≤ capturedAmount` [**INV-7** — 좌변이 누계여야 나눠 환불로 우회되지 않는다]
6. 산식 계산 — `잔여 채무` · `회수액` · `반환액` · `소멸액` [BR-43 ① 산식이 정본]
7. ★ **미수 소멸 먼저** — `소멸액 > 0` **이고** 그 미수가 `OPEN`일 때만 `writeOff` [V4·V5 · VF9 · 자금 이동 없음]
8. 계좌 `refund(반환액, recoverable)` — **반환액이 0이어도 반드시 부른다**(차단 플래그 재평가) · 유입액은 FIFO로 **다른 미수**를 먼저 회수한다 [BR-34 · 예외 ② 자기 미수 제외]
9. `returnedTotal` 증가 + `CaptureBatch.markProcessed` — **전부 한 커밋**(E4) [RC-1·RC-2 검사]
10. `Refunded` 발행 → C5 원장이 역분개로 번역(귀속 영업일 = **파일 표기 영업일** — BR-14) · C4 정산이 `refundTotal`에 집계 [BR-21·43]

### 산식 (BR-43 ①이 정본 — 여기서는 단계 순서만 고정한다)

```
잔여 채무 = capturedAmount − refundedTotal
회수액    = withdrawnAmount + 미수.recoveredAmount − returnedTotal     ← 저장하지 않고 매번 계산
반환액    = max(0, 회수액 − 잔여 채무)                                  ← 초과 회수분만
소멸액    = max(0, 미수.outstanding() − max(0, 잔여 채무 − 회수액))
```

## 4. 예외 시나리오 ★

| # | 트리거 | 시스템 반응 → 종결 | 근거 |
|---|---|---|---|
| A | 같은 취소 레코드 재처리 | 건너뛴다 — 막지 않으면 **환불이 두 번 반영되어 고객에게 두 배**가 간다 | BR-23 · BR-33 HS8 · BF8 |
| B | **상한 초과**(`refundedTotal + 요청액 > capturedAmount`) | 격리 → **M5 취소 불일치**. 상태는 정상이고 **금액이 금지를 만든다** | **F18** · INV-7 · BR-43 ① |
| C | 원거래가 **미매입**(`AUTHORIZED`·`EXPIRED`) | 금지가 아니라 **`void`로 강등** — 반환할 자금이 없다. 누적 취소액을 넘으면 격리(M5) | **F7** · §4.3 → UC-08 |
| D | 원거래가 `DECLINED`·`VOIDED`·`REFUNDED` | 거절/격리 — 매입된 적이 없거나 이미 전액 환불됐다 | F22 · (매입 레코드였다면 M8 — BR-51) |
| E | 원거래가 **`SETTLED`** | 금지가 아니라 ★ **원거래 불변 + 차감 거래**로 처리 — 마감된 순액을 되돌리지 않는다 | **F16** · T16 · BR-43 ② · BR-47 |
| F | 잔여 채무가 회수액보다 큼 (미수가 남아 있다) | **반환액 0** — 미수만 소멸한다. 그래도 계좌 `refund(0)`을 호출한다 | BR-43 산식 · `account.md` `refund(0)` 정상 |
| G | 자기 미수가 이미 `CLOSED` | `writeOff`를 **호출하지 않는다** — 조건 없이 부르면 두 번째 환불에서 `VF2`로 **정상 환불이 통째로 실패**한다 | **VF9** · VF2 |
| H | 소멸과 반환액 입금의 **순서가 뒤바뀜** | 자기 미수가 회수 대상에 들어가 `recover`가 잔여를 줄이고, 뒤이은 `writeOff`가 초과로 거절되어 **환불 전체가 롤백**된다 — 소멸 선행이 계약이다 | BR-34 예외 ② · 승인 `refund()` 절차 5→6 |
| I | 반환액이 계좌에 **다른 미수**를 만남 | FIFO(발생 영업일, 미수 식별자)로 회수하고 **잔여분만** 잔액 증가. **보류 미수는 대상에서 빠진다** | BR-34 · VF3 |
| J | E4 커밋 실패 | 전체 롤백 — `markProcessed`가 같은 커밋이라 재처리가 안전하다. 갈리면 **부분 환불이 상한 안에서 두 번 성공**해 이중 반환 | E4 ④ |
| K | 환불로 마지막 미수가 종결됨 | 계좌가 `receivableBlockLifted`를 **재평가**해 차단이 자동으로 풀린다 | 계좌 INV-3 · BR-45 |
| L | 원거래가 **보류 중**(`frozen = true`) | **반영된다** — 보류는 `expire`·`capture`만 막는다. 환불은 고객 계좌로 **들어오는** 방향이라 BR-50이 막으려는 *"조사 중 출금"* 이 아니고, 채권 소멸(`writeOff`)도 보류와 무관하다 | F17 범위 · 미수 V4·V5(보류 무관) · BR-28 |
| M | 취소 레코드가 원거래와 맞지 않음(매칭 실패) | 격리 → **M5**(취소 레코드가 주어인 유형) — 매입 레코드의 M2와 구분된다 | BR-12 · BR-09 M5 · AU5 (b)·(c) 기각 근거 |
| N | 고객이 미수를 **먼저 갚은 뒤** 전액 환불 | 반환액이 매입 시 출금액을 **넘는다**(10만 매입·7만 출금·3만 상환 → 10만 반환). 상한을 출금액으로 고정하면 이 규칙이 기각한 모델로 되돌아간다 | BR-43 ① · RC-1 · AU9 (b) 기각 |

## 5. 대체 시나리오

- **`SETTLED` 원거래의 차감 거래 경로** — 목표(고객에게 돌려준다)는 같지만 **원거래를 한 글자도 건드리지 않는** 유일한 경로다. 승인 상태·`settledBusinessDate`는 불변이고 자금은 **다음 영업일 순액의 차감**으로 흐른다(승인 SM §4.3 마지막 행 · BR-43 ②). ★ 이 경로 때문에 `SETTLED`가 종료 상태로 성립한다.
- 매입 **전** 취소(UC-08)는 대체 경로가 아니다 — 접수 채널(실시간 전문)과 자금 흐름(홀딩 해제 · 역분개 없음)이 다르며 BR-26이 유형 자체를 가른다.

## 6. 인가·감사 ★

- 주체 판정: 배치 진입 시 1회(AU-3·AU-4) — 시스템 주체 + **경로 식별자**(매입 배치). 소유자 필터 없음(AU-2), 근거는 **BR-54 예외 ②**. 반환 대상 계좌는 요청이 지목하지 않고 **원승인 참조로 유도**되므로 IDOR 표면이 아니다.
- L7: **해당 없음** — 파일 채널에는 응답 상대가 없다(§4의 금지는 응답 코드가 아니라 격리 + 불일치로 표현된다). 격리 사유(M5 등)는 운영자 화면용이며 매입사에게 새지 않는다.
- 감사: 성공 = **E4 커밋 내 outbox**(AD-2 — 주체 = 배치 경로, 대상 = 승인/계좌/미수, 축소액·소멸액·반환액) · 격리 = 배치 변경 + 불일치 적재(BR-40 · `별도` 경로) · **자금 이벤트 `Refunded`에는 행위자를 싣지 않는다**(AD-7 ② — 원장은 자금 언어다. 행위자는 감사 수집이 나른다).

## 7. 계약 (요약 — **상세 블록은 계약 전수 패스로 이월**)

> 이 패스는 **인터페이스·주체·L7 판정**만 확정한다. 레코드 스키마(축소액·원거래 참조)·격리 코드·멱등·예산·감사 상세 블록은 **Phase 4 계약 전수 패스**에서 채운다.

| 인터페이스 | 주체 (유형·단계·스코프) | L7 대상 |
|---|---|---|
| 파일 **`CAPTURE-FILE`의 취소 레코드**(BR-33 — **별도 인터페이스가 아니라 UC-10과 같은 파일**이다. 레코드 유형으로 갈린다) | 시스템 — 매입 배치 경로(BR-54 예외 ②, 경로 식별자 C-1) | 해당 없음 — 응답 상대가 없는 채널(§6) |
| 이벤트 `Refunded`(승인ID · **`accountId`** · `reductionAmount` · `writtenOffAmount` · `returnedAmount` · `recoveredAmount` · `creditedAmount` · 영업일 — `authorization.md` §6 정본) | — (E4 같은 트랜잭션) · 구독 = **C5 원장 · C4 정산** | 해당 없음 — 축 필드·S-9 **[미정 · 계약 전수]**(IS-5) |
| 이벤트 `RefundCredited`(계좌 — ⚠️ **원장 아님**: 승인 `Refunded`가 이미 기표한다) | — (E4 같은 트랜잭션) | 해당 없음 — 구독자·S-9 **[미정 · 계약 전수]** |

## 8. 규칙 연결 표

| 단계/예외 | 규칙·조작 |
|---|---|
| §3-1·2 / §4-A | BR-33 · BR-23 · `CaptureBatch.markProcessed`(E4) |
| §3-3 / §4-M | BR-12·17 · M5 |
| §3-4 / §4-C·D·E | BR-26 · 승인 SM §4.3 · F7·F16·F22 · T16 |
| §3-5 / §4-B | **INV-7** · F18 · BR-11 계열 상한 |
| §3-6 / §4-F·N | BR-43 ① 산식 · RC-1·RC-2 · `withdrawnAmount` 불변(INV-8) · INV-9 |
| §3-7 / §4-G·H·L | `Receivable.writeOff` · V4·V5 · VF9·VF2 · BR-34 예외 ② · BR-28 |
| §3-8 / §4-I·K | `Account.refund` · BR-34 · VF3 · 계좌 INV-3 · BR-45 |
| §3-9 / §4-J | E4 (승인 + 자기 미수 + 계좌 + 회수 대상 미수 + 배치) · 락 순서(미수 → 승인 → 계좌) |
| §3-10 / §5 | BR-14·21·43 ②·47 · R5 원장 ACL · R8 정산 |
| §6 | BR-54 예외 ② · BR-57 · AD-2·7·8 |

## 9. 열린 의문

| # | 의문 |
|---|---|
| **UC11-1** | **환불 배치 주기** — BR-43 미확정 수치. 매입 파일과 같은 주기인지(같은 파일에 실려 오므로 사실상 같아 보이나 규칙은 별도 항목으로 남겨 두었다) |
| **UC11-2** | ★ **AU7 미해결 상속** — `SETTLED` 환불의 **차감 거래를 어느 애그리게이트가 소유하는가.** 취소 레코드로 접수하기로 했으나 그 레코드는 **어느 상태 머신에도 속하지 않아 추적이 약하다**(§5의 유일 경로가 여기에 걸려 있다) |
| **UC11-3** | 취소 레코드의 **원거래 매칭 실패**가 M5인지 별도 유형인지 — M5 정의(*"취소 레코드가 원거래와 맞지 않거나 누적 취소액이 초과한다"*)가 덮는다고 읽었으나, M2·M8이 매입 레코드용으로 분리된 것과 대칭인지 확인 필요 |
| **UC11-4** | 누적 축소액의 **소유자** — DC-002가 정하기로 한 항목(BR-43 ② 주석). `refundedTotal`이 승인에 있으므로 닫힌 것으로 읽었으나, 정산 쪽 `refundTotal`과 이름이 비슷해 계약 전수에서 표기를 갈라야 한다 |

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v0.1 | 2026-08-05 | 최초 작성 (Phase 4 전수 패스) — 예외 14종 = 승인 SM(F7·F16·F18·F22·F17 범위) + 미수 SM(VF2·VF3·VF9) + BR-34·43·45 산식 경계. §7은 요약 수준(상세 블록 이월). ★ 보류 중 환불이 반영되는 근거를 명시(BR-50은 출금 방향의 규칙 — §4-L) |
