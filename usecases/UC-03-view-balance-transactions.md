# UC-03. 내 잔액과 거래를 조회한다

- 작성일: 2026-08-05
- 상태: **확정** (파일럿 — 듀얼 1패스 반영, 2026-08-05)
- 액터: **고객** (카드소지자 — 첫 고객 대면 API. 전수 스캔 확인: 이 표면은 지금까지 존재하지 않았다)
- 권한 요건: 고객 = **자기 소유만** (BR-54·58). 운영자 조회는 별도 UC(스코프 축 — 범위 밖)
- 트랜잭션 경계: **조회 — 경계 없음** (ADR-007: 조회 전용 경로 자유 · ★ W-4: 자금 판단 결과를 반환하지 않는다 — 표시용)
- 관련 규칙: BR-31·54·57·58 · ADR-007 W-4 · ADR-016 AU-1~5 · ADR-017 AD-4 · ADR-018 IS-6 (참조만)

## 1. 목표 (한 문장)

고객이 자기 계좌의 **가용잔액·계좌잔액**과 거래 내역(승인만 된 것 / 매입 완료된 것 구분 — BR-31)을 본다.

## 2. 사전 조건 / 사후 조건

- 사전: 인증됨(C7 — 주체·소유 계좌 목록이 요청 컨텍스트에 전파, R4).
- 사후(성공): 상태 무변화(조회). **조회 사실이 감사에 남는다**(BR-57 — 비동기 적재, AD-4).
- 사후(거절): 상태 무변화 + **거절 시도 기록**(독립 커밋).
- 사후(감사 적재 실패): ★ **조회는 성공**하되 실패가 M18로 적재된다(AD-4 — 무음 유실 금지).

## 3. 주요 성공 시나리오

1. 인증된 요청 — 주체(고객)·소유 스코프 전파 [R4 · AU-3]
2. `GET .../balance` — **소유자 조건이 계약에 내장**된 조회 (`findById(id, requesterScope)` 형태 — AU-1)
3. 응답: 가용잔액 · 계좌잔액 (**표시용** — 이 값으로 자금 판단을 하지 않는다, W-4) [BR-31]
4. `GET .../transactions` — 승인/매입 구분 표시 · 커서 페이징 [BR-31 · phase-backlog 조회 기준]
5. 조회 감사 비동기 적재 [AD-4]

## 4. 예외 시나리오 ★

| # | 트리거 | 시스템 반응 → 종결 | 근거 |
|---|---|---|---|
| A | **타인 계좌 식별자 지목** (IDOR — L2) | ★ `NOT_FOUND` — **부재와 구별 불가**(존재 비노출) + 시도 기록 | BR-58 (L7) · QS-08 |
| B | 미인증 요청 | 인증 오류 (C7 소관 — 인가 이전) | R4 |
| C | 페이징 경계(과대 size·만료 커서) | 상한 강제·커서 재발급 — **전체 적재 금지** | 리뷰 렌즈(자원) · 상한 [미정 — 수치 표] |
| D | 감사 적재 실패 | 조회 성공 + **M18 적재** — 조용한 유실 금지 | AD-4 · R16 |
| E | 집계·목록 우회 시도 (L3·L4) | 목록·합계도 같은 소유자 조건 — 계약 형태가 막는다 | BR-58 · C-3 |

## 5. 대체 시나리오

- 카드별 거래 필터(자기 카드 목록 안에서) — 같은 목표의 좁힌 경로. 소유자 조건 동일.

## 6. 인가·감사 ★

- 주체 판정: 진입 1회(AU-4) — 고객은 **자기 소유 계좌·카드만**(AU-2). 판정은 인프로세스(AU-3 — 응답 예산 안).
- L7: §4-A — 이 UC가 **QS-08 전수 시험의 원형**이다("내 것이 보인다"는 검증이 아니다).
- 감사: 조회도 남는다(3겹 근거 — 감독규정 13조 ①8호·안전성기준 8조·PCI 10.2.1). 항목 = AD-8(주체·대상·스코프·결과).
- 캐시: 현재 없음. **도입되면 키에 주체 범위 필수**(IS-6 — 이 계약이 그 규정의 착지점).

## 7. 계약 (계약 블록)

#### `GET /me/accounts/{accountId}/balance` — 잔액 조회

| 항목 | 내용 |
|---|---|
| 주체 | 고객 (본인 — `me` 스코프. `accountId`가 소유 목록 밖이면 §4-A) |
| 요청 | 경로 파라미터만 |
| 응답(성공) | 가용잔액 · 계좌잔액 (BR-31 — 둘 다. **표시용** — W-4) |
| 오류 표 ★ | `NOT_FOUND`(부재 **= 타인 소유** — 동일 형태, L7) |
| 멱등 | 조회 — 자연 멱등 |
| 예산 | 실시간 조회 — 목표치 [미검증] (승인 경로 예산과 별개 — Phase 5 실측) |
| 감사 | 주체·계좌·"잔액 조회"·성공/거절 (AD-4 비동기 · 거절은 독립 커밋) |

#### `GET /me/accounts/{accountId}/transactions?cursor=&size=` — 거래 내역

| 항목 | 내용 |
|---|---|
| 주체 | 고객 (본인) |
| 요청 | 커서 · size(상한 [미정] — 강제) · 기간 필터 · ★ **cardId 필터(선택 — 자기 카드 목록 안: 2차 소유 판정, L4. §5 대체 시나리오의 계약)** |
| 응답(성공) | 거래 목록 — **승인만/매입완료 구분 표시**(BR-31) · 다음 커서 |
| 오류 표 ★ | `NOT_FOUND`(동일 형태 — L7) · `INVALID_CURSOR` |
| 멱등 | 조회 — 자연 멱등 (커서 = offset 아님 — backlog 조회 기준) |
| 예산 | [미검증] |
| 감사 | 잔액 조회와 동일 + 조회 범위(기간·카드 필터) |

## 8. 규칙 연결 표

| 단계/예외 | 규칙·조작 |
|---|---|
| §3-2·3 | BR-31·58 · AU-1·2 · W-4 |
| §3-4 / §4-C·E | BR-31·58 · 커서 페이징 (조회 기준) |
| §3-5 / §4-D | BR-57 · AD-4 · M18 · R16 |
| §4-A / §6 | BR-58 (L7) · QS-08 |

## 9. 열린 의문

| # | 의문 |
|---|---|
| ~~UC3-1~~ | ✅ **닫힘 (리뷰 반영)** — ★ **합친다(C1 입금·정정 + C3 승인·매입)**: BR-31이 "승인만 된 건"의 표시를 요구하므로 원천은 필연적으로 C3를 포함한다. 소유 축 필터는 원천별 각 1회(`ownerId` — IS-1이 C3 승인에도 축을 저장한 이유), 정렬 = 발생 시각, 조회 전용 조립이라 ADR-007 허용·W-4 준수(표시용) |
| UC3-2 | 조회 응답 예산 목표치 — [미검증], QS 수치 대장 등재 대상 |

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v0.2 | 2026-08-05 | **듀얼 1패스 반영** — UC3-1 닫힘(원천 = C1+C3 조립 — BR-31이 강제) · cardId 필터 계약(L4 — §5와 대응) · 표 정리 |
| v0.1 | 2026-08-05 | 파일럿 — 첫 고객 대면 API. QS-08 전수 시험의 원형(L7·소유자 조건 내장 계약·조회 감사·M18) |
