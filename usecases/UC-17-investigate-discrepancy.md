# UC-17. 불일치를 조사하고 처리한다

- 작성일: 2026-08-05
- 상태: **검토대기 (계약 블록 완결)**
- 액터: **운영자 담당자** (BR-54 ② 운영자 주체)
- 권한 요건: **담당자** 단계 (BR-55 배정 표 — *불일치 조사 착수 / 처리 완료* 행) · 스코프 = 대상 `subject`의 귀속 축 (**§9 UC17-1 — 축이 유형마다 다르다**). **BR-56 대상 아님** — 재발견 복귀(INV-4)가 사후 검증을 대신한다
- 트랜잭션 경계: **없음 — 불일치 단일 애그리게이트**(조작 대장 `investigate`·`resolve` = `—`. 상류를 바꾸지 않으므로 자금 무이동)
- 관련 규칙: BR-09·41·42·48·53·54·55·57·58 · `Discrepancy` INV-1·2·**3**·**4** · 불일치 SM D2·D3·D4·D5 · DF1·DF2·DF3·DF4·DF6·DF7 (참조만 — F-24)

## 1. 목표 (한 문장)

운영자가 대사·격리가 적재한 불일치를 붙잡아 조사하고, **근거를 남기고** 처리 상태로 닫는다.

> ★ **이 유스케이스는 아무것도 고치지 않는다.** 상류를 고치는 것은 사람이고, 그 수단은 각 컨텍스트의 관리 오퍼레이션(UC-12~16·19)이다. 여기서 남는 것은 **판단의 기록**뿐이다(`Discrepancy` INV-1 — 자동 정정 조작이 아예 없다).

## 2. 사전 조건 / 사후 조건

- 사전(조사 착수): 대상 불일치가 **`OPEN`(미처리)** 이다(**D2**). `INVESTIGATING`·`RESOLVED`에서는 착수할 수 없다(DF4·DF3).
- 사전(처리 완료): 상태가 **`OPEN` 또는 `INVESTIGATING`** 이고 **`resolution`이 비어 있지 않다**(**D3** · **INV-3**).
- 사후(조사 착수 성공): `INVESTIGATING` · **담당 운영자 기록** · `DiscrepancyInvestigating` 발행(payload에 담당 운영자 — AD-7 ①).
- 사후(처리 완료 성공): `RESOLVED` · `resolution` 저장 · `DiscrepancyResolved` 발행(운영자·`resolution`).
- 사후(★ 종결이 아니다): **`RESOLVED`는 종료 상태가 아니다.** 다음 대사에서 같은 `(유형, 범위, 대상)`이 또 탐지되면 **`OPEN`으로 복귀**하고 `resolution`은 **보존**된다(**INV-4** · **D4** · BR-48). *"고쳤다는데 또 나온다"* 가 눈에 보이는 신호로 남는다.
- 사후(거절): 상태·기록 무변화 + 거절 시도 기록(독립 커밋 — AD-2).

## 3. 주요 성공 시나리오

1. 운영자가 미처리 목록에서 건을 고른다 — 우선순위 근거 = `firstDetectedOn`(며칠째) · `detectionCount`(≈ 미해결 영업일 수) [BR-48]
2. `investigate(operator)` — `OPEN → INVESTIGATING`, **담당자 기록** [**D2** · BR-09]
3. 조사 — 유형별로 다른 조치를 본다 (예: **M2**는 매입사 오발송·우리 유실 의심 / **M8**은 매입사가 우리 거절·취소 응답을 못 받았을 가능성 → 전문 로그 대조) [`Discrepancy` §4 유형 표 · BR-51]
4. 필요하면 **상류를 고친다 — 이 UC 밖에서**: 해당 컨텍스트의 관리 오퍼레이션(UC-12 보류/해제 · UC-13 차단 해제 · UC-14 격리 승격 · UC-15 정산 재개 · UC-16 원장 역분개 · UC-19 DLQ 재투입) [컨텍스트 맵 §8 · **DF2**]
5. `resolve(operator, resolution)` — **근거 필수** → `RESOLVED` [**D3** · **INV-3**]
6. 조사 착수·처리 완료 각각이 행위자와 함께 감사에 남는다 [BR-57 · AD-8]
7. (사후 검증) 상류가 실제로 안 고쳐졌다면 다음 대사가 같은 건을 다시 적재해 **`OPEN`으로 복귀**시킨다 — `detectionCount`는 **영업일당 최대 1회** 오른다 [**D4** · **INV-4** · BR-48]

## 4. 예외 시나리오 ★

| # | 트리거 | 시스템 반응 → 종결 | 근거 |
|---|---|---|---|
| A | **`resolution` 없이 처리 완료** | 거절 `RESOLUTION_REQUIRED` — 처리했는데 근거가 없으면 **나중에 아무도 판단을 재구성할 수 없다** | **DF1** · **INV-3** |
| B | **조사 중인 건에 다시 조사 착수**(다른 운영자) | 거절 `ALREADY_INVESTIGATING` — 두 사람이 같은 건을 조사하면 조치가 충돌한다 | **DF4** |
| C | ★ **같은 운영자가 조사 착수를 재호출** | ★ **똑같이 거절된다** — 담당자가 이미 그 사람이라 바꿀 것이 없다. **멱등 no-op이 아니다** | **DF4** (명시 조항) |
| D | **`RESOLVED` 건에 조사 착수** | 거절 `DISCREPANCY_RESOLVED` — 재조사는 **재발견으로 `OPEN`에 돌아온 뒤**에만 가능하다. 상태만 되돌리면 `detectionCount`가 실제와 어긋난다 | **DF3** |
| E | **`RESOLVED` 건에 다른 `resolution`으로 재처리** | 거절 `DISCREPANCY_RESOLVED` — 덮어쓰면 **왜 그렇게 판단했는지가 사라지고** 오판의 흔적이 지워진다 | **DF7** |
| F | **조사를 건너뛰고 바로 처리** | ★ **정상 경로다** — `D3`은 `OPEN`에서도 허용된다. 원인이 명백한 건까지 조사 단계를 강제하면 형식적 클릭 두 번이 될 뿐이다. **막아야 할 것은 근거 없는 처리(INV-3)이지 빠른 처리가 아니다** | **D3** (§3 주석) |
| G | **처리했는데 다음 대사에서 또 발견** | `RESOLVED → OPEN` 복귀 · `resolution` **보존** · `lastDetectedOn` 갱신 · 영업일이 바뀐 경우에만 `detectionCount` +1 | **D4** · **INV-4** · BR-48 |
| H | 같은 영업일에 대사를 여러 번 재실행 / 매입 격리와 대사가 같은 날 둘 다 탐지 | 상태 불변 · **`detectionCount`는 1만 증가** — 배치 재실행이 멱등하다 | **D5** · **INV-2** · BR-48 |
| I | **대사 배치의 재발견과 운영자 `resolve`의 경합** | 애그리게이트 락에서 **하나만 성공**. 운영자가 이기면 다음 대사에서 다시 잡히므로 손실이 없다 | 불일치 SM §6 |
| J | 상류 데이터를 이 화면에서 고치려는 시도 | **조작이 없다** — 호출할 메서드가 존재하지 않는다(컴파일 단계 강제). 상류는 §3-4의 관리 오퍼레이션으로 | **DF2** · **INV-1** · BR-09 |
| K | 불일치 삭제 시도 | **조작이 없다** — 불일치는 지워지지 않는다(은폐 방지) | **DF6** |
| L | **`INVESTIGATING`에서 담당자가 잊고 방치** | ★ **복구 경로가 없다** — `INVESTIGATING → OPEN` 전이가 아예 없어 `OPEN`보다 더 안 보인다 **[§9 UC17-2 · DCS1]** | 불일치 SM **DCS1** |
| M | 오래된 `OPEN` 건 | **자동으로 닫히지 않는다** — 시간 기반 전이가 없다. 자동 종료는 *"아무도 안 본 것이 처리된 것으로 바뀌는"* 은폐다. 대신 `firstDetectedOn`으로 지속 기간이 드러난다 | 불일치 SM §7 |
| N | 같은 불일치가 3영업일 연속 미해결 | **통지**(적재와 별개 경로) — 임계 수치 **미확정** | BR-53 ③ · BR-48(DC4) |
| O | 스코프 밖·부재 건 지목 | 거절 — 응답이 "없음"과 구별되지 않는다 + 시도 기록. ★ 단 **스코프 축이 유형마다 다르다**(§6) | BR-58 (L7) · IS-4 |

## 5. 대체 시나리오

- **조사 없이 바로 처리**(§4-F) — 목표는 같고 경로가 짧다. 원인이 명백한 건(예: 이미 원인을 아는 M16 드레인 미완)에서 정상이며, 근거 요구(INV-3)는 동일하게 걸린다.
- **처리하지 않고 상류만 고친다** — 다음 대사가 재탐지하지 않으면 건은 `OPEN`에 남는다. 목록에서 사라지지 않으므로 **결국 이 UC를 거쳐야 닫힌다**(닫는 유일한 조작이 `resolve`다).

## 6. 인가·감사 ★

- **판정 순서** (UC-02 §7 선례): ① **단계**(담당자 미달 → `FORBIDDEN_LEVEL`, 대상 조회 전) → ② **스코프·존재**(`NOT_FOUND` — 부재·스코프 밖 동일, L7) → ③ **상태**(§4-A~E).
- 판정 시점: 진입 시 1회(AU-4).
- ★ **스코프 축이 유형에 따라 갈린다**: **계좌가 유도되는 유형**(직접 보유 = M9~M14 — `subject`가 계좌ID/(계좌ID, 영업일) · **원승인/원입금 경유 = M1~M8·M19**)은 계좌 개설 조직으로 판정할 수 있다(IS-4). **영업일·파일·경로·감사 단위 유형**(M15·M16·M17·M18)은 귀속 축이 없어 **전사만**이다. (UC17-1이 이 갈림을 **BR-55 v4.5 판정 함수형**으로 확정했다 — 초기에는 BR-55 특칙 목록에 불일치 조사가 없던 것이 §9 UC17-1의 근거였다.)
- 감사(AD-2 경로 규칙): 성공 = **조작 커밋 내 outbox** / 거절 = **독립 커밋** / 유실 = **M18**(R16). ⚠️ **재귀 차단**: M18을 조사·처리하는 이 조작의 감사 기록 자체는 또 M18을 만들 수 있으나, **감사 append 행위와 M18 적재 조작은 감사 대상이 아니다**(AD-5 재귀 차단).
- 감사 항목(AD-8): 주체·시각·대상(`(유형, 범위, 대상)`)·스코프·종류(조사/처리)·결과 + `resolution`.
- 이벤트에 행위자를 **싣는다** — `DiscrepancyInvestigating`·`DiscrepancyResolved`는 구독이 운영인 **운영자 전용 이벤트**다(AD-7 ①).
- 승인 흐름: **해당 없음** — BR-56 5조작 목록에 불일치 조사·처리가 없다. 대신 **재발견 복귀(INV-4)** 가 사후 검증 장치이며, BR-55 배정 표가 그 이유를 명시한다. *(U-6 근거)*

## 7. 계약 (계약 블록)

> 승인 흐름 없음(§6) — BR-56 5조작 목록 밖이다. 재발견 복귀(INV-4)가 사후 검증을 대신한다.
> ★ **스코프 축 = 유도 스코프** (2026-08-06 **UC17-1 확정** — 사용자 합의, UC16-1과 한 원칙 · BR-55 **v4.5** 유도 특칙, **판정 함수형**): `subject`에서 **계좌가 유도되는 유형**(직접 보유 = M9~M14 · ★ **원승인/원입금 경유 = M1~M8·M19** — 리뷰 F2 보완)은 **그 계좌 개설 조직 스코프**, **유도 불가 유형**(영업일·파일·경로·감사 단위 = M15·M16·M17·M18)은 **전사만**. L7 대상 집합 = 스코프 안 유도 결과.

#### `GET /ops/discrepancies?status=&type=&scope=&from=&to=&cursor=&size=` — 불일치 목록 (§3-1의 전제 조회)

| 항목 | 내용 |
|---|---|
| 주체 | 운영자 **조회** 단계 이상(BR-55 배정 표 *운영자 목록·상태·불일치 조회* 행 — 상위 단계가 하위를 포함하므로 담당자·책임자도 해당) · 스코프 = ★ **유도**(UC17-1 확정 — 계좌 단위 유형은 개설 조직 / 축 없는 유형은 전사만. 목록은 스코프 안 유도 결과만) · 판정 시점 = 진입 1회(AU-4) |
| 요청 | `status`(`OPEN`·`INVESTIGATING`·`RESOLVED` — 복수 허용, 기본값 = 미종결 2종) · `type`(M1~M19) · `scope`(**외부**(BR-30) / **내부**(BR-41) — `ReconciliationScope`) · `from`·`to`(`firstDetectedOn` 기간) · **`cursor` · `size`(상한 [미정 — 수치 표] · 강제)** — ★ 페이징 형태는 **UC-03 `GET /me/accounts/{id}/transactions` 선례**를 그대로 탄다: **커서 = offset이 아니라 정렬 전순서 키** |
| 응답(성공) | 건 목록 — `discrepancyId` · **`(type, scope, subject)`**(유일 키 — INV-2) · `status` · `firstDetectedOn` · `lastDetectedOn` · **`detectionCount`**(≈ 미해결 영업일 수 — BR-48) · `expected`·`actual`(금액 불일치인 경우) · `resolution` 유무 · **담당 운영자**(`INVESTIGATING`인 건) · 다음 커서. ★ **정렬 = `firstDetectedOn` 오름차순**(오래 지속된 건이 위로 — §3-1의 우선순위 근거이자 불일치 SM §7이 *"오래된 건이 목록 위로 올라오게 하면 된다"* 라고 적은 것) **+ 동값 타이브레이커 `(type, scope, subject)` 전순서**(UC-03 UC3-1 선례 — 동시각 타이브레이커가 있어야 커서가 전순서 키가 된다). ★ **BR-58 C-3**: 목록·집계도 **같은 스코프 조건** — 스코프 밖 건은 나타나지 않으며 총건수·집계도 같은 조건 위에서 낸다 |
| 오류 표 ★ | ① `FORBIDDEN_LEVEL`(조회 단계 미달 — 대상 조회 전) → ② **스코프 밖 건은 오류가 아니라 결과에서 빠진다**(목록의 L7 형태 — *없는 것*과 *남의 것*이 응답에서 구별되지 않는다, BR-58 목록 조건) → ③ `ARG_MISMATCH`(알 수 없는 `status`·`type`·`scope` 값 · `size` 상한 초과 — **상한 강제**, 전체 적재 금지) · `INVALID_CURSOR`(만료·위조 커서 — UC-03 선례 코드) |
| 멱등 | 조회 — **자연 멱등**. 커서는 전순서 키이므로 재호출이 같은 창을 준다(offset 아님) |
| 예산 | 실시간 아님(운영 화면) — 목표치 **[미검증]** |
| 감사 | 경로(AD-2/AD-4) = ★ **조회이므로 비동기 적재**(AD-4) · **거절 → 독립 커밋** · ★ **적재 실패는 조회를 막지 않고 실패 사실이 M18로 적재**된다(R16 — 무음 유실 금지). ⚠️ **재귀 차단**: M18을 보여 주는 이 목록의 감사 기록이 또 M18을 만들 수 있으나 **감사 append 행위와 M18 적재 조작은 감사 대상이 아니다**(AD-5). 항목(AD-8) = `sourceRef`(= 조회 요청 식별자 — AD-3) · 주체 · 시각 · **대상 = 조회 범위**(필터·기간) · ★ 스코프 = **판정에 실제 쓰인 값**(K-4 · UC17-1에 묶임) · 종류(불일치 목록 조회) · 결과 |

#### `POST /ops/discrepancies/{discrepancyId}/investigation` — 조사 착수

| 항목 | 내용 |
|---|---|
| 주체 | 운영자 **담당자**(BR-55 *불일치 조사 착수 / 처리 완료* 행) · 스코프 = ★ **유도**(UC17-1 확정 — 위와 동일) · 진입 1회(AU-4) |
| 요청 | 경로 `discrepancyId`만 — ★ **사유 인자가 없다**(`investigate(operator)` 정본 시그니처. 판단의 근거는 **처리 시점의 `resolution`** 이 진다) |
| 응답(성공) | `INVESTIGATING` · **담당 운영자** · `(type, scope, subject)` · `detectionCount`·`firstDetectedOn`(조사 착수 시점의 지속 지표). 반환 범위 = 스코프 안(BR-58) |
| 오류 표 ★ | **K-1 판정 순서** — ① `FORBIDDEN_LEVEL`(담당자 미달 — 대상 조회 전) → ② `NOT_FOUND`(**부재 · 스코프 밖 동일 형태**, L7 · §4-O — 대상 집합은 UC17-1에 묶인다) → ③ `ALREADY_INVESTIGATING`(**DF4 응답 열 정본** — ★ **다른 운영자도 같은 운영자도 동일하게 거절**된다. 멱등 no-op이 아니다, §4-C. ★ **정본이 이미 K-1 CDS3와 같은 판정**이다) · `DISCREPANCY_RESOLVED`(**DF3 응답 열 정본** — 재조사는 **재발견으로 `OPEN`에 돌아온 뒤**에만 가능하다) |
| 멱등 | **비멱등 — 재호출은 명시 거절**(DF4). ★ 거절이 멱등 경계다 |
| 예산 | 실시간 아님(관리 조작) — **불일치 단일 애그리게이트 락**(§4-I: 대사 배치의 재발견과 경합하면 하나만 성공. 운영자가 이기면 다음 대사가 다시 잡으므로 손실이 없다) |
| 감사 | 경로(AD-2) = **성공 → 조작 커밋 내 outbox** · **거절 → 독립 커밋** · 조회 아님 · C8 자기 조작 아님 · 유실 = **M18**(R16 · 위 재귀 차단 동일). 항목(AD-8) = `sourceRef` · 주체 · 시각 · 대상 **`(type, scope, subject)`** · ★ 스코프 = **판정에 실제 쓰인 값**(K-4 · UC17-1) · 종류(조사 착수) · 결과 |

#### `POST /ops/discrepancies/{discrepancyId}/resolution` — 처리 완료

| 항목 | 내용 |
|---|---|
| 주체 | 〃 (조사 착수와 같은 단계·같은 스코프 축) |
| 요청 | 경로 `discrepancyId` · ★ **`resolution` 필수 — 비어 있지 않아야 한다**(INV-3 · D3 조건). 공백만 있는 문자열도 거절한다(*근거 없는 처리*를 막는 것이 이 계약의 요점이다) |
| 응답(성공) | `RESOLVED` · `resolution` · 처리 운영자 · ★ **종료가 아님을 고지**: 다음 대사에서 같은 `(type, scope, subject)`가 또 탐지되면 **`OPEN`으로 복귀**하고 `resolution`은 **보존**된다(INV-4 · D4 · BR-48) |
| 오류 표 ★ | ① `FORBIDDEN_LEVEL` → ② `NOT_FOUND`(부재·스코프 밖 동일 — L7) → ③ **③ 안에서는 상태 먼저, 인자 나중**: `DISCREPANCY_RESOLVED`(**DF7 응답 열 정본** — `RESOLVED` 건의 재처리. 덮어쓰면 **왜 그렇게 판단했는지가 사라진다**) → `RESOLUTION_REQUIRED`(**DF1 응답 열 정본** — `resolution` 누락·공백). ★ **순서 근거**: DF1은 *모든 상태*라 종료 상태에서 인자까지 검사하면 같은 요청이 두 코드 중 무엇으로도 거절될 수 있다. 상태를 먼저 보면 응답이 하나로 정해지고, 둘 다 스코프 안 ID에만 반환되므로 탐색 채널이 되지 않는다. · ★ **`OPEN`에서 바로 호출해도 오류가 아니다** — D3이 `OPEN`을 허용한다(§4-F: 막아야 할 것은 근거 없는 처리이지 빠른 처리가 아니다) |
| 멱등 | **비멱등 — 재처리는 명시 거절**(DF7). ★ **이 계약의 멱등 경계는 시간에 따라 다시 열린다** — 재발견으로 `OPEN`에 복귀하면 같은 건을 다시 처리할 수 있다(INV-4). 다른 조작의 종료 상태와 다른 유일한 성질이다 |
| 예산 | 실시간 아님 — 단일 애그리게이트 락(§4-I) |
| 감사 | 조사 착수와 동일 경로 · 항목(AD-8) + ★ **`resolution`**(판단의 기록이 감사의 본체다 — 이 UC는 아무것도 고치지 않는다) |

#### 이벤트 `DiscrepancyInvestigating` · `DiscrepancyResolved` — 위 두 조작 성공 시 (기존 — `discrepancy.md` §5.1이 정본. 축·버전은 이 계약이 최초 설계)

| 항목 | 내용 |
|---|---|
| payload | **`(type, scope, subject)`** · ★ **담당/처리 운영자(행위자 포함)** — 구독이 운영인 **운영자 전용 이벤트**(AD-7 ① · 색인 §3 *포함 ✓* 확정)이며 정본과 일치 · `resolution`(`Resolved`만) · ★ **축**: `subject`가 **계좌 단위인 유형**(M9~M14)은 **`accountId`**(K-4 기존 파티션 축 유지) · **영업일·파일·경로·감사 단위**(M15·M16·M17·M18)는 축 값이 없어 **"파티션 0"**(ADR-014 **O-13**이 계좌ID 없는 이벤트에 쓴 형태를 그대로 적용) · ★ **`M19`(기한 초과 매입)는 축 없는 그룹이 아니다** — `subject`에서 **원승인을 경유해 계좌가 유도**되므로 축 = **`accountId`** 다(BR-55 **v4.5** 판정 함수형 — *직접 보유 = M9~M14 · 원승인/원입금 경유 = M1~M8·M19 · 유도 불가 = M15~M18*. 2026-08-06 리뷰 **F2·F-07**) — ★ **UC17-1(스코프 축이 유형마다 갈린다)과 같은 뿌리이며, 축이 이 UC에서 두 번째로 갈리는 자리다** · ★ **`ownerId` 비포함**(K-4 보정 확정 — **이벤트 payload 전면 비포함**. 불일치가 IS-1 6종이 아니라는 이유에 더해, IS-1 6종 자체가 **저장 모델** 대상이다) |
| 구독 | **운영(C8)**. 감사 수집은 이 구독이 아니라 **감사 전용 outbox**(AD-2) · ★ BR-53의 **통지는 적재·구독과 별개 경로**다(discrepancy §5.1 주석) |
| 멱등 키 | 수신측 유일 제약 = **`sourceRef` = `sourceEventId`**(AD-3 — at-least-once) |
| 스키마 버전 | **`schemaVersion: 1`**(K-3) · 의미 필드 추가 = 새 타입(S-6a) |

#### (참고) 이벤트 `DiscrepancyRecorded` · `DiscrepancyRedetected` — **이 UC의 조작이 아니다**

| 항목 | 내용 |
|---|---|
| payload | 정본(`discrepancy.md` §5.1) 그대로 — ★ **행위자 비포함**(시스템 적재 — 색인 §3 확정). 발행 = `recordOrTouch()`이고 호출 주체는 **대사 배치**(BR-30·41) · **매입 배치 격리**(즉시 — BR-51) · **C8**(M18 적재 — R16)이다 |
| 계약 정본 소관 | **UC-18**(대사 수동 실행) · **UC-10**(매입 파일 청산 — 격리 즉시 적재) · C8 M18 경로. **여기서는 이 UC의 상태 전이 입력**(`RESOLVED → OPEN` 복귀 — D4)임을 확인만 한다 |
| L7 | ✕ — **시스템 주체는 소유자 조건 면제**(BR-54 예외 · AU-2) |

#### 화면 전환도 (운영자 — 02 양식이 말한 실질 UI 2종 중 하나)

```
[불일치 목록] --건 선택--> [불일치 상세] --조사 착수(담당자)--> [상세: INVESTIGATING]
[불일치 상세] --바로 처리(§4-F 정상 경로)--> [처리 입력: resolution 필수] --> [상세: RESOLVED]
[상세: INVESTIGATING] --처리--> [처리 입력] --> [상세: RESOLVED]
[상세] --상류 조치가 필요하면--> (이 UC 밖: UC-12·13·14·15·16·19 화면)
```

> ★ **`RESOLVED` 상세에서 목록으로 돌아가는 경로가 끝이 아니다** — 재발견되면 같은 건이 `OPEN`으로 목록에 다시 나타난다(INV-4). 화면이 *"처리 완료 = 사라짐"* 으로 그려지면 이 UC의 요점이 무너진다.
> ⚠️ **UC12-1(승인 보류/미수 보류의 목록 구분)·UC17-5(보류 격리 목록과의 관계)는 이 전환도가 답하지 않는다** — 화면 항목 정의는 계약이 아니므로(02 양식 §2.3) 두 의문은 §9에 그대로 남는다.

## 8. 규칙 연결 표

| 단계/예외 | 규칙·조작·전이 |
|---|---|
| §3-1 / §4-M·N | BR-48(`firstDetectedOn`·`detectionCount`) · BR-53 ③ · 불일치 SM §7 |
| §3-2 / §4-B·C·D | BR-09 · `investigate(operator)` · **D2** · **DF3**·**DF4** |
| §3-3 | BR-09·30·41·51 · `Discrepancy` §4 유형 표(M1~M18) |
| §3-4 / §4-J·K | **INV-1** · **DF2**·**DF6** · 컨텍스트 맵 §8(관리 오퍼레이션) → UC-12·13·14·15·16·19 |
| §3-5 / §4-A·E·F | `resolve(operator, resolution)` · **D3** · **INV-3** · **DF1**·**DF7** |
| §3-7 / §4-G·H·I | **BR-48** · **INV-2·4** · **D4**·**D5** · 불일치 SM §6 |
| §4-O / §6 | BR-54·55·57·58 · IS-4 · AD-2·5·7·8 · AU-2·4 |

## 9. 열린 의문

| # | 의문 |
|---|---|
| ~~UC17-1~~ | ✅ **확정 (2026-08-06 사용자 합의 — 유도 스코프)**: **계좌 유도 유형**(직접 보유 M9~M14 · **원승인/원입금 경유 M1~M8·M19**) = 그 계좌 개설 조직 / **유도 불가 유형**(M15·M16·M17·M18) = 전사만 — UC16-1(역분개 유도)과 한 원칙. BR-55 **v4.5** 유도 특칙에 **판정 함수형**으로 등재(리뷰 F2 보완 — 초기 표기의 *"M15~M19 = 전사만"* 은 M19를 잘못 묶은 것이었다) |
| UC17-2 | ★ **DCS1 인용 (불일치 SM §8 정본)** — *"`INVESTIGATING`에서 오래 방치되면? 담당자가 붙잡아 놓고 잊으면 `OPEN`보다 더 안 보인다. 현재 `INVESTIGATING → OPEN` 경로가 아예 없다 — 조사 포기·담당자 변경 조작을 명세에 추가할 것인가"*. §4-L이 그 공백의 유스케이스 층 발현이다. ⚠️ v0.2에서 **명세에 없던 `release()`를 제거하며 DCS1으로 이관**한 이력이 있으므로, 추가 판정은 그 경위를 함께 봐야 한다 |
| UC17-3 | 미해결 지속 통지 임계 — **미확정 수치**(DC4 · BR-48 · BR-53 ③이 "3영업일 연속"을 이미 말하는데 BR-48 쪽은 미확정으로 남아 있다. 두 수치가 같은 것인지 확인 필요) |
| ~~UC17-4~~ | ✅ **닫힘 (2026-08-06 계약 전수 · 리뷰 F-08)** — §3-1의 *"미처리 목록에서 고른다"* 는 **이 UC의 단계**로 배정됐고, 계약이 **§7에 신설**됐다(`GET /ops/discrepancies?status=&type=&scope=&from=&to=&cursor=&size=` — 페이징·정렬은 UC-03 `transactions` 선례). ✓ **색인 등재됨** — 엔드포인트 색인 §2(*리뷰 F-1이 발견한 누락*)와 도출표의 **조회 표면(4번째 원천)** 이 짝이다(C-6 같은 커밋 등재). 별도 UC를 만들지 않는 이유 = 조회가 **액터의 독립 목표가 아니라 조사·처리의 전제**이기 때문이다(U-2) |
| UC17-5 | 조사 대상 건이 **보류(UC-12)와 겹칠 때의 순서** — 보류 격리는 불일치가 아니지만(BR-50), 같은 승인에 대해 M4·M7 같은 다른 격리가 동시에 있을 수 있다. 조사 화면에서 두 목록의 관계가 미정 |

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v0.5 | 2026-08-06 | **듀얼 리뷰 수정 — F2·F-07 · F-08**: 유도 스코프·이벤트 축의 유형 갈림을 **BR-55 v4.5 판정 함수형**으로 정정 — ★ **M19는 축 없는 그룹이 아니라 원승인 경유 계좌 유도**다(축 = `accountId`). 축 없는 그룹은 **M15~M18**로 유지(§6·§7 preamble·§7 이벤트 블록·§9 UC17-1) · **UC17-4 닫힘**(F-08 — 목록 조회 계약이 §7에 신설·색인 등재됐다. 조회는 조사·처리의 전제이지 독립 UC가 아니다) |
| v0.4 | 2026-08-06 | **UC17-1 닫힘**(유도 스코프 — 사용자 확정) — §7 마커 4곳 해소 |
| v0.3 | 2026-08-06 | **U-2 판정 반영 — 마커 해소·의문 닫기**: 이벤트 payload `ownerId` **전면 비포함** 근거 갱신(K-4 보정). ★ **UC17-1(스코프 축)은 U-2 판정 대장에 없어 열린 채로 둔다** — §7 각 블록의 UC17-1 대기 표기는 **정본 공백**의 표시이지 이 패스가 닫을 항목이 아니다(재판정 금지) |
| v0.2 | 2026-08-06 | **계약 전수 — §7 상세 블록**(요약 표 → API 3블록(★ **목록 `GET /ops/discrepancies` 포함** — 페이징·정렬·필터는 UC-03 `transactions` 선례) + 이벤트 2블록 + 참고 블록 + 화면 전환도. K-1 판정 순서 · SM 응답 열 코드 그대로(DF1 `RESOLUTION_REQUIRED`·DF3·DF7 `DISCREPANCY_RESOLVED`·DF4 `ALREADY_INVESTIGATING` — ★ 정본이 이미 CDS3와 같은 판정) · 축 = 계좌 단위 유형만 `accountId`·나머지 파티션 0(O-13 형태)/`ownerId` 비대상/`schemaVersion: 1` · 조회 감사 = AD-4 비동기 + M18 + 재귀 차단 · K-4 감사 스코프. ★ **스코프 축 = UC17-1 대기 표기 유지**) |
| v0.1 | 2026-08-05 | 초안 — Phase 4 전수 패스. 예외 15종 = 불일치 SM 금지 전이 6종(DF1·DF2·DF3·DF4·DF6·DF7) + D3의 `OPEN` 허용(§4-F, 정상 경로) + INV-4 재발견 복귀 + 시간 전이 부재. ★ 정본 공백 2건 발견(UC17-1 스코프 축 · UC17-4 목록 조회 UC 부재), DCS1 인용 |
