# UC-06. 한도를 변경한다

- 작성일: 2026-08-05
- 상태: **초안 (계약 블록 후속)**
- 액터: **고객(소지자 — 자기 카드·계좌) ∪ 운영자 담당자** (BR-46이 주어를 확정 · BR-55 배정 표)
- 권한 요건: 고객 = 자기 소유만 / 운영자 = **담당자** 단계 + 스코프 안 (BR-55 · IS-4). 2인 승인 대상 아님 — **가역이고 기존 승인을 무효화하지 않는다**(BR-56 목록 밖 · 배정 표 "왜" 칸)
- 트랜잭션 경계: **없음 — 단일 애그리게이트 갱신** (카드 `changeLimit` = 상태 불변 · 계좌 한도 = 계좌 자기 필드. 자금 무이동)
- 관련 규칙: BR-05·07·15·44·46·54·55·57·58 · 카드 INV-3·PRE-1·2 · 계좌 INV-4·PRE-3 · 카드 RC-1 · 계좌 RC-3 · 카드 SM C4·CF5·CDS1 (참조만 — F-24)

## 1. 목표 (한 문장)

소지자 또는 운영자 담당자가 **카드 한도(1회·1일) 또는 계좌 1일 한도**를 바꿔, 이후 신규 승인이 새 한도로 판단되게 한다 (이미 성립한 승인은 건드리지 않는다 — BR-46).

## 2. 사전 조건 / 사후 조건

- 사전: 대상이 요청자 범위 안이다(고객 = 소유 / 운영자 = 스코프). 카드 층이면 카드가 **해지 상태가 아니다**(CF5).
- 사후(성공): `limit`(카드) 또는 `dailyLimit`(계좌)이 **교체**된다. ★ **`usage`·`dailyUsage`는 건드리지 않는다** — 그 값은 승인들의 `limitContribution` 합이므로 변경하면 등식(카드 RC-1 · 계좌 RC-3)이 깨진다. 기존 승인·홀딩·미수 전부 불변(BR-46). 변경 주체가 감사에 남는다.
- 사후(성공 — 한도를 낮춘 경우): **새 한도 < 현재 사용액 상태가 정상으로 존재할 수 있다.** 카드 INV-1·계좌 INV-4의 검증 위치가 `useLimit()`·`useAccountLimit()` **후**로 한정된 이유가 이 경로다 — 한도 검사가 불변식이 아니라 사전조건인 근거(카드 PRE-1·2 · 계좌 PRE-3).
- 사후(거절): 무변화 + 시도 기록(독립 커밋 — AD-2).

## 3. 주요 성공 시나리오 (카드 한도 층)

1. 변경 요청 접수 — 주체 판정 **진입 1회**: 고객(본인 카드) / 운영자(담당자 단계 · 스코프 안) [BR-54·55 · AU-3·4]
2. 새 한도 검증 — `1회 ≤ 1일` [카드 **INV-3** · C4 조건] · 금액 표현 [BR-07]
3. `changeLimit(newLimit, requester)` — **상태 불변**, `limit` 교체 [카드 SM **C4**]
4. ★ 기존 승인 **비무효화** 확인 — 소급 무효가 없다는 것이 이 조작의 계약이다 [BR-46]
5. `LimitChanged` 발행 (자금 이벤트 아님 — 원장으로 가지 않는다, card.md §6)
6. 감사 기록 — **누가**(소지자인지 운영자인지)·대상·이전/이후 값 (성공 = 커밋 내 outbox — AD-2·AD-8) [BR-57]

> **계좌 한도 층은 §5-①** — 대상과 조작만 다르고 1~6의 형태는 같다. 계좌 쪽 조작 = **`changeDailyLimit(newLimit, requester)`** (2026-08-06 UC6-1 확정 — account.md §5).

## 4. 예외 시나리오 ★

| # | 트리거 | 시스템 반응 → 종결 | 근거 |
|---|---|---|---|
| A | **타인 카드·계좌 지목** (고객) | `NOT_FOUND` — 부재와 동일 형태 + 시도 기록 | BR-58 (L7) · AU-5 |
| B | 운영자 **스코프 밖** 대상 | 〃 (단계 판정 → 스코프·존재 → 상태 순서) | BR-58 · IS-4 · UC-02 판정 순서 |
| C | **해지된 카드** | 거절 `CARD_TERMINATED` — 쓰이지 않을 한도 | 카드 SM **CF5** (UC-05 §4-F와 같은 칸) |
| D | **정지 중인 카드** | ★ **허용된다**(C4가 `SUSPENDED`에서도 O) — 무해하지만 *"운영자가 정지된 카드를 만지고 있다"* 는 신호라 감사가 그것을 구분해야 한다 | 카드 SM **C4** · **CDS1**(열린 의문 — §9 UC6-2에 인용) |
| E | `1회 > 1일` 요청 | 거절 — 1회 한도가 1일 한도보다 큰 모순 | 카드 **INV-3** · C4 조건 |
| F | **새 한도 < 이미 쌓인 사용액** | ★ **허용된다** — 기존 승인은 무효가 되지 않고, **이후 신규 승인만** 새 한도로 거절된다 | **BR-46** · 카드 PRE-1·2 · 계좌 PRE-3 |
| G | 변경이 `usage`·`dailyUsage`를 함께 초기화 | **금지** — 등식 우변(승인 `limitContribution`)은 그대로인데 좌변만 움직이면 **M10·M11이 발화**한다 | 카드 RC-1 · 계좌 RC-3 · BR-41 |
| H | 계좌 한도 변경 요청 | `changeDailyLimit` — 카드 층과 같은 형태(§5-①). `dailyUsage` 불변(§4-G와 같은 근거 — RC-3) | account.md §5 (**UC6-1 확정**) |
| I | 변경과 승인의 **경합** | 카드·계좌 단위 낙관적 락 — 하나만 성공. **승인이 이겼다면 그 승인은 이전 한도로 유효**(소급 무효 금지) | 카드 SM §6 · 계좌 §8 · BR-46 |
| J | 한도 0 또는 하한 미만 요청 | **정본에 하한 규정이 없다** — 0 한도는 사실상 사용 정지인데 그 의미가 어디에도 정의돼 있지 않다 | BR-05·44 수치 미확정 → §9 UC6-4 |

## 5. 대체 시나리오

- ① **계좌 1일 한도 변경** — 같은 목표(한도를 바꾼다), 다른 층. 계좌 한도는 그 계좌의 **모든 카드 사용액 합계**에 적용되므로 대상·엔드포인트가 갈린다(BR-44). 조작 = `changeDailyLimit(newLimit, requester)` · 이벤트 = `AccountDailyLimitChanged`(UC6-1 확정).
- ② **운영자 담당자 대행** — 고객이 요청하면 담당자가 대행한다. 같은 목표·같은 전이, **주체 기록만 다르다**(감사가 구분 — UC-04 §5와 같은 형태).

## 6. 인가·감사 ★

- 판정 순서: ① **단계**(운영자 — 자기 속성이라 대상 조회 없이 판정. 고객은 유형 판정) → ② **스코프·존재**(`NOT_FOUND` 동일 형태) → ③ **상태**(CF5). UC-02가 확정한 순서 — 대상을 본 뒤 단계를 판정하면 단계 오류가 존재 탐색 채널이 된다.
- 두 주체의 스코프 축이 다르다: 고객 = **소유**(자기 카드·계좌), 운영자 = **조직**(IS-4).
- L7: §4-A·B — 두 주체 모두 범위 밖 = 부재와 동일. 사람 주체 채널이라 C-2 채널 조항 적용 ✅.
- 감사: 성공 = 커밋 내 outbox(AD-2) / 거절 = 독립 커밋(AD-2). 항목 = AD-8 + **주체 유형 구분**(소지자/운영자) — CDS1이 요구한 신호가 여기서 착지한다(§4-D).
- 조회 아님 → AD-4 비적용. *(U-6 근거)*

## 7. 계약 (요약 — ★ 상세 블록은 **계약 전수 패스로 이월**)

> 이번 패스는 **엔드포인트·주체·L7 대상 여부**만 고정한다. 요청/응답/오류 표/멱등/예산/감사 칸은 계약 전수 패스에서 02 양식대로 채운다.

| 인터페이스 | 주체 (유형·단계·스코프) | L7 대상 |
|---|---|---|
| `PUT /me/cards/{cardId}/limits` | 고객(본인) — `me` 스코프 | ✅ 부재·타인 동일 |
| `PUT /ops/cards/{cardId}/limits` | 운영자 **담당자** · 스코프 안 | ✅ 부재·스코프 밖 동일 |
| `PUT /me/accounts/{accountId}/daily-limit` | 고객(본인) | ✅ 〃 |
| `PUT /ops/accounts/{accountId}/daily-limit` | 운영자 **담당자** · 스코프 안 | ✅ 〃 |

- 경로를 고객/운영자로 가르는 것은 UC-04 선례(주체 계약을 경로가 가른다).
- 이벤트 `LimitChanged`(카드): **주체 혼합 이벤트**(고객∪운영자, 비자금)이므로 **행위자 비포함** — AD-7 ② 확장의 `CardSuspended` 판정(UC-04 UC4-2)이 그대로 적용된다. 행위자는 감사가 나른다.
- 이벤트 계좌 한도 변경분 = **`AccountDailyLimitChanged`** (UC6-1 확정 — account.md §6): 카드 `LimitChanged`와 같은 판정으로 **행위자 비포함**(주체 혼합·비자금 — AD-7 ②).
- 축 필드·스키마 버전(S-9)은 계약 전수 패스로 이월(IS-5).

## 8. 규칙 연결 표

| 단계/예외 | 규칙·조작·전이 |
|---|---|
| §3-1 / §4-A·B / §6 | BR-54·55·58 · IS-4 · AU-3·4·5 |
| §3-2 / §4-E·J | 카드 **INV-3** · BR-05·07·44 (수치 미확정) |
| §3-3 / §4-C·D | 카드 SM **C4**·**CF5**·**CDS1** · `changeLimit(newLimit, requester)` |
| §3-4 / §4-F·I | **BR-46** · 카드 PRE-1·2 · 계좌 PRE-3 · INV-1·INV-4의 검증 위치 |
| §4-G | 카드 RC-1 · 계좌 RC-3 · BR-41 (M10·M11) |
| §4-H / §5-① | BR-44·46 · account.md §5 `changeDailyLimit` (UC6-1 확정) |
| §3-5·6 | BR-57 · AD-2·7·8 · card.md §6 |

## 9. 열린 의문

| # | 의문 |
|---|---|
| ~~UC6-1~~ | ✅ **확정 (2026-08-06 사용자 합의 — 조작 신설)**: `changeDailyLimit(newLimit, requester)` + `AccountDailyLimitChanged`(행위자 비포함 ②) — account.md §5·§6. 계좌판 INV-3(1회≤1일 교차 조건)에 해당하는 것은 **없다** — 계좌 한도는 1일 하나뿐. 하한·0 허용은 UC6-4에 남는다 |
| **UC6-2** | **CDS1 (카드 SM §8 인용)** — *"정지 중인 카드의 한도 변경(C4)을 허용하는 것이 맞는가. 무해하지만, 운영자가 정지된 카드를 만지고 있다는 신호를 놓칠 수 있다"*. 이 UC는 **허용을 전제로 감사에 주체 유형을 남기는 것**(§6)으로 착지시켰으나, 허용 자체의 재확인은 정본 소관이다 |
| **UC6-3** | **두 층의 교차 검증 규칙이 없다.** BR-44 수치 칸은 *"카드 1일 한도보다 크되 카드 수 × 카드 한도보다 작아야 의미가 있다"* 고 하지만 그것은 **의미 조건이지 불변식이 아니다** — 카드 1일 한도를 계좌 1일 한도보다 크게 설정하는 것을 막는 규칙이 어디에도 없다(막아야 하는지도 미판정) |
| **UC6-4** | 한도의 **하한·0 허용 여부**(§4-J) — 0 한도의 의미(사실상 사용 정지)가 정본에 없다. BR-05·44의 수치 미확정 항목과 함께 |

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v0.2 | 2026-08-06 | **UC6-1 닫힘** — 계좌 한도 변경 조작·이벤트 신설 확정 반영(§3·§4-H·§5-①·§7·§8). 남은 의문 = UC6-2~4 |
| v0.1 | 2026-08-05 | 초안 (Phase 4 전수 패스) — 두 층(BR-05·44)·두 주체(BR-46·55)를 한 목표로 묶고, 카드 SM C4·CF5·CDS1과 예외 전수 대응. ★ **정본 공백 발견: 계좌 한도 변경 조작 부재(UC6-1)** · 교차 검증 규칙 부재(UC6-3). §7은 요약 수준 — 상세 블록은 계약 전수 패스로 이월 |
