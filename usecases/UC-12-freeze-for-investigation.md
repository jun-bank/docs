# UC-12. 조사 대상을 보류/해제한다

- 작성일: 2026-08-05
- 상태: **검토대기 (계약 블록 완결)**
- 액터: **운영자 담당자** (BR-54 ② 운영자 주체)
- 권한 요건: **담당자** 단계 (BR-55 배정 표 — 승인 보류/해제 · 미수 보류/해제 2행) · 스코프 = 대상의 계좌 개설 조직 (IS-4). **BR-56 대상 아님** — 가역이고 자금 간접이다
- 트랜잭션 경계: **없음 — 단일 애그리게이트 플래그 전이** (조작 대장에서 `freeze`·`unfreeze` 4건 모두 경계 `—`. 자금 무이동)
- 관련 규칙: BR-28·34·50·54·55·57·58 · 승인 SM T15·F9·F17 · 미수 SM V6·VF3·VF7 · `Receivable` INV-5 · `Authorization`·`Receivable` 조작 사전조건 (참조만 — F-24)

## 1. 목표 (한 문장)

운영자가 조사가 필요한 **승인 또는 미수**를 자동 처리(만료·매입 반영·회수)에서 빼 두었다가, 조사가 끝나면 되돌린다.

> **왜 한 유스케이스인가 (U-1)**: 목표는 *"조사 동안 자동 처리를 멈춘다"* 하나이고, 대상이 둘(승인·미수)로 갈린 것은 BR-28이 **보류 대상을 둘로 나눈 결과**다. 다만 그 둘이 운영 화면에서 섞이면 운영자가 잘못된 쪽을 푼다 — 미수 SM **RCS1**이 열어 둔 의문이며 §9에 인용한다.

## 2. 사전 조건 / 사후 조건

- 사전(승인 보류): 대상 승인이 **종료 상태가 아니다**(`Authorization.freeze` 사전조건 · F9). `REQUESTED`는 트랜잭션 안에만 있어 운영자가 손댈 수 없다(매트릭스 `-`).
- 사전(미수 보류): 대상 미수가 **미결(`OPEN`)** 이다(`Receivable.freeze` 사전조건 · VF7).
- 사전(해제 공통): 대상이 **이미 보류 중**이다(`unfreeze` 사전조건 `frozen = true`).
- 사후(보류 성공): `frozen = true`. **상태는 바뀌지 않는다**(T15 · V6 — 보류는 직교 플래그). 효과는 §3의 세 갈래(만료 제외·매입 격리·회수 제외). 행위자가 감사에 남는다(BR-57).
- 사후(해제 성공): `frozen = false`. ★ **기한은 연장되지 않았다** — 해제 시점에 홀딩 기한이 이미 지났다면 **다음 만료 배치에서 즉시 만료**된다(BR-28). 보류 중 격리된 매입 레코드는 자동 반영되지 않는다 — **UC-14**(2인 승인)를 거친다.
- 사후(거절): 플래그 무변화 + 거절 시도 기록(독립 커밋 — AD-2).

## 3. 주요 성공 시나리오

1. 조사 필요 인지 — 대상 선택(승인 / 미수). 주체 판정: 담당자 단계 · 계좌 개설 조직 스코프 [BR-54·55 · IS-4]
2. **승인 보류** `Authorization.freeze(operator)` — `frozen = true`, 상태 불변 [승인 SM **T15**]
   - 효과 ①: 만료 배치가 이 건을 건너뛴다 — `expire`의 조건이 `frozen = false`다 [T7 · BR-28]
   - 효과 ②: 매입 레코드가 도착하면 **반영하지 않고 격리**된다 [**F17** · BR-50]. ★ 이 격리는 **불일치를 만들지 않고 M1(미매입)으로도 잡히지 않는다** — 의도된 지연이다
   - 효과 ③(비효과): **정산은 막지 않는다** — BR-28의 제외 목록에 정산이 없다 [`markSettled` 열 · T15]
3. **미수 보류** `Receivable.freeze(operator)` — `frozen = true`, 상태 불변 [미수 SM **V6**]
   - 효과: 회수 **대상 선정에서 빠진다**(`recoverable` 목록에 안 들어간다) [`Receivable` **INV-5** · BR-34 예외 ① · VF3]
   - 비효과: `writeOff`(환불에 의한 소멸)는 **막지 않는다** — 우리 조사와 무관한 외부 사실이다 [V4·V5]
4. 조사 수행 (불일치 건이면 **UC-17**과 짝을 이룬다 — 다만 보류 격리는 불일치가 아니다)
5. **해제** `unfreeze(operator)` — 승인은 **종료 상태에서도 허용**(T15 · F9 주석), 미수는 `CLOSED`에서도 허용(V6 · 매트릭스 `CLOSED` 행)
6. 해제 후 자동 처리 복귀 — ★ **만료 기한이 지났으면 다음 배치에서 즉시 만료**(BR-28) · 회수 대상 복귀(BR-34) · **격리 매입은 자동 복귀하지 않는다**(→ UC-14)
7. 보류·해제 각각이 행위자와 함께 감사에 남는다 [BR-57 · AD-8]

## 4. 예외 시나리오 ★

| # | 트리거 | 시스템 반응 → 종결 | 근거 |
|---|---|---|---|
| A | **종료 상태 승인에 `freeze`** (`DECLINED`·`VOIDED`·`REFUNDED`·`SETTLED`) | 거절 `AUTH_TERMINAL` — 조사할 진행 중 처리가 없다 | 승인 SM **F9** |
| B | `REQUESTED` 승인에 `freeze`·`unfreeze` | 경로 부재 — 트랜잭션 안에만 존재해 운영자가 지목할 수 없다 | 매트릭스 `REQUESTED` 행 `-` |
| C | **종결(`CLOSED`) 미수에 `freeze`** | 거절 `RECEIVABLE_CLOSED` — 진행 중 회수가 없다 | 미수 SM **VF7** |
| D | **보류가 아닌 대상에 `unfreeze`** | 거절 — `frozen = true`가 사전조건이다. 이 거절이 멱등 경계다 (⚠️ 리뷰 정정: 처음 인용한 "UC-04 `NOT_SUSPENDED`"는 실재하지 않는 코드였다. 카드의 대응 칸(`NORMAL × resume`)은 당시 ◎ 무시였으나 **2026-08-06 CDS3 통일로 X `ALREADY_NORMAL`(CF10)** 이 되어 이 거절과 같은 판정이 됐다) | `Authorization.unfreeze`·`Receivable.unfreeze` 사전조건 (⚠️ 상태 매트릭스는 `frozen` 축을 세지 않으므로 이 거절은 매트릭스에 안 보인다 — 애그리게이트 사전조건이 정본) |
| E | **보류 중 승인에 매입 레코드 도착** | 반영 안 함 → 격리. **불일치 아님 · M1도 아님** | **F17** · BR-50 · `CaptureBatch` 격리 사유 표 |
| F | **보류 중 승인에 망취소·승인취소 도착** | ★ **통과한다** — 보류는 `expire`·`capture`만 막는다. `frozen = true`인 채 `VOIDED`에 도달한다 | T4·T13 · F9 주석 |
| G | F의 뒤처리 — 종료 상태에 남은 보류 | `unfreeze`가 종료 상태에서도 허용되어 목록에서 내릴 수 있다 | T15 · F9 주석(비대칭의 이유) |
| H | 보류 중 미수가 환불로 `CLOSED` 도달 | 정상 — `writeOff`는 보류가 막지 않는다. `frozen = true`인 채 `CLOSED`가 되므로 **`CLOSED`에서 `unfreeze`만 열려 있다** | V4·V5 · 미수 매트릭스 `CLOSED` 행 |
| I | **보류 전환과 매입 반영의 경합** | 승인 애그리게이트 락에서 하나만 성공. 매입이 이겼으면 운영자에게 *"이미 매입됨"* 을 알린다 | 승인 SM §6 동시성 |
| J | **보류와 회수의 경합** | 보류가 미수 락을 잡는다. 회수가 이겼다면 그 회수는 유효 — 보류 이전에 들어온 자금이다 | 미수 SM §6 동시성 |
| K | 해제 시점에 홀딩 기한이 이미 지남 | ★ **다음 만료 배치에서 즉시 만료** — 기한을 연장하지 않는다 | **BR-28** (검증 방법 ②) |
| L | 보류가 무기한으로 길어짐 | 상한 = `operation.freeze.max-duration` — **수치 미확정**. 격리의 상한이기도 하다 | BR-28 수치 · BR-50 예외 |
| M | 스코프 밖·부재 대상 지목 | 거절 — 응답이 **"없음"과 구별되지 않는다** + 시도 기록 | BR-58 (L7) · IS-4 |
| N | 승인 보류와 미수 보류를 **혼동해 반대쪽을 해제** | ★ **시스템이 막지 못한다** — 둘은 별개 조작이고 각각 유효하다. 화면 구분이 유일한 방어선(RCS1) | BR-28 · 미수 SM **RCS1** (§9) |

## 5. 대체 시나리오

- **대상이 둘인 것 자체가 대체 경로다.** 같은 목표(조사 중 자동 처리 정지)를 **승인 축**(만료·매입을 멈춘다)과 **미수 축**(회수를 멈춘다)으로 각각 이룬다 — 하나로 묶으면 *"승인은 정상인데 그 미수만 조사"* 를 표현할 수 없다(BR-28 주석). 승인과 그 승인에서 발생한 미수를 **둘 다** 보류해야 하는 경우 두 조작을 각각 호출한다.
- 그 밖의 대체 경로 없음 — 자동 처리를 멈추는 다른 수단이 설계에 없다(BR-28이 분쟁·차지백의 최소 대체물이다). *(U-6 근거)*

## 6. 인가·감사 ★

- **판정 순서** (UC-02 §7 선례): ① **단계**(담당자 미달 → `FORBIDDEN_LEVEL` — 자기 속성이라 대상 조회 전에 판정한다) → ② **스코프·존재**(`NOT_FOUND` — 부재·스코프 밖 동일, L7) → ③ **상태·플래그**(§4-A·C·D). 대상을 본 뒤 단계를 판정하면 단계 오류가 존재 탐색 채널이 된다.
- 판정 시점: 운영자 API 진입 시 **1회**(AU-4 — 트랜잭션 안에서 재판정하지 않는다).
- L7: §4-M — 스코프 밖 승인·미수는 부재와 같은 응답. 스코프 축 = 대상의 **계좌 개설 조직**(IS-4 — 승인·미수 둘 다 계좌 귀속이 있으므로 전사 특칙 대상이 아니다).
- 감사(AD-2 경로 규칙): **성공 = 조작 커밋 내 감사 outbox** · **거절 = 독립 커밋**(거절되면 조작 트랜잭션이 롤백되므로) · 유실 시 **M18**(R16).
- 감사 항목(AD-8): 주체·시각·대상(승인ID/미수ID)·**스코프(계좌 소유 축)**·종류(보류/해제)·결과·사유.
- 이벤트에 행위자를 **싣는다** — `Frozen`·`Unfrozen`·`ReceivableFrozen`·`ReceivableUnfrozen`은 구독이 운영인 **운영자 전용 이벤트**다(AD-7 ①. 자금 이벤트·주체 혼합 이벤트와 다르다).
- 승인 흐름: **해당 없음** — BR-56 5조작 목록에 보류/해제가 없다(가역·자금 간접이라 담당자 단독. BR-55 배정 표). *(U-6 근거)*

## 7. 계약 (계약 블록)

> 승인 흐름 없음(§6) — BR-56 5조작 목록 밖이므로 담당자 단독 조작 4건이다.
> 경로를 승인/미수로 **분리**하는 것이 RCS1(두 보류의 혼동)에 대한 계약 층 방어선이다 — 한 엔드포인트에 `targetType`을 받으면 오지목이 조용히 성공한다.

#### `POST /ops/authorizations/{authorizationId}/freeze` — 승인 보류

| 항목 | 내용 |
|---|---|
| 주체 | 운영자 **담당자**(BR-55 배정 표 *승인 보류/해제* 행) · 스코프 = 대상 승인의 **계좌 개설 조직**(IS-4) · 판정 시점 = 진입 1회(AU-4) |
| 요청 | 경로 `authorizationId` · 보류 사유(조사 사유 — BR-57 필수 항목. 코드 체계 **[미정 — UC-04 정지 사유 코드와 같은 체계로 이월]**) |
| 응답(성공) | `authorizationId` · `frozen: true` · **현재 승인 상태**(★ 불변 — T15) · 효과 고지 3종: 만료 배치 제외(T7) · 매입 도착 시 격리(F17, **불일치 아님**) · **정산은 막지 않음**(§3-2 효과 ③). ★ 경합에서 매입이 이겼다면 상태가 `CAPTURED`로 돌아와 *"이미 매입됨"* 이 응답에 드러난다(§4-I — `CAPTURED`는 종료 상태가 아니므로 보류 자체는 성공한다). 반환 범위 = 스코프 안 승인만(BR-58) |
| 오류 표 ★ | **K-1 판정 순서 고정** — ① `FORBIDDEN_LEVEL`(담당자 미달 — 자기 속성이라 **대상 조회 전**) → ② `NOT_FOUND`(부재 · 스코프 밖 · **`REQUESTED` 승인**(§4-B — 트랜잭션 안에만 존재해 지목 자체가 불가, 매트릭스 `-`) **전부 동일 형태**, L7) → ③ `AUTH_TERMINAL`(종료 상태 = `DECLINED`·`VOIDED`·`REFUNDED`·`SETTLED` — **F9 응답 열 정본**) · ★ **`ALREADY_FROZEN`**(§4-D 계열 — 이미 `frozen = true`인 승인에 재보류. **확정: 통합1(CDS3 소급) — 명시 거절**. 무음 수용은 *"내가 방금 걸었다"* 와 *"이미 남이 걸어 뒀다"* 를 구별 못 하게 한다(CF9 논거). ✓ **정본 반영됨** — `authorization.md` **v2.1** `freeze` 사전조건에 `frozen = false` 추가. 신설 코드 대장 등재(K-1 ⑶)) |
| 멱등 | **비멱등 — 재보류는 명시 거절**(`ALREADY_FROZEN`). 플래그 전이가 멱등 경계이고 그 거절이 경계를 드러낸다(K-1 CDS3) |
| 예산 | 실시간 아님(관리 조작) — 단 **승인 애그리게이트 락**을 잡는다(§4-I 경합: 보류 전환과 매입 반영 중 하나만 성공) |
| 감사 | 경로(AD-2) = **성공 → 조작 커밋 내 outbox** · **거절 → 독립 커밋** · 조회 아님(AD-4 해당 없음) · C8 자기 조작 아님(AD-5 해당 없음) · 유실 = **M18**(R16). 항목(AD-8) = `sourceRef`(멱등 키 — AD-3) · 주체 · 시각 · 대상(`authorizationId`) · ★ **스코프 = 판정에 실제 쓰인 값**(그 승인이 걸린 계좌의 개설 조직 `BranchId` — K-4) · 종류(승인 보류) · 결과 · 사유 |

#### `DELETE /ops/authorizations/{authorizationId}/freeze` — 승인 보류 해제

| 항목 | 내용 |
|---|---|
| 주체 | 〃 (보류와 대칭 — 같은 단계·같은 스코프 축) |
| 요청 | 경로 `authorizationId` · 해제 사유(**[미정 — 보류와 같은 체계]**) |
| 응답(성공) | `frozen: false` · 현재 승인 상태(불변) · ★ **기한 미연장 고지** — 홀딩 기한이 이미 지났으면 **다음 만료 배치에서 즉시 만료**된다(§4-K · BR-28) · ★ **격리 매입은 자동 복귀하지 않는다**(→ UC-14 2인 승인) |
| 오류 표 ★ | ① `FORBIDDEN_LEVEL` → ② `NOT_FOUND`(부재·스코프 밖·`REQUESTED` 동일 — L7) → ③ **`ALREADY_UNFROZEN`**(§4-D — `unfreeze` 사전조건 `frozen = true` 위반. ★ **신설 코드**: 정본 금지 표에 응답 열이 없어 **K-1 CDS3 + 카드 CF10 `ALREADY_NORMAL` 명명 패턴**에서 유도했다. ⚠️ 상태 매트릭스는 `frozen` 축을 세지 않으므로 이 거절은 매트릭스가 아니라 **애그리게이트 사전조건이 정본**이다) · ★ **종료 상태 오류는 없다** — `unfreeze`는 종료 상태를 포함해 허용된다(T15 · F9 주석의 의도적 비대칭 — 보류 목록에서 내릴 유일한 경로) |
| 멱등 | **비멱등 — 재해제는 명시 거절**(`ALREADY_UNFROZEN`). 거절이 멱등 경계다(K-1 CDS3) |
| 예산 | 실시간 아님 |
| 감사 | 보류와 동일 경로·항목 · 종류 = 승인 보류 해제 |

#### `POST /ops/receivables/{receivableId}/freeze` — 미수 보류

| 항목 | 내용 |
|---|---|
| 주체 | 운영자 **담당자**(BR-55 *미수 보류/해제* 행) · 스코프 = 미수의 **`accountId` 귀속 계좌의 개설 조직**(IS-4) · 진입 1회 |
| 요청 | 경로 `receivableId` · 보류 사유(**[미정 — 같은 체계]**) |
| 응답(성공) | `receivableId` · `accountId` · `frozen: true` · **현재 미수 상태**(불변 — V6) · 효과 고지: **회수 대상 선정에서 제외**(INV-5 · VF3 — 오류가 아니라 선정 단계에서 빠진다) · 비효과 고지: **`writeOff`는 막지 않는다**(§3-3 · V4·V5). 반환 범위 = 스코프 안(BR-58) |
| 오류 표 ★ | ① `FORBIDDEN_LEVEL` → ② `NOT_FOUND`(부재·스코프 밖 동일 — L7) → ③ `RECEIVABLE_CLOSED`(종결 미수 = **VF7 응답 열 정본**) · ★ **`ALREADY_FROZEN`**(이미 `frozen = true`인 미수에 재보류. **확정: 통합1 — 승인 보류와 한 판정**(갈리면 RCS1(두 보류 혼동)이 계약 층에서 다시 벌어진다). ✓ **정본 반영됨** — `receivable.md` **v1.8** `freeze` 사전조건에 `frozen = false` 추가) |
| 멱등 | **비멱등 — 재보류는 명시 거절**(`ALREADY_FROZEN` — 승인 보류와 동일. K-1 CDS3) |
| 예산 | 실시간 아님 — **미수 애그리게이트 락**(§4-J 경합: 보류가 락을 잡는다. 회수가 이겼다면 그 회수는 유효 — 보류 이전에 들어온 자금이다) |
| 감사 | 경로(AD-2) = 성공 → 커밋 내 outbox · 거절 → 독립 커밋 · 유실 = M18. 항목(AD-8) = `sourceRef` · 주체 · 시각 · 대상(`receivableId`) · ★ 스코프 = **판정에 쓰인 값**(그 미수의 `accountId` 계좌 개설 조직 — K-4) · 종류(미수 보류) · 결과 · 사유 |

#### `DELETE /ops/receivables/{receivableId}/freeze` — 미수 보류 해제

| 항목 | 내용 |
|---|---|
| 주체 | 〃 |
| 요청 | 경로 `receivableId` · 해제 사유(**[미정]**) |
| 응답(성공) | `frozen: false` · 현재 미수 상태(불변) · 효과 고지: **회수 대상 복귀**(BR-34) |
| 오류 표 ★ | ① `FORBIDDEN_LEVEL` → ② `NOT_FOUND`(동일 형태 — L7) → ③ **`ALREADY_UNFROZEN`**(§4-D — 사전조건 `frozen = true` 위반. 승인 쪽과 **같은 신설 코드**) · ★ **`RECEIVABLE_CLOSED`가 없다** — `CLOSED`에서도 `unfreeze`만 열려 있다(V6 매트릭스 · §4-H: 보류 중 미수가 환불로 종결될 수 있고, 그때 내릴 경로가 필요하다) |
| 멱등 | **비멱등 — 재해제는 명시 거절**(K-1 CDS3) |
| 예산 | 실시간 아님 |
| 감사 | 미수 보류와 동일 경로·항목 · 종류 = 미수 보류 해제 |

#### 이벤트 `Frozen` · `Unfrozen` — 승인 보류/해제 성공 시 (기존 — `authorization.md` §6이 정본. 축·버전은 이 계약이 최초 설계)

| 항목 | 내용 |
|---|---|
| payload | `authorizationId` · ★ **운영자(행위자 포함)** — 구독이 운영인 **운영자 전용 이벤트**(AD-7 ① · 색인 §3 *포함 ✓* 확정)이며 정본 payload와 일치 · **축 = `accountId`**(K-4 — 기존 파티션 축 유지. 이 시점이 최초 설계다 — IS-5) · ★ **`ownerId` 비포함**(K-4 보정 확정 — **이벤트 payload에 `ownerId`를 얹지 않는다(전면 비포함)**. IS-1 선반영 6종은 **저장 모델** 대상이고, 운영 이벤트의 스코프는 **소비 시점에 판정**한다) |
| 구독 | **운영(C8)**. ★ 감사 수집은 이 구독이 아니라 **감사 전용 outbox 레코드**가 나른다(AD-2) |
| 멱등 키 | 수신측 유일 제약 = **`sourceRef` = `sourceEventId`**(AD-3 — outbox는 at-least-once) |
| 스키마 버전 | **`schemaVersion: 1`**(K-3). ★ 의미 필드 추가는 버전 올림이 아니라 **새 이벤트 타입**이다(S-6a) |

#### 이벤트 `ReceivableFrozen` · `ReceivableUnfrozen` — 미수 보류/해제 성공 시 (기존 — `receivable.md` §6이 정본)

| 항목 | 내용 |
|---|---|
| payload | `receivableId` · **`accountId`**(정본 이미 포함 — **축 = `accountId`**, K-4) · ★ **운영자(행위자 포함)** — 운영자 전용 이벤트(AD-7 ① — ★ 이 이벤트가 그 선의 **기존 선례**다) · ★ **`ownerId` 비포함**(K-4 보정 확정 — 전면 비포함. IS-1 6종은 저장 모델 대상) |
| 구독 | 운영(C8) · 감사는 AD-2 outbox |
| 멱등 키 | `sourceRef` = `sourceEventId`(AD-3) |
| 스키마 버전 | **`schemaVersion: 1`**(K-3) |

## 8. 규칙 연결 표

| 단계/예외 | 규칙·조작·전이 |
|---|---|
| §3-1 / §4-M / §6 | BR-54·55·57·58 · IS-4 · AU-4 · AD-2·5·8 |
| §3-2 / §4-A·B·F·G·I | BR-28 · `Authorization.freeze`·`unfreeze` · 승인 SM **T15**·**F9**·T7 |
| §3-2 효과 ② / §4-E | BR-50 · 승인 SM **F17** · `CaptureBatch` 격리 사유 표 (→ **UC-14**) |
| §3-3 / §4-C·D·H·J | BR-28·34 · `Receivable.freeze`·`unfreeze` · 미수 SM **V6**·**VF3**·**VF7** · `Receivable` **INV-5** |
| §3-6 / §4-K·L | **BR-28**(해제 후 즉시 만료 · 최대 기간 미확정) · BR-50 예외(격리 상한) |
| §4-N / §9 | BR-28 (보류 2종 분리) · 미수 SM **RCS1** |

## 9. 열린 의문

| # | 의문 |
|---|---|
| UC12-1 | ★ **RCS1 인용 (미수 SM §8 정본)** — *"미수 보류와 승인 보류를 운영자가 어떻게 구분해 보나. 둘은 이제 별개인데(BR-28), 화면에서 섞여 보이면 운영자가 잘못된 쪽을 푼다"*. 본 UC는 **경로 분리**(§7)를 계약 층 방어선으로 제안하지만, **목록 화면의 구분 표기**는 여전히 미정이다 |
| UC12-2 | 보류 최대 기간 `operation.freeze.max-duration` — **미확정 수치**(BR-28). 상한 도달 시 동작(자동 해제인가 통지인가)이 정본에 없다. 자동 해제라면 §4-K(즉시 만료)와 결합해 **조사 중 자금이 풀린다** |
| UC12-3 | 승인 SM **AU6 인용** — 보류 해제 후 재처리 배치의 **주기·트리거**(운영자 건별 지시인가 자동 재투입인가). 본 UC는 §3-6에서 *"자동 복귀하지 않는다"* 로 적었고 UC-14가 건별 지시를 전제한다 — AU6이 자동으로 판정되면 UC-14의 2인 승인 시점이 바뀐다 |
| ~~UC12-4~~ | ✅ **확정 (2026-08-06 — UC14-2와 동시)**: 매입 배치가 격리 사유를 **M8로 자동 재분류**하고 불일치로 적재한다(`reclassifyIsolated` B9 — capture-batch SM·BR-50) |

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v0.5 | 2026-08-06 | **U-2 판정 반영 — 마커 해소·의문 닫기**: 재보류 = `ALREADY_FROZEN` 명시 거절 확정(통합1 — 승인·미수 한 판정. 정본 사전조건 반영됨: authorization v2.1·receivable v1.8) · 이벤트 payload `ownerId` 제거(K-4 보정 — 전면 비포함) |
| v0.4 | 2026-08-06 | **계약 전수 — §7 상세 블록**(요약 표 → API 4블록 + 이벤트 2블록. K-1 판정 순서·SM 응답 열 코드(F9 `AUTH_TERMINAL`·VF7 `RECEIVABLE_CLOSED`)·`ALREADY_UNFROZEN` 신설(CDS3+CF10 유도)·축/`ownerId`/`schemaVersion: 1`·AD-2 경로 4종·K-4 감사 스코프. ★ 판정 대기 1건 = **재보류(`frozen = true`)의 응답** — 승인·미수 사전조건 양쪽에 `frozen = false`가 없어 유도 불가. **v0.5에서 해소**) |
| v0.3 | 2026-08-06 | **UC12-4 닫힘** — 자동 재분류(UC14-2 확정)의 착지 반영 |
| v0.1 | 2026-08-05 | 초안 — Phase 4 전수 패스. 예외 14종 = 승인 SM(F9·F17·T15 비대칭) + 미수 SM(VF3·VF7·V6) + 애그리게이트 사전조건(§4-D — 매트릭스에 안 보이는 거절) + BR-28 즉시 만료 + L7. §7은 요약(상세는 계약 전수 패스로 이월) |
