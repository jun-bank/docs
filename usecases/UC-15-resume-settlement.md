# UC-15. 실패한 정산을 강제 재개한다

- 작성일: 2026-08-05
- 상태: **검토대기 (계약 블록 완결)**
- 액터: **운영자 2인** — 요청 = 담당자(maker) · 승인 = 책임자(checker) (BR-55 · 같은 사람 불가)
- 권한 요건: **BR-56 ④** (책임자 승인 조작) · 스코프 = ★ **전사 스코프만** (BR-55 특칙 — 대상이 영업일이라 고객·조직 귀속 축이 없다)
- 트랜잭션 경계: ★ **E1~E5 아님 — 자금 무이동** (커밋은 둘: ① C8 `consume` + 재개 지시 이벤트 outbox INSERT가 같은 커밋 ② C4 수신측 `resumeByOperator` 커밋 — **R15**, 원자적으로 묶이지 않는다)
- 관련 규칙: BR-37·52·53·54·55·56·57·58 · Settlement INV-4 · 정산 SM S6·SF1·SF6·SF8·SF10 · ApprovalRequest INV-1~4·A1~A5 · ADR-014 O-13·O-16 · ADR-017 AD-2·5·6·8 (참조만 — F-24)

## 1. 목표 (한 문장)

자동 재시도가 소진되어 `FAILED`로 굳은 영업일의 정산을 **2인 승인을 거쳐 재개**해, 그날 순액이 확정될 수 있게 한다.

## 2. 사전 조건 / 사후 조건

- 사전: 대상 정산이 `FAILED` **이고** `retryCount ≥ 임계`다 (S6 사전조건 — 임계 미만이면 SF10). 요청자가 **전사 스코프** 담당자다. 임계값은 **[미확정]**(BR-37 — 미확정 수치 표).
- 사후(성공): 승인 `CONSUMED` + 지시 이벤트 발행(**같은 커밋** — R15) → C4 수신 → `retryCount = 0` · 상태 `CALCULATING` · **사유 기록 필수** · `SettlementResumedByOperator` 발행 · 감사 기록. ★ **이 UC의 목표는 재개까지다** — 그 뒤 `calculate` 성공 여부는 정산 배치 경로(운영 절차)에 속한다.
- 사후(거절/만료): 정산 상태 **무변화**. 요청이 `REJECTED`/`EXPIRED`(종료 — 재사용 불가), 사유가 감사에 남는다.
- 사후(승인은 됐으나 지시가 정체): 승인은 이미 `CONSUMED`인데 재개는 일어나지 않았다 — ★ **유실이 아니라 정체**(M17)다. **새 승인 없이 재투입**이 경로이고(UC-19), 이중 실행은 **지시 ID 멱등**이 막는다.
- 사후(수신측 거절 — 대상 상태가 바뀌었다): 정산 무변화 + 거절 사실이 감사에 남는다(§4-H).

## 3. 주요 성공 시나리오

1. 담당자 A가 `escalate`로 운영자 목록에 오른 미결 정산을 확인한다 [S5 · BR-53 ④ 통지]
2. A → C8 `request(정산 강제 재개, **지시 ID** · businessDate · 사유)` — ★ 지시 ID는 maker가 생성해 요청 인자에 싣는다(UC-16과 통일 — INV-3 검사 대상이 되고, EXPIRED 후 재요청은 새 ID) — `PENDING` [A1 · 감사 직접 append]
3. 책임자 B → C8 `approve(B)` — **B ≠ A** 검사 → `APPROVED` [A2 · INV-1]
4. ★ 승인 시점에 C8이 **`consume` + 재개 지시 이벤트 outbox INSERT를 같은 커밋**으로 수행 [A4 ④ 경로 · **R15** · AD-6] — **실행 엔드포인트는 없다**
5. 릴레이가 지시 이벤트를 C4로 전달 — 계좌ID 없는 이벤트라 **파티션 0** [ADR-014 O-6·O-8]
6. C4 수신 — **지시 ID 멱등** 검사 후 `resumeByOperator(operator, reason)` 호출. 기술 실행자는 수신 시스템이고 **업무 귀속 = maker**(지시 이벤트에 maker·checker가 실린다) [A4 · settlement.md §4]
7. `retryCount = 0` · `CALCULATING` · `SettlementResumedByOperator` 발행 · 감사 outbox (커밋 내 — AD-2) [S6]
8. 이후 정산 배치가 `calculate`로 `COMPLETED`에 도달한다 — **이 UC 밖**(운영 절차)

## 4. 예외 시나리오 ★

| # | 트리거 | 시스템 반응 → 종결 | 근거 |
|---|---|---|---|
| A | **A가 자기 요청을 승인** | 거절 — 상태 무관 조건 위반 | ApprovalRequest INV-1 · AF7 |
| B | **승인 없이 재개 시도** | ★ **경로 자체가 없다** — 운영자용 재개 엔드포인트가 존재하지 않고, C4는 **지시 이벤트로만** 트리거된다 | AF1 · R15 · settlement.md `resumeByOperator` 사전조건 |
| C | 대상 정산이 `PENDING`·`CALCULATING`·`COMPLETED` | 거절 `SETTLEMENT_NOT_FAILED` — 실패한 적 없는 건의 재개는 근거가 없다 | **SF8** |
| D | `FAILED`이지만 `retryCount < 임계` | 거절 `SETTLEMENT_RETRY_REMAINING` — ★ 허용하면 `retryCount = 0` 초기화가 **SF6의 임계 자체를 우회**한다 | **SF10** |
| E | **승인 후 인자(businessDate) 변경 실행** | `consume` 거절 — 실행 인자 ≠ 요청 인자 | INV-3 (바꿔치기 방지) |
| F | 지시 이벤트 **중복 전달**(at-least-once) | 수신측이 **지시 ID 멱등**으로 무연산 — ★ 두 번 재개되면 `retryCount`가 또 0으로 밀려 SF6 임계가 무의미해진다 | R15 · A4 · ADR-014 O-1 |
| G | 지시 이벤트가 **DLQ에 갇힘** | 승인 `CONSUMED` 유지 · **M17 적재** · 재투입이 유일 경로(UC-19) — **새 승인 불요** | ADR-014 O-12·O-13 · approval-request §5 주석 |
| H | 승인~수신 사이에 대상이 `COMPLETED`가 됨 | 수신측 거절 `SETTLEMENT_NOT_FAILED` — ★ **확정 순액은 되돌리지 않는다**. 지시의 종결 처리는 §9 UC15-2 | **SF1**(INV-4) · SF8 |
| I | 책임자 **거절** | `REJECTED` + **사유 필수** — 감사에 남는다 | A3 |
| J | 승인 대기 상한 경과 | `EXPIRED` — 재요청 필요 (상한 **[미확정]**) | A5 · AR1 |
| K | **지점 스코프 운영자**가 요청 | 거절 — 전사 스코프 조작이다. ★ 판정은 **요청자 자기 속성**이라 대상 조회 전에 끝난다 | BR-55 특칙 |
| L | 재개 후 **같은 원인으로 재실패** | `fail` → `retryCount` 다시 증가 → 임계 초과 → `escalate`. 원인 미해결이면 이 UC가 반복된다 — 통지 임계가 그 신호다 | S3·S5 · BR-53 ④ (수치 **[미확정]**) |
| M | 드레인 미완 상태에서 재개 | ★ **막지 않는다** — 드레인은 **대사**의 전제조건(BR-52 ⑤)이지 정산 산출의 조건이 아니다. 재개해도 대사는 M16을 적재하며 안 돈다 | BR-52 · BR-42 (층위 구분) |

## 5. 대체 시나리오

- **임계 도달 전 자동 재시도**(S4 `retry`) — 목표(순액 확정)는 같고 주체가 시스템이다. 임계 안에서 성공하면 이 UC는 발생하지 않는다. 임계를 넘으면 자동 경로는 닫히고 **이 UC가 `FAILED`의 유일한 탈출구**가 된다(SF6 주석).

## 6. 인가·감사 ★

- 주체 판정 순서: ★ **① 단계(담당자/책임자) + 전사 스코프 — 둘 다 요청자의 자기 속성이라 대상 조회 전에 판정**(FORBIDDEN_LEVEL이 대상 존재 탐색 채널이 되지 않는다 — UC-02 선례) → **② 대상 정산 존재** → **③ 상태·`retryCount`**(SF8·SF10).
- L7: ★ **존재 비노출의 대상이 아니다** — 대상 식별자가 **영업일**이라 추측 가능한 공개 축이고 고객·조직 귀속이 없다(BR-58의 소유 축이 성립하지 않는다). 대신 **거절 시도는 기록**한다(독립 커밋 — AD-2). *(U-6 근거)*
- 감사 경로: 요청·승인·거절·만료 = **C8 직접 append**(AD-5) / C4 재개 실행 = **커밋 내 outbox**(AD-2 · R13) / 거절·실패 = **독립 커밋**(AD-2) / 목록 조회 = 비동기(AD-4).
- 항목: AD-8 — 주체·시각·대상(영업일)·스코프·종류·결과 + **사유·요청자·승인자**(BR-56 조작).

## 7. 계약 (계약 블록)

> **공통 승인 계약의 정본 = UC-02 §7** (`POST /ops/approval-requests` · `/{id}/approve` · `/reject` · `GET /ops/approval-requests`) — 복사하지 않고 참조한다(U-7 · F-24). 이 UC가 정의하는 것은 **④의 조작별 인자**와 ★ **R15 지시 이벤트**다.
> ★ **실행 엔드포인트가 없다**(R15) — 승인 커밋이 `consume` + 지시 이벤트 outbox INSERT를 함께 수행하고(A4 ④), **실행 1회는 수신측 지시 ID 멱등**이 보장한다.
> 오류 코드 3계층 = K-1 ⑴ 공통 4종(`FORBIDDEN_LEVEL`·`NOT_FOUND`·`INVALID_STATE`·`ARG_MISMATCH`) / ⑵ ★ **정산 SM 금지 표의 응답 열이 정본**(SF1·SF8·SF10 — 계약이 재발명하지 않는다) / ⑶ 고유 신설 **없음**.
> 운영자 화면 전환도는 공통이다 — **UC-02 §7 참조**(승인 대기 목록 → 요청 상세 → 승인/거절). ★ 단 ④에는 *"실행 확인"* 화면이 없다(실행 엔드포인트 부재).

#### `POST /ops/approval-requests` — ④ **정산 강제 재개** 요청 (조작별 인자·오류만 — 공통 계약은 UC-02 §7)

| 항목 | 내용 |
|---|---|
| 주체 | 운영자 **담당자** 이상 · ★ **전사 스코프**(BR-55 특칙 — 대상이 영업일이라 조직 축이 없다) · 판정 = 진입 1회(AU-4). 단계·스코프 **둘 다 요청자 자기 속성**이라 대상 조회 전에 끝난다(§6) |
| 요청 | 대상 조작 = **정산 강제 재개**(BR-56 ④ 목록 안) · 인자 = ★ **`instructionId`**(maker 생성 — INV-3 검사 대상 · `EXPIRED` 후 재요청은 **새 ID**) · `businessDate` · **사유(필수)** |
| 응답(성공) | 요청 ID · `PENDING` · 반향된 `instructionId`(재투입·추적의 키 — UC-19 §3-1의 `subject`와 대조된다) |
| 오류 표 ★ | **K-1 판정 순서** ① `FORBIDDEN_LEVEL` — 단계 미달 **및 전사 스코프 미보유**(§4-K): 둘 다 자기 속성이고 전사 조작엔 L7 축이 없어 **코드를 가르지 않는다**(★ 확정 **P-6** — `FORBIDDEN_SCOPE` 신설 없음. K-1 ⑶ · **UC-14·18·19와 통일**) → ② ★ **없다** — 대상 정산의 **존재·상태를 C8이 볼 수 없다**(확정 **D-㉡**: 정산 상태의 **권위는 수신측(C4) 단독** · 아래) → ③ `INVALID_OPERATION`(BR-56 목록 밖) · `ARG_MISMATCH`(`businessDate`·`instructionId` 형식·필수 사유 누락) · ★ **`DUPLICATE_INSTRUCTION`**(같은 `instructionId` 재요청 — **확정 D-유일**: `instructionId`는 **요청 단계 유일**. 정본 = **UC-02 §7 v0.7**(④⑤ 공통 — 여기서는 인용만). 신설 코드 대장 등재) |
| 멱등 | 공통 계약 상속 = **비멱등**(UC-02 §7 · UC2-1 — 중복 `PENDING`은 방치 통지가 잡는다). ★ 단 `instructionId`가 **maker 생성**이라 같은 값의 요청 둘이 승인되면 두 번째 지시가 수신측에서 **무연산**이 된다 — 그래서 **요청 단계에서 유일 제약으로 끊는다**(`DUPLICATE_INSTRUCTION` — 무연산이 승인 뒤로 미뤄지면 *"승인했는데 아무 일도 안 일어났다"* 는 **상태 오인**이 된다) |
| 예산 | 실시간 아님 (관리 조작) |
| 감사 | ★ **C8 자기 조작 = 자기 저장소 직접 append**(AD-5) · AD-8 = `sourceRef`(요청 식별자 — AD-3) · 주체(요청자) · 시각 · 대상(`businessDate`) · ★ **스코프 = `전사`**(K-4 — 판정에 실제 쓰인 값) · 종류 · 결과 + **사유·요청자**. **거절 = 독립 커밋**(AD-2 · §4-K) |

> ★ **확정 (D-㉡) — 요청 시점에 대상 정산의 존재·상태(`FAILED`·`retryCount ≥ 임계`)를 검사하지 않는다. 권위는 수신측(C4) 단독이다.**
> C4는 **별도 배포**이고 R15가 존재하는 이유가 *"배포 경계를 넘는 동기 호출·원자 커밋을 만들지 않는다"*(ADR-010)이다 — 따라서 C8은 요청 시점에 정산 상태를 **볼 수 없다**. SF8·SF10 거절은 전부 **승인 이후 수신측**에서 일어나고(§4-C·D·H), 거절은 **감사·불일치(M) 적재로 관측**된다.
> ⚠️ **승인 낭비를 감수한다** — 2인 승인을 다 받은 요청이 수신 시점에 거절될 수 있고, 그 종결 처리는 **§9 UC15-2**(정본 공백)로 남는다.
> ★ **화면이 무엇을 정산 상태 소스로 쓰는가는 후속이다**(D-㉡ — `escalate` 목록(§3-1)의 소스가 구독 복제인지 조회인지. **참고 표시일 뿐 권위가 아니므로** 이 계약은 그것에 의존하지 않는다. C8→C4 관계 신설은 이 패스에서 하지 않는다 — 신설된 관계는 R17·R18 둘뿐이다).

> ★ **확정 (D-유일) — 같은 `instructionId`로 두 번 요청하면 `DUPLICATE_INSTRUCTION` 명시 거절.**
> C8이 `instructionId`에 **요청 단계 유일 제약**을 건다 — K-1 CDS3의 *"무음 무시는 상태 오인을 만든다"* 와 같은 근거로, **무연산이 승인 뒤로 미뤄지는 것을 요청 단계에서 끊는다**(제약이 없으면 두 번째 지시가 수신측 무연산(§4-F)으로 사라지고 승인만 소비된다). 정본 착지 = **UC-02 §7 v0.7**(④⑤ 공통 계약) — 이 UC는 인용만 한다.

#### `POST /ops/approval-requests/{id}/approve` · `/reject` — ④ 승인/거절 (정본 = UC-02 §7 · 이 UC의 차이만)

| 항목 | 내용 |
|---|---|
| 주체 | 운영자 **책임자** · **전사 스코프** · ★ `checker ≠ maker`(INV-1 · AF7) |
| 요청 | (`reject`는 **사유 필수** — A3) |
| 응답(성공) | 요청 ID · 전이 상태(`APPROVED`/`REJECTED`) · ★ **승인 응답의 의미는 "지시가 발행됐다"까지다** — 같은 커밋에서 `consume` + 지시 outbox INSERT가 끝난다(R15 · A4 ④). **재개 완료를 뜻하지 않는다**(§2 사후 — 정체는 M17) |
| 오류 표 ★ | ① `FORBIDDEN_LEVEL`(단계·전사 — 자기 속성) → ② `NOT_FOUND`(요청 ID 부재·스코프 밖 **동일 형태** — L7) → ③ `SELF_APPROVAL`(INV-1 · AF7 — 스코프 안 ID에만 반환되므로 탐색 채널이 아니다) · 종료 상태 재호출 = **`ALREADY_APPROVED`·`ALREADY_REJECTED`·`ALREADY_CONSUMED`·`ALREADY_EXPIRED`**(AF2~AF6) |
| 멱등 | 상태 전이가 멱등 경계 — 종료 상태는 위 `ALREADY_*`(무음 무시 금지) |
| 예산 | 실시간 아님. ★ 승인 커밋은 **C8 단일 커밋**(consume + outbox INSERT)이라 배포 경계를 넘지 않는다 |
| 감사 | **AD-5 직접 append** + 승인자·사유. ★ **지시 발행 사실도 같은 커밋에 남는다**(대상 = `instructionId`) — 승인과 지시가 갈라지면 §4-G의 정체를 추적할 축이 없다 |

> ★ **확정 (통합1 CDS3 소급) — 종료 상태 재호출은 `ALREADY_*` 4종이다**(`ALREADY_APPROVED`·`ALREADY_REJECTED`·`ALREADY_CONSUMED`·`ALREADY_EXPIRED` — 구 `INVALID_STATE` 단일 코드 대체).
> ✓ **정본 반영됨** — **UC-02 §7 v0.7**(공통 계약 정본 개정) + `approval-request.md` SM 주석(비어 있던 K-1 ⑵ 응답 열의 짝). 근거: *"이미 소비됨"* 과 *"만료됨"* 은 운영자에게 **다른 행동을 요구**하므로 무엇이었는지가 응답에 보여야 상태 오인이 없다. 이 UC는 인용만 한다.

#### 이벤트 (신설) `SettlementResumeInstructed` — ★ **R15 지시 이벤트** · 승인 커밋과 **같은 커밋**에 outbox INSERT

> ★ **이름 확정 (D-이름) = `SettlementResumeInstructed`** — K-2 *"과거분사 사실명"*. C8이 낸 사실은 *"재개가 지시되었다"* 이고, 결과 이벤트 `SettlementResumedByOperator`(C4 발행)와 **지시/결과가 이름으로 갈린다**(`SettlementResumeApproved`는 `ApprovalRequest`의 `APPROVED` 상태와 같은 말이 되어 C8 내부 상태 전이와 컨텍스트 간 지시가 한 이름으로 겹친다 — 채택하지 않는다). ✓ **색인 등재됨**(`usecases/README.md` §3 — UC-16의 `JournalReversalInstructed`와 **같은 규칙으로 동시 확정**).

| 항목 | 내용 |
|---|---|
| payload | ★ **`instructionId`**(멱등 키) · `approvalRequestId` · **`businessDate`**(조작 인자 — INV-3으로 요청 인자와 동일 보장) · **`maker`**(요청자 = ★ 업무 귀속 — A4 ④) · **`checker`**(승인자) · **`reason`** · `approvedAt`(승인 커밋 시각) — ★ **행위자 포함**(AD-6 · AD-7: C8 지시 이벤트는 **원장 번역 입력이 아니므로** maker·checker를 싣는다). 축 필드 = ★ **없다** — 계좌ID 없는 이벤트라 **파티션 0**(ADR-014 O-6·O-8) · ★ **`ownerId` 비포함**(K-4 보정 확정 — **이벤트 payload 전면 비포함**. 운영 이벤트의 스코프는 **소비 시점 판정**이고 IS-1 6종은 저장 모델 대상이다) · 테넌트 축은 형태만(IS-7) |
| 구독 | **C4 정산** 단독. ★ 감사는 구독이 아니다 — C8 자기 조작이라 직접 append(AD-5 · AD-2 단서) |
| 멱등 키 | ★ **`instructionId`** — 수신측 `resumeByOperator(instructionId, operator, reason)`가 **수신 기록·유일**로 소유하고 **재수신 = 무연산**(settlement.md §4 v1.8 · §4-F). at-least-once 전제(ADR-014 O-1) · DLQ 재투입도 같은 키로 막힌다(§4-G · UC-19 §4-G) |
| 스키마 버전 | ★ **`schemaVersion: 1`**(K-3). 의미 필드 추가는 버전 올림이 아니라 **새 이벤트 타입**(S-6a) |

#### (수신) C4 `resumeByOperator` — 지시 이벤트 처리 계약 ★ **엔드포인트가 아니다**

| 항목 | 내용 |
|---|---|
| 주체 | **시스템**(릴레이 경로 — 경로 식별자 필수 · C-1 · BR-54 ③). ★ 업무 귀속 = payload의 `maker`(A4 ④) |
| 요청 | 지시 이벤트 payload 그대로. **다른 호출 표면이 없다**(§4-B — 운영자용 재개 엔드포인트 부재) |
| 응답(성공) | ★ **응답 상대가 없다**(비동기). 사후 = `retryCount = 0` · `CALCULATING`(S6) · `instructionId`·사유 저장 · `SettlementResumedByOperator` 발행 · 감사 outbox — **한 커밋** |
| 오류 표 ★ | ★ 아래는 응답 코드가 아니라 **감사·불일치에 남는 거절 사유 값**이다. **정산 SM 금지 표의 응답 열이 정본**(K-1 ⑵): `SETTLEMENT_NOT_FAILED`(**SF8** — `PENDING`·`CALCULATING`·`COMPLETED`. §4-C · 승인~수신 간극의 §4-H) · `SETTLEMENT_RETRY_REMAINING`(**SF10** — 임계 미만. §4-D) · 대상 영업일 부재 = `NOT_FOUND`(★ L7 ✕ — 축이 없어 **부재만** 있다) · ★ **중복 `instructionId`는 오류가 아니라 무연산**(§4-F) |
| 멱등 | ★ **`instructionId` 유일 — 무연산**(원장 INV-9와 같은 형태). 두 번 재개되면 `retryCount`가 또 0으로 밀려 SF6 임계가 무의미해진다(§4-F) |
| 예산 | 실시간 아님 — 비동기 릴레이. 전달 지연·재시도·DLQ 상한은 **ADR-014 소관**(O-10·O-11) |
| 감사 | 성공 = **커밋 내 outbox**(AD-2 · R13) — 주체 = 경로 식별자 **+ 업무 귀속 `maker`**(두 주체) · 대상 = `businessDate` · **스코프 = `전사`**(K-4) · 결과 + 사유·요청자·승인자(BR-56 조작). **거절(SF8·SF10) = 독립 커밋**(AD-2) — ★ 거절 이후의 **종결 처리는 정본 공백**(§9 UC15-2: 감사만 남기고 폐기인가, 불일치로 적재인가) |

#### 이벤트 `SettlementResumedByOperator` (기존 — **정본 = settlement.md §4.1**. 계약 확인 블록)

| 항목 | 내용 |
|---|---|
| payload | `businessDate` · **운영자** · **사유** ✓ 정본과 일치 — ★ **행위자 포함**(AD-7 ① 운영자 전용 이벤트 — 구독이 운영이다). 축 필드 = 없다(파티션 0) · ★ **`ownerId` 비포함**(K-4 보정 — 전면 비포함) · ⚠️ **[정본 이월 — `settlement.md` §4.1 소관]**: `instructionId`를 payload에도 싣는지(지시↔결과 대조와 §4-G 정체 추적이 쉬워진다 — 권고 = 싣는다). ★ **U-2 판정 대장에 없는 항목**이며, 정본 소관이라 이 UC가 확정하지 않는다 |
| 구독 | **운영**(정본). 감사는 C4 커밋 내 감사 outbox가 별도로 나른다(AD-2) |
| 멱등 키 | 수신측 = 알림 경로뿐이라 상태 변경이 없다(무해). ★ 이 이벤트의 **1회성은 발신측 `instructionId` 무연산**이 이미 보장한다 |
| 스키마 버전 | **`schemaVersion: 1`**(K-3 — 전 이벤트 일괄) |

### 이 §7의 판정 착지 (U-2 반영 — 2026-08-06)

| # | 질문 | ★ 결정 |
|---|---|---|
| ㉠ | 지시 이벤트 이름 | ✅ **`SettlementResumeInstructed`**(D-이름 — 결과 이벤트와 이름이 갈린다. 색인 등재됨. UC15-1 닫힘) |
| ㉡ | 요청 시점 정산 상태 선검사 경로(C8→C4) | ✅ **수신측(C4) 단독 권위** — 선검사 없음, 거절은 감사·M 적재로 관측. **화면 소스는 후속**(관계 신설 없음) |
| ㉢ | 같은 `instructionId` 재요청 | ✅ **요청 단계 유일 — `DUPLICATE_INSTRUCTION`**(D-유일. 정본 = UC-02 §7 v0.7) |
| ㉣ | 종료 상태 재호출 코드 | ✅ **`ALREADY_*` 4종**(통합1 CDS3 소급 — UC-02 §7 v0.7 개정 + approval-request SM 주석) |
| ㉤ | `SettlementResumedByOperator` payload에 `instructionId` 추가 여부 | ⏳ **정본 이월** — `settlement.md` §4.1 소관(U-2 대장에 없음. 권고 = 추가) |
| — | 수치 = 대기 상한(A5·AR1) · `retryCount` 임계(BR-37) · 재실패 통지 임계(BR-53 ④) 전부 **[미확정]** | — (이 패스는 수치를 확정하지 않는다) |

## 8. 규칙 연결 표

| 단계/예외 | 규칙·조작 |
|---|---|
| §3-2·3 / §4-A·E·I·J | BR-55·56 · ApprovalRequest A1·A2·A3·A5 · INV-1·3 |
| §3-4·5 / §4-B·F·G | ★ **R15**(코어 밖 조작 = consume + 지시 이벤트 같은 커밋) · A4 · AD-6 · ADR-014 O-6·O-8·O-13 |
| §3-6·7 / §4-C·D·H·L | BR-37 · `resumeByOperator` · 정산 SM **S6 · SF1 · SF6 · SF8 · SF10** · Settlement INV-4 |
| §4-K / §6 | BR-54·55(전사 특칙)·57·58 · AD-2·4·5·8 |
| §4-M | BR-42 · BR-52 (트리거 ↔ 전제조건의 층위) |
| (이 UC가 쓰지 않는 금지 전이) | SF2·SF3·SF4·SF5·SF7·SF9·SF11 = `close`·`calculate`·`fail`·`retry`·`escalate` 경로 — **정산 배치(운영 절차)** 소관 *(U-3 양방향 확인)* |

## 9. 열린 의문

| # | 의문 |
|---|---|
| ~~UC15-1~~ | ✅ **닫힘 (2026-08-06 U-2 — D-이름 · K-3)**: 이름 = **`SettlementResumeInstructed`** · payload = `instructionId`·`approvalRequestId`·`businessDate`·`maker`·`checker`·`reason`·`approvedAt`(행위자 포함 — AD-6 · `ownerId` 비포함) · `schemaVersion: 1`. 색인 §3 등재됨 |
| UC15-2 | ★ 승인~수신 간극에서 대상 상태가 바뀌어 **수신측이 지시를 거절**했을 때의 종결 — 감사만 남기고 폐기인가, 불일치로 적재인가. **정본 공백**(approval-request AR2 "APPROVED 유효기간"과 인접) |
| UC15-3 | 재개→재실패가 반복될 때의 상한·통지 임계 — BR-53 ④ **[미확정]** |
| UC15-4 | `retryCount` 임계값 — BR-37 **[미확정]** (미확정 수치 표) |

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v0.3 | 2026-08-06 | **U-2 판정 반영 — 마커 해소·의문 닫기**: 지시 이벤트 이름 확정(D-이름 — **UC15-1 닫힘**) · 정산 상태 = 수신측 단독 권위·화면 소스 후속(D-㉡) · `instructionId` 요청 단계 유일 = `DUPLICATE_INSTRUCTION`(D-유일 · 정본 = UC-02 §7 v0.7) · 종료 상태 재호출 = `ALREADY_*` 4종(통합1) · 전사 미보유 = `FORBIDDEN_LEVEL`(P-6) · 이벤트 `ownerId` 비포함 명시(K-4 보정). ㉤는 정본(settlement.md) 이월 |
| v0.2 | 2026-08-06 | **계약 전수 — §7 상세 블록** (요약 표 → 계약 블록 5종 · K-1~4 적용 · ★ **R15 지시 이벤트 계약 신설**(`SettlementResumeInstructed` — payload·`instructionId` 멱등·`schemaVersion: 1`) · 수신 처리 계약(SF8·SF10 = 거절 사유 값) · 판정 대기 5건) |
| v0.1 | 2026-08-05 | 최초 작성 — Phase 4 전수 패스. ★ **R15 경로의 첫 유스케이스**(실행 엔드포인트 부재 · consume+지시 이벤트 같은 커밋 · 지시 ID 멱등) · SF10/SF8/SF1 대응 · 전사 스코프 특칙 |
