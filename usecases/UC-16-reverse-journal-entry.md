# UC-16. 원장 오기표를 역분개로 정정한다

- 작성일: 2026-08-05
- 상태: **초안 (계약 블록 후속)** — §7은 요약 수준, 상세 블록은 계약 전수 패스로 이월
- 액터: **운영자 2인** — 요청 = 담당자(maker) · 승인 = 책임자(checker) (BR-55 · 같은 사람 불가)
- 권한 요건: **BR-56 ⑤** (책임자 승인 조작 — 감독규정 제28조 ② 직접 대상) · 스코프 = ★ **계좌 유도**(2026-08-06 UC16-1 확정 — 전표 `subjectId` 계좌들의 개설 조직 **전부**가 요청자 스코프 안이어야 허용, `subjectId` 없는 전표는 **전사만**. BR-55 유도 특칙)
- 트랜잭션 경계: ★ **E1~E5 아님 — 자금 무이동**(BR-40: 기표는 자금 이동과 별도 논리 단위). 커밋은 둘 — ① C8 `consume` + 정정 지시 이벤트 outbox INSERT가 같은 커밋 ② C5 `reverse` 커밋(전표 하나 = 한 트랜잭션) — **R15**
- 관련 규칙: BR-10·40·47·54·55·56·57·58 · JournalEntry INV-1·2·4·5·6·9 · ApprovalRequest INV-1~4·A1~A5 · Settlement INV-4 · ADR-014 O-13 · ADR-017 AD-2·5·6·8 (참조만 — F-24)

## 1. 목표 (한 문장)

잘못 기표된 전표를 **원전표를 건드리지 않고**, 2인 승인을 거쳐 **역분개 전표를 추가**해 정정한다.

## 2. 사전 조건 / 사후 조건

- 사전: 원전표가 존재한다(`reverse` 사전조건). 오기표라는 판단 근거가 있다 — 대개 대사 불일치(M6·M12·M14 등)나 조사 결과(UC-17). 요청자 = 전사 담당자.
- 사후(성공): **새 전표 1건** — `reversalOf` = 원전표 · 차대 반대·동액(INV-5) · `businessDate` = **정정 시점의 영업일**(INV-6 · BR-47) · `instructionId` 저장(INV-9) · `JournalReversed` 발행 · 승인 `CONSUMED` · 감사.
- 사후(성공) ★ **원전표는 그대로다**(INV-2 · BR-10) — 수정·삭제 조작이 애초에 없다.
- 사후(성공) ★ **마감된 영업일의 순액은 움직이지 않는다** — 역분개가 정정 시점 영업일에 잡히기 때문이다(BR-47 · Settlement INV-4).
- 사후(거절/만료): 원장 **무변화** · 요청 `REJECTED`/`EXPIRED` · 사유가 감사에 남는다.
- 사후(승인 후 지시 정체): 승인 `CONSUMED`인데 전표는 없다 — **정체(M17)**이며 재투입이 경로(UC-19). 재투입해도 **INV-9가 이중 기표를 막는다**.

## 3. 주요 성공 시나리오

1. 담당자 A가 오기표를 식별한다 — 근거 = 대사 불일치 또는 조사 결과 [BR-41 · UC-17]
2. A → C8 `request(원장 역분개, instructionId · 원전표 ID · 사유)` — `PENDING` [A1 · 감사 직접 append]
3. 책임자 B → `approve(B)` — **B ≠ A** → `APPROVED` [A2 · INV-1]
4. ★ 승인 시점에 C8이 **`consume` + 정정 지시 이벤트 outbox INSERT를 같은 커밋**으로 [A4 ⑤ 경로 · **R15** · AD-6] — **실행 엔드포인트는 없다**
5. 릴레이가 C5로 전달 — 계좌ID 없는 이벤트라 **파티션 0** [ADR-014 O-6·O-8]
6. C5 `reverse(instructionId, originalId, at, operator)` — **INV-9 유일 검사** → 새 전표 생성 [journal-entry §4]
7. `JournalReversed` 발행 · 감사 outbox(커밋 내 — AD-2). 업무 귀속 = maker, 기술 실행자 = C5 [A4]

## 4. 예외 시나리오 ★

| # | 트리거 | 시스템 반응 → 종결 | 근거 |
|---|---|---|---|
| A | **A가 자기 요청을 승인** | 거절 | ApprovalRequest INV-1 · AF7 |
| B | **승인 없이 역분개 시도** | ★ **경로가 없다** — `reverse`는 **승인된 정정 지시 이벤트로만** 트리거된다 | AF1 · R15 · journal-entry §4 사전조건 |
| C | 원전표 없음 | 거절 — 근거 없는 정정 차단 | `reverse` 사전조건 |
| D | 같은 `instructionId` 재도착 (재전달·DLQ 재투입) | ★ **무연산** — 유일 제약이 두 번째를 막는다. 막지 못하면 되돌리는 수단이 **또 다른 역분개(=또 2인 승인)** 뿐이다 | **INV-9** |
| E | **승인 후 인자(원전표 ID) 변경 실행** | `consume` 거절 | INV-3 |
| F | 원전표 **수정·삭제** 시도 | ★ **조작 부재로 강제** — 호출할 메서드가 없다 | INV-2 · BR-10 |
| G | **부분 금액** 역분개 시도 | 거절 — 역분개는 전액 대칭이다. *"거래를 얼마로 줄인다"* 는 환불의 언어이고 역분개가 아니다 | **INV-5** · journal-entry §4 "환불을 역분개로 번역하지 않는 이유" |
| H | **마감된 영업일**의 전표를 정정 | ★ **허용된다** — 반직관이지만 정상. 귀속은 **정정 시점 영업일**이라 D일 순액은 불변이다 | **INV-6** · BR-47 · Settlement INV-4 |
| I | 역분개 전표의 차대 불균형·통화 혼재 | ★ 발생하지 않는다 — 원전표에서 대칭 도출되므로 INV-1·INV-4가 구조적으로 성립한다 *(U-6 근거 1줄)* | INV-1·4·5 |
| J | 책임자 **거절** | `REJECTED` + **사유 필수** | A3 |
| K | 승인 대기 상한 경과 | `EXPIRED` (상한 **[미확정]**) | A5 · AR1 |
| L | 지시 이벤트가 **DLQ에 갇힘** | 승인 `CONSUMED` 유지 · **M17 적재** · 재투입이 경로(UC-19) — 새 승인 불요 | ADR-014 O-12·O-13 · approval-request §5 주석 |
| M | **역분개의 역분개**(정정의 정정) | 새 승인 요청으로 가능하되 원전표는 여전히 불변 — ★ 체인 허용 여부·`reversalOf` 의미가 **정본 공백**(§9 UC16-2) | INV-2·5 (체인 금지 규정 없음) |
| N | **스코프 밖 대상 전표** | 거절 — ★ 유도 스코프라 **대상 조회 후** 판정된다. 범위 밖 = **부재와 동일 응답**(L7). `subjectId` 없는 전표는 전사 스코프만 | BR-55 유도 특칙 (**UC16-1 확정**) |
| O | 지시가 도달했는데 **원전표가 아직 기표 전** | `reverse` 사전조건 미충족 → 거절. ★ 원전표(계좌 파티션)와 지시(파티션 0)는 **다른 파티션이라 순서 보장이 없다** — 종결 처리는 §9 UC16-4 | ADR-014 O-8 (순서는 같은 파티션 안에서만) |

## 5. 대체 시나리오

- 없음 — **원장 정정의 유일한 경로**다(BR-10: 정정은 역분개 전표 추가로만 · 수정·삭제 조작이 존재하지 않는다). 환불·입금 정정은 이 경로가 아니다: 환불은 자기 전표를 쓰고(journal-entry §4), 입금 정정은 계좌 조작(UC-02)이 원인 이벤트를 낸다. *(U-6 근거)*

## 6. 인가·감사 ★

- 주체 판정 순서: ★ **① 단계**(자기 속성 — 대상 조회 전) → **② 원전표 존재 + 유도 스코프**(전표 `subjectId` 계좌들의 개설 조직 — 부재·범위 밖 동일 응답 L7. `subjectId` 없는 전표 = 전사만) → **③ 상태·인자**(INV-3·5). (2026-08-06 UC16-1 확정 — 스코프가 대상 유도형이라 ②에서 존재와 함께 판정된다. 원칙(단계→스코프·존재→상태)은 유지)
- L7: 원전표 식별자에 대해 **부재와 범위 밖을 구별하지 않는다**(BR-58). ★ 전표의 `subjectId`(계좌 등 보조부 식별자)는 **소유 축과 동형이되 값이 다르다** — 고객은 계좌 경유로만 유도된다(IS-2). 거절 시도는 기록(독립 커밋 — AD-2).
- 감사 경로: 요청·승인·거절·만료 = **C8 직접 append**(AD-5) / C5 역분개 실행 = **커밋 내 outbox**(AD-2 · R13) / 거절·실패 = **독립 커밋** / 조회 = 비동기(AD-4).
- 항목: AD-8 — 주체·시각·대상(원전표)·스코프·종류·결과 + **사유·요청자·승인자**. ★ **원장과 감사는 별개 기록이다** — 원장은 자금을, 감사는 행위자를 남긴다(BR-57 · ADR-017 기각안 A).

## 7. 계약 (요약 — 상세 블록은 **계약 전수 패스로 이월**)

> BR-56 공통 승인 계약의 **정본은 UC-02 §7**이다 — 참조만 하고 복사하지 않는다(U-7). 이 UC가 정의하는 것은 **조작별 인자**뿐이다.

| 인터페이스 | 주체 (유형·단계·스코프) | L7 대상 |
|---|---|---|
| `POST /ops/approval-requests` — 대상 조작 = **원장 역분개**, 인자 = `instructionId` · 원전표 ID · 사유 (정본 = UC-02 §7) | 운영자 **담당자** · ★ **계좌 유도 스코프**(UC16-1 확정) | ✅ 원전표 부재·범위 밖 동일 |
| `POST /ops/approval-requests/{id}/approve` · `/reject` (정본 = UC-02 §7) | 운영자 **책임자** · `checker ≠ maker` | ✅ |
| ★ **실행 엔드포인트 없음** (R15) — 승인 시 C8이 `consume` + 지시 이벤트를 같은 커밋으로 발행 | — | — |
| 지시 이벤트 **C8 → C5** (이름·payload **[미정]** · 멱등 키 = **`instructionId`** → 수신측 **INV-9**가 소유 구현) | 시스템(릴레이 경로 — 파티션 0) | — |
| 이벤트 `JournalReversed` (기존 — **정본 = journal-entry.md §4**) — ★ **행위자 포함 확정**(AD-7 ①·② 겹침의 ① 우선, 업무 귀속 maker — 2026-08-06) | 시스템(C5) | — |
| (조회) 전표·역분개 이력 조회 — 대상 축·주체는 **계약 전수 시** 확정 **[미정]** | 운영자 (단계 **[미정]**) | ✅ |

- 오류·응답·멱등·예산·감사 상세 칸, 스키마 버전(S-9) = **계약 전수 패스로 이월**.

## 8. 규칙 연결 표

| 단계/예외 | 규칙·조작 |
|---|---|
| §3-2·3 / §4-A·E·J·K | BR-55·56 · ApprovalRequest A1·A2·A3·A5 · INV-1·3 |
| §3-4·5 / §4-B·D·L·O | ★ **R15** · A4 · AD-6 · **JournalEntry INV-9** · ADR-014 O-6·O-8·O-12·O-13 |
| §3-6·7 / §4-C·F·G·I·M | BR-10 · `reverse` · **INV-1·2·4·5** |
| §4-H | **BR-47 · INV-6** · Settlement INV-4 (마감 영업일 순액 불변) |
| §4-N / §6 | BR-54·55·57·58 · IS-2 · AD-2·4·5·8 |
| (이 UC가 쓰지 않는 것) | `post`(INV-8 — 정상 기표 경로) = **원장 ACL 운영 절차** 소관 *(U-3 양방향 확인)* |

## 9. 열린 의문

| # | 의문 |
|---|---|
| ~~UC16-1~~ | ✅ **확정 (2026-08-06 사용자 합의 — 계좌 유도)**: 전사 특칙 확장이 아니라 **유도 스코프** — `subjectId` 계좌들의 개설 조직 전부가 스코프 안일 때 허용, 없으면 전사만(BR-55 유도 특칙). 판정 위치가 ②(존재와 함께)로 이동 — 권고(전사)와 달리 세밀 쪽을 선택 |
| UC16-2 | 역분개 전표를 **다시 역분개**할 수 있는가 — `reversalOf` 체인의 허용·의미가 규정되지 않았다(INV-5는 대칭만 요구) |
| UC16-3 | 정정 지시 이벤트의 **이름·payload·스키마 버전** — [미정] (maker·checker·사유·`instructionId` 필수) |
| UC16-4 | ★ 지시가 원전표보다 **먼저 도착**할 수 있는가(다른 파티션 — O-8은 같은 파티션 안에서만 순서). 거절 후 재시도 → DLQ가 되는데, 이것이 정상 경로인지 결함인지 **정본 판정 필요** |

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v0.2 | 2026-08-06 | **UC16-1 닫힘**(계좌 유도 스코프) + `JournalReversed` 행위자 포함 확정 반영(헤더·§4-N·§6·§7) |
| v0.1 | 2026-08-05 | 최초 작성 — Phase 4 전수 패스. R15 경로(②) · **INV-9 지시 ID 멱등**의 착지 · INV-6/BR-47(마감 영업일 순액 불변) · INV-2 원전표 불변. ★ 정본 공백 2건 식별(UC16-1 스코프 축 · UC16-4 파티션 간 순서) |
