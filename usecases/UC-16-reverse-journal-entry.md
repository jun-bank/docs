# UC-16. 원장 오기표를 역분개로 정정한다

- 작성일: 2026-08-05
- 상태: **검토대기 (계약 블록 완결)**
- 액터: **운영자 2인** — 요청 = 담당자(maker) · 승인 = 책임자(checker) (BR-55 · 같은 사람 불가)
- 권한 요건: **BR-56 ⑤** (책임자 승인 조작 — 감독규정 제28조 ② 직접 대상) · 스코프 = ★ **계좌 유도**(2026-08-06 UC16-1 확정 — 전표 `subjectId` 계좌들의 개설 조직 **전부**가 요청자 스코프 안이어야 허용, `subjectId` 없는 전표는 **전사만**. BR-55 유도 특칙)
- 트랜잭션 경계: ★ **E1~E5 아님 — 자금 무이동**(BR-40: 기표는 자금 이동과 별도 논리 단위). 커밋은 둘 — ① C8 `consume` + 정정 지시 이벤트 outbox INSERT가 같은 커밋 ② C5 `reverse` 커밋(전표 하나 = 한 트랜잭션) — **R15**
- 관련 규칙: BR-10·40·47·54·55·56·57·58 · JournalEntry INV-1·2·4·5·6·9 · ApprovalRequest INV-1~4·A1~A5 · Settlement INV-4 · ADR-014 O-13 · ADR-017 AD-2·5·6·8 (참조만 — F-24)

## 1. 목표 (한 문장)

잘못 기표된 전표를 **원전표를 건드리지 않고**, 2인 승인을 거쳐 **역분개 전표를 추가**해 정정한다.

## 2. 사전 조건 / 사후 조건

- 사전: 원전표가 존재한다(`reverse` 사전조건). 오기표라는 판단 근거가 있다 — 대개 대사 불일치(M6·M12·M14 등)나 조사 결과(UC-17). 요청자 = 전사 담당자.
- 사후(성공): **새 전표 1건** — `reversalOf` = 원전표 · 차대 반대·동액(INV-5) · `businessDate` = **정정 시점의 영업일**(INV-6 · BR-47) · `instructionId` 저장(INV-9) · `JournalReversed` 발행 · 승인 `CONSUMED` · 감사.
- 사후(성공) ★ **원전표는 그대로다**(INV-2 · BR-10) — 수정·삭제 조작이 애초에 없다.
- 사후(성공) ★ **마감된 영업일의 순액은 움직이지 않는다** — 역분개가 정정 시점 영업일에 잡히기 때문이다(BR-47 · Settlement INV-4).
- 사후(거절/만료): 원장 **무변화** · 요청 `REJECTED`/`EXPIRED` · 사유가 감사에 남는다.
- 사후(승인 후 지시 정체): 승인 `CONSUMED`인데 전표는 없다 — **정체(M17)**이며 재투입이 경로(UC-19). 재투입해도 **INV-9가 이중 기표를 막는다**.

## 3. 주요 성공 시나리오

1. 담당자 A가 오기표를 식별한다 — 근거 = 대사 불일치 또는 조사 결과 [BR-41 · UC-17]
2. A → C8 `request(원장 역분개, instructionId · 원전표 ID · 사유)` — `PENDING` [A1 · 감사 직접 append]
3. 책임자 B → `approve(B)` — **B ≠ A** → `APPROVED` [A2 · INV-1]
4. ★ 승인 시점에 C8이 **`consume` + 정정 지시 이벤트 outbox INSERT를 같은 커밋**으로 [A4 ⑤ 경로 · **R15** · AD-6] — **실행 엔드포인트는 없다**
5. 릴레이가 C5로 전달 — 계좌ID 없는 이벤트라 **파티션 0** [ADR-014 O-6·O-8]
6. C5 `reverse(instructionId, originalId, at, operator)` — **INV-9 유일 검사** → 새 전표 생성 [journal-entry §4]
7. `JournalReversed` 발행 · 감사 outbox(커밋 내 — AD-2). 업무 귀속 = maker, 기술 실행자 = C5 [A4]

## 4. 예외 시나리오 ★

| # | 트리거 | 시스템 반응 → 종결 | 근거 |
|---|---|---|---|
| A | **A가 자기 요청을 승인** | 거절 | ApprovalRequest INV-1 · AF7 |
| B | **승인 없이 역분개 시도** | ★ **경로가 없다** — `reverse`는 **승인된 정정 지시 이벤트로만** 트리거된다 | AF1 · R15 · journal-entry §4 사전조건 |
| C | 원전표 없음 | 거절 — 근거 없는 정정 차단 | `reverse` 사전조건 |
| D | 같은 `instructionId` 재도착 (재전달·DLQ 재투입) | ★ **무연산** — 유일 제약이 두 번째를 막는다. 막지 못하면 되돌리는 수단이 **또 다른 역분개(=또 2인 승인)** 뿐이다 | **INV-9** |
| E | **승인 후 인자(원전표 ID) 변경 실행** | `consume` 거절 | INV-3 |
| F | 원전표 **수정·삭제** 시도 | ★ **조작 부재로 강제** — 호출할 메서드가 없다 | INV-2 · BR-10 |
| G | **부분 금액** 역분개 시도 | 거절 — 역분개는 전액 대칭이다. *"거래를 얼마로 줄인다"* 는 환불의 언어이고 역분개가 아니다 | **INV-5** · journal-entry §4 "환불을 역분개로 번역하지 않는 이유" |
| H | **마감된 영업일**의 전표를 정정 | ★ **허용된다** — 반직관이지만 정상. 귀속은 **정정 시점 영업일**이라 D일 순액은 불변이다 | **INV-6** · BR-47 · Settlement INV-4 |
| I | 역분개 전표의 차대 불균형·통화 혼재 | ★ 발생하지 않는다 — 원전표에서 대칭 도출되므로 INV-1·INV-4가 구조적으로 성립한다 *(U-6 근거 1줄)* | INV-1·4·5 |
| J | 책임자 **거절** | `REJECTED` + **사유 필수** | A3 |
| K | 승인 대기 상한 경과 | `EXPIRED` (상한 **[미확정]**) | A5 · AR1 |
| L | 지시 이벤트가 **DLQ에 갇힘** | 승인 `CONSUMED` 유지 · **M17 적재** · 재투입이 경로(UC-19) — 새 승인 불요 | ADR-014 O-12·O-13 · approval-request §5 주석 |
| M | **역분개의 역분개**(정정의 정정) | 새 승인 요청으로 가능하되 원전표는 여전히 불변 — ★ 체인 허용 여부·`reversalOf` 의미가 **정본 공백**(§9 UC16-2) | INV-2·5 (체인 금지 규정 없음) |
| N | **스코프 밖 대상 전표** | 거절 — ★ 유도 스코프라 **대상 조회 후** 판정된다. 범위 밖 = **부재와 동일 응답**(L7). `subjectId` 없는 전표는 전사 스코프만 | BR-55 유도 특칙 (**UC16-1 확정**) |
| O | 지시가 도달했는데 **원전표가 아직 기표 전** | `reverse` 사전조건 미충족 → 거절. ★ 원전표(계좌 파티션)와 지시(파티션 0)는 **다른 파티션이라 순서 보장이 없다** — 종결 처리는 §9 UC16-4 | ADR-014 O-8 (순서는 같은 파티션 안에서만) |

## 5. 대체 시나리오

- 없음 — **원장 정정의 유일한 경로**다(BR-10: 정정은 역분개 전표 추가로만 · 수정·삭제 조작이 존재하지 않는다). 환불·입금 정정은 이 경로가 아니다: 환불은 자기 전표를 쓰고(journal-entry §4), 입금 정정은 계좌 조작(UC-02)이 원인 이벤트를 낸다. *(U-6 근거)*

## 6. 인가·감사 ★

- 주체 판정 순서: ★ **① 단계**(자기 속성 — 대상 조회 전) → **② 원전표 존재 + 유도 스코프**(전표 `subjectId` 계좌들의 개설 조직 — 부재·범위 밖 동일 응답 L7. `subjectId` 없는 전표 = 전사만) → **③ 상태·인자**(INV-3·5). (2026-08-06 UC16-1 확정 — 스코프가 대상 유도형이라 ②에서 존재와 함께 판정된다. 원칙(단계→스코프·존재→상태)은 유지)
- L7: 원전표 식별자에 대해 **부재와 범위 밖을 구별하지 않는다**(BR-58). ★ 전표의 `subjectId`(계좌 등 보조부 식별자)는 **소유 축과 동형이되 값이 다르다** — 고객은 계좌 경유로만 유도된다(IS-2). 거절 시도는 기록(독립 커밋 — AD-2).
- 감사 경로: 요청·승인·거절·만료 = **C8 직접 append**(AD-5) / C5 역분개 실행 = **커밋 내 outbox**(AD-2 · R13) / 거절·실패 = **독립 커밋** / 조회 = 비동기(AD-4).
- 항목: AD-8 — 주체·시각·대상(원전표)·스코프·종류·결과 + **사유·요청자·승인자**. ★ **원장과 감사는 별개 기록이다** — 원장은 자금을, 감사는 행위자를 남긴다(BR-57 · ADR-017 기각안 A).

## 7. 계약 (계약 블록)

> **공통 승인 계약의 정본 = UC-02 §7** (`POST /ops/approval-requests` · `/{id}/approve` · `/reject` · `GET /ops/approval-requests`) — 복사하지 않고 참조한다(U-7 · F-24). 이 UC가 정의하는 것은 **⑤의 조작별 인자**와 ★ **R15 지시 이벤트**다.
> ★ **실행 엔드포인트가 없다**(R15) — 승인 커밋이 `consume` + 지시 이벤트 outbox INSERT를 함께 수행하고(A4 ⑤), **실행 1회는 수신측 `instructionId` 유일(INV-9)** 이 보장한다.
> 오류 코드 3계층 = K-1 ⑴ 공통 4종 / ⑵ ★ **전표에는 상태 머신이 없다** — SM 응답 열이 존재하지 않으므로 거절 사유는 **불변식(INV-2·5·9)과 `reverse` 사전조건**에서 나온다 / ⑶ 고유 신설 **없음**.
> 운영자 화면 전환도는 공통이다 — **UC-02 §7 참조**. ★ ⑤에도 *"실행 확인"* 화면이 없다(실행 엔드포인트 부재).

#### `POST /ops/approval-requests` — ⑤ **원장 역분개** 요청 (조작별 인자·오류만 — 공통 계약은 UC-02 §7)

| 항목 | 내용 |
|---|---|
| 주체 | 운영자 **담당자** 이상 · ★ **계좌 유도 스코프**(UC16-1 확정 — 원전표 `subjectId` 계좌들의 **개설 조직 전부**가 요청자 스코프 안이어야 허용 · `subjectId` 없는 전표는 **전사만**) · 판정 = 진입 1회(AU-4) |
| 요청 | 대상 조작 = **원장 역분개**(BR-56 ⑤ 목록 안) · 인자 = ★ **`instructionId`**(maker 생성 — INV-3 검사 대상 · `EXPIRED` 후 재요청은 **새 ID**) · **원전표 ID** · **사유(필수)** — ★ **금액·부분 인자는 없다**(역분개는 전액 대칭 — INV-5 · §4-G) |
| 응답(성공) | 요청 ID · `PENDING` · 반향된 `instructionId` |
| 오류 표 ★ | **K-1 판정 순서** ① `FORBIDDEN_LEVEL`(단계 미달 — 자기 속성, 대상 조회 전) → ② ★ **`NOT_FOUND` — 원전표 부재·유도 스코프 밖이 동일 형태**(L7 · §4-N. **스코프가 대상 유도형이라 존재와 함께 ②에서 판정**된다 — UC16-1 확정. 원칙(단계→스코프·존재→상태)은 유지) → ③ `INVALID_OPERATION`(BR-56 목록 밖) · `ARG_MISMATCH`(`instructionId`·원전표 ID 형식·사유 누락) · ★ **`DUPLICATE_INSTRUCTION`**(같은 `instructionId` 재요청 — **확정 D-유일**: `instructionId`는 **요청 단계 유일**. 정본 = **UC-02 §7 v0.7**(④⑤ 공통 — 인용만) · UC-15와 한 판정) |
| 멱등 | 공통 계약 상속 = **비멱등**(UC-02 §7 · UC2-1). ★ 단 같은 원전표에 **두 지시가 승인되면 두 번째는 INV-9가 아니라 "다른 `instructionId`"** 이므로 **역분개가 둘 생긴다** — 중복 정정의 방어선은 멱등이 아니라 **승인 절차와 §4-M(역분개의 역분개)** 뿐이다(⚠️ *원전표당 미종결 요청 1건* 제약의 신설 여부는 **§9 UC16-2**에 묶인 미해소 항목 — U-2 판정 대장 밖이라 이 패스가 확정하지 않는다) |
| 예산 | 실시간 아님 (관리 조작) |
| 감사 | **C8 자기 조작 = 직접 append**(AD-5) · AD-8 = `sourceRef`(요청 식별자 — AD-3) · 주체 · 시각 · 대상(**원전표 ID**) · ★ **스코프 = 판정에 실제 쓰인 값**(K-4 — 유도된 **개설 조직 값들** · `subjectId` 없으면 `전사`) · 종류 · 결과 + **사유·요청자**. **거절 = 독립 커밋**(AD-2 · §4-N) |

> ★ **확정 (통합4) — 전표 조회 경로 = `R18` 신설. ✓ 정본 반영됨(`context-map.md` v1.7).**
> UC16-1이 스코프 판정 위치를 ②(대상 조회 후)로 확정했으므로 **요청·승인 시점에 C5의 전표 `subjectId`를 읽어야 하는데** context-map에 C8 → C5 조회 관계가 없었다(R15는 C8→C5 **지시 발행** 단방향, R14는 C1·C3 한정).
> ★ **R18 = C8 운영 → C5 원장 · 순응자(Conformist) · 읽기 전용 동기 조회 — 용도는 유도 스코프 판정으로 한정**(R9와 같은 읽기 전용 형태). ADR-010의 금지선은 *"코어가 위성에 동기 의존"* 이라 **방향이 반대**여서 저촉되지 않는다.
> ⚠️ **대가**: 조회 가용성이 승인 경로에 묶인다. 대안 ⓑ(구독 복제)·ⓒ(수신측으로 판정 이월)는 채택하지 않았다 — ⓒ는 요청 단계 L7을 무너뜨려(§4-N의 *"부재와 동일 응답"*) UC16-1 확정을 되돌린다.

#### `POST /ops/approval-requests/{id}/approve` · `/reject` — ⑤ 승인/거절 (정본 = UC-02 §7 · 이 UC의 차이만)

| 항목 | 내용 |
|---|---|
| 주체 | 운영자 **책임자** · ★ **유도 스코프 동일 적용**(BR-55 특칙 — 승인 단계에서 스코프 통제가 끊기지 않는다: maker·checker가 **같은 유도 스코프 조건**을 통과해야 한다) · `checker ≠ maker`(INV-1 · AF7) |
| 요청 | (`reject`는 **사유 필수** — A3) |
| 응답(성공) | 요청 ID · 전이 상태 · ★ **승인 응답의 의미는 "지시가 발행됐다"까지다**(R15) — **기표 완료가 아니다**(정체는 M17 — §2 사후) |
| 오류 표 ★ | ① `FORBIDDEN_LEVEL` → ② `NOT_FOUND`(요청 ID 부재·**유도 스코프 밖 동일** — L7) → ③ `SELF_APPROVAL`(INV-1 · AF7) · 종료 상태 재호출 = **`ALREADY_APPROVED`·`ALREADY_REJECTED`·`ALREADY_CONSUMED`·`ALREADY_EXPIRED`**(AF2~AF6 — K-1 CDS3. ★ 코드 정합은 **UC-15 §7 ㉣과 같은 판정**) |
| 멱등 | 상태 전이가 멱등 경계 — 종료 상태는 위 `ALREADY_*` |
| 예산 | 실시간 아님. 승인 커밋은 **C8 단일 커밋**(consume + outbox INSERT — 배포 경계를 넘지 않는다) |
| 감사 | **AD-5 직접 append** + 승인자·사유 · 스코프 = 유도된 조직 값(K-4). ★ 지시 발행 사실도 같은 커밋(대상 = `instructionId`) |

#### 이벤트 (신설) `JournalReversalInstructed` — ★ **R15 지시 이벤트** · 승인 커밋과 **같은 커밋**에 outbox INSERT

> ★ **이름 확정 (D-이름) = `JournalReversalInstructed`** — K-2 과거분사 사실명 · 결과 이벤트 `JournalReversed`(C5 발행)와 **지시/결과가 이름으로 갈린다** · UC-15의 `SettlementResumeInstructed`와 **같은 규칙으로 동시 확정**(`JournalReversalApproved`는 `ApprovalRequest`의 `APPROVED` 상태와 겹쳐 채택하지 않는다). ✓ **색인 등재됨**(`usecases/README.md` §3 · UC16-3 닫힘).

| 항목 | 내용 |
|---|---|
| payload | ★ **`instructionId`**(멱등 키 → 수신측 **INV-9**가 소유 구현) · `approvalRequestId` · **`originalEntryId`**(조작 인자 — INV-3으로 요청 인자와 동일 보장) · **`maker`**(요청자 = ★ 업무 귀속 · `reverse(…, operator)` 인자의 원천) · **`checker`** · **`reason`** · `approvedAt`(승인 커밋 시각 — ★ **추적용이다. `reverse`의 `at`이 아니다** — D-㉢ 확정, 아래) — ★ **행위자 포함**(AD-6 · AD-7: C8 지시 이벤트는 **원장 번역 입력이 아니다** — 번역 입력의 정의 = 원장 ACL이 전표로 번역하는 자금 이벤트(R5~R7)뿐). 축 필드 = ★ **없다**(계좌ID 없는 이벤트 → **파티션 0** — O-6·O-8 · K-4) |
| 구독 | **C5 원장** 단독. 감사는 구독이 아니다(AD-2 · C8 자기 조작은 AD-5) |
| 멱등 키 | ★ **`instructionId`** — 수신측 `reverse(instructionId, originalId, at, operator)`가 **전표에 저장하고 유일 제약**으로 소유한다(**INV-9** — *"지시 ID 멱등"* 의 소유 구현). at-least-once 전제 · **DLQ 재투입도 같은 키로 막힌다**(§4-D·L · UC-19 §4-G) |
| 스키마 버전 | ★ **`schemaVersion: 1`**(K-3). 의미 필드 추가는 **새 이벤트 타입**(S-6a) |

> ★ **확정 (D-㉢) — `reverse(…, at, …)`의 `at` = C5의 기표 커밋 시점이다. `approvedAt`이 아니다.**
> INV-6/BR-47은 역분개의 `businessDate`를 **정정 시점의 영업일**로 못박는데 `at`의 원천이 정해져 있지 않았다 — 지시가 DLQ에 갇혀 이틀 뒤 재투입되면(§4-L) 두 값이 갈린다.
> 근거: *"정정 시점"* 을 문자 그대로 읽으면 **실제로 정정 전표를 만드는 순간**이고, 이때만 **마감 영업일 순액 불변**(INV-6 · Settlement INV-4)이 **어떤 지연에서도** 성립한다. `approvedAt`을 쓰면 지연 시 **이미 마감된 영업일에 기표**하게 되어 INV-6이 막으려던 것을 되살린다.
> ★ `approvedAt`은 payload에 **추적용으로만** 싣는다(§4-H).

#### (수신) C5 `reverse` — 지시 이벤트 처리 계약 ★ **엔드포인트가 아니다**

| 항목 | 내용 |
|---|---|
| 주체 | **시스템**(릴레이 경로 — 경로 식별자 필수 · C-1). ★ 업무 귀속 = payload의 `maker`(A4 ⑤ · `reverse`의 `operator` 인자) |
| 요청 | 지시 이벤트 payload 그대로. **다른 호출 표면이 없다**(§4-B) |
| 응답(성공) | ★ **응답 상대가 없다**(비동기). 사후 = **새 전표 1건**(`reversalOf` = 원전표 · 차대 반대·동액 INV-5 · `businessDate` = 정정 시점 INV-6 · **`instructionId` 저장** INV-9) · **원전표 불변**(INV-2) · `JournalReversed` 발행 · 감사 outbox — **한 커밋**(전표 하나 = 한 트랜잭션) |
| 오류 표 ★ | ★ 아래는 응답 코드가 아니라 **거절 사유 값(감사·M 적재)** 또는 **재시도 대상**이다. **원전표 부재** = `reverse` 사전조건 미충족 → ★ **처리 실패 → 재시도 → M17**(§4-C·O. 원전표(계좌 파티션)와 지시(파티션 0)는 **다른 파티션이라 순서 보장이 없다** — 정상인지 결함인지 §9 UC16-4) · **중복 `instructionId`는 오류가 아니라 무연산**(INV-9 · §4-D) · **부분 금액·차대 불균형은 발생하지 않는다**(원전표에서 대칭 도출 — §4-G·I) |
| 멱등 | ★ **`instructionId` 유일(INV-9) — 무연산**. 막지 못하면 되돌리는 수단이 **또 다른 역분개(=또 2인 승인)** 뿐이다(§4-D) |
| 예산 | 실시간 아님 — 비동기 릴레이. 재시도·DLQ 상한은 **ADR-014 소관**(O-10·O-11) |
| 감사 | 성공 = **커밋 내 outbox**(AD-2 · R13) — 주체 = 경로 식별자 **+ 업무 귀속 `maker`** · 대상 = 원전표 ID·새 전표 ID · **스코프 = 유도된 조직 값**(K-4) · 결과 + 사유·요청자·승인자. **거절·실패 = 독립 커밋**(AD-2). ★ **원장과 감사는 별개 기록**이다(§6 — 원장은 자금을, 감사는 행위자를) |

#### 이벤트 `JournalReversed` (기존 — **정본 = journal-entry.md §4**. 계약 확인 블록)

| 항목 | 내용 |
|---|---|
| payload | **원전표 ID · 새 전표 ID · `instructionId` · 행위자(업무 귀속 maker)** ✓ 정본과 일치 — ★ **행위자 포함 확정**(AD-7 ①·② 겹침의 ① 우선: 자금 이벤트지만 **발생 경로가 운영자 전용**이고, C5 **자신이 발행하는 정정 기록 이벤트**라 번역 입력이 아니다 — 2026-08-06 · ADR-017 v0.2) |
| 구독 | 정본 미지정 — 운영·감사 경로. ★ **원장 ACL의 번역 입력이 아니다**(C5 자신이 낸다) |
| 멱등 키 | 수신측 유일 제약은 발생하지 않는다(상태 변경 구독자 없음). ★ 이 이벤트의 1회성은 **발신측 INV-9**가 이미 보장한다 |
| 스키마 버전 | **`schemaVersion: 1`**(K-3 — 전 이벤트 일괄) |

#### (조회) 전표·역분개 이력 — ★ **이 패스에서 신설하지 않는다**

| 항목 | 내용 |
|---|---|
| 상태 | ★ **엔드포인트 색인 미등재** — 신설하면 **같은 커밋으로 색인에 등재**해야 한다(C-6). 이 UC의 §3~4 어느 단계도 이 조회를 전제하지 않으므로(오기표 식별의 근거는 **대사 불일치·조사 결과** — §3-1) 지금 신설하지 않는다 |
| 후속 (대장 밖) | ★ **운영자용 전표 조회 표면**(`GET /ops/journal-entries?subjectId=&businessDate=`)은 **U-2 판정 대장에 없다** — 이 패스는 위 *상태* 행대로 **신설하지 않는다**(원장 열람 수요는 감사 조회 **UC-20**과 불일치 조사 **UC-17**이 덮는다). ⚠️ **R18과 혼동 금지** — R18은 **C8 내부의 유도 스코프 판정용 읽기 경로**이지 운영자에게 노출되는 조회 엔드포인트가 아니다. 신설하려면 **주체 단계**(조회/담당자/책임자)와 **축**(`subjectId` 계좌 유도 스코프 — 이 UC의 판정 승계)을 함께 정하고 **같은 커밋으로 색인 등재**(C-6)해야 한다 |

### 이 §7의 판정 착지 (U-2 반영 — 2026-08-06)

| # | 질문 | ★ 결정 |
|---|---|---|
| ㉠ | 지시 이벤트 이름 | ✅ **`JournalReversalInstructed`**(D-이름 — UC-15와 같은 규칙. 색인 등재됨 · UC16-3 닫힘) |
| ㉡ | 유도 스코프 판정용 **C8 → C5 전표 조회 경로** | ✅ **R18 신설**(통합4 — 읽기 전용 동기 조회 · 유도 스코프 판정 한정). ✓ `context-map.md` **v1.7** 반영됨 |
| ㉢ | `reverse`의 `at`(정정 시점) 원천 | ✅ **기표 커밋 시점**(`approvedAt` 아님 — INV-6 · Settlement INV-4 보존. `approvedAt`은 추적용) |
| ㉣ | 같은 원전표에 **다른 `instructionId`** 요청 둘이 승인되면 역분개가 둘 | ⏳ **미해소 — U-2 대장 밖**. *원전표당 미종결 요청 1건* 제약은 §4-M·**UC16-2**(역분개의 역분개)와 함께 판정한다 |
| ㉤ | 운영자용 전표 조회 표면 신설 여부·주체·축 | ⏳ **이 패스는 신설 안 함**(UC-17·UC-20이 덮는다 — 대장 밖. R18과 별개다) |
| — | 지시 ID 요청 단계 유일 = **`DUPLICATE_INSTRUCTION`**(D-유일) · 종료 상태 재호출 = **`ALREADY_*` 4종**(통합1) — 둘 다 정본 = UC-02 §7 v0.7 · 수치(대기 상한 A5·AR1)는 **[미확정]** | ✅ / — |

## 8. 규칙 연결 표

| 단계/예외 | 규칙·조작 |
|---|---|
| §3-2·3 / §4-A·E·J·K | BR-55·56 · ApprovalRequest A1·A2·A3·A5 · INV-1·3 |
| §3-4·5 / §4-B·D·L·O | ★ **R15** · A4 · AD-6 · **JournalEntry INV-9** · ADR-014 O-6·O-8·O-12·O-13 |
| §3-6·7 / §4-C·F·G·I·M | BR-10 · `reverse` · **INV-1·2·4·5** |
| §4-H | **BR-47 · INV-6** · Settlement INV-4 (마감 영업일 순액 불변) |
| §4-N / §6 | BR-54·55·57·58 · IS-2 · AD-2·4·5·8 |
| (이 UC가 쓰지 않는 것) | `post`(INV-8 — 정상 기표 경로) = **원장 ACL 운영 절차** 소관 *(U-3 양방향 확인)* |

## 9. 열린 의문

| # | 의문 |
|---|---|
| ~~UC16-1~~ | ✅ **확정 (2026-08-06 사용자 합의 — 계좌 유도)**: 전사 특칙 확장이 아니라 **유도 스코프** — `subjectId` 계좌들의 개설 조직 전부가 스코프 안일 때 허용, 없으면 전사만(BR-55 유도 특칙). 판정 위치가 ②(존재와 함께)로 이동 — 권고(전사)와 달리 세밀 쪽을 선택 |
| UC16-2 | 역분개 전표를 **다시 역분개**할 수 있는가 — `reversalOf` 체인의 허용·의미가 규정되지 않았다(INV-5는 대칭만 요구) |
| ~~UC16-3~~ | ✅ **닫힘 (2026-08-06 U-2 — D-이름 · K-3)**: 이름 = **`JournalReversalInstructed`** · payload = `instructionId`·`approvalRequestId`·`originalEntryId`·`maker`·`checker`·`reason`·`approvedAt`(행위자 포함 — AD-6 · 축 없음 · `ownerId` 비포함) · `schemaVersion: 1`. 색인 §3 등재됨 |
| UC16-4 | ★ 지시가 원전표보다 **먼저 도착**할 수 있는가(다른 파티션 — O-8은 같은 파티션 안에서만 순서). 거절 후 재시도 → DLQ가 되는데, 이것이 정상 경로인지 결함인지 **정본 판정 대상**(U-2 대장 밖 — 미해소) |

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v0.4 | 2026-08-06 | **U-2 판정 반영 — 마커 해소·의문 닫기**: 지시 이벤트 이름 = `JournalReversalInstructed`(D-이름 — **UC16-3 닫힘**) · 전표 조회 경로 = **R18 신설**(통합4 — context-map v1.7 반영됨) · `reverse`의 `at` = **기표 커밋 시점**(D-㉢ — `approvedAt` 아님, INV-6) · `instructionId` 요청 단계 유일 = `DUPLICATE_INSTRUCTION`(D-유일) · 종료 상태 재호출 = `ALREADY_*` 4종(통합1). ㉣·㉤·UC16-4는 대장 밖 미해소로 표시 |
| v0.3 | 2026-08-06 | **계약 전수 — §7 상세 블록** (요약 표 → 계약 블록 6종 · K-1~4 적용 · ★ **R15 지시 이벤트 계약 신설**(`JournalReversalInstructed` — payload·`instructionId` 멱등 = INV-9·`schemaVersion: 1`) · 유도 스코프의 판정 순서(②) 승계 · 수신 처리 계약(원전표 부재 = 재시도→M17) · 판정 대기 5건) |
| v0.2 | 2026-08-06 | **UC16-1 닫힘**(계좌 유도 스코프) + `JournalReversed` 행위자 포함 확정 반영(헤더·§4-N·§6·§7) |
| v0.1 | 2026-08-05 | 최초 작성 — Phase 4 전수 패스. R15 경로(②) · **INV-9 지시 ID 멱등**의 착지 · INV-6/BR-47(마감 영업일 순액 불변) · INV-2 원전표 불변. ★ 정본 공백 2건 식별(UC16-1 스코프 축 · UC16-4 파티션 간 순서) |
