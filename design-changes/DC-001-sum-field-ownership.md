# DC-001: 합계 필드의 소유와 검증

- 작성일: 2026-08-04
- 상태: **적용중** — 단계 8 재리뷰 완료(치명 6). 단계 9~11 진행 중
- 발동 조건: **T1**(같은 자리 2회+ 재발) · **T2**(라운드마다 같은 뿌리) · **T3**(사람이 지켜야 하는 불변식) · **T4**(기각 근거가 채택안에도 적용)
- 영향 Phase: **Phase 2** (도메인 정의) — Phase 1 규칙 일부 포함
- 절차: `study/project-workflow/design-change/README.md`

---

## 1. 한 줄 요약

> **한 애그리게이트가 다른 애그리게이트들의 합을 필드로 들고 "저쪽이 막아준다"로 처리한 것**을, **소유를 명확히 하거나 검증 장치를 붙이는 것**으로 바꾼다. 왜냐하면 그 패턴의 필드 3개가 **치명 결함 9건 중 6건**을 냈다.

---

## 2. 실패 이력 ★

### 라운드별 원문

| 라운드 | 지적 | 심각도 | 그때 무엇을 고쳤나 | 결과 |
|---|---|---|---|---|
| **1차** | *"T9(지연 매입)가 T8의 부수 효과를 `〃`로 복사해, 만료로 이미 풀린 홀딩을 다시 해제한다. 계좌는 홀딩을 **합계(`holdTotal`)로만** 들기 때문에 사전조건이 **다른 승인의 점유 덕분에 통과**하고 그 승인들의 점유가 깎인다"* | **치명** | T9 부수 효과를 명시 분리, `holdingFunds`가 참일 때만 해제 | ✅ 그 경로는 막힘 — **패턴은 그대로** |
| **1차** | *"환불액 상한(BR-43 ①)이 어떤 전이 조건에도 없다 — 받지 않은 미수분까지 반환된다"* | 높음 | INV-7 신설 (`≤ 매입액 − 발생 미수액`) | ❌ 2차에 재발 |
| **2차** | *"환불할수록 INV-7 상한이 다시 커진다. 환불 후 `receivableAmount`를 차감하면서 상한의 우변도 증가한다"* | **치명** | 상한 기준을 불변값 `withdrawnAmount`로 교체 | ❌ 3차에 재발 |
| **3차** | *"전액 환불의 '대응 미수 소멸'이 계좌 명세와 충돌한다 — 같은 3만을 두 번 회수한다. 계좌 조작 목록에 **미수를 소멸시키는 조작이 아예 없다**"* | **치명** | 계좌 `refund()`에 소멸 단계 신설, 소멸→상계→잔액 순서 | ❌ 4차에 재발 |
| **4차** | *"계좌 `receivable` 합계를 줄이는 경로가 사라졌다 — '산식의 파생'은 승인 안에만 있다. `account.receivable = Σ(승인별 잔여 미수)`를 보장하는 불변식이 **한 문서에도 없다**"* | **치명** | — (여기서 멈춤) | — |
| **4차** | *"`SETTLED` 전액 환불 뒤에도 `recoverReceivable`이 열려 있어 소멸한 채권을 회수한다"* | **치명** | — | — |

### 무엇이 반복됐나

**환불↔미수 접합부를 세 번 고쳤고 세 번 다시 깨졌다.** 고칠 때마다 형태가 바뀌었다:

```
1차  상한이 없다              → 상한을 만들었다
2차  상한이 스스로 커진다       → 상한을 불변값으로 바꿨다
3차  계좌 상계가 반환액을 먹는다 → 소멸 단계를 넣었다
4차  계좌 합계를 줄일 데가 없다  → ???
```

**매번 "이번엔 진짜 고쳤다"고 판단했고, 매번 다음 라운드가 같은 자리를 다시 지적했다.**

그리고 3차의 수정(계좌에 소멸 조작 신설)과 4차의 지적(계좌에 감소 경로 없음)은 **정반대로 보이지만 둘 다 옳다** — 빼야 하는데(4차), 남의 몫을 깎으면 안 된다(3차). 이 둘을 동시에 만족시킬 방법이 현재 구조에 없다.

---

## 3. 공통 뿌리 ★

| 항목 | 내용 |
|---|---|
| **뿌리 결정** | `study/project-workflow/phase2/04-aggregate-format.md` §4 작성법 — 불변식 판정 |
| **그 결정의 원문** | *"불변식은 애그리게이트 안에서만 검증 가능해야 한다. 검증하려고 다른 애그리게이트를 조회해야 한다면: **경계가 잘못됐거나 / 그건 불변식이 아니라 프로세스 규칙(정책)이다**"* |
| **왜 이것이 뿌리인가** | 갈림길은 있는데 **어느 쪽인지 판정하는 방법이 없다.** 그래서 매번 오른쪽(프로세스 규칙)으로 갔고, 왼쪽(경계가 잘못됐다)은 한 번도 검토되지 않았다 |

### 그 결과 생긴 패턴

**한 애그리게이트가 다른 애그리게이트들의 합을 필드로 들고, 그 합이 맞다는 것을 "다른 쪽이 막아준다"로 처리했다.**

| 필드 | 실체 | 그때 적은 근거 | 결과 |
|---|---|---|---|
| 계좌 `holdTotal` | Σ(승인별 홀딩) | *"개별 홀딩의 존재는 승인(C3)이 소유한다 — **이중 해제는 승인 상태가 막는다**"* | **1차 치명** |
| 계좌 `receivable` | Σ(승인별 잔여미수) | *"미수는 잔액과 함께 움직인다(BR-34)"* — 등식은 어디에도 없음 | **2·3·4차 치명** |
| 카드 `usage` | Σ(그 카드 승인들의 한도사용액) | *"한도 사용액 — C2 카드"* — 복원 책임이 승인 전이에 흩어짐 | **1차 높음** (T7·T9·T13 이중 복원) |

> ★ **세 필드 전부 사고를 냈다.** 우연이 아니라 **같은 구조가 같은 방식으로 깨진 것**이다.
>
> **"다른 애그리게이트의 상태가 막아준다"는 구조가 아니라 약속이다.** 약속은 문서에만 있고, 구현자가 한 줄 빠뜨리면 조용히 틀린다 — 셋 다 에러가 나지 않고 **숫자만 틀렸다.**

---

## 4. 기각 근거 재심 (T4) ★

미수를 어디에 둘지는 **AU9에서 이미 한 번 결정**했다. 그때 기각한 선택지를 다시 읽는다.

| 당시 기각한 선택지 | 당시 기각 근거 | 지금도 유효한가 |
|---|---|---|
| **(c) 미수를 승인별로 소유** | *"`account.md`가 홀딩과 미수를 합계로 드는 이유를 이미 적었다 — **목록을 들면 계좌가 승인 수만큼 커진다.** 배분 문제는 해결되지만 그 결정을 뒤집는 대가가 더 크다"* | ❌ **성립하지 않았다** |

**왜 성립하지 않았나** — 채택안 (a)도 결국 승인 목록을 조회한다:

| 채택안 (a)가 실제로 요구하는 것 | 근거 |
|---|---|
| 상계 시 **승인 목록 조회** → FIFO 배분 | BR-34 확장 |
| 보류 제외를 위해 **승인들의 `frozen` 조회** | 4차 Q4 |
| 환불 시 **승인의 잔여미수 감소분**을 계좌에 반영 | 4차 Q1 |
| `계좌 receivable = Σ(승인 잔여미수)` 불변식 필요 | 4차 Q1 |

> ⚠️ **초판의 논증은 틀렸다** (재리뷰 지적). *"저장만 안 할 뿐 부담은 같다"* 고 썼는데, **저장 크기·락 범위와 조회 비용은 다른 축**이다. 같은 양식이 ①(파생값 — 저장하지 않고 조회)을 선택지로 두는 것 자체가 둘이 다르다는 전제다.
>
> **정정된 논증**: (c)의 기각 근거 *"목록을 들면 계좌가 승인 수만큼 커진다"* 는 **저장 비용에만** 유효했고, **배분 순서(FIFO)·보류 제외·회수 진행도**를 다루지 못했다. 그 셋은 저장 여부와 무관하게 **어딘가에 있어야 하는 정보**이며, (a)는 그것들을 **승인·계좌·규칙에 흩뿌렸다.** 기각 근거가 결정의 핵심 축을 비켜 갔으므로 **성립하지 않았다** — T4에 해당한다.
>
> ⚠️ **그리고 재심 결과가 §5.2 선택지에 반영되지 않았다.** (c)의 기각이 무효라고 판정해 놓고 (A)(B)(C) 어디에도 (c)가 없다. **재심이 이미 정한 결론(②)의 정당화에만 쓰인 것** — 사후 합리화다. 다만 **결론은 유효하다**: T1·T2(같은 자리 4라운드 재발)가 §2 원문으로 충분히 입증돼 있고, ②는 (c)를 애그리게이트 수준으로 실현한 것이므로 실질적으로 (c)를 채택한 셈이다.

---

## 5. 선택지와 영향 범위

### 5.1 먼저 — 세 필드를 각각 판정한다

새로 만든 **합계 필드 판정**(양식 §4)을 적용하면 셋의 답이 다르다.

| 필드 | 저장 대신 매번 계산하면 안 되는 이유 | 값에 상태·수명·개별 규칙이 붙나 | **판정** |
|---|---|---|---|
| 계좌 `receivable` | (성능 문제 없음 — 승인 차단 판단은 "0인가"만 필요) | ✅ **붙는다** — 발생 영업일(FIFO 키) · 보류 여부 · 회수 진행도 | **② 별도 애그리게이트** |
| 계좌 `holdTotal` | ✅ 실시간 승인 경로(p99 3초)에서 가용잔액 계산에 매번 필요 | ❌ 홀딩은 승인 수명에 종속 | **③ 의식적 예외** |
| 카드 `usage` | ✅ 실시간 승인 경로에서 한도 검사에 매번 필요 | ❌ 기준일 리셋 외 규칙 없음 | **③ 의식적 예외** |

> **`receivable`만 ②인 이유가 명확하다.** 미수에는 **FIFO 정렬 키 · 보류 플래그 · 회수 진행도**가 붙는데, 그것들을 계좌 합계에 담을 수 없어서 승인에 흩뿌렸고 그 흩어짐이 네 라운드의 치명을 만들었다.
>
> 정산 `captureTotal`·`refundTotal`도 합계 필드지만 **마감 시점 스냅샷이고 이후 불변**이라(INV-4) 위 셋과 성격이 다르다. STS3에서 집계 주체를 확정했으므로 ③으로 등재만 한다.

### 5.2 선택지

| 선택지 | 내용 | 여는 문서 | 결과 |
|---|---|---|---|
| **(A)** | **판정대로 — `receivable`은 ②, `holdTotal`·`usage`는 ③** | 규칙 2 · 애그리게이트 5 · 상태 머신 3 · 신규 2 | ✅ **제안** |
| (B) | `receivable`만 분리, 나머지는 손대지 않음 | 규칙 2 · 애그리게이트 3 · 신규 2 | 미수는 낫지만 **홀딩·한도는 약속인 채로 남는다** |
| (C) | 셋 다 ③ (등식·경계·탐지만 추가) | 규칙 2 · 애그리게이트 3 | **네 번째 패치** — FIFO 키·보류가 여전히 갈 곳이 없다 |

### 5.3 (A)의 영향 범위

| 문서 | 무엇이 바뀌나 |
|---|---|
| `product/01-business-rules.md` | BR-20·34·43 재작성 (미수 소유·상계·환불) · BR-04·05·24에 등식·탐지 추가 |
| `product/00-product-definition.md` | 없음 |
| **`domain/aggregates/receivable.md`** | **신규** — 미수 애그리게이트 |
| **`domain/state-machines/receivable.md`** | **신규** — 미결 / 부분회수 / 회수완료 / 소멸 |
| `domain/aggregates/account.md` | `receivable` 필드 제거 · INV-3·4·5·PRE-2 재작성 · `holdTotal`에 ③ 3종 추가 |
| `domain/aggregates/authorization.md` | `recoveredAmount` 제거(미수가 소유) · 환불 산식 조정 |
| `domain/aggregates/card.md` | `usage`에 ③ 3종 추가 |
| `domain/aggregates/README.md` | E3 예외 재작성 (배분이 미수 애그리게이트로 이동) |
| `domain/state-machines/authorization.md` | T15·F23 재작성 · 매트릭스 열 조정 |
| `domain/state-machines/card.md` | ③ 근거 1줄 |
| `domain/context-map.md` | 미수 소유권 행 · C1↔C3 관계 재검토 |
| `domain/event-storming.md` | 미수 이벤트 재배치 |

**신규 2 · 수정 10.** 상태 머신 총계는 7 → 8종.

### 5.4 안고 가는 위험

| 위험 | 왜 감수하나 | 실현되면 무엇을 보나 |
|---|---|---|
| **애그리게이트 10종** — 도메인이 커 보인다 | 미수는 **생명주기와 개별 규칙이 있는 개념**이다. 숨긴 대가가 치명 4건이었다 | 미수 상태 머신의 전이가 3개 이하면 과했다는 신호 |
| **승인 차단(PRE-2)이 조회를 요구** | "이 계좌에 미결 미수가 있는가"는 **존재 여부만** 필요해 인덱스로 해결 가능 | 실시간 경로 p99가 흔들리면 캐시 도입 — **그때는 ③으로 등재하고 3종을 적는다** |
| **`holdTotal`·`usage`는 여전히 합계 필드** | 실시간 경로 비용이 실재한다 | ③의 탐지 장치(대사)가 불일치를 잡으면 그때 ②를 재검토 |
| **Phase 2를 다시 연다** | 지금 열지 않으면 Phase 3 트랜잭션 설계가 틀린 경계 위에 올라간다 | — |

---

## 6. 사용자 합의

| 항목 | 내용 |
|---|---|
| **질문한 것** | ① 미수 모델링 — 별도 애그리게이트 / 파생값 / 현 구조 패치 ② 정산 완료 후 환불 — 금액 필드 갱신 / 차감 거래 애그리게이트 신설 |
| **답변** | ① **미수를 별도 애그리게이트로 분리** ② **차감 거래 애그리게이트 신설** |
| **답변 뒤 제기된 것** | *"그냥 수치를 바꿔버리면 문제 있는 거 아니야?"* → 정산된 거래의 금액을 덮어쓰면 **감사 시점 값이 사라진다**(BR-10 append-only와 같은 논리). ②의 선택이 원칙상 더 일관됨을 확인 |
| **답변에 없어 내가 판단한 것** | ⓐ `holdTotal`·`usage`를 ②가 아니라 **③**으로 둔 것 — 실시간 경로 비용 때문 ⓑ 정산 `captureTotal`을 ③ 등재만 하는 것 — 마감 스냅샷이라 성격이 다름 ⓒ 미수 상태 머신을 4상태로 잡은 것 |
| **합의 (2026-08-04)** | **(A) 판정대로 진행** — `receivable`은 ②, `holdTotal`·`usage`는 ③. 영향 범위 신규 2 · 수정 10 승인 |
| **작업 방식** | **절차 단계마다 커밋한다** — 한 번에 몰아 커밋하면 어느 단계에서 무엇이 깨졌는지 되짚을 수 없다 |

> ⚠️ **차감 거래(RefundTransaction)는 이 문서의 범위 밖이다.** 사용자가 신설을 선택했으나, 그 설계는 **원거래별 환불 누계를 어디가 소유하느냐**를 먼저 풀어야 한다(4차 Q2). 미수 소유가 정리된 뒤 **DC-002**로 다룬다 — 같은 뿌리(소유 불명확)이므로 순서를 지킨다.

---

## 7. 변경 내역

순서는 **기준소스(BR) → 애그리게이트 → 상태 머신**. **각 단계마다 커밋한다.**

| 단계 | 문서 | 무엇을 | 커밋 | 상태 |
|---|---|---|---|---|
| **0** | 절차·양식·진단 | 토대 변경 절차 · 합계 필드 판정 · DC-001 §1~§6 | `66952c7` | ✅ |
| **1** | `product/01-business-rules.md` | BR-20·34·43 재작성(미수 소유·상계·환불) · BR-04·05·24 ③ 3종 · BR-41 탐지 | `91ec6e4` | ✅ |
| **2** | `domain/aggregates/receivable.md` **신규** · BR-28 · 애그리게이트 README | 미수 애그리게이트 · 보류 분리 · E3 재작성 + E4 신설 | `e2f315c` | ✅ |
| **3** | `domain/aggregates/account.md` · `authorization.md` · `card.md` · `README.md` | `receivable` 제거 · `recoveredAmount` 이관 · `holdTotal`·`usage`에 ③ 3종 | `4f6446f` | ✅ |
| **4** | `domain/state-machines/receivable.md` **신규** · 상태 머신 README | 미수 상태 머신 · 설계 판단 ⑦ | `b95a951` | ✅ |
| **5** | `domain/state-machines/authorization.md` · `card.md` · `README.md` | T15·F23 제거 · 매트릭스 88칸 · ③ 근거 | `730630d` | ✅ |
| **6** | `domain/context-map.md` · `event-storming.md` | 소유권 행 · 이벤트 재배치 · 합계 필드 경고 | `3770115` | ✅ |
| **7** | DC-001 §8 | 기존 findings 재판정 — 소멸 7·잔존 5·변형 3 | `a8625c6` | ✅ |
| **8** | — | 재리뷰 (§9 검증) — **codex ∥ Opus, 중복 제거 18건 · 치명 6** | — | ✅ |
| **9** | 절차·양식 | **절차 결함 4건 보강**(P1~P4) + §10 채점 | `185f90b` | ✅ |
| **10** | 기준소스 → 파생 | **치명 6 + 높음 5 수정** — 10a `1fdc953` · 10b `c45270e` · 10c | `86ab494` | ✅ |
| **11** | DC-001 §8·§10 · 리뷰 ledger | **재판정 정정**(Q3 소멸 → 변형) · **리뷰 ledger 신설** | `3b8ee69` · `074d4bb` | ✅ |
| **12** | 애그리게이트·상태 머신·기준소스 | **R6 22건 반영 — 표 기반 기계 검산 도입**(변경 매트릭스 · 폐기 심볼 · ③ 체크리스트) · E5 신설 · M11 신설 | `833c4d4` | ✅ |
| **13** | — | **R7 듀얼 리뷰** — codex 8 · Opus, **치명 5**. 두 리뷰어 모두 **T5 발동** 판정 | — | ✅ |
| **14** | `study/project-workflow/**` | **검증을 주의력에서 구조로** — `verification-by-construction.md` 신설 + 실측 가드레일을 양식·작성법·완료 기준으로 회수 | `6177932` · `5027a84` | ✅ |
| **15** | `tools/**` · 애그리게이트 README | ★ **재슬라이스 — 매트릭스를 생성물로.** 손으로 쓴 18행 격자 → 문서에서 생성한 **58행 조작 대장**. 경계 참여자 기계 판독 + 상호 강제 검사 | `1664551` | ✅ |
| **16** | `authorization.md` · `01-business-rules.md` | **스크립트가 잡은 2건** — INV-10 잔재 · BR-09 10종 vs 9종 | `86e6397` | ✅ |
| **17** | `account.md` · `journal-entry.md` · `settlement.md` · README | **R7 치명 3건** — E5 정정 멱등 · 원장 이중 기표 · 정산 ③ 미완 | `e1fa335` | ✅ |
| **18** | — | **R8** — 강화된 스크립트 ∥ 사람 듀얼 리뷰, 판정 = *스크립트가 잡은 결함 > 사람이 잡은 결함* | | ⏳ |

---

### 7.1 T5 재슬라이스 — 무엇을 되돌렸나 ★ (R7)

R7에서 **두 리뷰어가 모두 T5 발동**(*"한 결함을 고치면 다른 곳이 깨지는 것이 반복"*)을 판정했다.
그런데 **되돌릴 대상이 도메인 경계가 아니었다.**

| 판단 근거 | 값 |
|---|---|
| 치명 중 **재슬라이스로 사라지는 것** | **0건** — 전부 "목록에서 빠짐"이지 경계 오류가 아니다 |
| 산식 층(BR-43 ① 환불) | **R6·R7 연속 반례 0** |
| 거짓 양성 | 0건 |
| 반면 **검산 장치**가 통과시킨 치명 | **4건** (U1이 지적) |

> **되돌린 것은 경계가 아니라 표와 스크립트다.** 도메인 모델은 두 라운드 연속으로
> 반례가 나오지 않았고, 깨진 것은 **그 모델을 문서에서 대조하는 방법**이었다.
>
> ★ **T5가 발동했다고 해서 대상이 자동으로 설계인 것은 아니다.** 발동 조건은
> *"반복해서 깨진다"* 이고, **무엇이 반복해서 깨졌는지**는 따로 세어야 한다.
> 여기서는 결함이 전부 **"목록에서 하나 빠짐"** 계열이었고, 그건 설계가 아니라 **대조의 실패**다
> (`study/project-workflow/verification-by-construction.md`).

---

## 8. 기존 findings 재판정 ★★

> 4차 듀얼 리뷰(codex 9 · Opus 13, 중복 제거 **15건**)를 전부 판정한다.
> **소멸을 기록하지 않으면 다음 리뷰가 같은 것을 또 지적한다.**

### 소멸 — 6건

그 결함이 **존재할 구조가 없어졌다.** 고치지 않는다.

| finding | 원 지적 | 어떻게 사라졌나 |
|---|---|---|
| **Q1** (치명) | 계좌 `receivable` 합계를 줄이는 경로가 없다 | **계좌에 `receivable` 필드가 없다.** 줄일 대상 자체가 사라졌다 (단계 3) |
| **Q4** (Opus) / **Q3** (codex) | 보류 미수 제외가 상계 알고리즘에 없다. 상계액과 배분 합계가 어긋난다 | **회수 대상을 먼저 고르고 그 합으로 회수액을 정한다**(BR-34). 보류는 대상 선정에서 걸러지므로 어긋날 수 없다 (단계 1·3) |
| **Q5** (codex) | T15의 금액 실패 경로가 매트릭스·F 행에 없다 | **T15가 사라졌다.** 대응하는 금액 조건은 미수 **VF4**(`amount ≤ outstanding()`)가 소유한다 |
| **Q8** (Opus) | FIFO 정렬 키 `매입 영업일`이 승인에 존재하지 않는다 | **미수가 `incurredBusinessDate`를 소유**한다. 정렬 키가 정렬 대상과 같은 애그리게이트에 있다 (단계 2) |
| **Q9** (Opus) | E3와 E2의 락 획득 순서가 반대라 교착 | **미수를 `(발생 영업일, 식별자)` 순으로 먼저 잠근다** — FIFO 키가 락 순서 키를 겸한다 (`receivable.md` 상태 머신 §6) |
| **Q12** (Opus) | 반환액 0인 환불에서 `account.refund(amount > 0)` 사전조건이 깨진다 | **`refund(amount ≥ 0)`으로 완화**하고 `refund(0)`이 정상임을 명시 (단계 3) |

### 잔존 — 5건

토대와 무관하다. **고쳐야 한다.**

| finding | 원 지적 | 왜 남았나 |
|---|---|---|
| **Q6** | 삭제된 `receivableAmount`와 옛 상한(`withdrawnAmount`)을 참조하는 절이 남아 있다 | 잔재 정리 — 단계 3에서 승인 §5를 교체했으나 **F18 주석 등 다른 곳은 미확인** |
| **Q7** (codex) / **Q7** (Opus) | `Refunded` 이벤트에 거래 축소액·반환액·미수 소멸액 분해가 없어 원장이 역분개 금액을 모른다 | 이벤트 설계 문제. **미수가 `ReceivableWrittenOff`를 따로 발행**해 일부 해소됐으나, 승인의 `Refunded`가 여전히 한 금액만 싣는다 |
| **Q10** (Opus) | `SETTLED` 차감 거래를 C3 버킷에 넣을 조작이 없다 (`applyCancellation` 부재) | 매입 배치의 조작 목록 문제 — DC-001 범위 밖 |
| **Q11** (Opus) | 매입 격리와 대사가 서로 다른 `subject`로 적재해 DCS2가 무력화 | 불일치 유일성 키 문제 — DC-001 범위 밖 |
| **Q13** (Opus) | F23 상태 범위와 매트릭스 불일치 · `INVESTIGATING` 탈출 경로 부재 | **F23은 소멸**(제거됨). `INVESTIGATING`(DCS1)은 잔존 |

### 변형 — 3건

같은 문제가 **다른 형태로** 남았다. **새 finding으로 다시 쓴다** — 옛 서술을 고치면 안 된다.

| 원 finding | 원 지적 | 새 형태 | 새 번호 |
|---|---|---|---|
| **Q2** (양쪽, 치명) | `SETTLED` 환불이 `refundedTotal`을 안 올려 상한을 우회 | **여전히 우회한다.** 미수 분리로 회수 쪽은 정리됐지만 **누적 축소액의 소유는 미정** — BR-43 ②에 명시하고 **DC-002**로 이관 | **DC-002** |
| **Q2** (codex, 치명) | 차감 거래 소유자 미정이라 상한·회수액을 강제할 곳이 없다 | 〃 | **DC-002** |
| **Q5** (Opus) | E3가 한 트랜잭션인데 이벤트 표는 비동기 구독 | **E3의 대상이 승인들 → 미수들로 바뀌었다.** `ReceivableRecoveryAllocated`는 제거됐고 미수가 자기 이벤트를 발행하지만, **E3 안에서 발행인지 뒤인지**가 여전히 미정 | **DC-001-a** |
| **Q3** (Opus, 치명) | `SETTLED` 전액 환불 뒤에도 미수를 계속 회수 | ★ **메커니즘만 죽고 결과는 살아 있다.** T15는 제거됐지만 `SETTLED` 환불이 `refund()`를 안 타므로 미수가 `OPEN`으로 남고 후속 입금이 `recover()`로 회수한다 — **같은 사고, 다른 경로** | **DC-002** |

### 8.1 재판정 정정 (2026-08-04, R5)

**Q3를 "소멸"로 판정한 것이 오판이었다.**

| | 초판 | 정정 |
|---|---|---|
| 판정 | 소멸 | **변형** |
| 근거 | *"`recoverReceivable`(T15)이 제거됐고 종결된 미수는 VF1이 막는다"* | **VF1이 발동하지 않는다** — `SETTLED` 환불은 승인 `refund()`도 E4도 타지 않아 미수가 `OPEN`으로 남는다. 차감 거래가 `writeOff()`를 부르는 계약도 아직 없다 |

**무엇을 잘못 물었나**

```
잘못된 질문:  지적된 메커니즘(T15)이 아직 있는가?          → 없다 → 소멸
옳은 질문:    그 나쁜 결과에 도달하는 경로가 하나라도 남았나? → 남았다 → 변형
```

> ★ **이 오판이 절차 결함 P4를 낳았고, 그것이 토대 변경 절차 ⑧에 반영됐다** — *"소멸 판정에는 반드시 '이 결과에 도달할 경로가 없다'의 근거를 적는다. 경로 부재를 못 보이면 변형이다."*
>
> **소멸을 잘못 판정하면 결함이 목록에서 지워진 채 살아남는다.** 절차가 경고한 것은 *"소멸을 기록 안 하면 또 지적된다"* 였는데, **실제로 더 위험한 것은 그 반대**였다.

**집계: 소멸 6 / 잔존 5 / 변형 4 = 15건**

> ⚠️ **초판은 소멸 7 / 변형 3이었다.** Q3를 소멸로 판정한 것이 **오판**이었고, R5가 그것을 잡았다 — 아래 §8.1.

> ★ **치명 3건 중 소멸은 Q1 하나다.** Q2·Q3는 **둘 다 `SETTLED` 경로**이고 **DC-002로 이관**됐다 — **이관은 지연이지 해소가 아니다.**
>
> ⚠️ **소멸 6건을 고치려 들었다면** 이미 없는 구조를 위해 규칙을 만들고, 그 규칙이 다음 라운드의 새 지적을 낳았을 것이다. 이것이 ⑧을 건너뛰면 안 되는 이유다.

### 이 변경이 새로 만든 것 — DC-001-a

| # | 내용 | 왜 지금 안 닫나 |
|---|---|---|
| **DC-001-a** | **E3·E4에서 이벤트를 커밋 안에서 발행하는가, 뒤에 발행하는가.** 미수의 `ReceivableRecovered`·`ReceivableWrittenOff`를 원장이 받는 시점이 정해지지 않았다 | BR-40이 **원장 기표를 별도 논리 단위**로 두었으므로, 이것은 Phase 3 트랜잭션 설계와 함께 정해야 한다 |

---

## 9. 검증

> §2의 실패 이력이 **재현되지 않는지** 확인한다. 새 설계가 좋아 보이는 것과 옛 실패를 막는 것은 다르다.

### 단계 8 재리뷰 결과 (2026-08-04)

| 옛 실패 | 새 구조에서 | 검증 결과 |
|---|---|---|
| **1차** — 이미 풀린 홀딩 재해제 | `holdTotal`을 ③으로 등재, 등식 INV-6 + 탐지 BR-41 | ❌ **미검증** — **등식의 우변이 존재하지 않는다.** 승인이 홀딩액을 안 가지며(§7이 명시적으로 금지), 그래서 BR-41의 주입 테스트를 **실행할 수 없다** (D3) |
| **2·3·4차** — 환불↔미수 접합부 | 미수가 자기 회수·소멸을 소유 | ⚠️ **부분** — 산식의 자금 보존은 4개 시나리오에서 확인됐으나, **INV-10이 산식과 모순**(D1)하고 `writeOff` 순서가 미정(D6)이라 정상 경로가 실패한다 |
| **4차** — 계좌 합계 감소 경로 없음 | 계좌에 `receivable` 필드가 없다 | ✅ **해소** — 줄일 대상 자체가 사라졌다 |
| **1차** — 한도 이중 복원 | `usage`를 ③으로 등재, 등식 INV-5 + 탐지 | ❌ **미검증** — 홀딩과 같은 이유. 게다가 **E1이 복원 경로를 덮지 않는다**(D4) |

**4건 중 해소 1 · 부분 1 · 미검증 2.**

> ★ **③으로 등재한 두 필드가 검증에 실패했다.** 등식·경계·탐지 셋을 적었지만 **등식의 우변을 계산할 정본이 없고**(D3) **경계는 승인 성립만 덮는다**(D4). 양식이 정한 기준 — *"셋 중 하나라도 없으면 그 필드는 다시 약속으로 돌아간다"* — 에 그대로 걸린다.
>
> **방향은 유효하다.** 양쪽 리뷰어가 미수 분리(②)를 옳은 방향으로 판정했고, 제거된 치명(Q1·Q8·Q9)은 **불변식과 FIFO 키의 소유**가 만든 것이다. **문제는 방향이 아니라 실행이다.**

### 이번 재리뷰가 드러낸 **절차** 결함 4건

> 셋은 **내가 만든 절차가 잡았어야 할 것**이다. 단계 9에서 보강한다.

| # | 무엇이 샜나 | 어디서 드러났나 |
|---|---|---|
| **P1** | **교차 애그리게이트 술어의 분류 규칙이 없다.** 양식 §4는 *합계 필드*만 판정하고, "이 술어를 INV로 둘 것인가 PRE인가 프로세스 규칙인가"는 여전히 방법이 없다 | 뿌리로 지목한 원칙을 **새 구조가 4번 어겼다** — 계좌 INV-3·4·6·7 (D8) |
| **P2** | **반증 신호를 등록만 하고 채점하지 않는다.** 변천 문서 §5.4에 "실현되면 무엇을 보나"를 적지만, 그것을 **판정하는 단계가 절차에 없다** | *"전이 3개 이하면 과했다는 신호"* 가 발화했는데 §8·§9에 판정 없음 (D13) |
| **P3** | **커밋 전에 실제 diff를 확인하지 않는다.** 단계 2 커밋 메시지에 *"E3 재작성 · E4 신설"* 이라 적었으나 **파일은 안 바뀌었다** | E4가 목록에 없는데 5곳에서 사용 (D2) |
| **P4** | **재판정의 "소멸" 기준이 느슨하다.** *메커니즘*이 사라진 것과 *결과*가 사라진 것을 구분하지 않았다 | Q3를 소멸로 판정했으나 `SETTLED` 경로로 **결과가 그대로 재현**된다 |

---

## 10. 반증 신호 채점 ★

> §5.4에 등록한 위험을 판정한다. **채점하지 않은 기준은 장식이다.**

| 신호 (§5.4) | 발화했나 | 판정 | 근거 |
|---|---|---|---|
| **미수 상태 머신의 전이가 3개 이하면 과했다는 신호** | ✅ **발화** — 상태를 바꾸는 전이는 V1·V3·V5 **3개**(V2·V4는 자기 루프, V6은 상태 불변) | **기각** | 제거된 치명(Q1·Q8·Q9)은 **상태 수가 아니라 INV-1·INV-4·FIFO 키의 소유**가 만들었다. 그 셋은 계좌·승인 어디에도 자리가 없었다. 상태가 얇은 것은 **분리의 목적이 상태 머신이 아니었기** 때문이다 |
| **승인 차단(PRE-2)이 조회를 요구 — 실시간 경로 p99** | ❌ 미발화 | 보류 | Phase 5 부하 테스트에서 측정. 흔들리면 캐시 도입 후 **③으로 등재하고 3종을 적는다** |
| **`holdTotal`·`usage`가 여전히 합계 필드** | ✅ **발화** — ③의 등식 우변이 존재하지 않고 경계가 성립 안 함(§9) | **수용 → 처리 완료** | 단계 10·12에서 우변(`heldAmount`·`limitContribution`)과 경계(E1·E2)와 탐지(M9·M10·M11)를 채웠다. ②로 승격하지 않고 ③을 완성하는 쪽을 택했다 — 실시간 경로 비용이 실재하기 때문 |
| **예외가 다섯 번째를 요구하면 경계를 다시 본다** (애그리게이트 README) | ✅ **발화** — `reverseDeposit`이 계좌+미수를 함께 바꾸는데 E1~E4 밖 (R6) | **기각** | 경계를 다시 본 결과 **다섯 개가 전부 "자금이 움직이는 경로"** 이고, 자금 이동은 원래 여러 소유자를 건드린다. 문제는 **예외 수가 아니라 참여자를 빠뜨리는 것**이었고 그것은 **변경 매트릭스로 기계 검산**한다. 여섯 번째에 다시 본다 |
| **애그리게이트 10종 — 도메인이 커 보인다** | ❌ 미발화 | 보류 | 미수 분리 자체는 양쪽 리뷰어가 유효 판정 |

> ★ **셋째 신호가 발화했고 수용했다.** ③이 성립하지 않는다면 처음부터 ②였어야 한다는 뜻이므로, 단계 10에서 **판정을 다시 한다.**

---

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v0.3 | 2026-08-04 | **단계 11** — §8 **재판정 정정**(Q3 소멸 → 변형, 집계 소멸 6·변형 4) + §8.1에 *무엇을 잘못 물었나* 기록. **리뷰 ledger**(`DC-001-review-log.md`) 신설 — 5라운드 89건·치명 15건 전건 추적과 뿌리 귀속 |
| v0.2 | 2026-08-04 | 단계 8 재리뷰 결과(§9) · 절차 결함 4건 등재 · **§10 반증 신호 채점 신설** — 4개 중 2개 발화, 하나는 기각(상태 수는 분리의 목적이 아니었다) 하나는 **수용**(③ 미성립 → 단계 10에서 판정 재실시) |
| v0.1 | 2026-08-04 | §1~§6 작성 — 실패 이력 4라운드 수집, 뿌리를 **양식의 불변식 판정 부재** 하나로 특정, AU9 (c) 기각 근거 재심에서 T4 확인, 세 필드를 ②③으로 판정, 영향 범위 신규 2·수정 10 산정. **§6 합의 대기** |
