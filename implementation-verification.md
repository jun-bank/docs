# [구현 검증] 중앙 대장

> 검증 정책 v2(V2-3) — 문서 단계에서 판정하지 않고 **구현·테스트에서 실제 오류인지 판정**할 항목의 전수 집계. 각 문서의 의문 표에 `[구현 검증]` 태그와 함께, 여기 **같은 커밋으로 등재**한다(빠지면 결함). Phase 5 착수 시 이 대장이 실험·테스트 목록의 입력이다.
>
> ★ **칸 사용 규칙**(PD-7 · 소비 절차 = `study/project-workflow/phase5/05-parallel-documentation.md` **§2.1 ⓪~⑷**): **선언값**은 ⓪ **측정 전에** · **판정 결과**는 ⑴ 판정일·기준 SHA·실행 명령·관측값·**선언값 대비 판정**·발동한 되돌림 조건 · **판정 승인**은 ⑵ **승인자·승인일**(탐색 태그 = 독립 판정자 / 그 외 = 리뷰 — 승인 없이 본문 태그를 해소하지 않는다)을 적고, ★ **아직 도래하지 않은 칸은 `—`** 로 둔다(빈칸으로 비우지 않는다 — 판정 불성립도 결과다 · PD-9).

| # | 항목 | 출처 문서 | 무엇을 구현이 판정하나 | ★ 선언값(⓪) | ★ 판정 결과(⑴) | ★ 판정 승인(⑵) |
|---|---|---|---|---|---|---|
| IV-1 | 스코프 판정·인가 캐시의 최대 수명 상한 | ADR-016 · operator.md(OP2) | 실측 지연·회수 창의 허용 한계 — 수치는 부하·운영 관측 후에만 의미 | — | — | — |
| IV-2 | 트리 스코프의 목록 필터 전개 비용(재귀 CTE vs IN 집합) | org-unit.md(OU3) · ADR-012 | 실행계획·인덱스 영향 — 조직 수 실측 없이는 판정 불가 | — | — | — |
| IV-3 | 트리 깊이 상한 | org-unit.md 미확정 수치 | 판정 비용 실측과 함께 | — | — | — |
| IV-4 | 재인증 주기·임시 대행 최대 기간·break-glass 시간 상한 | BR-59·61 미확정 수치 | 운영 리듬 실측 | — | — | — |
| IV-5 | 페이징 size 상한·커서 형식 세부 | UC-03·17·19·20 | 실제 응답 크기·지연 | — | — | — |
| IV-6 | R15 지시 ↔ 원전표 파티션 간 순서(재투입 비복원 포함) | UC16-4·UC19-2 · ADR-014 O-8 | 릴레이 구현에서만 재현·판정 가능 | — | — | — |
| IV-7 | 대행(DELEGATION) grant의 승인 흐름 상호작용 세부 | role-grant.md(RG3) | 승인 워크플로 구현 접촉 필요 | — | — | — |
| IV-8 | linkedCustomerId 신원 대조 절차 | operator.md(OP3) | 실명 확인 연동 방식 — 외부 시스템 접촉 | — | — | — |
| IV-9 | C7 전파 컨텍스트의 직렬화 형식·크기 | ADR-016 AU-3 | 요청 경로 구현 | — | — | — |
| IV-10 | 만료·재인증 초과 grant가 캐시 수명 안에서 통과하는 창의 실제 크기 | 루프1 L1-18 | 캐시 구현 후 측정 | — | — | — |
| **IV-11** | **입금 채널의 속도 제한 방식·상한**(요청/초·기관별 창) · **1회 입금 상한**과의 관계 | `interfaces/README.md`(X-10 ⑷) · `interfaces/deposit-advice.md` §3·D-6 · 리뷰 R-05 | 판정 순서(①·② 선행)가 계좌 열거를 좁힌 **뒤에도 남는 노출**의 크기 — 정상 형태 반복 조회를 어디서 끊어야 입금원 정상 트래픽을 죽이지 않는지는 **실트래픽 없이는 판정 불가** | — | — | — |
| **IV-12** | **요청 지문 keyed hash의 알고리즘 선정·키 보관·키 교체 절차** | `interfaces/README.md`(X-9) · 리뷰 R-09 | 기관과 공유하는 키의 배포·교체가 **무중단으로 되는지**(교체 창에 두 키 병행 검증이 필요한지)는 구현·운영 접촉이 있어야 판정된다. 알고리즘은 그때의 표준 권고를 따른다 | — | — | — |
| **IV-13** | ★ **발행–소비 조기 신호의 지표 정의와 임계** — ⑴ **outbox age**(파티션별 미전달 최대 경과) ⑵ **전달 실패율**(재시도 진입 비율) ⑶ **`DLQ_UNRESOLVED` 건수**. 각각의 **측정 주기·경보 임계**와 *"커트오프가 막히기 전에 뜨는가"* | **ADR-023**(OB-5) · ADR-014 O-10~O-16 · BR-52 | 임계는 **평상시 전달 지연 분포**를 모르면 정할 수 없다 — 낮게 잡으면 상시 경보가 되어 M17과 같은 무력화 경로를 타고, 높게 잡으면 **M16보다 늦게** 떠서 조기 신호가 아니게 된다(ADR-023 R1). 폴링 주기·배치 크기 실측이 선행 입력 | — | — | — |
| **IV-14** | ★ **mTLS·서명 키의 발급·보관·교체 절차**(병행 검증 창) — ⑴ **내부 mTLS 인증서**(릴레이 → inbox · ADR-021 ET-3) ⑵ **서명된 내부 컨텍스트의 서명 키**(ADR-019 CH-3) ⑶ **배포 요청 인증키**(HMAC·OIDC · ADR-024 RL-8). 각각의 **보관 위치·교체 주기**와 ★ **신·구 키를 동시에 인정하는 병행 검증 창**이 **무중단으로 성립하는가** | **ADR-019**(CH-3 · §4) · **ADR-021**(ET-3) · **ADR-024**(RL-8) · 리뷰 N11 | 교체는 *"바꾸면 된다"* 가 아니다 — **구 키로 서명된 컨텍스트·요청이 살아 있는 동안** 신 키를 받아들이려면 병행 검증 창이 필요하고, 그 길이는 **컨텍스트 수명(IV-1)·재전송 규약·배포 순서**에 걸린다. 무중단 여부는 **실제 교체를 한 번 해봐야** 판정된다(IV-12가 **기관과 공유하는** 키에 대해 같은 형태로 등재돼 있다 — 이쪽은 **내부 키·인증서**다) | — | — | — |
| **IV-15** | **통합 계층 기동+실행 시간의 빌드당 상한** — ★ **측정 전에 상한을 선언**해야 첫 측정이 판정이 된다(선언 없이 측정하면 되돌림 조건 TL-2 R1이 인상으로 결정된다) → 첫 측정으로 E2 §5 예산 행을 채우고 TL-2 R1·격리 단위(DV-3)를 판정 | dev-conventions.md **TL-2 R1** · test-strategy.md §5 · E3 리뷰 F-06 | 상한 값 자체는 하드웨어·케이스 수에 걸려 문서로 정할 수 없다 — 단 **선언(상한) → 측정 → 판정** 순서는 규약이 고정한다. 구현 착수(첫 통합 테스트)에서 상한 선언부터 | — | — | — |
| **IV-16** | 헬스체크 판정 수치(연속 N회·간격 T·상한 D — CDV-1) | operations/cicd.md CD-1 | 플랩·기동 시간 실측이 있어야 N·T·D가 판정된다 | — | — | — |
| **IV-17** | 사람 채널 드레인 대기 상한(CDV-2) | operations/cicd.md CD-2 ⒜ | in-flight 분포 실측 전에는 상한이 인상이다 | — | — | — |
| **IV-18** | 전문 정지 창 상한(CDV-3) — ⚠️ CH-7 [미확정]에 걸림: ADR-024 R1 재판정 시 함께 연다 | operations/cicd.md CD-2 ⒝ · CH-7 | 기관 재전송 규약이 정지 창 허용치를 정한다 | — | — | — |
| **IV-19** | 배포 창 락 lease 길이·갱신 주기(CDV-4) — ★ 하한 = 배포 전 구간 합(이미지 조회+SSH 왕복(② — 컨테이너 판 CDV-14)+green 기동+헬스체크 D+사람 채널 드레인 CDV-2+전문 정지 창 CDV-3+blue 종료)보다 커야 한다(E4 리뷰 정정 — 이전 문면 "D+CDV-3"은 하한 미달) · 측정 전 선언값 먼저 | operations/cicd.md CD-3 | 배포 소요 실측 전에는 만료가 판정 불가 | — | — | — |
| **IV-20** | 아티팩트 기본 보관 개수 N(CDV-5 — 하한 = max(N, 열린 expand-contract 창)) | operations/cicd.md CD-5 | 디스크·릴리스 빈도 실측 | — | — | — |
| **IV-21** | 브랜치 보호 설정값(CDV-6) — 설정 시점에 dev-conventions PR-1 승격·집계 개정 동반 | operations/cicd.md §3.2 · dev-conventions.md PR-1 | 저장소 생성 직후·첫 구현 커밋 전 | — | — | — |
| **IV-22** | 배포 키의 러너 로그 노출 방지 수단(CDV-7) | operations/cicd.md CD-4 | 러너 환경 실물에서만 판정 | — | — | — |
| **IV-23** | 호스팅 러너에서 컨테이너 런타임 성립(CDV-8) — 뒤집히면 TL-2 R3 발동 | operations/cicd.md §6 | 첫 CI 실행이 판정한다 | — | — | — |
| **IV-24** | 위성 정지 구간 상한(CDV-9) — 릴레이 재시도 소진 → DLQ_UNRESOLVED(IV-13) → BR-52 미완으로 새는 경로의 허용치 | operations/cicd.md CD-4 ⑷ · ADR-014 재시도 규약 | 재시도 간격·소진 시간 실측이 상한을 정한다 · 측정 전 선언값 먼저 | — | — | — |
| **IV-25** | CI 플랫폼 아티팩트 보관이 §6.1 ⑶(재업로드 거절·체크섬·보관 하한)을 만족하나(CDV-10) | operations/cicd.md §6.3 | 플랫폼 보존 정책 실확인 전에는 "만족할 수 있다"는 판정이 아니다(E4 리뷰 F-16) | — | — | — |
| **IV-26** | CI 플랫폼 OIDC/HMAC 발급 성립·오라클 프록시 경유 도달성(CDV-11) | operations/cicd.md §6.2 | 첫 파이프라인 실행이 판정 — 실패 시 RL-7 재판정 | — | — | — |
| **IV-27** | ★ **리포 공개 설정과 이슈 form `required: true`의 실동작** — private면 필수 칸 강제가 성립하지 않는다(조직 Issue Types 가용성도 같은 판정에 걸린다) | git-conventions.md **GC-18**·GV-1 | GitHub 공식은 *"`required`는 public 리포에서만 동작"* 이라 적는다 — 우리 리포가 어느 쪽인지와 실제 폼 동작은 **저장소를 봐야** 판정된다. ⚠️ 판정 전 yml form으로 전환하면 *"필수라 적혀 있는데 강제되지 않는"* 상태가 되어 md 템플릿보다 나쁘다 | — | — | — |
| **IV-28** | ★ **커밋 형식 검사 도구의 실행 환경** — commitlint는 Node 도구인데 스택은 Kotlin/Gradle(C-05)이다. Node 런타임 도입 / JVM 대체 구현 / CI 전용 검사 중 무엇이 성립하나 + **라벨 파일 ↔ 리포 라벨 diff 검사** 수단 | git-conventions.md **GC-1·GC-4~9·GC-23·GC-29**·GV-2 · dev-conventions **PR-7** | ★ **규칙 값은 문서가 이미 정본이다**(type-enum 8+1 · header-max-length 100 · subject-case 소문자) — 판정 대상은 **값이 아니라 실행 환경**이다. 러너·훅 설치 지점(로컬 commit-msg vs CI)에 따라 rebase 머지(GC-13) 시 검사 범위가 달라진다 | — | — | — |
| **IV-29** | ★ **Gradle에서 「커밋 → 버전」 자동화가 성립하나** — release-please `java` 전략 + `extra-files`로 버전 파일(`gradle.properties` 등)이 갱신되는가 | git-conventions.md **GC-31**·GV-3 | ⚠️ **release-please에 Gradle 전용 전략이 없고**(maven 전략은 `pom.xml` 전용 · java 전략은 파일을 스스로 갱신하지 않는다) **axion-release는 Conventional Commits를 읽지 않는다**(태그 기반). 둘을 잇는 정평 있는 관습이 리서치에서 확인되지 않았다 — **실제로 붙여봐야** 판정된다. GC-31 도입 조건이 발동하기 전에는 열지 않는다 | — | — | — |
| **IV-30** | ★ **버전 카탈로그 발행·소비와 Gradle dependency locking이 5리포에서 성립하나** — `version-catalog` 플러그인 발행 → 소비 측 `from("...")` 임포트 · 락 파일 커밋 · GitHub Packages 인증(`GITHUB_TOKEN`) | git-conventions.md **GC-34·GC-35·GC-36** | 카탈로그 임포트와 락 파일은 **빌드가 판정**한다(문서로는 성립 여부를 알 수 없다). ⚠️ GitHub Packages는 **owner 소문자만** 지원하므로 org 이름과 아티팩트 좌표의 실제 조합도 첫 발행이 판정한다 | — | — | — |
| **IV-31** | **Dependabot `versioning-strategy`의 값 목록과 `gradle` ecosystem 조합** | git-conventions.md **GC-34** | ⚠️ r5 리서치에서 **원문 회수 실패**(미확인 공백 4) — `auto`/`increase`/`widen`/`lockfile-only` 등으로 알려져 있으나 1차 출처 미확인이고, gradle에서 어느 값이 유효한지는 **설정 파일을 실제로 돌려봐야** 판정된다 | — | — | — |
| **IV-32** | 레지스트리 무료 한도·이미지 크기·호스트 디스크의 관계(CDV-12) — 크기×보관 개수 ≤ 최소 여유 호스트, 총량 ≤ 플랫폼 한도. 넘으면 정리가 롤백 사본을 지운다(RL-5 불성립 → ADR-024 R6) | cicd CD-5·§6.4 | — | — | — |
| **IV-33** | compose 헬스체크·재시작 정책과 CD-1의 관계(CDV-13) — 재시작이 "연속 N회"를 감추지 않는 수단(재시작 횟수를 판정 입력에)이 성립하나. 그린 위장의 컨테이너 판 | cicd CD-1·CD-4 ⑶ | — | — | — |
| **IV-34** | 배포 1회 실소요 재산정(CDV-14) — ② 항이 이미지 조회+원격 SSH 왕복으로 바뀜. IV-19 lease 하한 관계식의 입력 | cicd CD-3 · ADR-027 §6 | — | — | — |
| **IV-35** | 배포 SSH 키의 명령·출발지 제한 실효성·교체(IV-14의 넷째 대상) + 호스트별 레지스트리 조회 자격 보관·교체(CDV-15 — DO-9) | cicd §5 ⑷⑸ · ADR-027 §6 | — | — | — |

> 등재 기준(V2-6): *"이 답이 틀렸다는 걸 문서만 읽고 알 수 있나?"* — 없으면 여기. 정본 충돌·판정 공백은 여기 오지 않는다(사용자 합의 대상).

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v2.0 | 2026-08-07 | E-배포 개정: IV-32~35(CDV-12~15 — 레지스트리 한도·compose 헬스·배포 소요·SSH 키 제한) 등재 · IV-19 문면 갱신 |
| v1.9 | 2026-08-07 | **깃 규약(`git-conventions.md`) 등재**: IV-27(이슈 form `required` — 리포 공개 설정 종속) · IV-28(커밋 형식 검사 도구의 실행 환경 — ★ **값이 아니라 환경**이 미확정) · IV-29(Gradle 버전 자동화 다리 — release-please·axion 모두 직결되지 않는다) · IV-30(카탈로그 발행·락 파일·Packages 인증) · IV-31(Dependabot `versioning-strategy` — 리서치 원문 회수 실패분). **26 → 31건** |
| v1.8 | 2026-08-06 | ★ **판정 열 신설 — 4열 → 7열**(E5 **PD-7** 이행 · 시점 = **T0**(구현 접촉 전) · 작업 기록 `plans/2026-08-06/t0-doc-judgments/`): **선언값(⓪)** · **판정 결과(⑴)** · **판정 승인(⑵)** 세 칸을 붙이고 **기존 IV-1~26 전 행은 `—`**(미도래)로 둔다. ⚠️ **판정 내용·등재 항목은 하나도 바뀌지 않았다 — 칸만 섰다.** 머리말에 칸 사용 규칙 1항 추가(소비 절차 §2.1 ⓪~⑷ 참조 — 특히 **⑵ 판정 승인이 실행자의 자기 판정을 막는 절단선**이다). ★ **왜 T0인가**: 대장은 **문서 repo 자산**이라 「첫 IV 소비 작업」에 붙이면 그 작업이 **구현 repo 소속**일 때 repo 경계(PD-6)·PR-2와 충돌한다 |
| v1.7 | 2026-08-06 | E4 재점검: IV-26(CDV-11 — OIDC/도달성) 등재 |
| v1.6 | 2026-08-06 | **E4 리뷰 반영**: IV-19 문면 정정(lease 하한 = 전 구간 합) · IV-24(위성 정지 상한)·IV-25(CI 보관 계약) 등재. CDV 공통: 스테이징 부재 — 측정 전 선언값 먼저(첫 실배포가 실험이 되지 않게) |
| v1.5 | 2026-08-06 | **E4 등재**: IV-16~23(CDV-1~8 — 배포·CI 수치·환경 판정 전수). IV-18은 CH-7, IV-21은 PR-1 승격, IV-23은 TL-2 R3과 연결 |
| v1.4 | 2026-08-06 | **E3 리뷰 반영**: **IV-15**(통합 계층 시간 예산 — 측정 전 상한 선언) 등재. TL-2 되돌림 조건 R1의 관측 가능성이 이 행에 걸린다 |
| v1.3 | 2026-08-06 | **E1 리뷰 반영 — N11**: **IV-14**(★ **mTLS·서명 키의 발급·보관·교체 — 병행 검증 창**) 등재. ADR-019 §4가 *"서명 컨텍스트의 발급·검증·키 관리"* 를 **새로 생기는 일**로 적고 ADR-021 ET-3이 내부 mTLS를 요구하는데 **키 수명·교체가 어디에도 없었다** — IV-12(기관 공유 키)의 내부 판이다. ADR-024 RL-8(배포 인증키)도 같은 행이 소유한다 |
| v1.2 | 2026-08-06 | **Phase 5 진입 아키텍처 반영** — **IV-13**(발행–소비 조기 신호의 지표·임계 — ADR-023 OB-5) 등재. 기술서 §4가 남긴 *"D-05를 포기했으니 그 지표를 어디서 보나"* 의 수치 축이다 |
| v1.1 | 2026-08-06 | **듀얼 리뷰 반영 — R-05·09**(인터페이스 규격서): **IV-11**(입금 채널 속도 제한·상한 — 계좌 열거 오라클의 잔여 노출) · **IV-12**(요청 지문 HMAC 알고리즘·키 관리) 등재 |
| v1.0 | 2026-08-06 | 신설 — 기존 이월·[미확정] 중 구현 판정 유형 10건 시드 등재 |
