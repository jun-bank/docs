# 아키텍처 기술서

- 작성일: 2026-08-05
- 상태: 검토대기 — **Phase 3 종료 시점**
- 양식: `study/project-workflow/phase3/06-architecture-doc-format.md`

> ★ **이 문서는 정본이 아니다.** 각 절이 가리키는 문서가 정본이다.
> 내용을 복사하면 그 순간 둘로 갈라진다 — Phase 2에서 열 라운드에 걸쳐 확인한 것이다(F-24).

---

## 1. 한 장 요약

| | |
|---|---|
| **무엇을 만드나** | 카드 결제 발급사 + 뱅킹 — 승인·매입·환불·미수·입금·정산·원장·대사 |
| **어떤 구조인가** | **배포 3개** — 실시간 코어(C1·C2·C3·C6·C7·★C8) · 정산(C4) · 원장(C5). ★ **한 MySQL 클러스터 · 배포별 스키마·계정 분리**(ADR-022 — 목적은 **소유권**이고 테넌트 격리가 아니다). 코어→위성은 **Outbox 비동기 · 릴레이 직접 전달**(ADR-021 — 브로커 없음) |
| **무엇을 잘하나** | **정합성(1) · 가용성(2) · 감사가능성(3 — ★ 행위자 축 포함) · 탐지가능성(4)** · ★ **격리(7)** (2026-08-05 멀티테넌시 설계 변경) |
| ★ **무엇을 포기했나** | 데이터 계층 수평 분할 · 과잉 처리량 · **테넌트 축·다국가**(★ D-04 재작성 — **격리 메커니즘은 채택됐다**) · 실시간 관측(APM·분산 추적 — 감사 기록은 범위 밖) · 보안 심화(⚠️ **실 카드 데이터를 받으면 즉시 철회**) |
| **가장 되돌리기 어려운 결정** | **ADR-010** 배포 단위 · **ADR-009** 파티션 키 · **ADR-003** 상태 저장 |

---

## 2. 읽는 순서

```
제약 → 품질 속성 → 품질 시나리오 → ADR → 뷰
```

| 문서 | 답하는 것 |
|---|---|
| [constraints.md](constraints.md) | 우리가 **고를 수 없는 것** · ★ **§0 프로젝트 목적** |
| [quality-attributes.md](quality-attributes.md) | 무엇을 잘하고 **무엇을 포기**하는가 · HA-1~HA-5 |
| [quality-scenarios.md](quality-scenarios.md) | 그것을 **어떻게 측정**하는가 · ★ **수치 근거 대장** |
| [adr/](adr/) | 되돌리기 어려운 결정 **25건** (★ 2026-08-05 멀티테넌시로 3건 · **2026-08-06 Phase 5 진입으로 5건 + E1 리뷰로 2건 추가**) |
| [views/](views/) | 컨텍스트 · 컨테이너 · **데이터** · 런타임 |

---

## 3. 결정 색인

| ADR | 결정 | 상태 | 되돌리기 |
|---|---|---|---|
| 001 | 단일 모듈러 모놀리스 | ~~대체됨(004)~~ | — |
| **002** | 모듈 구조 — **진입 컨텍스트가 조율** | 채택 | 중 |
| **003** | 상태 저장 + 원장 append-only (이벤트 소싱 기각) | 채택 | ★ **높음** |
| 004 | 코어 + 위성 3 (배포 4) | ~~대체됨(010)~~ | — |
| **005** | **DB Outbox + 폴링 릴레이** | 채택 | 중 |
| 006 | 마감 스냅샷 + 드레인 | ~~대체됨(011)~~ | — |
| **007** | 자금 경로는 **ORM 단일 경로** · 복제본 금지 | 채택 | 중 |
| **008** | 배치 **파티션 할당** | 채택 | 낮음 |
| **009** | 파티션 키 = **계좌ID** | 채택 | ★ **높음** |
| **010** | **배포 3개** (코어에 대사·인증 포함) | 채택 | ★ **높음** |
| **011** | 커트오프 축소 + **lazy close** | 채택 | 중 |
| **012** | 낙관적 락 + **경로별 격리 수준** | 채택 | 중 |
| **013** | **타임아웃 계층** · 풀 격리 | 채택 | 낮음 |
| **014** | 릴레이 운영 — 순서·DLQ·**fencing token** | 채택 | 낮음 |
| **015** | **expand-contract** 절차 · 블록 채번 | 채택 | 중 |
| ★ **016** | **인가 — 주체·스코프를 계약으로 강제** (타입 강제 · 인프로세스) | 채택 | 중 |
| ★ **017** | **감사 기록 — 행위자를 C8 별개 append-only로** (outbox 수집 · 승인 워크플로) | 채택 | 중 |
| ★ **018** | **격리 축 — "지금 안 하면 못 하는 것"만 선반영** (`ownerId` · 캐시 키 규정) | 채택 | 중 |
| ★ **019** | **채널 진입 — nginx + 코어가 인증·인가 소유 + 서명된 내부 컨텍스트** (전문 어댑터 = 코어 내) | 채택 | 중 |
| ★ **020** | **라우팅 — 정적 설정 · 디스커버리 불도입** | 채택 | 낮음 |
| ★ **021** | ★ **이벤트 전송 — 브로커 불도입 · 릴레이 직접 전달** (+ 위성 감사 → C8 경로) | 채택 | 중 |
| ★ **022** | **DB 토폴로지 — 한 클러스터 · 배포별 스키마·계정**(목적 = 소유권) | 채택 | ★ **높음** |
| ★ **023** | **관측 — 중앙 관측·분산 제어 · `traceparent` 전파 규약** | 채택 | 낮음 |
| ★ **024** | ★ **릴리스·배포 — 채널별 가용성**(사람 = 블루-그린 · 전문 = 수신 정지 창 · 배치 = 배포 창 락) | 채택 | 낮음 |
| ★ **025** | ★ **공통 라이브러리 — 계약·인프라만 공유**(payload·술어·도메인은 공유하지 않는다) | 채택 | 중 |

> ★ **019~025는 전부 "무엇을 안 넣는가"를 함께 적었다** — 도구를 기각할 때
> **도입 조건을 같이 쓰는 것이 C-08(도구 최소주의)의 실행 형태**다.
> 조건 없는 기각은 다음 사람이 근거 없이 다시 올린다.

---

## 4. 되돌리는 조건 모음 ★

**흩어져 있으면 아무도 안 본다.** 여기 모은다.

| 신호 | ADR | 관찰 방법 |
|---|---|---|
| QS-02(400 TPS)를 p99 3초로 못 지킴 | **010 R1** | 부하 시험 — ★ **분할이 아니라 증설이 먼저** |
| 대사 실행 중 QS-01(100 TPS) 침해 — 2회 이상 | **010 R1** | 배치 시간대 응답 지표 |
| 낙관적 락 **충돌률 5% 초과** | **012 R1** | 충돌 카운터 |
| 락 대기 타임아웃 **거절 1% 초과** | **013 R1** | 거절 사유 분류 |
| 브레이커 **하루 10회 이상** 열림 | **013 R2** | 브레이커 이벤트 |
| **총 커넥션이 max_connections 80% 초과** | **013 R4** | 커넥션 지표 |
| **DLQ 적재 주 1회 이상** | **014 R1** | M17 |
| **fencing token 거부 발생** | **014 R2** | B-9 적재 |
| M12·M14 불일치 **구조적 반복** | **003 R1** | 대사 결과 추이 |
| **거래가 없는 계좌**의 이동 행 부재로 대사가 틀림 | **011 R2** | 대사 결과 — M12 허위 불일치 |
| 모듈 간 금지 의존 **반복 검출** | **002 R4** | 아키텍처 테스트 |
| 플래그 **10개 초과** | **015 R2** | 플래그 목록 |
| ★ 라우팅 규칙이 **설정 수준을 넘는다**(집계·변환) | **019 R1** | nginx 설정 변경 이력 |
| ★ **배포 단위가 늘거나 동적으로 변한다**(ADR-010 개정) | **020 R1** | 배포 결정 — 디스커버리 재개의 유일한 트리거 |
| ★ 같은 이벤트의 **독립 소비자 3 이상** · **리플레이 요구** · 수신 정지가 자금 경로 지연 | **021 R1** (ET-9) | 수신자 수 · 운영 절차 · 지연 실측 |
| ★ **스키마 간 조인이 코드에 나타난다** | **022 R3** | 아키텍처 테스트 · 권한 거부 기록 |
| ★ **조기 신호 발화가 M16·M17 적재보다 늦은 사건 2회 이상** | **023 R1** | IV-13 지표 · 적재 시각 대조 |
| ★ **CH-7(전송 프로토콜 계층)이 확정된다** | **024 R1** | 전문 어댑터 배포 방식(수신 정지 창) 재판정 — ADR-019 R5와 같은 트리거 |
| ★ **공통 릴리스가 세 배포 동시 갱신을 강제 — 2회 이상** | **025 R1** | 공통에 payload·도메인 규칙이 들어갔는지 점검 |

★ **포기(D-01~D-06)의 재검토 조건도 여기 모은다** (2026-08-05 — D-04의 조건이 이 표에 없어 *"아무도 안 보는 자리"* 에 있었다):

| 포기 | 언제 다시 보나 |
|---|---|
| D-01 데이터 계층 수평 분할 | 단일 DB가 QS-01 피크를 못 받을 때 |
| ~~D-02 고가용성~~ | ★ **철회됨** (2026-08-05 — 채택 2순위로 승격, quality-attributes §2.1). 누락이 아니라 철회다 |
| D-03 과잉 처리량 | 실측 부하가 기준치를 넘을 때 |
| **D-04 테넌트 축·다국가** | ★ 테넌트(기관·브랜드) 요구 또는 다통화 요구 **수신 시** — v0.3 재작성으로 측정 가능해졌다 |
| **D-05** 실시간 관측 | 장애 원인 분석이 로그로 안 될 때. ★ **이미 부분 철회됐다**(quality-attributes §2.2) — **ADR-023**이 그 부분 철회의 실행 형태다(규약은 지금·도구는 나중, 탐지 소유는 대사에 잔류) |
| D-06 보안 심화 | 실 카드 데이터를 받게 되면 **즉시** |

> ⚠️ **관찰 방법이 "지표"인 것이 여럿이다.** D-05(실시간 관측)를 포기했으므로
> **그 지표를 어디서 보는지**가 Phase 5의 과제였다.
> ✅ ★ **ADR-023이 그 자리를 채운다** — 구조화 로그 + Micrometer 표준 노출이 v1의 답이고,
> 조기 신호 3종(outbox age · 전달 실패율 · DLQ 건수)의 **임계는 [구현 검증 — IV-13]** 이다.
> ⚠️ **수집기·대시보드 제품은 아직 없다** — 노출까지가 지금의 계약이다.

---

## 5. 런타임과 실패

| 흐름 | 뷰 | 실패 시 |
|---|---|---|
| E1 승인 | [04-runtime §1](views/04-runtime.md) | 롤백 · 망취소 수신 · 락 충돌 시 3초 넘으면 거절 |
| E2 매입 | §2 | outbox 재전달 · 격리 후 `promoteIsolated` |
| E4 환불 | §3 | 소멸 먼저 → 입금 순서 고정 |
| 커트오프 | §4 | 드레인 미완 → **대사 안 함** + M16 + BR-53 통지 |

---

## 6. ★ 정합성 점검

| # | 점검 | 결과 |
|---|---|---|
| **1** | 채택 품질 속성마다 시나리오가 있나 | ✅ 정합성 QS-01·02·03 · 가용성 QS-07 · **감사 QS-10**(★ 2026-08-05 신설 — "감사 QS-03"은 오기였고 QS-03은 정합성. QS-09는 구 체계 번호라 결번) · 탐지 QS-04 · 복구 QS-05 · 응답 QS-06 · **격리 QS-08**(신설) |
| **2** | ADR 근거가 제약(C) 또는 시나리오(QS)를 가리키나 | ⚠️ **부분** — 012~015는 **장애 사례**를 근거로 삼는다. QS로 환원되지 않는 근거가 있다(§7 O-1) |
| **3** | 제약에 걸리는 안을 고르지 않았나 | ✅ C-04(예산 0)는 **실행 제한**으로 좁혔고 설계는 그것을 전제하지 않는다 |
| **4** | ★ **트랜잭션 경계가 배포 단위를 넘지 않나** | ✅ **문서 모델 기준** — E1~E5 참여 조작 전부가 코어 컨텍스트(C1·C2·C3) 소유 + ★ **C8 `consume`이 ①~③ 실행 트랜잭션에 참여**(자금 무이동 참여자 — E 표 밖의 의식적 예외, C8도 코어라 배포는 안 넘는다. BR-56 · approval-request A4). ⚠️ **실제 트랜잭션 전파·커밋 경계 검증은 Phase 5** |
| **5** | ★ **데이터 소유권과 저장 위치가 일치하나** | ✅ **도메인 소유권 표 기준** C4 정산·C5 원장만 밖. ⚠️ 테이블별 쓰기 주체·스키마 검증은 Phase 4~5 |
| **6** | 포기한 품질을 요구하는 시나리오가 없나 | ✅ 샤딩·과잉 처리량·**테넌트 축**·실시간 관측을 **요구하는 QS 없음** (QS-08 격리는 **채택** 품질이다 — D-04 재작성으로 경계가 갈렸다) |
| **7** | Phase 2 수치 미확정이 대장에 있나 | ⚠️ **20건 남음** — 대장에는 있으나 **값이 미정**. ★ **2026-08-06 C7 루프 1(L1-14)로 +5**(재인증 주기·임시 대행 상한·break-glass 상한·인가 캐시 수명·트리 깊이) — 다섯 다 **`[구현 검증 — IV-n]`** 이며 문서 단계에서 확정하지 않는다(검증 정책 v2 V2-3 · 중앙 대장 `docs/implementation-verification.md`) |
| **8** | ★ **ADR끼리 모순되지 않나** | ⚠️ **아래 §7** — 리뷰가 **실제로 1건 잡았다**(ADR-002 × ADR-010) |

> ★ **"검산 통과"가 "모순 0건"을 뜻하지 않는다.** `check_docs.py`는 통과했는데
> **ADR-002가 대체된 ADR-004의 배포 구조를 들고 있었다.** 검사 ⑲는
> *배포 ADR의 코드블록에 컨텍스트 이름이 있는지*만 본다 — **다른 ADR이 모순되는지는 안 본다.**
> 이것이 `study/11` F-24(문서 잔재)가 우리에게 실현된 것이다.

---

## 7. ADR 간 정합 — 확인한 것과 남은 것

### 확인됨 ✅

| 쌍 | 확인 |
|---|---|
| ADR-024 ↔ ADR-019 CH-4 | 전문 어댑터가 코어 안이라 **코어 배포 = 사람 채널 무중단 + 전문 정지 창 동시 발생** — RL 표가 이 결합을 §1에 명시하나 |
| ADR-025 ↔ ADR-002 | common-lib 모듈이 core:shared·모듈 경계를 침범하지 않나(Money 재수출·payload 발행자 소유) |
|---|---|
| **003 × 011** | 상태 저장(003)이라 워터마크 재구성이 불가 → 011이 lazy close를 택한 것과 **일관** |
| **007 × 012** | 007이 ORM 단일 경로를 의무화 → 그것이 lost update 노출 → 012가 낙관적 락으로 덮는다. **012 없이 007만 있으면 F-01이 열린다** |
| **009 × 010** | 파티션 키(계좌ID)와 배포 경계가 **충돌하지 않는다** — 코어 안에서만 파티션한다 |
| **005 × 014** | 005의 선언(O-4 순서)을 014가 장치로 만든다 |
| **008 × 014** | B-1~B-5의 좀비 구멍을 014 B-6~B-9가 막는다 |
| **011 × 015** | lazy close의 배포 스큐 위험(F-17)을 015 S-10~S-13(플래그)이 다룬다 |
| **013 × 012** | ★ **두 번 고쳤다** — 첫 수정(공유 deadline 3초)도 T-1을 어겨 **내부 deadline 2.5초**로. 초안은 — 재시도 3회 × (락 0.5 + 백오프) + 앱 2.5초 = **4.1초 > 3초**. **T-11~T-13(공유 deadline)** 으로 결합 |
| **002 × 010** | ★ **실제 모순이었다** — ADR-002가 C6·C7을 위성으로 두고 있었다(대체된 ADR-004 구조). **v0.2에서 고침** + **M-6** 신설 |
| **014 × 003** | ★ **DLQ가 드레인 완료를 위조**할 수 있었다 — **O-15·O-16**(DLQ_UNRESOLVED는 완료가 아니다) |
| **015 × 원장 ACL** | ★ *"필드 추가는 안전"* 이 **회계 의미를 바꿀 수 있었다** — **S-6a** 신설 |
| **005 × 010 N-1** | ★ **역방향에 Outbox가 없었다** — 정산 완료 후 발행 전 종료면 **재발행 조작이 하나도 없어** 하루치 대사가 무음 정지. **O-17**(정산 outbox) |
| **012 × DC-005** | ★ *"범위 스캔이 없다"* 근거가 무효화됐고 **자식 행 seed 경쟁**이 생겼다 — **W-17** |
| **015 × DC-005** | ★ **플래그 off가 안전 상태가 아니다** — 구 동작이던 `DailySnapshot`이 폐기돼 off = 좌변 부재. **S-19** |
| **008 × 009 R3** | ★ 파티션 축이 **셋인데 하나로 단언**했다 — ADR-008 §2 표 |
| ★ **016 × 013** | 인가 판정이 자금 트랜잭션 안의 외부 호출이면 **T-10(커넥션 1개)·내부 deadline 2.5초와 충돌**한다 — **인프로세스·진입 시 1회**(AU-3·AU-4)로 회피 |
| ★ **017 × 005** | 감사 수집이 outbox 계약(O-1~O-5)을 재사용한다 — at-least-once 위에서 **수신 멱등**(AD-3)이 이중 적재를 막는다. **릴레이 부하 증가는 [미검증]** |
| ★ **018 × 012 · 009** | 축 컬럼이 **인덱스 선두·파티션 축을 건드리지 않는다**(IS-3) — 건드리면 갭 락 전파(012 계약)·파티션 재설계(009)가 재발한다 |
| ★ **019 × 016** | 진입점이 인가를 **모른다** — ADR-016 기각 B(*"게이트웨이는 이 계좌가 네 것인지 모른다"*)가 그대로 산다. 진입점이 하는 일은 라우팅·TLS·속도 제한뿐이고, R4 전파는 **서명된 컨텍스트**라 진입점 우회가 인증 우회가 되지 않는다(CH-3) |
| ★ **020 × 010** | 디스커버리 불도입의 **재개 조건이 ADR-010 개정 하나**에 묶인다 — *"대상이 몇 개인지 모른다"* 는 전제가 배포 3 고정에서 성립하지 않는다 |
| ★ **021 × 005 · 014 · BR-52** | ★ **브로커를 안 넣어서 정본이 그대로 산다** — `DELIVERED` = 수신측 반영(ET-2)이 유지되므로 드레인 완료 축이 **outbox 잔량 + `DLQ_UNRESOLVED`** 하나로 남는다. 브로커를 넣었다면 **커트오프에 컨슈머 축**이 하나 더 생겼다(ADR-021 §7에 예약) |
| ★ **021 × 017** | 감사 수집(AD-2)이 **배포 경계를 넘는 경로를 갖게 됐다**(ET-7) — R13은 문면상 전 컨텍스트인데 **C4·C5의 경로만 없었다.** 그대로 뒀으면 위성의 행위자 기록이 통째로 비고 BR-57이 경계에서 조용히 끊겼다 |
| ★ **022 × 010 · 007** | 스키마 분리가 **T4를 되살리지 않는다** — 같은 인스턴스라 R9가 진짜 값을 읽고, 복제본 금지(007)가 유지된다. ⚠️ **물리 분리는 R9 대안이 먼저**라는 순서를 DT-7이 명문화했다(데이터 뷰 §2가 미리 적어 둔 조건) |
| ★ **022 × 018** | 스키마 분리의 목적이 **소유권이지 테넌트 격리가 아니다**(DT-2) — **IS-7은 그대로**다. 목적을 안 적으면 다음 사람이 *"이미 갈랐으니 테넌트도"* 로 읽는다 |
| ★ **023 × 017** | **관측 ≠ 감사** — 로그·메트릭은 회전·유실이 정상인 매체이고 감사는 기록 의무다(OB-7). ADR-017 기각 D의 **역방향도 금지**된다(감사로 관측을 갈음하지 않는다) |
| ★ **023 × 014 · BR-53** | 조기 신호는 M16·M17·BR-53을 **대체하지 않고 앞선다**(OB-6) — *"대시보드가 초록이니 괜찮다"* 가 대사 결과를 이기면 탐지가 무력화된다 |

### ⚠️ 남은 것

| # | 무엇 | 왜 |
|---|---|---|
| **O-1** | **012~015의 근거가 QS로 환원되지 않는다** | 장애 사례가 근거다. *"우리에게 일어날 확률"* 을 재는 시나리오가 없다 |
| **O-2** | **013 초기값 9개 중 6개 · 014 상한 2개가 [미검증]** | 실측 전까지 **이 값을 근거로 다른 결정을 하면 안 된다** |
| **O-3** | **009 §4 E2·E4가 계좌 파티션에서 안 닫힌다** | 미해결로 명시했으나 **샤딩 시 발현**한다. ⚠️ ADR-008 §2 표(축 셋 명시)는 **축의 문서화이지 닫힘이 아니다** — 한때 닫힘 행이 중복 등재됐었다(F6 정리) |
| ~~**O-4**~~ | ~~012~015 미리뷰~~ | ✅ **닫힘** — Phase 3 마감 듀얼 리뷰 + 워커 3인 교차 검증. **치명 5 · 높음 7 · 중간 10 · 보통 3**을 전부 반영 |
| ~~**O-5**~~ | ~~멀티테넌시 공백 G-1~G-4~~ | ✅ **설계로 닫힘 (2026-08-05)** — BR-54~58 · ADR-016~018 · CM4(C8 승격) · QS-08·**10**. **검증(전수 시험·실측)은 Phase 4~5** |
| ~~**O-6**~~ | ~~원장 역분개의 2인 승인 여부 (JE3)~~ | ✅ **닫힘 (2026-08-05)** — 사용자 확정: **포함 = BR-56 ⑤** |

> ⚠️ **이 표의 O-n은 기술서의 "열린 문제" 번호다** — ADR-005의 Outbox 계약 규칙 O-1~O-18과 **별개 네임스페이스**다.
> 같은 기호가 두 뜻을 가지므로(예: 여기의 O-3 ↔ ADR-005 O-3 `sourceEventId`), 인용할 때는 **출처 문서를 함께 적는다**.

> ★ **O-4의 우려가 맞았다.** ADR 4건에서 **치명 3건**이 나왔고 셋 다
> *"각각은 맞는데 합치면 깨지는"* 형태였다 —
> **012×013(예산)** · **014×BR-52(드레인)** · **015×원장 ACL(회계 의미)**.
>
> **DC-003·DC-004에 이은 세 번째다.** 이 프로젝트에서 **가장 반복되는 결함 형태**이고,
> 그래서 §7의 쌍별 대조를 **관행으로 남긴다**.

---

## 8. 열린 것

| # | 미결 | 언제 닫나 |
|---|---|---|
| 미확정 수치 **23건**(정본 = BR 「미확정 수치」 표 행 수) | 값 | Phase 3~5 (실측 후) — ★ **5건은 `[구현 검증]` 이연**(IV-1·2·3·4·10) |
| 열린 의문 **SM 27건**(색인 정본 = state-machines/README) **+ 애그리게이트 각 문서 §미해결**(고정 수 선언 안 함 — 낡는 집계) | 상태 머신 · 애그리게이트 | Phase 4~5 |
| **O-1 ~ O-5** (§7) | ADR 정합 | **다음 리뷰** |
| Phase 0 문서 체계 | 건너뛴 상태 | Phase 6 |

---

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v1.0 | 2026-08-06 | **E1 리뷰 반영 — N5·N9**: §3 결정 색인에 ★ **ADR-024(릴리스·배포 — 채널별 가용성)** · ★ **ADR-025(공통 라이브러리 경계)** 등재(23건 → **25건**, §2 표도 갱신) · §4 되돌리는 조건에 **024 R1(CH-7 확정)** · **025 R1(공통 릴리스가 동시 갱신 강제 2회 이상)** 추가하고 **023 R1을 관측형 문면**(적재 시각 대조 · 2회 이상)으로 갱신 |
| v0.9 | 2026-08-06 | ★ **Phase 5 진입 아키텍처 반영 — ADR-019~023 등재**(18건 → **23건**): 채널 진입 · 라우팅 · 이벤트 전송 · DB 토폴로지 · 관측. §1 요약의 *"단일 MySQL"* 을 **ADR-022와 정합하게 정밀화**(한 클러스터 · 배포별 스키마·계정 — 목적은 소유권)하고 전달 방식에 *"릴레이 직접 전달(브로커 없음)"* 명시. §4 되돌리는 조건에 5행 추가 · **D-05 행에 부분 철회와 ADR-023 연결** · §4 말미의 *"그 지표를 어디서 보나"* 숙제를 **닫힘으로 갱신**(임계는 IV-13) · §7에 신규 쌍 대조 8행 |
| v0.8 | 2026-08-06 | **C7 리뷰 루프 1 반영 — L1-14**: 미확정 수치 집계 **15 → 23건(§8 표 정본)**(§6 점검 ⑦ · §8) — 신규 5건은 전부 `[구현 검증 — IV-n]` 이연이며 값은 문서 단계에서 정하지 않는다(검증 정책 v2 V2-3) |
| v0.7 | 2026-08-05 | **리뷰 루프 2·3** — §3 결정 색인에 **ADR-016~018 등재**(15건→18건 — 색인 누락은 루프 3 OQ2가 발견) · 점검 ①의 감사 시나리오 = **QS-10**(개명) · 점검 ④에 C8 consume 참여(의식적 예외) 명시 · 포기 표에 D-02 철회 행 · O-5 닫힘 근거 QS-10 정정 |
| v0.6 | 2026-08-05 | **리뷰 루프 1 반영 (L-01·15·16)** — O-6(JE3)는 v0.5 등재 직후 같은 날 사용자 확정으로 **닫혔다**(이력 "확인 대기"는 그 시점 기준 — 본문 ✅가 현재 상태다). 요약 표 코어 목록에 C8. ⑤ 원장 역분개의 실행 경로 = **이벤트(R15)** — 배포 경계 동기 호출 없음 |
| v0.5 | 2026-08-05 | ★ **멀티테넌시 설계 변경 반영 (B-5)** — 요약 표에 격리(7)·D-04 재작성 반영 · 점검 ①⑥ 갱신(QS-08·09 신설로 감사 공백 닫힘) · **§4에 포기 재검토 조건 표 신설**(D-04 조건이 "아무도 안 보는 자리"에 있던 문제) · §7에 **신규 ADR 쌍 대조 3행**(016×013 · 017×005 · 018×012·009) · O-5 닫힘 · **O-6 신설**(JE3 — 원장 역분개 승인 여부, 사용자 확인 대기) |
| v0.4 | 2026-08-05 | 잔재 정정(MT 사전 점검 F6·F7) — §7 남은 것 표에서 **O-3이 열림·닫힘으로 동시 등재**돼 있던 자기 모순 정리(닫힘 행 삭제 — ADR-009 §4가 "미해결로 남긴다"가 정본). **O-n 네임스페이스 주석 추가**(기술서 열린 문제 ↔ ADR-005 Outbox 규칙 — 같은 기호 두 뜻, 검사 ㉔가 못 보는 형태) |
| v0.3 | 2026-08-05 | 잔재 정정(MT 사전 점검 F5) — §4 #10이 **대체된 ADR-004 R2의 문장**("허위 불일치 상시 발생")을 011 R2로 인용하고 있었다(검사 ㉒ 유형 — ADR 번호는 맞아 미검출). ADR-011 R2의 실제 신호(거래 없는 계좌의 이동 행 부재)로 정정 |
| v0.2 | 2026-08-05 | 잔재 정정(MT 사전 점검 F4) — 정합성 점검 ①의 **거짓 ✅ 정직화**: "감사 QS-03"은 오기(QS-03 = 정합성). 감사가능성은 채택 3순위인데 대응 QS가 0건 — 공백을 ⚠️로 드러내고 신설을 O-5(멀티테넌시 설계)로 연결 |
| v0.1 | 2026-08-05 | 최초 작성 — Phase 3 종료 시점. ADR 15건(대체 3건 포함) · 뷰 4종 · **정합성 점검 8항목**. ④⑤는 **기계로 확인**했고 ⑧은 §7에 쌍별로 정리. ★ **O-4(012~015 미리뷰)** 를 가장 큰 위험으로 등재 |
