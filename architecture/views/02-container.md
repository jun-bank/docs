# 컨테이너 뷰

- 작성일: 2026-08-05
- 독자: 개발자 · 운영
- 답하는 질문: **배포 단위가 몇 개이고 어떻게 통신하나** · 무엇이 같은 트랜잭션인가
- 답하지 않는 질문: 모듈 내부(→ ADR-002) · 데이터 소유(→ 데이터 뷰) · 순서(→ 런타임 뷰)

---

## 1. 그림

```mermaid
graph TB
    subgraph CORE["실시간 코어 (배포 1)"]
        C1[C1 뱅킹<br/>계좌·미수·입금수신]
        C2[C2 카드]
        C3[C3 결제<br/>승인·예약·멱등·배치]
        C6[C6 대사]
        C7[C7 인증]
        C8[C8 운영<br/>감사·승인]
    end
    OB[(outbox)]
    subgraph SAT["위성"]
        C4[C4 정산<br/>배포 2]
        C5[C5 원장<br/>배포 3]
    end
    DB[(MySQL<br/>단일)]

    C3 -->|동기·같은 트랜잭션| C1
    C3 -->|동기·같은 트랜잭션| C2
    C7 -.동기.-> C1
    C7 -.동기.-> C3
    C1 -.승인 확인·소비 R14.-> C8
    C3 -.승인 확인·소비 R14.-> C8
    OB -->|릴레이 폴링·감사 R13| C8
    C8 -->|같은 커밋·지시 R15| OB
    CORE --- DB
    CORE -->|같은 커밋| OB
    OB -->|릴레이 폴링| C4
    OB -->|릴레이 폴링| C5
    C4 -->|같은 커밋| OB2[(outbox·정산)]
    OB2 -->|릴레이 폴링| C3
    OB2 -->|릴레이 폴링| C6
    C6 -.읽기.-> DB
    C8 -.읽기 전용 조회 R18.-> C5
```

## 2. 요소

| 배포 단위 | 담는 컨텍스트 | 왜 여기인가 |
|---|---|---|
| **실시간 코어** | C1 · C2 · C3 · **C6** · C7 · ★ **C8** | E1~E5가 **한 트랜잭션**을 요구(ADR-010) · **대사는 진짜 값을 봐야 한다** · C7은 R4가 동기 · ★ C8은 R14(승인 확인)가 동기 (2026-08-05 CM4) |
| **정산** | C4 | 집계를 받는다. 되돌아갈 것이 `settledBusinessDate` 하나 |
| **원장** | C5 | append-only 수신. 되돌아갈 것이 없다 |

> ★ **분할선은 "무엇이 나가는가"가 아니라 "무엇이 돌아와야 하는가"가 정했다**(DC-004).

## 3. 관계

| 출발 | 도착 | 무엇을 | 동기/비동기 | 실패하면 |
|---|---|---|---|---|
| C3 | C1 · C2 | 잔액·홀딩·한도 조작 | ★ **동기 · 같은 트랜잭션** | 롤백 — 부분 반영 없음 |
| 코어 | outbox | 이벤트 | **같은 커밋** | 사실도 같이 롤백 — **유실 불가**(ADR-005) |
| 릴레이 | C4 · C5 | 이벤트 전달 | **비동기 폴링** | 재시도 → 상한 초과 시 **DLQ + M17**(ADR-014) |
| C4 | **정산 outbox** | 이벤트 | ★ **같은 커밋** | 사실도 같이 롤백 — **유실 불가** |
| 릴레이 | C3 (R11) | `SettlementCompleted` | 비동기 폴링 | 재시도 → **DLQ + M17** |
| 릴레이 | C6 (R12) | `SettlementFailed` | 비동기 폴링 | 재시도 → **DLQ + M17** |
| C6 | DB | 대사 읽기 | ★ **같은 DB 직접 읽기** | — (사본이 아니다) |
| ★ **C8** | **C5 (R18)** | 유도 스코프 판정용 **원전표 조회**(원장 역분개 요청·승인이 원전표 `subjectId`를 읽어야 한다 — BR-55 · UC16-1) | ★ **동기 · 읽기 전용**(순응자) | 스코프를 판정할 수 없다 → **요청 거절**. 자금 경로는 무영향 |

> ★ **배포 경계를 넘는 동기 호출의 금지선은 "쓰기·조작"에 걸린다**(ADR-010).
> **읽기 전용은 예외이며 R9(대사)·R18(C8 → C5) 둘이다** — 둘 다 순응자이고
> 상류를 바꾸지 않는다. ⚠️ **읽기라도 배포 경계를 넘으면 지연·가용성이 옮는다** —
> 그래서 R18은 **자금 경로가 아닌 관리 조작에서만** 쓴다.

## 4. 트랜잭션 경계가 어디까지인가 ★

```
E1 승인    C3 + C1 + C2   ← 코어 안. 한 커밋
E2 매입    C3 + C1 + C2   ← 코어 안
E3 회수    C1             ← 코어 안
E4 환불    C3 + C1        ← 코어 안
E5 정정    C1             ← 코어 안

원장 기표 · 정산 집계  ← ★ 경계 밖. Outbox 경유(BR-40)
```

> ★ **E1~E5가 전부 코어 안이다.** 배포 단위를 넘는 트랜잭션이 **없다**.

## 5. 자원 격리 (ADR-013)

| 풀 | 용도 |
|---|---|
| 자금 경로 | E1~E5 |
| 조회 전용 | 조회 API |
| **배치** | 대사·정산·만료 — **작게** |

**총 커넥션 = 인스턴스 수 × 풀 합계 < `max_connections`** (T-9)

## 6. 이 뷰에서 읽어야 할 결정

| ADR | |
|---|---|
| **ADR-010** | 배포 3개 · 분할 기준 |
| **ADR-005·014** | Outbox · 릴레이 운영 |
| **ADR-013** | 풀 격리 |
| **ADR-002** | 코어 내부 모듈 구조 |
| ★ **ADR-019** | 채널 진입 — 무엇이 어디로 들어오나 |
| ★ **ADR-021** | 배포 간 전달 = 수신 inbox API(브로커 없음) |
| ★ **ADR-022** | 저장소 토폴로지 — 한 클러스터·배포별 스키마 |

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v0.4 | 2026-08-06 | ★ **R18 반영**(Phase 5 진입 — 선검증 F-03 부수): context-map v1.7이 신설한 **C8 → C5 읽기 전용 조회**가 이 뷰에 없었다. 다이어그램 화살표 + 관계 표 행 + ★ **"배포 경계 동기 금지는 쓰기·조작 한정, 읽기 전용 예외는 R9·R18 둘"** 이라는 선을 명시. 읽어야 할 결정에 **ADR-019·021·022** 등재 |
| v0.3 | 2026-08-05 | **리뷰 루프 2·3** — ★ R14 하류에서 **C4 제거**(④는 R15 지시 이벤트 — v0.2 이력의 "C1·C3·C4"는 그 시점 설계이고 루프 2 F-03으로 재설계됐다). C8→outbox 지시(R15) 화살표 |
| v0.2 | 2026-08-05 | **멀티테넌시 리뷰 루프 1 (L-09)** — C8 추가(코어): 승인 확인 R14 동기(C1·C3·C4 — ★ C4는 위성→코어 방향이라 ADR-010의 금지선("코어가 위성에 동기 의존")에 안 걸린다) · 감사 수집 R13(릴레이 경유) · C8→outbox(정정 지시 R15·M18 R16) |
| v0.1 | 2026-08-05 | 최초 작성 — ADR-010(배포 3) 반영 |
