# 제약

- 작성일: 2026-08-05
- 상태: 검토대기
- 양식: `study/project-workflow/phase3/01-constraints-format.md`
- 입력: 사용자 인터뷰 (2026-08-05)

> **제약은 선택지를 좁히는 것이 일이다.** 여기서 지워진 안은 ADR에서 다시 논하지 않는다.

---

## 0. 이 프로젝트의 목적 ★★

**설계 경험을 쌓는 것이다.** 사용자 진술(2026-08-05):

> *"최대한 크게 하되, 이때 이 큰 걸 **아주 작은 단위로 끊어보는 연습**을 하려는 것."*

이 목적이 **제약 판단의 기준 자체를 바꾼다.**

**산출물이 둘이다.**

| # | 산출물 | 어디 |
|---|---|---|
| ① | **결제 시스템** | `product/` `domain/` `architecture/` … |
| ② | ★ **설계 워크플로우 자체** | `study/project-workflow/` |

> 사용자 진술: *"설계 워크플로우를 만들면서 흐름을 보고 이를 바탕으로 설계 역량을 키우고,
> 이 경험을 바탕으로 나중에 실무에서는 **필요한 부분만 부분적으로 활용**해서 응용 가능하다."*
>
> ★ **②가 ①보다 오래 산다.** 결제 시스템은 이 프로젝트로 끝나지만 워크플로우는 다음 일에 쓰인다.
> 그래서 **워크플로우 문서는 "부분 채택 가능"해야 한다** — 전부 따라야만 쓸모 있는 절차는
> 실무에서 안 쓰인다.

| | 보통의 프로젝트 | 이 프로젝트 |
|---|---|---|
| 운영 인력·개발 인력 | 고정된 자원 → **선택지를 지운다** | ★ **필요한 만큼 늘어난다고 가정** |
| 좋은 결정의 기준 | 운영 편의 · 납기 | **설계의 정확성** · 끊는 연습의 학습 가치 |
| 복잡도 | 줄이는 것이 미덕 | **실제 은행이 하는 만큼**은 감당한다 |

> ⚠️ **"최대한 크게"가 오버엔지니어링의 허가는 아니다.** 사용자가 같은 대화에서
> *"오버엔지니어링보단 실제 은행 서비스를 기준으로"* 라고 못박았다.
> **기준선은 실제 은행이고, 그 이상은 하지 않는다** — 품질 속성 **D-03(과잉 처리량)** 과 같은 판단이다.

> ★ **"작은 단위로 끊는다"가 일정 방식도 정한다.** 사용자 진술: *"애자일 형태로 기본 파이프라인을
> 만들고 거기에 여러 조건들을 붙이는 방향."* 이것은 **범위를 줄이는 것이 아니라 순서를 정하는 것**이다
> — 전부 만들되 뼈대부터. Walking Skeleton의 원래 뜻과 같다.

---

## 1. 제약 목록

| # | 제약 | 종류 | 출처 | 무엇을 못 하게 되나 | 협상 가능한가 |
|---|---|---|---|---|---|
| ~~C-01~~ | ~~개발 인력 1명~~ | 조직 | 사용자 | — | ★ **철회 (2026-08-05)** — 필요한 만큼 늘어난다고 가정 (§0) |
| ~~C-02~~ | ~~운영 인력 1명~~ | 조직 | 사용자 | — | ★ **철회 (2026-08-05)** — 필요한 만큼 늘어난다고 가정 (§0). **가장 많은 것을 지우던 제약이 사라졌다** |
| ~~C-03~~ | ~~기간 2주~~ | 일정 | 사용자 | — | ★ **철회 (2026-08-05)** — *"범위 축소하지 마라, 다 만든다고 생각해"*. 일정은 증분 순서로 관리한다 (§0) |
| **C-04** | **인프라 예산 0** | 비용 | 사용자 | 관리형 클라우드 서비스 · 유료 APM·모니터링 SaaS. ⚠️ **실행 환경만 제한한다** — 설계가 그것을 전제하면 안 된다(§1.1) | **불가** |
| **C-05** | ★ **언어 = Kotlin / JVM** (2026-08-06 개정 — 구 *"Java 25"*) | 기술 | 사용자 | 다른 런타임 · JVM 밖 생태계 | **불가** · ★ 적용 대상 = 서비스 레이어 한정(인프라 도구는 Go — ADR-028 L-1·L-2, 2026-08-07) |
| **C-06** | **프레임워크 = Spring** | 기술 | 사용자 | 다른 애플리케이션 프레임워크 | **불가** |
| **C-07** | **RDBMS = MySQL** | 기술 | 사용자 | 문서·그래프 DB를 **주 저장소**로 · MySQL이 지원하지 않는 격리 수준·기능 전제 | **불가** |
| ★ **C-08** | ★ **도구 최소주의 — 요구 → 계약 → 도구 순서로만 도입하고, 도입할 때는 "도입 조건"을 함께 적는다** (2026-08-06 사용자 확정) | 방법 | 사용자 | 요구가 실증되지 않은 미들웨어·스택 도입 · *"나중에 필요하면"* 같은 판정 불가 유예 | **불가** |
| ★ **C-09** | ★ **스테이징 환경이 없다 — 실서버 3대(`.9`·`.158`·`.164`)가 전부다** (2026-08-06 사용자 승인으로 승격 — 출처 `operations/cicd.md` §3.1) | 환경 | 사용자 · **C-04**(예산 0) · 학습 환경 | *"운영에 올리기 전에 스테이징에서 확인한다"* 를 전제하는 절차 · 운영 밖에서의 인수(QS)·탐색 태그 판정 · **배포 절차 자체의 사전 검증** | ★ **도입 조건 충족 시**(아래 — *"나중에 필요하면"* 은 판정 불가다 · C-08) |

### 1.0 ★ 2026-08-06 개정 — 스택 제약과 신규 제약 (Phase 5 진입)

| | 무엇이 바뀌었나 |
|---|---|
| ★ **C-05 개정** | **Java 25 → Kotlin / JVM.** 사용자 확정(2026-08-06 Phase 5 진입 인터뷰 — *"권장 Java 대신 Kotlin"*)이며 **이 개정 자체가 그 확정의 기록**이다. ⚠️ **JVM·Spring(C-06)·MySQL(C-07)은 그대로**이므로 **기존 ADR의 근거는 하나도 무효화되지 않는다** — 바뀐 것은 언어이고, 지워지는 것은 *"Java 25 미지원 라이브러리"* 라는 좁힘뿐이다 |
| **C-07 재확인** | ★ **MySQL은 불변이다.** Phase 5 진입 스케치가 PostgreSQL 전환을 한 번 후보로 올렸으나 **부결**했다 — C-07은 협상 불가이고, 이미 **C-07을 근거로 내린 결정들**(ADR-016 기각 A: *"MySQL에 행 수준 보안이 없다"* · ADR-012 격리 수준 · ADR-022 클러스터 토폴로지)이 그 위에 서 있다. **DB 제품 변경은 사용자 별도 지시로만** 다시 연다 |
| ★ **C-08 신설** | **도구 최소주의** — *"요구 → 계약 → 도구"* 순서로만 도입하고, **도입 조건을 함께 적는다**(2026-08-06 사용자 확정) |
| ★ **C-09 신설**(승격) | ★ **스테이징 없음** — `operations/cicd.md` §3.1이 **「ADR 승격 대상」**(CF-13)으로 표시했던 **잠정 판정**을 **2026-08-06 사용자 승인**으로 제약 정본에 올렸다(E5 이월표 **A-3** · 승인 경로 = `cicd.md` **CDT-6**). ⚠️ **판정 내용은 그대로 이관**했고 **대가·도입 조건이 함께 온다**(아래) |

#### C-08이 실제로 무엇을 자르나

```
✗ 안 하는 것    "MSA니까 게이트웨이·디스커버리·브로커는 당연히"
✓ 하는 것       요구를 세고 → 계약(순서·멱등·유실 방지)을 먼저 정하고
                → 그 계약을 지금 것으로 못 지킬 때만 도구를 넣는다
                → 넣지 않기로 했으면 ★ 「무엇이 관찰되면 다시 보나」를 적는다
```

| C-08이 요구하는 것 | 왜 |
|---|---|
| 도구를 **기각할 때 도입 조건을 함께** 적는다 | *"나중에 필요하면"* 은 **판정 불가**다. 조건이 없으면 다음 사람이 근거 없이 다시 올린다 |
| 도구가 **푸는 요구를 먼저 센다** | Phase 5 진입 선검증에서 브로커 도입이 **파생 결함 6건**을 만들었는데 그것이 푸는 요구는 **하나도 실증되지 않았다**(ADR-021 §1) |
| **보유 인프라를 먼저 본다** | 도구 하나 = 운영 대상 하나. 이미 운영 중인 것(nginx)이 하는 일을 새 제품으로 바꾸지 않는다(ADR-019 CH-1) |

> ★ **이것은 §0의 *"최대한 크게"* 와 충돌하지 않는다.** §0이 이미 못박았다 —
> *"오버엔지니어링보단 실제 은행 서비스를 기준으로"*. **크게 만드는 것은 도메인이고,
> 도구를 늘리는 것은 크게 만드는 것이 아니다.**

#### ★ C-09가 무엇을 대가로 치르나 (`cicd.md` §3.1 문면 이관 — 2026-08-06 승격)

| | |
|---|---|
| **무엇이 없나** | **스테이징이 존재하지 않는다**(C-04 예산 0 · 학습 환경). 실재하는 환경은 **개발자 로컬**(단위·통합·계약) · **CI 러너**(빌드 + 머지 게이트) · ★ **운영 실서버 3대**(`.9` nginx·MySQL·코어 / `.158` 정산 / `.164` 원장 + 오라클 리버스 프록시)뿐이다 |
| ★ **판정할 수 없게 되는 것** | ★ **인수(QS)·탐색 태그의 상당수가 운영 또는 임시 구성에서만 판정된다.** 이것이 E2가 **마일스톤 게이트를 머지 게이트와 분리**한 구조적 이유와 맞물린다 — ★ **없는 환경을 전제로 *"올리기 전에 확인"* 을 적지 않는다** |
| ⚠️ ★ **대가 — 첫 실배포가 곧 실험이다** | 배포 수치(`cicd.md` §7의 **CDV-1·2·3·4·9** — 헬스체크 N·T·D · 드레인 상한 · lease · 위성 정지 상한)는 **배포를 실제로 돌려야** 값이 나오는데 **돌릴 수 있는 환경이 운영 하나**다. 그래서 ⑴ **첫 코어 배포는 "배포"가 아니라 "탐색 태그의 실행"** 이고 ⑵ 그때 쓰는 상한들은 **측정 결과가 아니라 측정 전 선언값**이다(**IV-15**와 같은 형태 — *"선언 없이 측정하면 되돌림 조건이 인상으로 결정된다"*). ⚠️ ★ **이것을 적지 않으면 첫 배포의 실패가 「값이 틀렸다」가 아니라 「절차가 틀렸다」로 읽힌다** |
| ★ **도입 조건**(C-08 형식 — ★ **축이 둘이다**) | ⓐ **인수·탐색 시나리오를 운영에서 실행할 수 없다고 판정된 사건이 2회 누적**될 때(예: 부하 주입이 실거래 경로를 위협 · 결함 주입이 되돌릴 수 없음) ⓑ ★ **배포 절차 축** — **배포 절차 자체의 실패**(정지 창 미종료 · 부분 배포 상태 · 락 stale)가 **운영에서 2회 실측**될 때. ⚠️ **ⓑ가 없으면** *"배포 절차는 운영에서만 검증된다"* 는 사실이 **도입 조건 어디에도 반영되지 않는다**. 관측 대장 = `dev-conventions.md` §9 |
| ⚠️ **승격 경로**(재론하지 않는다) | 판정 자체는 **E4**(`cicd.md` §3.1)가 내렸고 **2026-08-06 사용자 승인**으로 여기 정본이 됐다 — 작업 기록 = `plans/2026-08-06/t0-doc-judgments/`. ★ **문면·근거·도입 조건을 그대로 이관**했으며 **내용은 바뀌지 않았다** |

### 1.1 조직 제약 철회가 무엇을 바꾸고 무엇을 안 바꾸나 ★★

C-01·C-02·C-03이 철회됐다. **설계 공간이 실제로 넓어진다** — 그 셋이 지우던 것들:

| 지워졌던 것 | 지금 |
|---|---|
| 다중 배포 단위 | **후보로 복원** — ADR-001 재검토 대상 |
| 브로커 상시 운영 | **후보로 복원** |
| 분산 추적·중앙 로그 수집 | **후보로 복원** |
| 전 유스케이스 구현 | **한다** |

**그러나 다음은 조직 제약과 무관하므로 그대로다.**

| 남는 것 | 왜 |
|---|---|
| ★ **E1·E2·E4가 C1·C2·C3를 한 커밋으로 요구한다** | **도메인 사실**이다. 인원이 늘어도 안 바뀐다 |
| ★ **대사(C6)는 다섯 컨텍스트의 "진짜 값"을 봐야 한다** | R9 순응자·읽기 전용. **사본을 보면 감사가 아니다** |
| **C-04 예산 0** | 실행 환경 제한 |
| **C-05~07** 기술 스택 | |

> ★ **정합성은 인원으로 살 수 없다.** 사람을 늘려도 분산 트랜잭션은 분산 트랜잭션이다.
> 그래서 **ADR-001의 안 B 기각은 유지되고, 안 C·E는 재검토 대상**이 된다.

### 1.2 C-04와 설계의 관계 — 실행과 설계를 가른다

예산 0은 **지금 무엇을 띄울 수 있는가**만 제한한다. **설계가 그것을 전제하면 안 된다** —
품질 속성 §2.1이 고가용성에서 내린 판단과 같다.

```
설계    다중 인스턴스·브로커·복제를 전제할 수 있다
실행    지금은 로컬 단일 구성으로 돌린다
```

### 1.3 (참고) 철회 전 판단 — C-01이 절반만 완화되던 이유

사용자 판단: *"제약은 괜찮아, 너랑 구현하잖아."*

**맞다. 다만 무엇이 완화되고 무엇이 안 되는지를 가른다.**

| | 완화되나 | 실측 근거 |
|---|---|---|
| **코드 작성 속도** | ✅ **크게 완화** | Phase 2에서 문서 11종·상태 머신 8종·규칙 51개를 3일에 작성 |
| **검증 부담** | ❌ **오히려 는다** | 그 산출물에 **치명 36건**이 있었다. 리뷰어 둘 × **10라운드**가 필요했다 |
| **운영 대응** | ❌ **전혀 완화 안 됨** | 장애는 사람이 받는다. AI는 호출돼야 움직인다 (C-02) |

> ★ **생산량이 늘면 검증 부담도 같이 는다.** 이것이 이 프로젝트의 가장 큰 실측이다
> (`study/project-workflow/docs-only-limits.md`). 빨리 만드는 것은 해결된 문제이고,
> **빨리 만든 것이 맞는지 아는 것**이 남은 문제다.
>
> **그래서 C-01 완화가 설계 공간을 넓히지 않는다** — 지우는 주체는 C-02이기 때문이다.

### 1.4 (참고) 철회 전 판단 — C-02가 가장 많은 것을 지우던 이유

운영 인력이 **개발자 본인 1명**이라는 것은 *"운영 복잡도가 곧 개발 시간을 먹는다"* 는 뜻이다.

| 지워지는 것 | 왜 |
|---|---|
| **다중 배포 단위(MSA)** | 배포·모니터링·장애 대응이 단위 수만큼 곱해진다 |
| **메시지 브로커 상시 운영** | Kafka·RabbitMQ는 그 자체가 운영 대상이다 (C-04와 겹침) |
| **분산 추적·중앙 로그 수집 스택** | 운영할 사람이 없다 |
| **수동 개입 전제 복구 절차** | 새벽에 깨어 있을 사람이 없다 → **자동 재시도 + 실패 시 적재**로 간다 |

> ★ **이것이 이 프로젝트에서 가장 중요한 제약이다.** Phase 2가 만든 도메인은 컨텍스트 6개이지만,
> **컨텍스트 ≠ 서비스**다. 배포 단위 수는 **C-02가 정한다** — C-01이 완화돼도 이 제약은 남는다.

### 1.5 (참고) 철회 전 판단 — C-03이 범위를 줄이던 이유

2주는 **"전부 만든다"가 불가능한 기간**이다. 양식(`01` §4)이 정한 대로 일정 제약은
**범위 축소의 근거**로 쓴다 — 품질을 낮추는 근거가 아니다.

```
줄이는 것    유스케이스 수 · 외부 연동 실체 · 운영 도구
줄이지 않는 것  자금 보존 · 대사 · 감사가능성   ← 도메인이 요구하는 것
```

> ⚠️ **load-bearing 가정**: 2주는 **Phase 3~5(아키텍처 + 샘플 유스케이스 E2E)** 의 기간으로 본다.
> Phase 6~9까지 포함이면 범위를 더 줄여야 한다 — **확인 필요**(§4 R1).

---

## 2. 제약이 아닌 것 (확인함)

| 후보 | 왜 제약이 아닌가 |
|---|---|
| **MSA로 만든다** | 사용자가 *"MSA 구조라면"* 이라고 조건부로 언급했다. **아직 정해진 것이 아니고**, **C-02**가 이 안을 강하게 제한한다 → **ADR에서 판정** |
| **게이트웨이를 둔다** | 위와 같다. 배포 단위가 하나면 필요 없다 → **ADR에서 판정** |
| **특정 라이브러리 채택** | 사용자가 *"기능별로 필요한 것을 정리·분리하는 게 맞다"* 고 명시했다. **구조 확정 후 Phase 5에서 고른다** |
| **성능이 좋아야 한다** | 어기면 *"느려진다"* 이므로 품질 속성이다 → `quality-attributes.md` |
| **정합성이 중요하다** | 위와 같다. 품질 속성이며, 이 프로젝트에서 **1순위 후보**다 |

> ★ **"MSA"와 "게이트웨이"를 제약에서 뺀 것이 이 문서의 판정 중 가장 중요하다.**
> 사용자도 *"우선 전체 구조부터 정하는 게 맞다"* 고 했다 — **구조는 제약이 아니라 결정**이고,
> 결정은 근거(제약 + 품질 시나리오)를 갖춰야 한다.

---

## 3. 라이브러리·알고리즘은 여기서 정하지 않는다

사용자 방침: *"바로 이거 이거 쓰기보단 각 프로세스별 필요한 라이브러리나 알고리즘을 정리·분리하는 게 맞다."*

| 시점 | 정하는 것 |
|---|---|
| **Phase 3 (지금)** | 배포 단위 수 · 통신 방식 · 일관성 모델 · 데이터 저장 위치 |
| **Phase 4** | 계약(API·이벤트 스키마) |
| **Phase 5** | ★ **프로세스별로 필요한 능력을 먼저 적고**, 그 능력을 채우는 라이브러리를 고른다 |

```
Phase 5에서 쓰는 순서
  ① 이 프로세스가 필요로 하는 능력      "멱등 저장", "영업일 배치 스케줄", "재시도 백오프"
  ② 그 능력의 판단 기준                  C-02(운영 1명)에 걸리는가
  ③ 후보와 기각 이유                     ADR 또는 기반 결정서
```

> ⚠️ **미정을 미정으로 남기는 것이 이 절의 목적이다.** 지금 라이브러리를 정하면
> **구조가 라이브러리에 끌려간다** — Phase 2에서 *"저장 구조가 도메인을 지배한다"* 를
> 흔한 실수로 등재한 것과 같은 형태다.

---

## 4. 협상 시도 기록

| # | 제약 | 시도 | 결과 |
|---|---|---|---|
| **R1** | **C-03 기간 2주** | 2주가 어느 Phase까지인지 확인 필요 (Phase 3~5인가, 6~9 포함인가) | ⏳ **미확인** — Phase 3~5로 가정하고 진행 |
| **R2** | C-04 예산 0 | 무료 티어·로컬 컨테이너로 충분한지 확인 | 시도 안 함 — 학습 프로젝트이므로 로컬로 충분하다고 판단 |

> **R1이 열려 있다.** 답에 따라 범위가 달라지므로 §1 C-03의 *load-bearing 가정*으로 표시했다.

---

## 5. 이 제약이 남기는 설계 공간

**지워지고 남은 것**을 적어둔다 — ADR이 여기서 출발한다.

| 결정할 것 | 제약이 남긴 후보 |
|---|---|
| **배포 단위 수** | 단일 배포(모듈러 모놀리스) / 2~3개 분할 — **6개 분할은 C-02가 지웠다** (C-01 완화와 무관) |
| **컨텍스트 간 통신** | 인-프로세스 이벤트 / DB 기반 Outbox — **브로커 상시 운영은 C-02·C-04가 지웠다** |
| **일관성 모델** | 트랜잭션 경계(E1~E5) 안은 **강한 일관성**(MySQL 트랜잭션), 밖은 **최종 일관성 + 대사** |
| **배치 실행** | 앱 내 스케줄러 / 외부 크론 — **오케스트레이터는 C-02가 지웠다** |
| **저장소 분리** | 단일 스키마 / 스키마 분리 — 소유권(Phase 2)을 지키는 선에서 |

> ★ **트랜잭션 경계 E1~E5가 분할의 하한이다.** E2(매입 반영)는 승인·계좌·카드·배치·미수가
> **한 커밋**이어야 하므로, 이 다섯이 서로 다른 서비스에 있으면 **분산 트랜잭션이 된다.**
> Phase 2가 열 라운드에 걸쳐 막은 사고가 아키텍처에서 되살아나는 경로다.

---

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v0.5 | 2026-08-06 | ★ **C-09 신설 — 스테이징 없음**(2026-08-06 사용자 승인으로 승격. E5 이월표 **A-3** · 승인 경로 = `operations/cicd.md` **CDT-6** · 작업 기록 `plans/2026-08-06/t0-doc-judgments/`). ⚠️ **새 판정이 아니라 위치 이동**이다 — 판정은 **E4**(`cicd.md` §3.1)가 내렸고 그때까지 **잠정**(CF-13 「ADR 승격 대상」)이었다. **문면·근거·도입 조건을 그대로 이관**했다: 실재 환경 3층(로컬·CI 러너·운영 3대) · ★ **대가 = 첫 실배포가 곧 실험**(CDV-1·2·3·4·9의 실측 장소가 운영뿐 → **측정 전 선언값**이 필요하다 — IV-15와 같은 형태) · ★ **도입 조건 두 축**(ⓐ 인수·탐색을 운영에서 못 돌린다는 판정 2회 ⓑ **배포 절차 자체의 실패 2회 실측** — 관측 대장 `dev-conventions.md` §9). **C-01~C-08은 불변** |
| v0.4 | 2026-08-06 | ★ **Phase 5 진입 개정** — **C-05를 Kotlin/JVM으로 개정**(사용자 확정 — 이 개정이 그 기록. JVM·Spring·MySQL 불변이라 기존 ADR 근거는 무효화되지 않는다) · **C-07 MySQL 불변 재확인**(PostgreSQL 전환 후보 부결 — C-07 위에 선 결정들이 있다) · ★ **C-08 신설: 도구 최소주의 — "요구 → 계약 → 도구" 순서와 도입 조건 명시 의무**(사용자 확정). §1.0 신설로 셋을 한자리에 기록 |
| v0.3 | 2026-08-05 | ★ **§0 프로젝트 목적 신설** — *"최대한 크게 하되 작은 단위로 끊어보는 연습"*. 이 목적이 제약 판단의 기준 자체를 바꾼다(인원은 자원이 아니라 가정). **C-01·C-02·C-03 철회** — 조직·일정 제약이 지우던 후보(다중 배포·브로커·분산 추적)가 복원된다. 단 **E1·E2·E4의 한 커밋 요구와 대사의 진짜 값 요구는 도메인 사실이라 그대로**다. §1.2에서 C-04를 *실행 제한*으로 좁혔다 |
| v0.2 | 2026-08-05 | **C-01 완화** — 사용자 판단(*"너랑 구현하잖아"*). 단 §1.1에 **완화되는 것과 안 되는 것**을 갈랐다: 코드 작성 속도는 크게 완화되지만 **검증 부담은 오히려 늘고**(Phase 2 실측 — 3일 산출물에 치명 36건), **운영 대응은 전혀 완화되지 않는다**. 설계 공간을 지우는 주체는 C-02이므로 **완화가 후보를 넓히지 않는다** |
| v0.1 | 2026-08-05 | 최초 작성 — 사용자 인터뷰 반영. C-01~C-07. **MSA·게이트웨이를 제약이 아닌 것으로 판정**(ADR에서 결정) · 라이브러리는 Phase 5로 미룸 · C-03의 범위(Phase 3~5)를 load-bearing 가정으로 표시하고 R1에 협상 대상으로 등재 |
