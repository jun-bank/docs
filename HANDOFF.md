# 인계 — Phase 3 종료 + ★ 멀티테넌시 설계 변경 완료 시점

- 작성일: 2026-08-05 (v0.3 — 멀티테넌시 반영)
- 다음 작업: **Phase 4 유스케이스·계약** (아래 §5 — 멀티테넌시 백로그 포함)
- 읽는 사람: 새 세션의 나 자신 · 또는 이 프로젝트를 이어받는 사람

> ★ **이 문서 하나로 이어갈 수 있게 쓴다.** 대화 요약이 아니라 **저장소에 남는 사실**이다.
> 대화는 압축되면서 갈라지지만 이 문서와 `git log`는 안 갈라진다.

---

## 1. 이 프로젝트가 무엇인가

카드 결제 발급사 + 뱅킹 시스템을 **문서부터** 설계한다. 그런데 **산출물이 둘**이다.

| # | 산출물 | 수명 |
|---|---|---|
| ① | 결제 시스템 | 프로젝트와 함께 |
| ★ ② | **설계 워크플로우 자체** | **더 오래 산다** |

사용자가 명시한 목적(`architecture/constraints.md` §0):

> *"최대한 크게 하되, 이 큰 걸 **아주 작은 단위로 끊어보는 연습**을 하려는 것."*
> *"오버엔지니어링보단 **실제 은행 서비스를 기준**으로."*
> *"나중에 실무에서는 **필요한 부분만 부분적으로 활용**해서 응용 가능하다는 게 내 관점."*

**②가 부분 채택 가능해야 한다** — 그래서 규칙 하나하나에 *"왜"* 와 *"이 이유가 바뀌면"* 이 붙어 있다.

---

## 2. 지금 어디까지 왔나

```
Phase 0  문서 체계          ⏭ 건너뜀 (Phase 6에서)
Phase 1  제품 정의          ✅
Phase 2  도메인             ✅ 애그리게이트 11 · 조작 65 · 경계 E1~E5 · BR-01~53
Phase 3  아키텍처           ✅ ★ 마감 — ADR 15 · 뷰 4 · 기술서
★ 멀티테넌시 설계 변경      ✅ 2026-08-05 — BR-54~58 · C8 승격(CM4) · ADR-016~018 · 승인요청 SM
                              · M18 · QS-08/10 · D-04 재작성. 듀얼 리뷰 3루프(반영 ~60건).
                              작업 기록 = plans/2026-08-05/multitenancy/ (spec·log·ledger·리뷰 원문)
Phase 4  유스케이스·기능명세  ✅ **완료** (2026-08-06) — 양식 2종 v1.1 · UC-01~20 전수
                              (도출표 4원천·색인) · 정본 판정 9건(canon-decisions) ·
                              ★ **계약 전수 패스**(§7 전 칸 · 공통 규약 K-1~4 · 신설 코드
                              대장 · R15 지시 이벤트 · 판정 37 — contracts-pass). 이월:
                              UC15㉤·16㉣㉤·16-4/19-2(R15 파티션 순서)·11-2(AU7)·10-4(파일
                              규격) · 수치 [미확정] 전량. 다음: **C7 명세**(발동됨) · §5 잔여
Phase 5  샘플 구현 + 실험 38건 ⬜
Phase 6  개발 규약           ⬜
```

### 확정된 것 (뒤집지 마라 — 근거가 문서에 있다)

| 무엇 | 어디 |
|---|---|
| 애그리게이트 **11종** · 트랜잭션 경계 **E1~E5** | `domain/aggregates/README.md` |
| 바운디드 컨텍스트 ~~C1~C7~~ ★ **C1~C8** · 관계 ~~R1~R12~~ ★ **R1~R16** | `domain/context-map.md` |
| 업무 규칙 ~~BR-01~53~~ ★ **BR-01~58** · 불일치 유형 ~~M1~M17~~ ★ **M1~M18** | `product/01-business-rules.md` |
| **배포 3개** — 코어(C1·C2·C3·**C6**·C7) · 정산(C4) · 원장(C5) | **ADR-010** |
| 파티션 키 = **계좌ID** (단 축은 셋 — ADR-008 §2) | **ADR-009** |
| **상태 저장** + 원장 append-only (이벤트 소싱 기각) | **ADR-003** |
| 채택 품질 6 / **포기 6** | `architecture/quality-attributes.md` |
| ★ **컨텍스트 8종(C8 운영 승격 — CM4)** · 관계 **R13~R16** | `domain/context-map.md` §8 |
| ★ **BR-54~58** — 주체·권한 3단계·2인 승인 5조작·감사·조회 격리 | `product/01-business-rules.md` 15장 |
| ★ **격리 = 채택 7순위** · 감사가능성 = 행위자 축 포함 · **D-04 재작성**(테넌트 축·다국가만 포기) | quality-attributes v0.3 |
| ★ 인가 = 타입 강제·인프로세스 / 감사 = C8 append-only·outbox 수집 / 격리 축 = `ownerId` 선반영 | **ADR-016 · 017 · 018** |

---

## 3. ★ 되돌리면 사고가 재발하는 곳

**이 표가 이 문서에서 가장 중요하다.** 전부 *"왜 이렇게 돼 있지"* 하고 되돌리기 쉬운 형태인데,
되돌리면 **이미 겪은 사고가 그대로 재발**한다.

| 자리 | 몇 번 깨졌나 | 되돌리면 | 정본 |
|---|---|---|---|
| **한도 기준일** `limitBasisDate` | ★ **4회** | 부분 매입 시 한도 복원이 다시 깨진다 | DC-003 |
| **역방향 경로** (배포 4→3) | 치명 4건 | 대사를 다시 밖으로 빼면 T2·T4 부활 | DC-004 · ADR-010 |
| ★ **마감 잔액 = 영업일별 이동(증분)** | ★ **3회** | 스냅샷·복사로 되돌리면 **네 번째**가 온다 | **DC-006** |
| **상한과 좌변** | ★ **5회** | *"상한 적었는데 좌변이 없다"* | DC-002 |
| **합계 필드 판정** | 치명 9건 | 미수를 계좌로 되돌리면 재발 | DC-001 |
| **대사가 사본을 보면 안 된다** | 3회 확인 | 복제본 대사는 **네 번째** 실수다 | ADR-007 W-4 |
| **정산 outbox**(역방향) | 1회 | 빼면 **하루치 대사가 무음 정지**한다 | ADR-005 O-17 |
| ★ **승인-실행 프로토콜** — 코어 안 ①~③=동기 소비(R14) / 코어 밖 ④⑤=지시 이벤트(R15) | 리뷰 3루프에 걸쳐 2회 재설계 | ④나 ⑤를 동기 확인으로 되돌리면 **배포 경계를 넘는 동기 호출·원자 커밋**이 재발한다 | context-map R14·R15 · ADR-010 |
| ★ **원장 INV-9** — 같은 `instructionId` 역분개 금지 | 리뷰가 발견 | 빼면 지시 재전달·DLQ 재투입이 **역분개를 두 번 기표**한다 | journal-entry INV-9 |
| ★ **이벤트-행위자 선** — 운영 이벤트만 행위자 포함, 자금 이벤트는 비포함 | 1회(자기모순 후 확정) | 자금 이벤트에 행위자를 실으면 원장 언어가 오염된다 | ADR-017 AD-7 |

### ★ 다섯 번 반복된 결함 형태

> **"각각은 맞는데 합치면 깨진다"**

```
DC-003   한도 복원      R8 S8 과 R9 H1 이 각각은 맞는데 합치니 깨짐
DC-004   역방향 경로     밖으로 나가는 경로만 보고 밖에서 안을 보는 경로를 안 봤다
DC-005   마감 잔액       lazy close 통찰은 옳았고 그릇이 틀렸다
013×012  시간 예산       계층은 맞고 재시도도 맞는데 합치면 4.1초
005×010  역방향 Outbox   경로는 만들었는데 유실되지 않는 경로는 안 만들었다
★ DC-006 마감 잔액      ★ 정지 규칙 발동 — 같은 자리 3회. 패치를 멈추고 토대를 바꿨다
```

### ★ 정지 규칙 — 이 프로젝트에서 **실제로 발동한 적이 있다**

> core §3: *"같은 결함을 2회+ 고쳐도 또 깨지면 **틀린 토대를 다듬는 중**이다."*

**마감 잔액 자리가 세 번 깨졌다** — 구 표현 `DailySnapshot`(T2) → `closedBalance`(하루 어긋남)
→ `AccountDailyClose`(역순 귀속) — 구 표현이며 셋 다 폐기됐다(DC-006 §1).

진짜 원인은 **`balance`에 시간축이 없어 "복사"가 필요했던 것**이고,
원장이 안 깨진 이유는 **append-only 이동 기록이라 구성적**이기 때문이다.
★ **계좌도 이동을 적게** 바꾸니 세 실패 모드가 **전부 사라졌다**(더하기는 순서가 무관하다).

**그래서 기술서 §7이 ADR을 쌍으로 대조한다.** 새 ADR을 만들면 **기존 ADR과 쌍으로 대조**하라.

### ★ 두 번째로 자주 나온 형태 — "고쳤다고 적고 안 고침"

오늘 **5건**이다 — ★ **다섯 번째가 DC-006 자신**이었다(ADR-011 v0.3을 완료 선언했는데 이력에 없었다). `DC-005 §4 N-2·N-4` · `ADR-011 v0.2 이력` · `settlement.md`(markSettled 멱등화)가 전부 **선언만 하고 본문을 안 고쳤다.**

**검사 ㉒~㉖을 그래서 만들었고 즉시 29 + 37 + 17건을 더 찾았다.**

| 검사 | 무엇 |
|---|---|
| **㉒** | 채택 ADR이 **대체된 ADR을 정본처럼** 참조하나 |
| **㉓** | **폐기된 필드명**이 살아남았나 |
| **㉔** | 규칙 ID **중복 정의** |
| ★ **㉕** | DC 문서가 *"반영 완료"* 라 적은 대상 파일에 **그 DC 언급이 실제로 있나** |
| ★ **㉖** | **폐기된 개념어**(이름이 아니라 의미) — ㉓이 못 보는 것 |

> ★ **㉖이 필요한 이유**: ㉓은 **이름**을 본다. DC-006이 엔티티 이름을 바꿨을 때 검산은 통과했는데,
> 구 표현인 *"마감 행"* 이라는 **개념어**가 17곳에 남아 있었고
> **`ADR-011`에는 구 초기화 알고리즘(직전 행 값을 이월)까지 살아 있었다** — 증분에서 이월하면 Σ가 이중 계상된다. 그래도 기계가 못 잡는 것이 남는다 —
**"반영 완료"라고 적을 때는 본문을 다시 열어 확인하라.**

---

## 4. 검산 도구 — 먼저 돌려라

```bash
cd /home/jun/project/jun-bank/docs
python3 tools/check_docs.py      # 검사 ①~㉖
python3 tools/mutation_test.py   # 변이 27건
```

**통과 기준**

```
통과 — 애그리게이트 11 · 조작 65 · 경계 5
변이 27건 중 27건 검출 (100%)
```

| 파일 | 무엇 |
|---|---|
| `tools/docs_model.py` | 문서에서 정본 집합 추출 |
| `tools/gen_matrix.py` | **`ASSIGN` 65개 조작→경계** — ★ 사람이 판단하는 유일한 부분 |
| `tools/check_docs.py` | 검사 ①~㉖ |
| `tools/mutation_test.py` | 과거 결함을 주입해 검사가 잡는지 |

> ★ **"통과하는 검산은 결함이 없다를 뜻하지 않는다."**
> 오늘 `check_docs.py`가 통과한 상태에서 **치명 5건·잔재 29건**이 나왔다.
> 커버리지는 *(사람이 잡은 것 중 기계 검산 가능한 것 중 스크립트가 실제로 잡은 것) / (기계 검산 가능한 것)* 이다.

---

## 5. 다음 작업 — Phase 4 (멀티테넌시 백로그가 입력이다)

> ★ **원래 이 절의 다음 작업(멀티테넌시 설계 변경)은 2026-08-05 완료됐다** — 절차 ①~⑤를 전부 밟았고,
> 그 경험(산출물 ②)은 `plans/2026-08-05/multitenancy/log.md`(ledger·완료 요약)에 있다.
> 아래 G-1~G-4·D-04는 **역사 기록**으로 남긴다 — 전부 닫혔다.

### Phase 4로 넘기는 멀티테넌시 백로그 (각 문서 각주에 흩어진 것을 여기 모은다)

| # | 항목 | 정본(그때까지) |
|---|---|---|
| 1 | **C7 운영자 계정·권한 모델** (부여/변경/회수 이력) — 회원·자격증명·세션과 함께 명세 | BR-55 · context-map §4 |
| 2 | **C8 애그리게이트 정식 명세** (감사 기록·승인 요청) | ADR-017 **AD-8** + `state-machines/approval-request.md` |
| 3 | **엔드포인트 목록 생성 → 타인 식별자 전수 시험** (QS-08 측정) | BR-58 · ADR-016 §6 |
| 4 | **스코프 구조** (조직/지점 계층·전사 스코프 특칙 구현 형태) | BR-54·55 |
| 5 | 이벤트 계약에 **격리 축·행위자 필드** (S-6a 창구 — 지금 문서 모델의 선이 입력) | ADR-018 IS-5 · ADR-017 AD-7 |
| 6 | QS 수치 대장 등재(감사 outbox·보존) · R15 관계 유형(원장 ACL 지시 항목) · AR2(APPROVED 유효기간) · 복제본 갱신 규약 · 제품 정의서 시나리오 반영 · 감사 파기 구조 판정 | 리뷰 ledger open 항목 |

<details>
<summary>역사 기록 — 착수 시점의 공백과 절차 (완료됨)</summary>

### 왜 이것인가 — 사용자의 말

> *"실제 은행에서 **회원 레벨별 관리나 관리자 접근 단계**를 설계할 때 중요하다고 생각한다.
> 결국 은행이라는 게 그런 거잖아?"*

### 지금 뚫려 있는 것 (`study/09-multi-tenancy` §5)

| # | 무엇 | 왜 문제인가 |
|---|---|---|
| **G-1** | **인가(authorization) 모델이 없다** | C7은 *인증*이다. **누가 무엇을 할 수 있는지**가 없다 |
| ★ **G-2** | **운영자 권한 단계가 없다** | BR-28·37·42·45가 전부 *"운영자"* — **같은 사람인가?** 은행에서 이건 **내부 통제 부재**다 |
| **G-3** | 조회 경로의 소유자 조건 | 조회 API가 없어서 안 드러났을 뿐 |
| **G-4** | **감사 로그의 행위자** | 원장은 **자금**은 남기지만 **누가 했는지**를 안 남긴다 |

### 그리고 D-04 재검토

`quality-attributes.md`가 멀티테넌시를 **포기(D-04)** 했는데, 되돌리는 조건이
*"범위가 바뀔 때"* 라 **측정 불가능**하다. 요구사항이 들어오면 그 조건부터 다시 써야 한다.

### 절차 (이 프로젝트가 만든 것)

```
① 요구사항 수신 → 무엇이 바뀌는가 · 무엇이 안 바뀌는가를 먼저 가른다
② 기존 결정 중 무엇이 무효가 되는가 (ADR·BR·불변식 전수)
③ DC 문서를 연다 — 왜 깨졌나 · 진짜 원인 · 결정 · 무엇을 바꿨나 · 이 판단이 틀리면
④ 반영 → 검산 → ★ 듀얼 리뷰 (Opus 워커 ∥ codex)
⑤ ★ "반영 완료"를 적기 전에 본문을 다시 연다
```

---

</details>

---

## 6. 남은 것

### 값이 [미검증] — 실측 대상

| 출처 | 항목 |
|---|---|
| **ADR-013** | DB 쿼리 1초 · 락 대기 0.5초 · 브레이커 임계 · **재시도 백오프** · **다음 시도 최소 시간** · 매입사 5초 |
| **ADR-014** | 재시도 상한 5회 · 백오프 간격 |
| **ADR-012** | 충돌률 임계 5% |
| **BR-52·53** | 드레인 대기 상한 · 3영업일 미해결 · 정산 미결 |
| **QS-01** | 주간 집중 계수 1.5 · 명절 배수 3~5배 |
| **QS-04** | 대사 1시간 (RTO 근거는 D11로 폐기됨) |

**미확정 수치 표 15건** · **열린 의문 26 + 16건**.

> ★ **실측 전까지 이 값을 근거로 다른 결정을 하지 않는다.** 이것이 정책이다.

### 구조적으로 남은 것

| # | |
|---|---|
| **O-1** | ADR-012~015의 근거가 **장애 사례**라 QS로 환원되지 않는다 |
| **O-3** | E2·E4가 계좌 파티션에서 안 닫힌다 (샤딩 시 발현) |
| **팬텀 배치 탐지** | `ReceivableRecovered`에 **구독자가 없어** 사후 재구성 경로가 없다 → W-15로 발생 시점에 잡는다 |
| **관측 수단** | 되돌리는 조건 12개 중 여럿이 *"지표"* 인데 **D-05(실시간 관측)를 포기**했다 |

---

## 7. 문서 지도

```
product/01-business-rules.md      ★ 규칙 정본 — BR·M유형·수치는 여기가 우선
domain/aggregates/               애그리게이트 11 + README(경계·조작 대장)
domain/state-machines/           상태 전이 (★ 9종 — 승인요청 추가)
domain/context-map.md            R1~R12
architecture/README.md           ★ 기술서 — 여기부터 읽으면 지도가 선다
architecture/adr/                ★ ADR 18 (001·004·006 대체됨 · 016~018 = 2026-08-05 멀티테넌시)
architecture/views/              컨텍스트·컨테이너·데이터·런타임
design-changes/DC-001~006        ★ 왜 그렇게 됐는지가 여기 있다
study/                           이벤트소싱·멀티테넌시·대규모실패·실패유형 F-01~25
reference/                       리서치 원문 · 장애 사례
tools/                           검산
```

### 정본이 갈리면

**`product/01-business-rules.md`가 이긴다.** BR과 M유형은 거기가 정본이고,
ADR·애그리게이트 문서는 그것을 **어떻게** 하는지를 적는다.
(오늘 `O-16`이 `(BR-52)`를 인용했는데 **BR-52에 그 내용이 없어** 뚫린 사례가 있다.)

---

## 8. 이 프로젝트가 쓰는 작업 방식

| | |
|---|---|
| **듀얼 리뷰** | Opus 워커 ∥ codex → 종합 → 반영 → 재점검. **중간↑는 생략 금지** |
| **워커에게 경로를 주고 실파일을 읽게 한다** | 요약을 주면 요약을 검증한다 |
| **리뷰어 주장도 검증 대상** | codex R10의 *"6건 전부 통과"* 가 재현 안 된 적 있다 |
| **원문을 저장소에 남긴다** | 리뷰 원문을 잃은 적이 있다 → `plans/YYYY-MM-DD/` |
| **커밋 메시지에 왜를 적는다** | `git log`가 **압축 안 되는 기록**이다 |

---

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v0.3 | 2026-08-05 | ★ **멀티테넌시 설계 변경 완료 반영** — §2에 완료 행·확정 4행 추가, §3에 보호 자리 3곳 추가(승인-실행 프로토콜·INV-9·이벤트-행위자 선), §5를 Phase 4 백로그로 교체(원 시나리오는 역사 기록으로 접음), 지도에 ADR 18·상태머신 9종 반영 |
| v0.2 | 2026-08-05 | ★ **DC-006 반영** — 마감 잔액이 **세 번째로 깨져 정지 규칙이 발동**했다. §3에 그 사실과 *왜 토대를 바꿨는가* 를 남겼다 |
| v0.1 | 2026-08-05 | Phase 3 종료 시점 인계. ★ §3(되돌리면 재발하는 곳)이 핵심 — **다섯 번 반복된 "각각은 맞는데 합치면 깨진다"** 와 **"고쳤다고 적고 안 고침"** 두 형태를 명시했다 |
