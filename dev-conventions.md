# 개발 규약 — 규칙마다 무엇이 강제하나 (내용 정본)

- 작성일: 2026-08-06
- 상태: 검토대기
- 양식: `study/project-workflow/phase5/03-dev-conventions.md`
- 절차상 위치: **E3**(`study/project-workflow/phase5/01-entry-procedure.md` §1 — E2 테스트 전략 다음, E4 구성 관리·CI/CD 앞)
- 입력: `architecture/adr/ADR-002`(모듈) · `ADR-025`(공통 라이브러리) · `architecture/test-strategy.md`(E2) · `architecture/constraints.md` **C-05~C-08** · `usecases/README.md` §5(K-1~4 · 코드 대장) · `study/project-workflow/verification-policy-v2.md`

> ★ **이 문서는 규칙을 새로 만들지 않는다.** 모듈 경계는 ADR-002·025, 업무 규칙은 `product/01-business-rules.md`, 계층·게이트는 `architecture/test-strategy.md`가 정본이다. 이 문서가 새로 적는 것은 **⑴ 그 규칙이 코드에서 어떤 모양인가 ⑵ 무엇이 그것을 막나(「강제 수단」 칸)** 둘뿐이다.
> ⚠️ **정본이 갈리면 `product/01-business-rules.md`가 이긴다**(HANDOFF §7). 이 문서의 인용이 정본과 다르면 이 문서가 틀린 것이다.

## 강제 수단 표기 (전 절 공통)

| 표기 | 무엇이 막나 | 언제 알게 되나 |
|---|---|---|
| **컴파일** | 빌드 모듈 그래프 · 타입 시스템 | 빌드 즉시 — 코드가 존재할 수 없다 |
| **아키텍처 테스트** | 컴파일이 못 보는 구조 위반(리플렉션·모듈 내 레이어·타입 스밈) | 매 빌드 — ⚠️ **도구 미확정**(§6) |
| **테스트 게이트** | 단위·통합·계약 케이스 (머지 게이트 = E2 §3.0) | 매 빌드·머지 |
| **린트·자동 검사** | 포매터·정적 규칙·커밋 훅·생성물 drift 검사 | 커밋·빌드 — ⚠️ 일부 **도구 미확정**(§6) |
| **리뷰** | 사람 | PR |
| ★ **잠정 리뷰** | ★ **원래 위 중 하나가 막아야 하는데 도구가 아직 없어 지금은 사람이 보는 것.** 무엇이 확정되면 올라가는지를 §7이 전량 나열한다 | PR (한시적) |

> ★ **강제 사다리**: 컴파일 > 아키텍처 테스트 > 린트·자동 검사 > 리뷰 (ADR-002 §5의 강제력 표와 같은 순서). 규칙을 낮은 칸에 둔 행은 **왜 위로 못 올렸는지**를 비고에 적는다.

---

## 1. Kotlin 컨벤션 (C-05)

> **공식 Kotlin coding conventions를 기본으로 채택한다.** 이 절은 **프로젝트 고유 규칙만** 적는다 — 공식 스타일을 옮겨 적으면 갱신되지 않는 사본이 하나 더 생긴다.

| # | 규칙 | 근거(정본) | 강제 수단 | 비고 |
|---|---|---|---|---|
| **KO-1** | **공식 스타일을 그대로 쓴다.** 명명·들여쓰기·임포트 정렬·줄바꿈은 포매터가 정하고 **사람이 논쟁하지 않는다.** ★ 규약은 스타일 항목을 개별 열거하지 않는다 | C-05 · 검증 정책 v2 §1(표기 유형은 리뷰 렌즈 밖) | **린트** ⚠️ *현재 **잠정 리뷰*** | 포매터·린트 **도구와 설정값 = [구현 검증]**. 확정 전까지 스타일 지적은 리뷰 몫이고, 그것이 이 규칙을 조기 자동화해야 하는 이유다 |
| **KO-2** | ★ **불변 우선.** `val` 기본 · 상태 변경은 새 값 반환(`copy`) · ★ **가변 컬렉션을 경계 밖으로 노출하지 않는다**(반환 타입은 읽기 전용 컬렉션) | 애그리게이트 불변식(INV-*)이 *"객체 안에서만 성립"* 하려면 밖에서 고쳐질 수 없어야 한다 | **린트** ⚠️ *현재 **잠정 리뷰*** | 반환 타입은 컴파일이 막고(`List` vs `MutableList`), `var` 남용은 린트 규칙 — 도구 확정 시 자동 |
| **KO-3** | ★ **null 안전.** `!!` 금지 · 플랫폼 타입(전문·파일 파싱·JDBC 경계)은 **경계에서 즉시 검증**해 non-null 도메인 타입으로 바꾼다 — 도메인·application에 nullable이 흘러들지 않는다 | 시스템 채널 판정 순서 K-1(X-10 ① 인자 형식) — 형식 오류는 **경계에서** `ARG_MISMATCH`다 | **컴파일** (+ 린트: `!!` 금지) | 타입 시스템이 대부분을 막는다. 남는 것은 `!!`·플랫폼 타입 통과 2종뿐이라 린트로 좁힌다 |
| **KO-4** | ★ **값 타입 의무 — 원시 타입 금지.** 금액은 **`Money`**, 영업일은 **`BusinessDate`**. `Long`·`BigDecimal`·`Double`·`LocalDate`를 금액·영업일 자리에 쓰지 않는다. ★ **정본은 `common-kernel`이고 `core:shared`가 재수출**한다 — **두 벌로 정의하지 않는다** | **BR-07**(통화 + 최소단위 정수 · 부동소수 금지) · ADR-025 **CL-2 ⑴** · ADR-002 트리 `shared/` · `glossary.md`(영업일) | **컴파일** (+ **테스트 게이트**: BR-07 정적 규칙 케이스 — 금액에 float/double **0건** · 통화 필드 존재) | E2 §2.1에서 **BR-07은 주 계층 = 단위(정적 규칙 — 하한 예외)**. 즉 이 규칙은 **실행 없이 소스에서 판정**된다 |
| **KO-5** | ★ **시간은 명시 주입.** 도메인·application에서 시스템 시계를 **직접 호출하지 않는다**(현재 시각·오늘 날짜를 구하는 호출) — 시각 공급자를 **인자 또는 의존으로 받는다** | **BR-06**(마감과 정산 시각) · **BR-14**·**BR-25**(영업일 귀속)의 검증 방법 칸이 *"마감 직전·직후"* · *"금요일 마감 후 → 월요일 귀속"* 을 요구한다 — **시각을 고정할 수 없으면 그 케이스는 재현되지 않는다** | **아키텍처 테스트** ⚠️ *현재 **잠정 리뷰*** | 시각 공급자의 **타입·주입 방식은 [구현 검증]**. 규약이 정하는 것은 *"직접 부르지 않는다"* 까지다 |

---

## 2. 모듈 규칙 (ADR-002 · ADR-025)

> ★ **모듈 트리·의존 방향의 정본은 ADR-002 §3·§5이고, 공통 라이브러리 경계의 정본은 ADR-025 §2다.** 이 절은 그 규칙을 **다시 적지 않고** 무엇이 막는지만 적는다.

| # | 규칙 | 근거(정본) | 강제 수단 | 비고 |
|---|---|---|---|---|
| **MO-1** | **모듈 트리와 빌드 그래프는 ADR-002 §5가 정본**이다 — `core:banking`·`card`·`payment`·`auth`·`reconciliation`·`ops`·`shared` + 위성(`settlement`·`ledger`). ★ **규약은 그래프를 다시 그리지 않는다** | ADR-002 §3·§5 | **컴파일** | 빌드 모듈 분리 자체가 그래프다. Gradle 멀티모듈은 ADR-025 **CL-1**이 이미 적고 있다(인용 — E3가 새로 고르는 것이 아니다) |
| **MO-2** | ★ **다른 컨텍스트의 `domain/`을 직접 참조하지 않는다. `port/`만 호출한다.** `port/`는 **도메인 타입을 노출하지 않는다** — 값 타입(`shared/`)과 ID만 | ADR-002 **M-1·M-2** | **컴파일** | `port/`를 별도 빌드 모듈로 뺀다 — `core:payment`가 `core:banking:port`에는 의존하되 `core:banking:domain`에는 **의존할 수 없다** |
| **MO-3** | ★ **의존 방향은 아래로만.** `C3 → C1` · `C3 → C2`만이고 **역방향·순환 금지**. `C1 → C2` 의존도 없다 | ADR-002 **M-3·M-4** · 컨텍스트 맵 R2·R3 | **컴파일** | 순환은 빌드 도구가 거부한다 |
| **MO-4** | ★ **컴파일이 못 막는 것은 아키텍처 테스트가 막는다** — ⑴ **리플렉션 접근** ⑵ **같은 모듈 안의 레이어 위반**(adapter → domain 역참조 등) ⑶ ★ **`shared/`에 도메인 타입이 스며드는 것** | ADR-002 §5 경고문 그대로 | **아키텍처 테스트** ⚠️ *현재 **잠정 리뷰*** | ADR-002 §7이 통과 기준을 이미 적었다 — *"M-1~M-6 위반 **0건**"* |
| **MO-5** | ★ **위성(C4·C5)은 코어를 동기 호출하지 않는다**(이벤트로만) · **C6 대사는 코어 안에서 읽기만 한다**(쓰지 않는다) · **`ops` 역방향 금지**(코어 → `core:ops(port)`는 허용, 반대는 금지) | ADR-002 **M-5·M-6·M-7** | **아키텍처 테스트** ⚠️ *현재 **잠정 리뷰*** | ★ **배포 경계를 넘는 쓰기 클라이언트 타입이 존재하지 않아야 한다** — 위성에서 코어로 향하는 동기 클라이언트가 **코드에 없음**을 단언한다 |
| **MO-6** | ★ **공통 라이브러리 경계.** ⑴ **공통이 소유하는 계약은 봉투뿐**(ADR-021 ET-4 8항목) ⑵ ★ **전역 payload 모듈을 만들지 않는다** — payload는 **발행 컨텍스트가 소유**하고 수신측이 ACL로 번역한다 ⑶ **공통에 도메인 로직·조작·저장소·판정 술어를 두지 않는다**(BR-55 술어 구현은 C7 소유) ⑷ ★ **공통은 어느 업무 배포도 의존하지 않는다** | ADR-025 **CL-1·CL-5·CL-6 ⑶·CL-8** | **컴파일**(⑷ — 의존 방향) + **아키텍처 테스트**(⑴~⑶) ⚠️ *⑴~⑶ 현재 **잠정 리뷰*** | ADR-025 §6이 통과 기준을 이미 적었다 — *"공통 모듈이 업무 배포 코드·애그리게이트를 참조하지 않는다"* · *"한 발행자의 payload 필드 추가 → 다른 배포는 빌드가 안 깨진다"* |
| **MO-7** | ★ **멱등의 소유자를 흐리지 않는다.** `messaging-infra`가 주는 것은 **골격**(outbox 발행·릴레이 러너·inbox 골격)까지이고, **멱등 키의 선택·유니크 제약·핸들러 커밋 경계는 수신 컨텍스트가 소유**한다. C3의 멱등은 **도메인 사전조건**이므로 골격의 멱등 장치를 얹지 않는다 | ADR-025 **CL-3·CL-4** | **리뷰** | ⚠️ **자동화 불가** — *"무엇이 중복인가"* 는 도메인 지식이라 구조로 판정되지 않는다. 승격 조건 = 같은 오류(공통이 정한 키로 중복을 놓침)가 ADR-025 **R3**로 관측될 때, 그때는 규칙이 아니라 **설계를 되돌린다** |

---

## 3. 오류 규칙

> ★ **오류 코드의 유일 창구는 `usecases/README.md` §5의 신설 코드 대장(+ 선례 승계 코드)이다** — *"여기 없는 코드는 계약이 쓸 수 없다"*(K-1 ⑶).

| # | 규칙 | 근거(정본) | 강제 수단 | 비고 |
|---|---|---|---|---|
| **ER-1** | ★ **코드 대장이 유일 창구.** 새 오류 코드는 **대장 등재가 선행**한다 — 코드에서 먼저 만들고 나중에 등재하지 않는다. 대장에 없는 코드를 쓰는 계약·구현은 **결함**이다 | `usecases/README.md` §5 **K-1 ⑶** | **테스트 게이트**(계약 케이스 — 코드 상수 집합 ↔ 대장 집합 대조) + **리뷰**(등재 선행) | 대조는 **집합**으로 한다 — 수만 세면 다른 코드가 들어 있어도 통과한다(E2 §6.3과 같은 이유) |
| **ER-2** | ★ **오류 모델은 대장에서 생성한다. 수기 미러 금지.** 코드에서 **오류 코드 문자열을 손으로 쓰지 않는다** — 생성된 오류 모델(타입)만 던진다 | ADR-025 **CL-2 ⑵**(*"코드 대장을 기계 판독 원천으로 생성 — 수기 미러 금지(드리프트)"*) | **컴파일**(생성 타입만 존재 — 문자열 리터럴로는 만들 수 없다) + **린트·자동 검사**(생성물 drift 검사) | drift 검사는 `tools/gen_api_reference.py --check`(검사 ⑥b)와 같은 형태다 — ★ **선례이지 이 규약이 도구를 확정하는 것이 아니다.** 원천 형식·생성기 = [구현 검증] |
| **ER-3** | ★ **판정 순서를 고정한다.** 사람 채널 = ① 자기 속성 → ② 스코프·존재 + 대상 유효성 → ③ 상태·인자. **시스템 채널(전문·파일)은 `X-10`이 정본** — ① 인자 형식 → ② 멱등 단락 → ③ 대상 존재 → ④ 상태(**재수신이 대상 조회보다 앞선다**) | `usecases/README.md` §5 **K-1** · `interfaces/README.md` **X-10** | **테스트 게이트**(단위·계약 케이스) | 순서가 갈리면 **같은 요청이 채널마다 다른 코드로 거절**된다. 채널 분기는 규칙이지 실수가 아니다 |
| **ER-4** | ★ **채널 변환은 어댑터가 한다.** 문자 상수 → **4N 물리 응답 코드**(X-7) 매핑은 **adapter 층에만 존재**한다 — 도메인·application은 물리 코드를 모른다 | `interfaces/README.md` **X-7**(*"정본은 문자 상수"*) · ADR-002 M-2(값 타입과 ID만 노출) | **아키텍처 테스트**(도메인·application에 물리 코드 리터럴 **0건**) ⚠️ *현재 **잠정 리뷰*** + **테스트 게이트**(계약 케이스 — 매핑의 **존재와 유일성**) | ⚠️ E2 §3.2 그린 위장 항목: *"매핑이 있다"* 만 단언하면 위장이다 — **한 문자 상수 ↔ 한 값**의 **유일성**까지 단언한다 |
| **ER-5** | ★ **물리 코드의 숫자 값은 확정하지 않는다** — `[미확정 — 대외 협의]`. 규약도, 계약 테스트도 **값을 단언하지 않는다** | `interfaces/README.md` **X-7** | **리뷰** | ⚠️ **자동화 불가** — 값이 없으니 대조할 대상이 없다. 승격 조건 = **대외 협의로 값이 확정될 때**. 그때 ER-4의 계약 케이스가 값 단언으로 강화된다 |

---

## 4. 테스트 작성 규칙 (E2 인용)

> ★ **계층·태그·자산 배정·게이트의 정본은 `architecture/test-strategy.md`다.** 이 절은 **작성자가 지켜야 할 실행 규칙**만 인용한다 — 계층 정의를 다시 적지 않는다.

| # | 규칙 | 근거(정본) | 강제 수단 | 비고 |
|---|---|---|---|---|
| **TE-1** | ★ **주 계층 하나 + 보조 계층(의무).** 케이스를 쓸 때 보조 계층 케이스를 **빈칸으로 두지 않는다** — 주 하나만 적으면 *"그 하나만 짜면 된다"* 로 읽힌다 | E2 §1.0·§2 (양식 R4) | **리뷰** | ⚠️ 자동화 불가 — 배정은 판단이다. 승격 조건 = 케이스 대장(E2 §8 **TS-3**)이 만들어져 **배정 ↔ 케이스 집합 diff**가 가능해질 때 |
| **TE-2** | ★ **하한을 지킨다.** 트랜잭션 경계(E1~E5)의 **원자성·다중 레코드 동시 이동·순서**는 **통합 이상**이다 — **인메모리 등식으로 흉내 낸 것은 그 규칙을 검증한 것이 아니다.** 물리 규격·직렬화 표면은 **계약**, 측정 수치는 **인수** | E2 §1.0 하한 **L1~L3** | **리뷰** | ⚠️ 자동화 불가 — *"이 케이스가 실제로 경계를 태우나"* 는 사람만 본다. 승격 조건 = TE-1과 동일 |
| **TE-3** | ★ **red-first는 높음 stakes 한정**(자금 경로 E1~E5 · 권한 **BR-54**~**BR-61** · 마이그레이션 · public 계약)이며 **성립 조건 R-a~R-d를 만족해야 근거가 된다** — ⓐ 변경 전 코드에서 **실행 가능** ⓑ red의 원인이 **규칙 위반**(컴파일 실패·NPE·미구현 예외는 red가 아니다) ⓒ ★ **감도 증명**: green이 된 뒤 규칙을 일시 반전시키면 **다시 red** ⓓ 처음부터 green이면 ⓒ로 분기 | E2 §3.3 **R-a~R-d** | **리뷰**(blind 워커 회수 packet에 **감도 증명 결과** 포함) | ⚠️ 자동화 불가 — 감도 증명은 **의도적 반전 실행**이라 CI가 대신 못 한다. ⚠️ **중간·낮음으로 확대하지 않는다**(확대하면 테스트가 아니라 의식이 된다) |
| **TE-4** | ★ **소유 3축을 지킨다** — **설계 주체 / 검증 책임자 / 차단 주체.** 통합 계층은 ★ **spec에서 출발하고 구현 diff에서 출발하지 않는다**(분리 패스) · 계약은 **UC §7 소유자**가 설계 · **높음 stakes는 계층 무관하게 blind 워커**(구현 diff 미열람) · **탐색 판정은 독립 판정자** | E2 §1.2 · §3.3 | **리뷰** | ⚠️ 자동화 불가 — 누가 무엇을 읽었는지는 packet으로만 확인된다. 승격 조건 = **워커 packet의 기계 검사**가 가능해질 때 |
| **TE-5** | ★ **데이터 특칙 — "에러 없이 돌았다" ≠ 완료.** 배치·릴레이·대사는 **record-level 검증**(count · sample · orphan)으로 판정한다(**BR-23** *"총 반영 건수 = 파일 건수"* · **BR-41** *"anchor + Σ = 보조부 잔액"* · **BR-52** *"처리 + 격리 = 전체"*). ★ **부분실패 DONE 위장·무음 스킵·조용한 절단을 명시적으로 반증**한다 | E2 §3.2 | **테스트 게이트** + **리뷰** | ★ **그린 위장 점검 동반**: fail-open 골든(적재 0건 = 통과로 읽히면 안 된다 — **BR-09**·**BR-41** 취약) · exit 마스킹(`\| tail` 류 종료 코드 삼킴) |
| **TE-6** | ★ **픽스처 골격**(E2 §8 **TS-4**가 E3로 넘긴 판정) — ⑴ **영업일 달력·현재 시각**은 **KO-5의 주입 지점으로 고정**한다(별도 픽스처 장치를 만들지 않는다) ⑵ **모의 매입사·모의 입금원은 어댑터 경계에서 대체**한다(도메인에 테스트 분기를 넣지 않는다) ⑶ ★ **구체 픽스처 세트·데이터 빌더 형식은 `[구현 검증]`** | E2 §8 TS-4 · ADR-002 M-1·M-2 | **리뷰** | 골격만 정한다 — 실제 데이터 세트를 지금 정하면 **케이스 대장을 문서로 이중화**한다(TS-3과 같은 이유) |

---

## 5. PR·리뷰 절차

| # | 규칙 | 근거(정본) | 강제 수단 | 비고 |
|---|---|---|---|---|
| **PR-1** | ★ **브랜치 우선 — `main` 직접 작업 금지.** 작업 단위로 브랜치를 따고, 브랜치 base·이름은 **작업 시작 전에 확인**한다 | 기존 정책(작업 규칙 — 외부 발행·브랜치) | **리뷰** ⚠️ *잠정* | ★ **승격 조건 = 저장소 브랜치 보호 규칙 설정**(E4 소유 · 설정값 `[구현 검증]`). 설정되면 **자동 강제**로 올라간다 |
| **PR-2** | ★ **커밋은 단계 단위·의미 단위.** 여러 단계 작업은 **단계마다 커밋**하고 몰아서 하지 않는다. ★ **문서 작업과 구현 작업을 한 커밋에 섞지 않는다** | 기존 정책(스코프 — 문서·구현 분리) | **리뷰** | ⚠️ 자동화 불가 — *"의미 단위"* 는 판정 규칙이 없다. 다만 **경로 기준 혼합**(docs + 구현 동시 변경)은 훅으로 관측 가능 — 승격 조건 = 혼합 커밋이 2회 관측될 때 |
| **PR-3** | ★ **머지 게이트 = E2 §3.0 인용**(재정의 금지) — ⑴ **단위 · 통합 · 계약 전부 green** ⑵ **머지 전 최소 안전선 ①~⑥**(스모크 · 기존 테스트+불변식 최소 1개 · diff self-review · rollback/forward-fix 판단 · **public contract 영향** · 반증 질문 1회) ⑶ **관련 탐색 판정이 유효**. ★ **인수·탐색은 머지를 막지 않는다**(마일스톤 게이트) | E2 **§3.0**·§3.1 | **테스트 게이트**(⑴) + **리뷰**(⑵③) | ⚠️ ★ **안전선 ①(스모크)은 계층이 대체하지 못한다** — *"테스트가 다 녹색이어도 경로를 한 번도 안 태웠으면 스모크는 안 한 것"*. ⑤(public contract)도 **사람이 지목**해야 계약 테스트가 확인한다 |
| **PR-4** | ★ **리뷰 관점 = 구조·불변식 우선**(v2 렌즈). 보는 것: **경계 침범 · 불변식 위반 · 판정 충돌 · 권한/자금 경로 · 이중 적재**. ★ **안 보는 것: 표기·포맷·색인·문구 동기화** — 그 유형은 **린트·자동 검사·구현이 공짜로 잡는다** | 검증 정책 v2 **V2-1·V2-2**(실측: 구조 결함 ~25% · 동기화·표기 ~60%) | **리뷰** | 정의상 리뷰다(자동화 대상 아님). ★ **표기 지적을 리뷰에 넣지 않는 것 자체가 규칙**이다 — 넣으면 리뷰가 60% 유형으로 채워져 구조 결함을 놓친다 |
| **PR-5** | ★ **AI attribution 금지.** 커밋 메시지와 **모든 발행 본문**(PR·이슈·코멘트·릴리즈)에 `Co-Authored-By: Claude`·*"Generated with …"* 류를 **적지 않는다**. ★ **검증 과정의 출처(리뷰어·교차 검증 도구)도 커밋·주석에 기재하지 않는다** — 그 기록은 작업 로그가 소유한다 | 기존 정책(git·발행 규칙) | **린트·자동 검사**(커밋·발행 훅) + **리뷰** | ⚠️ 훅이 못 보는 표면이 있다(파일 경유 본문 · 일부 발행 명령 · 워커 내부 호출) — 그 구간은 **절차 준수**다 |
| **PR-6** | ★ **push·외부 발행은 사용자 확인 후.** 원격 브랜치 생성 · PR/이슈 생성 · 머지는 **각각 확인**한다. ★ **`--force`·강제 되돌리기·파괴적 삭제는 명시 요청 시만** | 기존 정책(git·외부 발행) | **린트·자동 검사**(push 확인 훅) + **리뷰** | 파괴적 조작 **직전에 현재 브랜치·HEAD·경로·대상을 재확인**한다(실측: 상태 오인이 최고 강도 사고 유형) |

---

## 6. ★ 테스트 도구 확정 (C-08 — 요구 → 계약 → 도구)

> ★ **E2가 소유한 것은 「성질」까지이고**(test-strategy §4.1 · §5 *"테스트 도구 스택 = [E3 소유]"*), **그 성질을 만족하는 도구의 지목·등재가 이 절이다.** 판정 기준은 **인용**하며 다시 쓰지 않는다.

### 6.1 판정 — E2 §4.1 「성질 충족 판정 기준」 대조

| 계층 | E2가 적은 충족 판정 기준 (인용) | 후보 | 판정 | 남은 것 |
|---|---|---|---|---|
| **① 단위** | 케이스 표(BR-05 한도−1/한도/한도+1 류)를 **데이터로 넘길 수 있나** | **JUnit5** | ✅ **충족** — 파라미터화로 케이스 표를 **데이터로** 넘긴다 · 예외 단언 제공 · C-05(Kotlin/JVM) 위에서 인메모리 실행 | — |
| **② 통합** | ⑴ **실 MySQL이 뜨나** ⑵ 테스트 간 **상태가 새지 않나** ⑶ 기동 비용이 **매 빌드 예산 안인가**(E2 §5 — **아직 미측정**) | **Testcontainers (MySQL 컨테이너)** | ⑴ ✅ **충족** — 실 MySQL 이미지를 띄운다(인메모리 DB로는 격리 수준·갭 락·데드락·`insert intention waiting`·`History list length`가 재현되지 않는다 — DB-X1~X8이 근거) ⑵ ⚠️ **조건부 충족** — 컨테이너 생명주기로 격리가 **가능하나**, 재사용 전략에 따라 상태가 샌다 ⑶ ★ **판정 보류 — 측정 전** | ⑵ **격리 단위**(컨테이너 / 스키마 / 트랜잭션 롤백)의 선택 = `[구현 검증]` ⑶ **기동 비용 첫 측정** → E2 §5 *"테스트 실행 시간 예산"* 행을 채운다 |
| **③ 계약** | 규격 표를 **테스트 데이터로 그대로** 쓸 수 있나 (*"표를 손으로 옮겨 적으면 표면 정본이 둘이 된다"*) | **JUnit5**(파라미터화 — 표 원천 로딩) | ⚠️ ★ **부분 충족** — 실행기는 표를 데이터로 받을 수 있으나, ★ **`interfaces/**` 규격 표의 기계 판독 원천이 없다.** 원천이 없으면 케이스가 표를 **손으로 미러**하고 그 순간 표면 정본이 둘이 된다 | ★ **규격 표의 기계 판독 원천** — ER-2(오류 모델 생성)와 **같은 형태의 문제**다. §8 **DV-1**로 등재 |
| **④ 인수** | E2가 **[미확정]** 으로 남김(§4.2 부하 도구 행) | — | ★ **지목하지 않는다** | 부하 도구 도입 조건 = **P-X2 또는 D-X13 착수 시점**(§6.3 인용) |
| ★ **탐색 태그** | 실행 계층(②·③·④)의 판정 기준을 그대로 따른다 — **탐색이 별도 도구 스택을 갖지 않는다** | 위와 동일 | ⚠️ **부분** — 컨테이너 제어로 **DB 측 주입**은 가능하나, ★ **애플리케이션 프로세스 강제 종료**(P-X3 20지점 `kill -9` 스윕 · P-X6·D-X8 `SIGSTOP`)는 **테스트 실행기 밖**(실행 스크립트·프로세스 제어)이다 | 프로세스 제어 수단 = `[구현 검증]`. ★ **새 도구를 지금 넣지 않는다** |

### 6.2 확정 기록 (C-08 형식)

| # | 도구 | 요구 (무엇이 이것을 부르나) | 계약 (무엇을 보장해야 하나) | 도입 조건 | ★ 되돌리는 조건 | 강제 수단 |
|---|---|---|---|---|---|---|
| **TL-1** | ★ **JUnit5** — 단위·통합·계약 계층의 **실행기** | ① 단위의 파라미터화·예외 단언 · ② ③의 케이스 실행 자체 | 케이스 표를 **데이터로** 받는다 · 실패가 **BR 문장으로 읽히는** 단언 메시지를 낼 수 있다(E2 R-b) | ★ **이미 충족** — C-05(Kotlin/JVM)에서 실행기 없이는 **어느 계층도 돌지 않는다**. 머지 게이트(E2 §3.0 ⑴)가 이것을 전제한다 | **R1** 규격 표·케이스 표를 **데이터로 못 받는** 사례가 2회 누적 **R2** Kotlin 전용 실행기가 아니면 표현할 수 없는 케이스가 실증될 때 — 그때도 **교체이지 추가가 아니다** | **테스트 게이트** |
| **TL-2** | ★ **Testcontainers (MySQL 컨테이너)** — ② 통합 계층의 **실 MySQL 격리** | E2 §1.1 ②(*"★ 실 MySQL(격리된 인스턴스)"*) · 하한 **L1**(경계 원자성·순서를 인메모리로 흉내 낸 것은 검증이 아니다) · **DB-X1~X8**(격리 수준·갭 락·데드락·롱 트랜잭션) | ⑴ **실 MySQL**이 뜬다 ⑵ 테스트 간 **상태가 새지 않는다** ⑶ **스키마 재현** ⑷ 스케줄러·릴레이가 **실동작**한다 | ★ **이미 충족** — C-07(MySQL 불변)이고, ②가 **머지 게이트 대상**이라 매 빌드에 실 MySQL이 필요하다 | **R1** ★ **기동 비용이 매 빌드 예산을 넘는 것이 첫 측정에서 드러날 때** → 공유 인스턴스 + **스키마 단위 격리**로 후퇴(도구 제거가 아니라 격리 단위 하향) **R2** 컨테이너 격리에도 **상태가 샌 사건이 2회** 누적 **R3** CI 실행 환경에서 컨테이너 런타임을 쓸 수 없을 때 → 그때는 **격리된 실 MySQL을 다른 수단으로** 제공한다(요구는 도구보다 오래 산다) | **테스트 게이트** |
| **TL-3** | ★ **도구를 더 넣으려면 도입 조건의 실증을 첨부한다**(★ 도구가 아니라 **절차 규칙**) | C-08(*"나중에 필요하면"* 은 판정 불가) | 후보의 도입 조건(§6.3)이 **관찰됐다는 근거**를 PR에 첨부 | ★ 해당 없음 — 절차 규칙 | ★ 해당 없음 — C-08이 폐기될 때만 | **리뷰** |

> ⚠️ ★ **이 절이 확정하지 않은 것**: **④ 인수 도구** · **포매터·린트 도구와 설정값**(KO-1·KO-2) · **아키텍처 테스트 도구**(MO-4~MO-6·KO-5·ER-4 — §6.3 인용) · **프로세스 제어 수단**(탐색 태그). ★ **빌드 도구는 ADR-025 CL-1이 이미 Gradle 멀티모듈로 적고 있다** — 인용이며 이 절의 확정이 아니다.

### 6.3 도입하지 않는 도구 — E2 §4.2 도입 조건 인용 (★ 보류 유지 · 재판정하지 않는다)

| 후보 | v1에서 안 넣는 이유 (E2 인용) | 도입 조건 (E2 인용) | E3 시점의 상태 |
|---|---|---|---|
| **부하 도구**(k6·Gatling·JMeter 류) | 인수·탐색 착수 전까지 **부하를 걸 대상이 없다** | QS-01(100 TPS)을 실제로 걸어야 하는 시점 = **P-X2 또는 D-X13 착수**. 그때 **하나만** 고르고 근거를 남긴다 | 보류 |
| ★ **아키텍처 테스트**(ArchUnit 류) | 모듈이 아직 없다 | ★ **공통 라이브러리 첫 커밋**(ADR-025 §6이 이미 요구 — ADR-002 모듈 규칙 강제도 같은 도구) | ★ **보류 — 단 조건이 가장 가깝다.** 이 규약의 **잠정 리뷰 8건 중 5건**(KO-5·MO-4·MO-5·MO-6 ⑴~⑶·ER-4)이 이 도구를 기다린다. ⚠️ **조건 충족 판단은 별도**이며(D6) 이 문서가 앞당기지 않는다 |
| **커버리지 측정**(JaCoCo·Kover) | **목표 수치를 정하지 않기로 했다** — 게이트 없는 측정은 대시보드일 뿐이다 | *"어느 BR의 케이스가 코드에 없다"* 를 **사람이 두 번 이상 놓친 것이 관측될 때**. 그때도 게이트는 **BR 배정 커버리지**이지 라인 커버리지가 아니다 | 보류 |
| **속성 기반 테스트** | 산식에 잘 맞으나 케이스가 **이미 정본에 열거**돼 있다 | 열거된 케이스가 **통과했는데 실환경에서 그 산식이 깨지는 것**이 1회 관측될 때 | 보류 |
| **변이 테스트**(코드 대상) | 문서 층에서 `tools/mutation_test.py`가 이미 같은 역할을 한다 | **코드 테스트가 "다 녹색인데 결함이 나온" 사례가 2회** 누적될 때 | 보류 |
| **계약 테스트 프레임워크**(Pact 류) | 상대가 **모의 외부**다 — 소비자 주도 계약의 상대편이 없다 | 실제 외부 기관과 규격을 주고받게 될 때 | 보류 |
| **메시지 브로커 기반 테스트 도구** | **브로커 자체를 안 넣는다**(ADR-021 ET-1) | ET-9의 브로커 도입 조건 3종 중 하나가 실증될 때 | 보류 |

---

## 7. 강제 수단 분포 — self-check

### 7.1 분포 (★ 주 강제 수단 1개씩으로 센다 — 병기된 보조 수단은 세지 않는다)

| 강제 수단 | 규칙 수 | 규칙 ID |
|---|---|---|
| **컴파일** | **6** | KO-3 · KO-4 · MO-1 · MO-2 · MO-3 · ER-2 |
| **테스트** | **10** | *아키텍처 테스트 **5***: KO-5 · MO-4 · MO-5 · MO-6 · ER-4 — *테스트 게이트 **5***: ER-1 · ER-3 · PR-3 · TL-1 · TL-2 |
| **린트·자동 검사** | **4** | KO-1 · KO-2 · PR-5 · PR-6 |
| **리뷰** | **12** | MO-7 · ER-5 · TE-1 · TE-2 · TE-3 · TE-4 · TE-5 · TE-6 · PR-1 · PR-2 · PR-4 · TL-3 |
| **계** | ★ **32** | ★ 강제 수단 빈칸 **0건** |

| 검산 | 값 |
|---|---|
| 절별 규칙 수 | §1 KO **5** + §2 MO **7** + §3 ER **5** + §4 TE **6** + §5 PR **6** + §6 TL **3** = ★ **32** |
| 강제 수단 합 | 컴파일 **6** + 테스트 **10** + 린트 **4** + 리뷰 **12** = ★ **32** ✅ 일치 |

> ⚠️ **TE-5는 테스트 게이트와 리뷰를 병기**하지만 주 강제 수단은 **리뷰**다 — record-level 검증을 케이스가 돌려도 **그린 위장**(fail-open 골든·exit 마스킹)은 *"통과하는 테스트"* 라 게이트가 못 잡는다. ★ 병기를 주로 올려 세면 **리뷰 몫이 실제보다 작아 보인다.**

### 7.2 ★ 리뷰로만 강제되는 규칙 — 전량과 승격 조건 (양식 D2)

| # | 왜 자동화할 수 없나 | ★ 승격 조건 (관찰되면 자동화로 올린다) |
|---|---|---|
| **MO-7** | *"무엇이 중복인가"* 는 도메인 지식이라 구조로 판정되지 않는다 | ADR-025 **R3**(공통이 정한 멱등 키로 수신측이 중복을 놓침)가 관측될 때 — 그때는 규칙이 아니라 **설계를 되돌린다** |
| **ER-5** | 값이 `[미확정]`이라 대조할 대상이 없다 | **대외 협의로 4N 값이 확정**될 때 → ER-4의 계약 케이스를 값 단언으로 강화 |
| **TE-1** | 계층 배정은 판단이다 | **케이스 대장**(E2 §8 TS-3)이 생겨 **배정 ↔ 케이스 집합 diff**가 가능해질 때 |
| **TE-2** | *"이 케이스가 실제로 경계를 태우나"* 는 실행 형태를 사람이 읽어야 안다 | TE-1과 동일 |
| **TE-3** | 감도 증명(R-c)은 **의도적 규칙 반전 실행**이라 CI가 대신 못 한다 | — (구조적. 다만 반전 스크립트가 재현 가능해지면 **부분 자동화** 후보) |
| **TE-4** | 누가 무엇을 읽었는지는 워커 packet으로만 확인된다 | **packet의 기계 검사**(실제 읽은 파일 · 실행 명령·결과 필드)가 가능해질 때 |
| **TE-5** | 그린 위장(fail-open 골든·exit 마스킹)은 **통과하는 테스트**라 게이트가 못 잡는다 | 위장 유형이 **패턴으로 반복 관측**될 때 → 검사 스크립트로 승격(`tools/` 선례) |
| **TE-6** | 픽스처 골격은 설계 판단이다 | 구체 세트가 `[구현 검증]`에서 확정되면 그 부분은 코드가 강제 |
| **PR-1** | 현재 저장소 설정이 없다 | ★ **브랜치 보호 규칙 설정**(E4) → **자동 강제로 승격** |
| **PR-2** | *"의미 단위"* 는 판정 규칙이 없다 | **docs + 구현 혼합 커밋**이 2회 관측되면 그 부분만 훅으로 승격(경로 기준은 관측 가능) |
| **PR-4** | 정의상 리뷰다 | — (승격 대상 아님. ★ 다만 **표기·색인 지적을 리뷰에서 빼는 것**이 이 규칙의 실행이며, 그 부분은 이미 검산 도구가 소유) |
| **TL-3** | 도입 조건의 실증 판정은 판단이다 | — (승격 대상 아님) |

### 7.3 ★ 잠정 리뷰 — 도구가 확정되면 자동으로 올라가는 규칙 (양식 D10)

| # | 지금 실제로 무엇이 막나 | 무엇이 확정되면 올라가나 |
|---|---|---|
| **KO-1** | 리뷰 | 포매터·린트 도구 + 설정값 (`[구현 검증]`) |
| **KO-2** | 리뷰 (반환 타입만 컴파일) | 린트 도구 |
| **KO-5** | 리뷰 | ★ **아키텍처 테스트 도구** (§6.3 — 조건 = 공통 라이브러리 첫 커밋) |
| **MO-4** | 리뷰 | ★ 아키텍처 테스트 도구 |
| **MO-5** | 리뷰 | ★ 아키텍처 테스트 도구 |
| **MO-6 ⑴~⑶** | 리뷰 (⑷ 의존 방향만 컴파일) | ★ 아키텍처 테스트 도구 |
| **ER-4** | 리뷰 (계약 케이스는 유일성만 단언 가능) | ★ 아키텍처 테스트 도구 |
| **PR-1** | 리뷰 | 브랜치 보호 규칙 설정(E4) |

> ★ **잠정 리뷰 8건 중 5건이 같은 도구 하나를 기다린다**(아키텍처 테스트). 그 도구의 도입 조건은 **공통 라이브러리 첫 커밋**이므로 Phase 5 착수와 거의 동시에 열린다 — ⚠️ **조건 충족 판단은 별도 작업**이고(양식 D6) 이 문서가 앞당기지 않는다.

---

## 8. 열린 의문

| # | 의문 | 처리 |
|---|---|---|
| **DV-1** | ★ **`interfaces/**` 규격 표(오프셋·길이·타입)의 기계 판독 원천이 없다.** 계약 계층의 충족 판정 기준이 *"표를 테스트 데이터로 그대로 쓸 수 있나"* 인데, 원천이 없으면 케이스가 표를 **손으로 미러**하고 **표면 정본이 둘**이 된다(§6.1 ③) | ER-2(오류 모델 생성)와 **같은 형태의 문제**다. 원천 형식·생성 절차 = **E4(구성 관리) 또는 구현 착수 시** 판단. ⚠️ 지금 만들면 **규격 정본을 규약이 재구성**하는 것이라 금지 영역이다 |
| **DV-2** | **탐색 태그의 프로세스 제어 수단**(P-X3 `kill -9` 20지점 스윕 · P-X6·D-X8 `SIGSTOP`)이 테스트 실행기 밖에 있다 | `[구현 검증]`. ★ **새 도구를 넣지 않는다** — 실행 스크립트로 되는지를 먼저 본다(C-08) |
| **DV-3** | **통합 계층 격리 단위**(컨테이너 / 스키마 / 트랜잭션 롤백)와 **기동 비용** | `[구현 검증]` — 첫 측정이 E2 §5 *"테스트 실행 시간 예산"* 행을 채우고, 그 값이 TL-2의 **되돌리는 조건 R1** 판정 입력이 된다 |
| **DV-4** | **포매터·린트 도구와 설정값**(KO-1·KO-2) | `[구현 검증]`. ⚠️ 확정 전까지 스타일이 **리뷰 몫**으로 남아 PR-4(구조 우선 렌즈)와 충돌한다 — 조기 확정 대상 |
| **DV-5** | 이 문서를 `README.md` 문서 지도·`HANDOFF.md`에 등재해야 하나 | ★ **정본 수정이므로 이 작업에서 하지 않는다.** 등재 여부 = 사용자 판단 |

---

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v1.0 | 2026-08-06 | 신설 — E3 산출물 ①(내용 정본). **규칙 32건**(KO 5 · MO 7 · ER 5 · TE 6 · PR 6 · TL 3)에 ★ **강제 수단 칸 전수 부여**(빈칸 0 — 컴파일 6 · 테스트 10 · 린트 4 · 리뷰 12) · **§1 Kotlin**(공식 스타일 채택 + 고유 4종: 불변 우선 · null 안전 · ★ **값 타입 의무(`Money`·`BusinessDate` — 원시 타입 금지, BR-07 · ADR-025 CL-2)** · 시간 명시 주입) · **§2 모듈**(ADR-002 M-1~M-7 · ADR-025 CL-1·CL-3~CL-8 인용 — 컴파일이 막는 것과 아키텍처 테스트가 막는 것을 분리) · **§3 오류**(코드 대장 **유일 창구** · **생성된 오류 모델만**(수기 문자열 금지) · 판정 순서 K-1/X-10 · **채널 변환은 어댑터** · 4N 값 미확정) · **§4 테스트 작성**(E2 인용 — 주+보조 의무 · 하한 L1~L3 · **red-first R-a~R-d는 높음 stakes 한정** · 소유 3축 · 데이터 특칙 · **픽스처 골격 = TS-4 판정 착지**) · **§5 PR·리뷰**(브랜치·커밋 단위 · **머지 게이트는 E2 §3.0 인용**(재정의 금지) · **구조·불변식 우선 렌즈** · AI attribution 금지 · push·발행 확인) · ★ **§6 도구 확정** — E2 §4.1 판정 기준 대조로 **JUnit5(TL-1)·Testcontainers MySQL(TL-2) 확정**(C-08 형식: 요구·계약·도입 조건·되돌리는 조건), ★ **계약 계층은 부분 충족**(규격 표의 기계 판독 원천 부재 → DV-1), ④ 인수·탐색 프로세스 제어는 미지목, 후보 7종은 **E2 도입 조건 인용으로 보류 유지** · **§7 분포 집계 + 리뷰 전용 12건의 승격 조건 + 잠정 리뷰 8건**(그중 5건이 아키텍처 테스트 도구 하나를 기다린다) · 수치·설정값 확정 **0건**(전부 `[구현 검증]`) |
