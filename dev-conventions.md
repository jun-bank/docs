# 개발 규약 — 규칙마다 무엇이 강제하나 (내용 정본)

- 작성일: 2026-08-06
- 상태: 검토대기
- 양식: `study/project-workflow/phase5/03-dev-conventions.md`
- 절차상 위치: **E3**(`study/project-workflow/phase5/01-entry-procedure.md` §1 — E2 테스트 전략 다음, E4 구성 관리·CI/CD 앞)
- 입력: `architecture/adr/ADR-002`(모듈) · `ADR-025`(공통 라이브러리) · `architecture/test-strategy.md`(E2) · `architecture/constraints.md` **C-05~C-08** · `usecases/README.md` §5(K-1~4 · 코드 대장) · `study/project-workflow/verification-policy-v2.md`

> ★ **이 문서는 규칙을 새로 만들지 않는다.** 모듈 경계는 ADR-002·025, 업무 규칙은 `product/01-business-rules.md`, 계층·게이트는 `architecture/test-strategy.md`가 정본이다. 이 문서가 새로 적는 것은 **⑴ 그 규칙이 코드에서 어떤 모양인가 ⑵ 무엇이 그것을 막나(「강제 수단」 칸)** 둘뿐이다.
> ⚠️ **정본이 갈리면 `product/01-business-rules.md`가 이긴다**(HANDOFF §7). 이 문서의 인용이 정본과 다르면 이 문서가 틀린 것이다.

## 강제 수단 표기 (전 절 공통)

| 표기 | 무엇이 막나 | 언제 알게 되나 |
|---|---|---|
| **컴파일** | 빌드 모듈 그래프 · 타입 시스템 | 빌드 즉시 — 코드가 존재할 수 없다 |
| **아키텍처 테스트** | 컴파일이 못 보는 구조 위반(리플렉션·모듈 내 레이어·타입 스밈) | 매 빌드 — ⚠️ **도구 미확정**(§6) |
| **테스트 게이트** | 단위·통합·계약 케이스 (머지 게이트 = E2 §3.0) | 매 빌드·머지 |
| **린트·자동 검사** | 포매터·정적 규칙·커밋 훅·생성물 drift 검사 | 커밋·빌드 — ⚠️ 일부 **도구 미확정**(§6) |
| **리뷰** | 사람 | PR |
| ★ **잠정 리뷰** | ★ **원래 위 중 하나가 막아야 하는데 도구가 아직 없어 지금은 사람이 보는 것.** 무엇이 확정되면 올라가는지를 §7이 전량 나열한다 | PR (한시적) |

> ★ **강제 사다리**: **컴파일 > 아키텍처 테스트 > 테스트 게이트 > 린트·자동 검사 > 리뷰** (ADR-002 §5의 강제력 표와 같은 순서). 규칙을 낮은 칸에 둔 행은 **왜 위로 못 올렸는지**를 비고에 적는다.
> ⚠️ ★ **아키텍처 테스트와 테스트 게이트는 같은 칸이 아니다** — 아키텍처 테스트는 **구조 위반 자체를 전수로 거부**하지만(규칙 하나 = 단언 하나), 테스트 게이트는 **케이스가 있는 만큼만** 막는다 — ★ **케이스가 빠진 것을 게이트는 스스로 알지 못한다.** 그래서 사다리에서 한 칸 아래다. §7.1은 둘을 나눠 센다.
> ⚠️ ★ **잠정 리뷰는 사다리에서 「리뷰」다.** 원래 있어야 할 칸을 적는 것은 **목표**이고, 지금 막는 것은 사람이다 — 이 문서는 두 값을 §7.1에서 **2열로** 나란히 센다(D10).

---

## 1. Kotlin 컨벤션 (C-05)

> **공식 Kotlin coding conventions를 기본으로 채택한다.** 이 절은 **프로젝트 고유 규칙만** 적는다 — 공식 스타일을 옮겨 적으면 갱신되지 않는 사본이 하나 더 생긴다.

| # | 규칙 | 근거(정본) | 강제 수단 | 비고 |
|---|---|---|---|---|
| **KO-1** | **공식 스타일을 그대로 쓴다.** 명명·들여쓰기·임포트 정렬·줄바꿈은 포매터가 정하고 **사람이 논쟁하지 않는다.** ★ 규약은 스타일 항목을 개별 열거하지 않는다 | C-05 · 검증 정책 v2 §1(표기 유형은 리뷰 렌즈 밖) | **린트** ⚠️ *현재 **잠정 리뷰*** | 포매터·린트 **도구와 설정값 = [구현 검증]**. 확정 전까지 스타일 지적은 리뷰 몫이고, 그것이 이 규칙을 조기 자동화해야 하는 이유다 |
| **KO-2** | ★ **불변 우선.** `val` 기본 · 상태 변경은 새 값 반환(`copy`) · ★ **가변 컬렉션을 경계 밖으로 노출하지 않는다** — 반환 타입은 읽기 전용 컬렉션이고, ★ **그 뒤의 실제 객체도 밖에서 바뀌지 않아야 한다**(방어 복사 또는 영속·불변 컬렉션) | 애그리게이트 불변식(INV-*)이 *"객체 안에서만 성립"* 하려면 밖에서 고쳐질 수 없어야 한다 | **린트·자동 검사**(`var` 남용 · 가변 컬렉션 반환) ⚠️ *현재 **잠정 리뷰*** + **테스트 게이트**(별칭 변경 감지) | ⚠️ ★ **`List` 반환 타입은 불변을 보장하지 않는다** — 읽기 전용 인터페이스 뒤의 객체가 **외부 별칭으로 변경**될 수 있다(애그리게이트가 자기 `MutableList`를 그대로 `List`로 내주면 그 별칭이 살아 있다). 컴파일이 막는 것은 *"수신자가 `add`를 부르는 것"* 까지이고, ★ **별칭 경로는 방어 복사·영속 컬렉션 + 변경 감지 테스트**(밖에서 고쳐도 애그리게이트가 안 바뀐다)가 막는다 |
| **KO-3** | ★ **null 안전.** `!!` 금지 · 플랫폼 타입(전문·파일 파싱·JDBC 경계)은 **경계에서 즉시 검증**해 non-null 도메인 타입으로 바꾼다 — 도메인·application에 nullable이 흘러들지 않는다 | 시스템 채널 판정 순서 K-1(X-10 ① 인자 형식) — 형식 오류는 **경계에서** `ARG_MISMATCH`다 | **린트·자동 검사**(정적 검사 — `!!` 0건 · domain/application의 nullable 선언·플랫폼 타입 유입 금지) ⚠️ *현재 **잠정 리뷰*** + **테스트 게이트**(경계 검증 케이스) | ⚠️ ★ **컴파일은 KO-3을 막지 못한다** — Kotlin은 domain·application 내부의 **nullable 선언·`!!`·플랫폼 타입 전달을 전부 컴파일한다.** non-null 시그니처는 **그 시그니처를 지나는 경계 하나**만 막는다. 그래서 주 강제 수단은 **금지 API(`!!`)·nullable 위치 정적 검사**이고, *"경계에서 실제로 검증했나"* 는 **경계 테스트**가 본다. 컴파일은 보조다 |
| **KO-4** | ★ **값 타입 의무 — 원시 타입 금지.** 금액은 **`Money`**, 영업일은 **`BusinessDate`**. `Long`·`BigDecimal`·`Double`·`LocalDate`를 금액·영업일 자리에 쓰지 않는다 | **BR-07**(통화 + 최소단위 정수 · 부동소수 금지) · ADR-025 **CL-2 ⑴** · ADR-002 트리 `shared/` · `glossary.md`(영업일) | **린트·자동 검사**(BR-07 정적 스캔) ⚠️ *현재 **잠정 리뷰*** + **컴파일**(보조) | ⚠️ ★ **의미 기반 원시 타입 금지는 컴파일러가 판별하지 못한다** — 새 금액 필드를 `Long`·`BigDecimal`로 선언해도 **그대로 컴파일된다.** E2 **§2.1**이 BR-07의 검증 수단을 *"★ **정적 분석** — 금액에 float/double 0건 · 통화 필드 존재"*(주 계층 **U** — 정적 규칙, 하한 예외)로 **이미 규정**했다. 컴파일이 막는 것은 ★ **이미 `Money`로 선언된 API에 다른 타입을 넣는 것**뿐이다(기선언 API 보조) |
| **KO-5** | ★ **시간은 명시 주입.** 도메인·application에서 시스템 시계를 **직접 호출하지 않는다**(현재 시각·오늘 날짜를 구하는 호출) — 시각 공급자를 **인자 또는 의존으로 받는다** | **BR-06**(마감과 정산 시각) · **BR-14**·**BR-25**(영업일 귀속)의 검증 방법 칸이 *"마감 직전·직후"* · *"금요일 마감 후 → 월요일 귀속"* 을 요구한다 — **시각을 고정할 수 없으면 그 케이스는 재현되지 않는다** | **린트·자동 검사**(금지 심볼 스캔 — domain·application에서 시스템 시계 호출 **0건**) ⚠️ *현재 **잠정 리뷰*** | ★ **아키텍처 테스트를 기다리지 않는다** — *"금지 심볼이 특정 패키지에 0건"* 은 **린트 규칙으로 충분**하고, 그래서 이 행의 승격은 **DV-4(포매터·린트 도구) 조기 확정**에 걸려 있지 아키텍처 테스트 도구에 걸려 있지 않다. 시각 공급자의 **타입·주입 방식은 [구현 검증]** — 규약이 정하는 것은 *"직접 부르지 않는다"* 까지다 |
| **KO-6** | ★ **값 타입의 import 경로도 유일 창구다.** `Money`·영업일의 **정본은 `common-kernel`** 이고, 업무 모듈은 ★ **`core:shared`가 재수출한 경로로만** import한다 — 업무 모듈이 `common-kernel`을 **직접 import하지 않고**, 같은 이름의 값 타입을 **자기 모듈에 다시 정의하지 않는다** | ADR-025 **CL-2 ⑴** — *"★ **`Money`·영업일의 정본은 `common-kernel`** 이고 **`core:shared`가 그것을 재수출**한다 … **두 벌로 정의하지 않는다**"* · **R5**(*"`Money`·오류 코드가 **공통과 코어에 각각 정의**돼 있다"* = CL-2 위반) | **아키텍처 테스트**(import 경로 단언 + 같은 값 타입 중복 정의 **0건**) ⚠️ *현재 **잠정 리뷰*** | ★ **오류 코드에 ER-1(코드 대장 유일 창구)이 있듯 값 타입에는 이 행이 있다.** *"어느 경로가 정본인가"* 를 적지 않으면 두 import 경로가 공존하다 **한쪽만 고쳐지는 날**이 온다(ADR-025 §3-E). ADR-025 §6 통과 기준 *"공통이 노출하는 `Money`가 **`core:shared`의 그 타입**이다(별도 정의가 없다)"* 의 코드 형태다 |

---

## 2. 모듈 규칙 (ADR-002 · ADR-025)

> ★ **모듈 트리·의존 방향의 정본은 ADR-002 §3·§5이고, 공통 라이브러리 경계의 정본은 ADR-025 §2다.** 이 절은 그 규칙을 **다시 적지 않고** 무엇이 막는지만 적는다.

| # | 규칙 | 근거(정본) | 강제 수단 | 비고 |
|---|---|---|---|---|
| **MO-1** | **모듈 트리와 빌드 그래프는 ADR-002 §5가 정본**이다 — `core:banking`·`card`·`payment`·`auth`·`reconciliation`·`ops`·`shared` + 위성(`settlement`·`ledger`). ★ **규약은 그래프를 다시 그리지 않는다** | ADR-002 §3·§5 | **아키텍처 테스트**(선언된 의존 그래프 ↔ ADR-002 §5 트리 단언) ⚠️ *현재 **잠정 리뷰**(빌드 스크립트 diff)* + **컴파일**(보조) | 빌드 모듈 분리 자체가 그래프이지만 ⚠️ ★ **빌드 도구가 거부하는 것은 순환뿐**이다 — 빌드 스크립트에 **의존 한 줄을 더하면 그대로 컴파일된다.** 그래서 주 강제 수단은 **선언 그래프의 단언**이고, 그 도구가 서기 전까지는 ★ **빌드 스크립트 diff를 사람이 본다**(리뷰 렌즈에 명시). Gradle 멀티모듈은 ADR-025 **CL-1**이 이미 적고 있다(인용 — E3가 새로 고르는 것이 아니다) |
| **MO-2** | ★ **다른 컨텍스트의 `domain/`을 직접 참조하지 않는다. `port/`만 호출한다.** `port/`는 **도메인 타입을 노출하지 않는다** — 값 타입(`shared/`)과 ID만 | ADR-002 **M-1·M-2** · §5(모듈 분리 결정) | **컴파일** | ★ ADR-002 §5 **원문 인용** — *"**`port/`를 별도 빌드 모듈로 뺀다.** 그러면 `core:payment`가 `core:banking:port`에는 의존하되 `core:banking:domain`에는 **컴파일 단계에서 의존할 수 없다.** M-1·M-2가 구조로 강제된다."* ★ **이 규약이 모듈을 신설하는 것이 아니라 ADR의 결정을 인용**한다. ⚠️ 단 *"어느 모듈에 의존을 선언했나"* 자체는 **MO-1의 그래프 단언**이 본다 |
| **MO-3** | ★ **의존 방향은 아래로만.** 기본 방향은 `C3 → C1` · `C3 → C2`이고 **역방향·순환 금지** · `C1 → C2` 의존도 없다. ★ **ADR-002 §5 트리가 적은 예외는 그대로 허용**한다 — `core:auth`(C7) → `core:banking(port)`·`core:payment(port)`(**R4 동기**) · `core:reconciliation`(C6) → 위 전부의 `port`(★ **읽기만** — M-6) · `core:banking`·`core:payment`·`core:auth` → **`core:ops(port)`**(**M-7** — R14 consume 호출이지 하향 의존 아님). ★ **역방향(`ops` → 다른 코어 모듈)은 금지** | ADR-002 **M-3·M-4·M-6·M-7** · §5 트리 · 컨텍스트 맵 R2·R3 | **아키텍처 테스트**(허용 간선 **닫힌 목록** 단언) ⚠️ *현재 **잠정 리뷰**(빌드 스크립트 diff)* + **컴파일**(보조 — 순환만) | ⚠️ ★ **순환은 빌드 도구가 거부하지만 역방향 한 줄은 거부하지 않는다** — `ops → banking` 의존을 선언해도 순환이 아니면 **컴파일된다.** 방향 규칙은 **허용 간선을 닫아 열거한 단언**이라야 강제된다. ⚠️ ★ *"C3→C1·C2만"* 으로만 적으면 **M-6·M-7이 규약 위반으로 읽힌다** — 예외의 정본은 ADR-002 §5 트리이고 이 행은 그것을 인용한다 |
| **MO-4** | ★ **컴파일이 못 막는 것은 아키텍처 테스트가 막는다** — ⑴ **리플렉션 접근** ⑵ **같은 모듈 안의 레이어 위반**(adapter → domain 역참조 등) ⑶ ★ **`shared/`에 도메인 타입이 스며드는 것** | ADR-002 §5 경고문 그대로 | **아키텍처 테스트** ⚠️ *현재 **잠정 리뷰*** | ADR-002 §7이 통과 기준을 이미 적었다 — *"M-1~M-6 위반 **0건**"*. ★ **도구 지목의 소유 = 공통 라이브러리 첫 커밋 작업**(§6.3) — 이 문서가 미리 고르지 않는다 |
| **MO-5** | ★ **위성(C4·C5)은 코어를 동기 호출하지 않는다**(이벤트로만) · **C6 대사는 코어 안에서 읽기만 한다**(쓰지 않는다) · **`ops` 역방향 금지**(코어 → `core:ops(port)`는 허용, 반대는 금지) | ADR-002 **M-5·M-6·M-7** | **아키텍처 테스트**(★ **허용 outbound 닫힌 목록**) ⚠️ *현재 **잠정 리뷰*** + **테스트 게이트**(런타임 경계 테스트) | ⚠️ ★ **"코어로 향하는 동기 클라이언트 타입이 없다"만으로는 증명되지 않는다** — **범용 HTTP·JDBC 클라이언트**나 **동적 주소 호출**은 타입 부재 검사에서 빠져나간다. 그래서 둘을 **병기**한다: ⑴ 위성 모듈의 **허용 outbound API를 닫아 열거**하고 그 목록 밖 호출 **0건**을 단언 ⑵ ★ **런타임 경계 테스트** — 위성에서 코어로의 동기 호출 시도가 **실제로 실패**함을 확인(C6은 쓰기 시도가 실패) |
| **MO-6** | ★ **공통 라이브러리 경계.** ⑴ **공통이 소유하는 계약은 봉투뿐**(ADR-021 ET-4 8항목) ⑵ ★ **전역 payload 모듈을 만들지 않는다** — payload는 **발행 컨텍스트가 소유**하고 수신측이 ACL로 번역한다 ⑶ **공통에 도메인 로직·조작·저장소·판정 술어를 두지 않는다**(BR-55 술어 구현은 C7 소유) ⑷ ★ **공통은 어느 업무 배포도 의존하지 않는다** | ADR-025 **CL-1·CL-5·CL-6 ⑶·CL-8** | **컴파일**(⑷ — 의존 방향) + **아키텍처 테스트**(⑴⑵ · ★ **⑶의 구조 부분**) ⚠️ *현재 **잠정 리뷰*** + **리뷰**(★ **⑶의 의미 부분**) | ADR-025 §6이 통과 기준을 이미 적었다 — *"공통 모듈이 업무 배포 코드·애그리게이트를 참조하지 않는다"* · *"한 발행자의 payload 필드 추가 → 다른 배포는 빌드가 안 깨진다"*. ⚠️ ★ **⑶은 한 덩어리로 강제되지 않는다** — **구조 부분**(공통 모듈에 **애그리게이트 타입·저장소 타입·업무 모듈 의존**이 존재하지 않는다 · 클래스가 어느 모듈에 있나)은 구조 검사로 판정되지만, *"이 함수가 **도메인 로직**인가 · **판정 술어**인가"* 는 **의미 판정**이라 사람만 본다. 나눠 적지 않으면 아키텍처 테스트가 못 잡는 몫이 잡히는 것처럼 읽힌다 |
| **MO-7** | ★ **멱등의 소유자를 흐리지 않는다.** `messaging-infra`가 주는 것은 **골격**(outbox 발행·릴레이 러너·inbox 골격)까지이고, **멱등 키의 선택·유니크 제약·핸들러 커밋 경계는 수신 컨텍스트가 소유**한다. C3의 멱등은 **도메인 사전조건**이므로 골격의 멱등 장치를 얹지 않는다 | ADR-025 **CL-3·CL-4** | **리뷰**(★ **메타 규칙** — §7.4) + **아키텍처 테스트**(보조 — 공통 모듈에 **멱등 키 정의·유니크 제약 스키마 0건**) ⚠️ *보조 현재 **잠정 리뷰*** | ★ **구조 부분은 자동화된다** — *"공통 모듈이 멱등 키를 정의하거나 유니크 제약 스키마를 소유하지 않는다"* 는 **구조 검사**다. ⚠️ **남는 의미 부분이 자동화 불가**다 — *"무엇이 중복인가"*(키 선택이 옳은가·커밋 경계가 맞는가)는 도메인 지식이라 구조로 판정되지 않는다. 승격 조건 = 같은 오류(공통이 정한 키로 수신측이 중복을 놓침)가 ADR-025 **R3**로 관측될 때, 그때는 규칙이 아니라 **설계를 되돌린다** |

---

## 3. 오류 규칙

> ★ **오류 코드의 유일 창구는 `usecases/README.md` §5의 신설 코드 대장(+ 선례 승계 코드)이다** — *"여기 없는 코드는 계약이 쓸 수 없다"*(K-1 ⑶).

| # | 규칙 | 근거(정본) | 강제 수단 | 비고 |
|---|---|---|---|---|
| **ER-1** | ★ **코드 대장이 유일 창구.** 새 오류 코드는 **대장 등재가 선행**한다 — 코드에서 먼저 만들고 나중에 등재하지 않는다. 대장에 없는 코드를 쓰는 계약·구현은 **결함**이다 | `usecases/README.md` §5 **K-1 ⑶** | **테스트 게이트**(계약 케이스 — 코드 상수 집합 ↔ 대장 집합 대조) + **리뷰**(등재 선행) | 대조는 **집합**으로 한다 — 수만 세면 다른 코드가 들어 있어도 통과한다(E2 §6.3과 같은 이유) |
| **ER-2** | ★ **오류 모델은 대장에서 생성한다. 수기 미러 금지.** 코드에서 **오류 코드 문자열을 손으로 쓰지 않는다** — 생성된 오류 모델(타입)만 던진다 | ADR-025 **CL-2 ⑵**(*"코드 대장을 기계 판독 원천으로 생성 — 수기 미러 금지(드리프트)"*) | **리뷰** ⚠️ ***잠정** — 생성기·drift 검사가 서면 **린트·자동 검사**로* | ⚠️ ★ **생성 타입만으로는 컴파일 강제되지 않는다** — **문자열 리터럴은 어디서든 쓸 수 있고**, 생성 타입 API는 *"전달 경로"* 만 좁힌다. ⚠️ 생성 타입이 `valueOf`·`fromCode` 류 **문자열 진입 경로**를 열어두면 그 경로로 대장 밖 코드가 그대로 들어온다(생성 타입이어도 안전하지 않다). ★ **§6.1 ③과 같은 형태의 문제** — 대장의 **기계 판독 원천과 생성기가 아직 없다**(**DV-1**). 원천이 서면 ⑴ **생성물 drift 검사** ⑵ **코드에 오류 코드 문자열 리터럴 0건 스캔**(+ 문자열 진입 API 금지)으로 올라간다. drift 검사는 `tools/gen_api_reference.py --check`(검사 ⑥b)가 **선례이지 이 규약이 도구를 확정하는 것이 아니다.** 원천 형식·생성기 = [구현 검증] |
| **ER-3** | ★ **판정 순서를 고정한다.** ★ **사람 채널 = 3단**(K-1 원문 — **멱등 단락 단계가 없다**): ① **자기 속성 전부**(단계 + 전사 스코프 보유 = `FORBIDDEN_LEVEL` — ★ **대상 조회 전**) → ② **스코프·존재**(`NOT_FOUND`) **+ 대상 유효성**(고유 코드 — `NOT_FOUND`로 접지 않는다) → ③ **상태·인자**. ★ **시스템 채널(전문·파일)은 `X-10`이 정본 — 4단**: ① 인자 형식 → ② 멱등 단락 → ③ 대상 존재 → ④ 상태(**재수신이 대상 조회보다 앞선다**). ★ **채널 인가 `FORBIDDEN_LEVEL`은 전문 처리 이전의 ACL 판정이라 ① 앞에 선다** | `usecases/README.md` §5 **K-1** · `interfaces/README.md` **X-10** | **테스트 게이트**(단위·계약 케이스) | 순서가 갈리면 **같은 요청이 채널마다 다른 코드로 거절**된다. 채널 분기는 규칙이지 실수가 아니다 — 그래서 **단계 수가 다르다는 것**(사람 3단 / 시스템 4단)을 규약이 그대로 옮긴다. ★ **순서 고정과 멱등 판정 소유는 다른 층이다** — 이 행이 고정하는 것은 *"② 멱등 단락이 ③ 대상 존재보다 앞선다"* 는 **자리**뿐이고, *"무엇이 중복인가"*(키 선택·유니크 제약·핸들러 커밋 경계)는 **MO-7 — 수신 컨텍스트 소유**다. 자리만 인용하고 키를 공통이 정하면 ADR-025 CL-3·CL-4 위반이다 |
| **ER-4** | ★ **채널 변환은 어댑터가 한다.** 문자 상수 → **4N 물리 응답 코드**(X-7) 매핑은 **adapter 층에만 존재**한다 — 도메인·application은 물리 코드를 모른다 | `interfaces/README.md` **X-7**(*"정본은 문자 상수"*) · ADR-002 M-2(값 타입과 ID만 노출) | **린트·자동 검사**(도메인·application에 물리 코드 리터럴 **0건** 스캔) ⚠️ *현재 **잠정 리뷰*** + **테스트 게이트**(계약 케이스 — 매핑의 **존재와 유일성**) | ⚠️ E2 §3.2 그린 위장 항목: *"매핑이 있다"* 만 단언하면 위장이다 — **한 문자 상수 ↔ 한 값**의 **유일성**까지 단언한다. ⚠️ ★ **그런데 ER-5로 물리 값이 `[미확정]`인 동안 유일성 단언은 공허해질 수 있다** — 값이 전부 placeholder(같은 더미·미할당)면 유일성이 **자동으로 거짓**이 되고, 매핑 표가 비면 **자동으로 참**이 된다(E2 §3.2 *"기대값 파일이 비었는데 통과"* = fail-open 골든). 그래서 케이스는 ⑴ **placeholder 값을 대상에서 제외**하고 ⑵ **값이 확정된 코드 집합에 대해서만** 유일성을 단언하며 ⑶ ★ **대상 건수를 출력**한다 — **0건이면 그 케이스는 아무것도 검증하지 않은 것**이다. 값 확정 전까지 이 단언은 ★ **잠정**이다. ★ **금지 리터럴 스캔은 아키텍처 테스트를 기다리지 않는다** — 린트로 충분하다(**DV-4**) |
| **ER-5** | ★ **물리 코드의 숫자 값은 확정하지 않는다** — `[미확정 — 대외 협의]`. 규약도, 계약 테스트도 **값을 단언하지 않는다** | `interfaces/README.md` **X-7** | **리뷰** | ⚠️ **자동화 불가** — 값이 없으니 대조할 대상이 없다. 승격 조건 = **대외 협의로 값이 확정될 때**. 그때 ER-4의 계약 케이스가 값 단언으로 강화된다 |

---

## 4. 테스트 작성 규칙 (E2 인용)

> ★ **계층·태그·자산 배정·게이트의 정본은 `architecture/test-strategy.md`다.** 이 절은 **작성자가 지켜야 할 실행 규칙**만 인용한다 — 계층 정의를 다시 적지 않는다.

| # | 규칙 | 근거(정본) | 강제 수단 | 비고 |
|---|---|---|---|---|
| **TE-1** | ★ **주 계층 하나 + 보조 계층(의무).** 케이스를 쓸 때 보조 계층 케이스를 **빈칸으로 두지 않는다** — 주 하나만 적으면 *"그 하나만 짜면 된다"* 로 읽힌다 | E2 §1.0·§2 (양식 R4) | **리뷰**(배정의 **타당성**) + **린트·자동 검사**(★ **스키마 검사** — 주/보조 계층 칸 **빈칸 0건**) | ★ **한 덩어리가 아니다** — *"보조 계층 칸이 채워져 있나"* 는 **스키마 검사**이고 *"그 배정이 옳은가"* 만 판단이다. ⚠️ 지금 스키마 검사가 없는 이유는 **판단이라서가 아니라 검사 대상(케이스 대장)이 아직 없어서**다 — 승격 조건 = 케이스 대장(E2 §8 **TS-3**)이 만들어질 때, 그때 **빈칸 검사는 즉시** 자동화되고 **배정 ↔ 케이스 집합 diff**가 열린다 |
| **TE-2** | ★ **하한을 지킨다.** 트랜잭션 경계(E1~E5)의 **원자성·다중 레코드 동시 이동·순서**는 **통합 이상**이다 — **인메모리 등식으로 흉내 낸 것은 그 규칙을 검증한 것이 아니다.** 물리 규격·직렬화 표면은 **계약**, 측정 수치는 **인수** | E2 §1.0 하한 **L1~L3** | **리뷰**(*"경계를 실제로 태우나"*) + **린트·자동 검사**(★ **스키마 검사** — 하한별 **테스트 source-set·태그** 일치) | ★ **분리한다** — 하한 L1~L3은 *"주 계층이 무엇이어야 하나"* 를 정하므로, **케이스가 그 계층의 source-set·태그에 있나**는 **기계 판정**이다(통합 하한 규칙이 단위 source-set에 있으면 위반). 남는 판단은 *"태그는 통합인데 실제로는 인메모리로 흉내 냈나"* 뿐이다. 승격 조건 = TE-1과 동일(케이스 대장 + source-set·태그 규약 확정) |
| **TE-3** | ★ **red-first는 높음 stakes 한정**(자금 경로 E1~E5 · 권한 **BR-54**~**BR-61** · 마이그레이션 · public 계약)이며 **성립 조건 R-a~R-d를 만족해야 근거가 된다** — ⓐ 변경 전 코드에서 **실행 가능** ⓑ red의 원인이 **규칙 위반**(컴파일 실패·NPE·미구현 예외는 red가 아니다) ⓒ ★ **감도 증명**: green이 된 뒤 규칙을 일시 반전시키면 **다시 red** ⓓ 처음부터 green이면 ⓒ로 분기 | E2 §3.3 **R-a~R-d** | **리뷰**(blind 워커 회수 packet에 **감도 증명 결과** 포함) | ⚠️ 감도 증명은 **의도적 반전 실행**이라 CI가 상시로 대신하지 못한다. ★ **단 ⓒ는 변이 테스트의 정의와 같은 조작**이다(규칙을 반전시켜 테스트가 red가 되는지 본다) — §6.3의 **변이 테스트**가 도입되면 **ⓒ 부분은 부분 자동화**된다(도입 조건은 E2 원문 그대로이며 이 문서가 재판정하지 않는다 — D6). ⚠️ **중간·낮음으로 확대하지 않는다**(확대하면 테스트가 아니라 의식이 된다) |
| **TE-4** | ★ **소유 3축을 지킨다** — **설계 주체 / 검증 책임자 / 차단 주체.** 통합 계층은 ★ **spec에서 출발하고 구현 diff에서 출발하지 않는다**(분리 패스) · 계약은 **UC §7 소유자**가 설계 · **높음 stakes는 계층 무관하게 blind 워커**(구현 diff 미열람) · **탐색 판정은 독립 판정자** | E2 §1.2 · §3.3 | **리뷰**(소유 배정의 **타당성**) + **린트·자동 검사**(★ **스키마 검사** — 워커 packet **필수 필드 존재**: task ID · 기준 SHA · 실제 읽은 파일 · 실행 명령+결과 · 미완료 · 감도 증명 결과) | ★ **분리한다** — packet의 **필수 필드가 채워졌나**는 스키마 검사이고, *"읽었다고 적힌 것이 실제로 그 계층의 spec인가 · 이 사람이 독립 판정자인가"* 만 판단이다. 승격 조건 = **packet 형식 확정 + 기계 검사** — 그때 필드 존재 검사는 즉시 자동화된다 |
| **TE-5** | ★ **데이터 특칙 — "에러 없이 돌았다" ≠ 완료.** 배치·릴레이·대사는 **record-level 검증**(count · sample · orphan)으로 판정한다(**BR-23** *"총 반영 건수 = 파일 건수"* · **BR-41** *"anchor + Σ = 보조부 잔액"* · **BR-52** *"처리 + 격리 = 전체"*). ★ **부분실패 DONE 위장·무음 스킵·조용한 절단을 명시적으로 반증**한다 | E2 §3.2 | **리뷰**(그린 위장 판정) + **테스트 게이트**(record-level 케이스) + **린트·자동 검사**(★ 검사 스크립트 — **record-level 단언 존재** · **빈 기대값 골든 0건** · **종료 코드 마스킹 패턴 0건**) | ★ **그린 위장 점검 동반**: fail-open 골든(적재 0건 = 통과로 읽히면 안 된다 — **BR-09**·**BR-41** 취약) · exit 마스킹(`\| tail` 류 종료 코드 삼킴). ★ **이 셋은 패턴이라 스크립트가 본다** — *"골든 파일이 비었다"* · *"파이프 뒤로 종료 코드가 삼켜졌다"* · *"배치 케이스에 count·sample·orphan 단언이 하나도 없다"* 는 기계 판정이다. 리뷰에 남는 것은 ★ *"이 단언이 **그 실패모드**를 실제로 반증하나"* 뿐이다 |
| **TE-6** | ★ **픽스처 골격**(E2 §8 **TS-4**가 E3로 넘긴 판정) — ⑴ **영업일 달력·현재 시각**은 **KO-5의 주입 지점으로 고정**한다(별도 픽스처 장치를 만들지 않는다) ⑵ **모의 매입사·모의 입금원은 어댑터 경계에서 대체**한다(도메인에 테스트 분기를 넣지 않는다) ⑶ ★ **구체 픽스처 세트·데이터 빌더 형식은 `[구현 검증]`** | E2 §8 TS-4 · ADR-002 M-1·M-2 | **리뷰**(골격 설계 판단) + **아키텍처 테스트**(★ **⑵의 구조 부분** — domain에 **테스트 분기 0건** · 모의 구현이 **adapter 모듈에만** 존재) ⚠️ *보조 현재 **잠정 리뷰*** | 골격만 정한다 — 실제 데이터 세트를 지금 정하면 **케이스 대장을 문서로 이중화**한다(TS-3과 같은 이유). ★ **⑵는 구조 규칙이다** — *"도메인에 테스트 전용 분기가 없다"* · *"모의 매입사·모의 입금원 구현이 adapter 밖에 없다"* 는 **MO-4 ⑵(모듈 내 레이어 위반)와 같은 검사**라 같은 도구가 본다. 리뷰에 남는 것은 ⑴(주입 지점 고정)·⑶(세트 형식)의 **설계 판단**이다 |

---

## 5. PR·리뷰 절차

| # | 규칙 | 근거(정본) | 강제 수단 | 비고 |
|---|---|---|---|---|
| **PR-1** | ★ **브랜치 우선 — `main` 직접 작업 금지.** 작업 단위로 브랜치를 따고, 브랜치 base·이름은 **작업 시작 전에 확인**한다 | 기존 정책(작업 규칙 — 외부 발행·브랜치) | **리뷰** ⚠️ *잠정* | ★ **승격 조건 = 저장소 브랜치 보호 규칙 설정**(E4 소유 · 설정값 `[구현 검증]`). 설정되면 **자동 강제**로 올라간다 |
| **PR-2** | ★ **커밋은 단계 단위·의미 단위.** 여러 단계 작업은 **단계마다 커밋**하고 몰아서 하지 않는다. ★ **문서 작업과 구현 작업을 한 커밋에 섞지 않는다** | 기존 정책(스코프 — 문서·구현 분리) | **리뷰**(*"의미 단위"*) + **린트·자동 검사**(★ **커밋 훅 — 경로 기준 혼합 커밋 거부**. `docs/**` 와 구현 경로가 **한 커밋에 함께 오면 거부**) | ★ **혼합 커밋 검사는 관측을 기다리지 않고 즉시 도입한다**(D3 — 올릴 수 있으면 올린다). 경로 기준은 **지금도 기계 판정 가능**하고, *"2회 관측 후 승격"* 은 **자동화할 수 있는 것을 사람 몫으로 유예**하는 것이라 D3 위반이다. ⚠️ 리뷰에 남는 것은 *"의미 단위"* 뿐 — 그 부분은 판정 규칙이 없다 |
| **PR-3** | ★ **머지 게이트 = E2 §3.0 인용**(재정의 금지) — 통과 조건 ⑴ **단위 · ★ 통합 · 계약 전부 green** ⑵ **머지 전 최소 안전선 ①~⑥**(§3.1) ⑶ ★ **관련 탐색 판정이 유효**(재실행 규칙에 걸리지 않았다). ★ **E2 §3.1 원문의 ①~⑥** = ① 스모크 — 변경 경로를 실제로 실행 ② 기존 테스트 + 불변식 최소 1개 직접 검증 ③ diff self-review — 의도 외 변경 ④ rollback / forward-fix 판단 ⑤ public contract 영향(API·DB 스키마·이벤트) ⑥ 반증 질문 1회 | E2 **§3.0**·§3.1 | **테스트 게이트**(⑴) + **리뷰**(⑵⑶) | ★ **탐색에 대한 E2 원문 두 문장을 그대로 옮긴다**(축약하면 갈린다): **마일스톤 게이트**(인수·탐색 판정)는 *"★ **머지를 막지 않는다.** 막게 하면 인수·탐색이 '머지 전에 통과시켜야 할 것'이 되어 **통과하도록 약하게 쓰인다**"* — 즉 **탐색을 머지마다 돌리지 않는다.** ⚠️ 그러나 **머지 게이트 ⑶은 별개**다: *"관련 탐색은 '최근 판정 유효'로만, 그리고 그 만료는 **해당 자산에 닿는 변경의 머지**만 막는다(전면 차단 아님)"* — **정본 개정으로 판정이 만료되면**(재실행 촉발) 그 자산이 걸린 변경은 **재실행 전까지 머지되지 않는다.** ★ 막지 않는 것은 **탐색의 실행**이고, 막는 것은 **만료된 판정을 안은 그 변경의 머지**다. ⚠️ ★ **안전선 ①(스모크)은 계층이 대체하지 못한다** — *"테스트가 다 녹색이어도 **경로를 한 번도 안 태웠으면** 스모크는 안 한 것"*. ⑤(public contract)도 **사람이 지목**해야 계약 테스트가 확인한다 |
| **PR-4** | ★ **리뷰 관점 = 구조·불변식 우선**(v2 렌즈). 보는 것: **경계 침범 · 불변식 위반 · 판정 충돌 · 권한/자금 경로 · 이중 적재**. ★ **안 보는 것: 표기·포맷·색인·문구 동기화** — 그 유형은 **린트·자동 검사·구현이 공짜로 잡는다** | 검증 정책 v2 **V2-1·V2-2**(실측: 구조 결함 ~25% · 동기화·표기 ~60%) | **리뷰**(★ **메타 규칙** — §7.4) | ★ **이 행은 다른 규칙의 강제 수단이 아니라 「리뷰라는 수단 자체를 어떻게 쓰나」를 정한다** — 그래서 자동 강제가 **정의상 성립하지 않는다**(§7.4). ★ **표기 지적을 리뷰에 넣지 않는 것 자체가 규칙**이다 — 넣으면 리뷰가 60% 유형으로 채워져 구조 결함을 놓친다 |
| **PR-5** | ★ **AI attribution 금지.** 커밋 메시지와 **모든 발행 본문**(PR·이슈·코멘트·릴리즈)에 `Co-Authored-By: Claude`·*"Generated with …"* 류를 **적지 않는다**. ★ **검증 과정의 출처(리뷰어·교차 검증 도구)도 커밋·주석에 기재하지 않는다** — 그 기록은 작업 로그가 소유한다 | 기존 정책(git·발행 규칙) | **린트·자동 검사**(커밋·발행 훅) + **리뷰** | ⚠️ 훅이 못 보는 표면이 있다(파일 경유 본문 · 일부 발행 명령 · 워커 내부 호출) — 그 구간은 **절차 준수**다 |
| **PR-6** | ★ **push·외부 발행은 사용자 확인 후.** 원격 브랜치 생성 · PR/이슈 생성 · 머지는 **각각 확인**한다. ★ **`--force`·강제 되돌리기·파괴적 삭제는 명시 요청 시만** | 기존 정책(git·외부 발행) | **린트·자동 검사**(push 확인 훅) + **리뷰** | 파괴적 조작 **직전에 현재 브랜치·HEAD·경로·대상을 재확인**한다(실측: 상태 오인이 최고 강도 사고 유형) |

---

## 6. ★ 테스트 도구 확정 (C-08 — 요구 → 계약 → 도구)

> ★ **E2가 소유한 것은 「성질」까지이고**(test-strategy §4.1 · §5 *"테스트 도구 스택 = [E3 소유]"*), **그 성질을 만족하는 도구의 지목·등재가 이 절이다.** 판정 기준은 **인용**하며 다시 쓰지 않는다.

### 6.1 판정 — E2 §4.1 「성질 충족 판정 기준」 대조

| 계층 | E2가 적은 충족 판정 기준 (인용) | 후보 | 판정 | 남은 것 |
|---|---|---|---|---|
| **① 단위** | 케이스 표(BR-05 한도−1/한도/한도+1 류)를 **데이터로 넘길 수 있나** | **JUnit5** | ⚠️ ★ **조건부 충족** — 파라미터화로 케이스 표를 **데이터로** 넘긴다(도구 범주로서). 단 ★ **Kotlin에서의 파라미터 소스 구성은 미실증** — 첫 케이스 작성이 스파이크다(`[구현 검증]` — 실증 실패 시 R2 발동) · 예외 단언 제공 · C-05(Kotlin/JVM) 위에서 인메모리 실행 | — |
| **② 통합** | ⑴ **실 MySQL이 뜨나** ⑵ 테스트 간 **상태가 새지 않나** ⑶ 기동 비용이 **매 빌드 예산 안인가**(E2 §5 — **아직 미측정**) | **Testcontainers (MySQL 컨테이너)** | ⚠️ ★ **부분 충족 — 확정 보류**: ⑴ ✅ **충족** — 실 MySQL 이미지를 띄운다(인메모리 DB로는 격리 수준·갭 락·데드락·`insert intention waiting`·`History list length`가 재현되지 않는다 — DB-X1~X8이 근거) ⑵ ⚠️ **조건부 충족** — 컨테이너 생명주기로 격리가 **가능하나**, 재사용 전략에 따라 상태가 샌다 ⑶ ★ **판정 보류 — 측정 전** | ⑵ **격리 단위**(컨테이너 / 스키마 / 트랜잭션 롤백)의 선택 = `[구현 검증]` **DV-3**. ⚠️ ★ **E2 §4.1은 *"테스트마다 격리된 인스턴스"* 를 요구한다** — 공유 컨테이너 + 스키마·롤백 격리는 그 **문면보다 약하다.** 그래서 격리 단위 선택은 ★ **E2가 요구한 성질(테스트 간 상태가 새지 않는다)을 하한**으로 하고, 그 하한을 만족함을 **첫 측정과 함께 실증**해야 채택된다 — 실증 못 하면 **인스턴스 격리로 되돌린다**(요구가 도구보다 오래 산다) ⚠️ ★ 역방향(약한 격리로의 후퇴 — 공유 인스턴스+스키마 격리 채택)은 **E2 §4.1 문면 개정(정본 재합의)을 동반해야만 가능하다** — 규약·측정이 단독으로 상위 요구를 치환하지 않는다(재점검 정정) ⑶ **기동 비용 첫 측정** → E2 §5 *"테스트 실행 시간 예산"* 행을 채운다 |
| **③ 계약** | 규격 표를 **테스트 데이터로 그대로** 쓸 수 있나 (*"표를 손으로 옮겨 적으면 표면 정본이 둘이 된다"*) | **JUnit5**(파라미터화 — 표 원천 로딩) | ⚠️ ★ **부분 충족** — 실행기는 표를 데이터로 받을 수 있으나, ★ **`interfaces/**` 규격 표의 기계 판독 원천이 없다.** 원천이 없으면 케이스가 표를 **손으로 미러**하고 그 순간 표면 정본이 둘이 된다 | ★ **규격 표의 기계 판독 원천** — ER-2(오류 모델 생성)와 **같은 형태의 문제**다. §8 **DV-1**로 등재 |
| **④ 인수** | E2가 **[미확정]** 으로 남김(§4.2 부하 도구 행) | — | ★ **지목하지 않는다** | 부하 도구 도입 조건 = **P-X2 또는 D-X13 착수 시점**(§6.3 인용) |
| ★ **탐색 태그** | 실행 계층(②·③·④)의 판정 기준을 그대로 따른다 — **탐색이 별도 도구 스택을 갖지 않는다** | 위와 동일 | ⚠️ **부분** — 컨테이너 제어로 **DB 측 주입**은 가능하나, ★ **애플리케이션 프로세스 강제 종료**(P-X3 20지점 `kill -9` 스윕 · P-X6·D-X8 `SIGSTOP`)는 **테스트 실행기 밖**(실행 스크립트·프로세스 제어)이다 | 프로세스 제어 수단 = `[구현 검증]`. ★ **새 도구를 지금 넣지 않는다** |

### 6.2 확정 기록 (C-08 형식)

| # | 도구 | 요구 (무엇이 이것을 부르나) | 계약 (무엇을 보장해야 하나) | 도입 조건 | ★ 되돌리는 조건 | 강제 수단 |
|---|---|---|---|---|---|---|
| **TL-1** | ★ **JUnit5** — 단위·통합·계약 계층의 **실행기** | ① 단위의 파라미터화·예외 단언 · ② ③의 케이스 실행 자체 | 케이스 표를 **데이터로** 받는다. ⚠️ ★ **단언 메시지가 BR 문장으로 읽히는 것은 도구 성질이 아니다** — 실행기는 임의 메시지를 실을 수 있을 뿐이고 **무엇을 적을지는 작성자가 고른다**(E2 R-b는 작성 규칙이며 TE-3·리뷰가 본다) | ★ **이미 충족 — 단 「실행기」 축에 한해서**: C-05(Kotlin/JVM)에서 실행기 없이는 **어느 계층도 돌지 않고**, 머지 게이트(E2 §3.0 ⑴)가 이것을 전제한다. ⚠️ **단언 메시지 품질은 이 판정의 근거가 아니다**(위 계약 칸) · 파라미터 소스 구성의 구체 형식 = `[구현 검증]` | **R1** ★ **실행기 자체가** 케이스 표를 데이터로 받지 못하는 사례(파라미터 소스로 표현 불가 · 표현하면 케이스가 깨짐)가 **2회 누적**될 때. ⚠️ ★ **「규격 표를 데이터로 못 받는다」는 R1이 아니다** — 그 원인은 실행기가 아니라 **기계 판독 원천의 부재**(**DV-1**)이고, 실행기를 바꿔도 해소되지 않는다 **R2** Kotlin 전용 실행기가 아니면 표현할 수 없는 케이스가 실증될 때 — 그때도 **교체이지 추가가 아니다** | **테스트 게이트** |
| **TL-2** | ★ **Testcontainers (MySQL 컨테이너)** — ② 통합 계층의 **실 MySQL 격리** | E2 §1.1 ②(*"★ 실 MySQL(격리된 인스턴스)"*) · 하한 **L1**(경계 원자성·순서를 인메모리로 흉내 낸 것은 검증이 아니다) · **DB-X1~X8**(격리 수준·갭 락·데드락·롱 트랜잭션) | ⑴ **실 MySQL**이 뜬다 ⑵ 테스트 간 **상태가 새지 않는다** ⑶ **스키마 재현** ⑷ 스케줄러·릴레이가 **실동작**한다 | ⚠️ ★ **부분 충족 / 확정 보류**(D5 ⑶) — ⑴ **충족**(C-07 MySQL 불변이고, ②가 **머지 게이트 대상**이라 매 빌드에 실 MySQL이 필요하다) ⑵ ★ **조건부** — 격리 단위 선택에 달렸고 그 선택이 아직 없다(**DV-3**). ⚠️ E2 §4.1의 *"테스트마다 격리된 인스턴스"* 가 **하한**이며 공유 컨테이너는 그 성질 실증 후에만 채택된다 ⑶ ★ **미측정** — E2 §5 *"테스트 실행 시간 예산"* 행이 **`[미확정]`** 이다. **3중 2가 미확정인 채로 "이미 충족"으로 적으면 §6.1 ③(부분 충족 표기)과 대칭이 깨진다** | **R1** ⚠️ ★ **예산 행이 비어 있어 "상한 초과"로는 발동할 수 없다** → 그래서 **측정 전에 상한을 먼저 선언**한다: **통합 계층 기동+실행 시간의 빌드당 상한 = `[구현 검증]`**(소유 = **구현 착수 시 첫 측정 작업** — 그 값이 E2 §5 예산 행을 채우고 **DV-3**이 대장이다). ★ **선언된 상한을 첫 측정이 넘으면** 공유 인스턴스(⚠️ ★ 이 후퇴는 E2 §4.1 문면(*"테스트마다 격리된 인스턴스"*)보다 약하므로 **E2 정본 개정(재합의)을 동반해야만 가능** — 규약이 단독으로 상위 요구를 치환하지 않는다) + **스키마 단위 격리**로 후퇴한다(도구 제거가 아니라 **격리 단위 하향**) **R2** 컨테이너 격리에도 **상태가 샌 사건이 2회** 누적(관측 대장 §9) **R3** CI 실행 환경에서 컨테이너 런타임을 쓸 수 없을 때 → 그때는 **격리된 실 MySQL을 다른 수단으로** 제공한다(요구는 도구보다 오래 산다) | **테스트 게이트** |
| **TL-3** | ★ **도구를 더 넣으려면 도입 조건의 실증을 첨부한다**(★ 도구가 아니라 **절차 규칙** — §7.4 메타) | C-08(*"나중에 필요하면"* 은 판정 불가) | 후보의 도입 조건(§6.3)이 **관찰됐다는 근거**를 PR에 첨부 | ★ 해당 없음 — 절차 규칙 | ★ 해당 없음 — C-08이 폐기될 때만 | **리뷰**(근거의 **충분성**) + **린트·자동 검사**(★ **스키마 검사** — 도구를 더하는 PR에 **근거 첨부 여부**·§6.3 후보 ID·관측 대장 행 참조가 **있나**) |

> ⚠️ ★ **이 절이 확정하지 않은 것**: **④ 인수 도구** · **포매터·정적 검사(린트) 도구와 설정값**(KO-1~KO-5·ER-4 — **DV-4**) · **오류 모델 생성기와 대장의 기계 판독 원천**(ER-2 — **DV-1**) · **아키텍처 테스트 도구**(KO-6·MO-1·MO-3~MO-6 — §6.3 인용, 지목 소유자 = 공통 라이브러리 첫 커밋) · **프로세스 제어 수단**(탐색 태그 — DV-2) · **통합 계층 격리 단위와 실행 시간 상한**(DV-3). ★ **빌드 도구는 ADR-025 CL-1이 이미 Gradle 멀티모듈로 적고 있다** — 인용이며 이 절의 확정이 아니다.

### 6.3 도입하지 않는 도구 — E2 §4.2 도입 조건 인용 (★ 보류 유지 · 재판정하지 않는다)

| 후보 | v1에서 안 넣는 이유 (E2 인용) | 도입 조건 (E2 인용) | E3 시점의 상태 |
|---|---|---|---|
| **부하 도구**(k6·Gatling·JMeter 류) | 인수·탐색 착수 전까지 **부하를 걸 대상이 없다** | QS-01(100 TPS)을 실제로 걸어야 하는 시점 = **P-X2 또는 D-X13 착수**. 그때 **하나만** 고르고 근거를 남긴다 | 보류 |
| ★ **아키텍처 테스트**(ArchUnit 류) | 모듈이 아직 없다 | ★ **공통 라이브러리 첫 커밋**(ADR-025 §6이 이미 요구 — ADR-002 모듈 규칙 강제도 같은 도구) | ★ **보류 — 단 조건이 가장 가깝다.** 이 규약의 **잠정 리뷰 14건 중 6건**(KO-6·MO-1·MO-3·MO-4·MO-5·MO-6 ⑴~⑶)이 이 도구를 기다린다(+MO-7·TE-6의 **보조** 구조 검사). ★ **도구 지목의 소유자 = 공통 라이브러리 첫 커밋 작업**이다 — 그 작업이 §6.2 형식(요구·계약·도입 조건·되돌리는 조건)으로 **등재**하고, 등재된 순간 위 6건이 **잠정 리뷰 → 아키텍처 테스트로 승격**된다. ⚠️ **조건 충족 판단 자체는 별도**이며(D6) 이 문서가 앞당기지 않는다 — 여기서 정하는 것은 *"누가 지목하나"* 까지다 |
| **커버리지 측정**(JaCoCo·Kover) | **목표 수치를 정하지 않기로 했다** — 게이트 없는 측정은 대시보드일 뿐이다 | *"어느 BR의 케이스가 코드에 없다"* 를 **사람이 두 번 이상 놓친 것이 관측될 때**. 그때도 게이트는 **BR 배정 커버리지**이지 라인 커버리지가 아니다 | 보류 |
| **속성 기반 테스트** | 산식에 잘 맞으나 케이스가 **이미 정본에 열거**돼 있다 | 열거된 케이스가 **통과했는데 실환경에서 그 산식이 깨지는 것**이 1회 관측될 때 | 보류 |
| **변이 테스트**(코드 대상) | 문서 층에서 `tools/mutation_test.py`가 이미 같은 역할을 한다 | **코드 테스트가 "다 녹색인데 결함이 나온" 사례가 2회** 누적될 때(관측 대장 §9) | 보류. ★ **단 TE-3 ⓒ 감도 증명은 이 도구의 정의와 같은 조작**이다(규칙을 반전시켜 테스트가 red가 되는지 본다) — 도입되면 TE-3의 **ⓒ 부분이 부분 자동화**된다(§7.2). ⚠️ 그것이 **도입 조건을 앞당기는 근거는 아니다**(D6 — 조건은 E2 원문 그대로 유지) |
| **계약 테스트 프레임워크**(Pact 류) | 상대가 **모의 외부**다 — 소비자 주도 계약의 상대편이 없다 | 실제 외부 기관과 규격을 주고받게 될 때 | 보류 |
| **메시지 브로커 기반 테스트 도구** | **브로커 자체를 안 넣는다**(ADR-021 ET-1) | ET-9의 브로커 도입 조건 3종 중 하나가 실증될 때 | 보류 |

---

## 7. 강제 수단 분포 — self-check

### 7.1 분포 — ★ 「현재 / 도구 확정 후」 2열 (주 강제 수단 1개씩으로 센다 — 병기된 보조 수단은 세지 않는다)

> ★ **한 열로 세면 거짓말이 된다.** *"아키텍처 테스트 5건"* 이라고만 적으면 **그 도구가 없는 오늘 그 5건을 사람이 보고 있다**는 사실이 숨는다(D10). 그래서 **지금 실제로 무엇이 막나**와 **도구가 서면 무엇이 막나**를 나눠 센다.
> ★ **아키텍처 테스트와 테스트 게이트는 칸을 나눈다** — 둘 다 "테스트"지만 **강제력이 다르다**: 아키텍처 테스트는 **구조 위반을 매 빌드에 거부**(사다리에서 컴파일 바로 아래)하고, 테스트 게이트는 **케이스가 있는 만큼만** 막는다(사다리에서 린트 위·아키텍처 테스트 아래 — 케이스 누락은 게이트가 스스로 알지 못한다).

| 강제 수단 (사다리 순) | ★ **현재** | 규칙 ID (현재) | ★ **도구 확정 후** | 규칙 ID (확정 후 이동분) |
|---|---|---|---|---|
| **컴파일** | **1** | MO-2 | **1** | — |
| **아키텍처 테스트** | ★ **0** | *(도구 미확정 — 전량 잠정 리뷰)* | **6** | ← KO-6 · MO-1 · MO-3 · MO-4 · MO-5 · MO-6 |
| **테스트 게이트** | ★ **3** | ER-1 · ER-3 · PR-3 (⚠️ TL-1 조건부·TL-2 확정 보류 — **판정이 끝나기 전에는 현재 강제로 세지 않는다**, 재점검 정정) | **5** | ← TL-1 · TL-2 (판정 확정 시) |
| **린트·자동 검사** | **2** | PR-5 · PR-6 | **10** | ← KO-1 · KO-2 · KO-3 · KO-4 · KO-5 · ER-2 · ER-4 · PR-1 |
| **리뷰** | ★ **27** | *잠정 **16***: KO-1~6 · MO-1 · MO-3 · MO-4 · MO-5 · MO-6 · ER-2 · ER-4 · PR-1 · ★ TL-1 · TL-2(판정 확정 대기 — 재점검 정정) — *리뷰 전용 **11***: MO-7 · ER-5 · TE-1~6 · PR-2 · PR-4 · TL-3 | **11** | (리뷰 전용 11건만 남는다) |
| **계** | ★ **33** | ★ 강제 수단 빈칸 **0건** | ★ **33** | — |

| 검산 | 값 |
|---|---|
| 절별 규칙 수 | §1 KO **6** + §2 MO **7** + §3 ER **5** + §4 TE **6** + §5 PR **6** + §6 TL **3** = ★ **33** |
| 현재 열 합 | 컴파일 **1** + 아키텍처 테스트 **0** + 테스트 게이트 **3** + 린트 **2** + 리뷰 **27** = ★ **33** ✅ 일치 |
| 확정 후 열 합 | 컴파일 **1** + 아키텍처 테스트 **6** + 테스트 게이트 **5** + 린트 **10** + 리뷰 **11** = ★ **33** ✅ 일치 |
| 이동분 | 잠정 리뷰 **16** = 아키텍처 테스트 **6** + 린트 **8** + ★ 테스트 게이트 **2**(TL-1·TL-2 — 판정 확정 시) ✅ (리뷰 27 − 16 = 11) |

> ⚠️ ★ **오늘 사람이 보는 규칙은 33건 중 25건이다**(76%). 그 대부분(**14건**)은 *"원래 기계가 막아야 하는데 도구가 없어서"* 이고, 그 14건이 기다리는 도구는 **아키텍처 테스트 1종 + 린트/생성기 2종**뿐이다 — ★ **그래서 DV-4(린트 도구)와 아키텍처 테스트 지목이 조기 확정 대상**이다(§8).
> ⚠️ **TE-5는 테스트 게이트·린트를 병기**하지만 주 강제 수단은 **리뷰**다 — record-level 검증을 케이스가 돌려도 **그린 위장**(fail-open 골든·exit 마스킹)의 *"이 단언이 그 실패모드를 반증하나"* 는 **통과하는 테스트**라 게이트가 못 잡는다. ★ 병기를 주로 올려 세면 **리뷰 몫이 실제보다 작아 보인다.**

### 7.2 ★ 리뷰로만 강제되는 규칙 — 전량과 승격 조건 (양식 D2)

> ★ **잠정 리뷰는 여기 오지 않는다**(§7.3이 소유). 잠정 리뷰는 *"원래 기계가 막아야 하는데 도구가 없는 것"* 이고 이 절은 *"사람이 보는 것이 배정 자체인 것"* 이다 — 두 집합은 **겹치지 않는다**(PR-1은 잠정이므로 §7.3에만 있다).
> ★ **자동 검사로 분리된 부분은 여기서 뺀다** — 아래 「리뷰에 남는 것」 열은 **의미 판정만** 적는다.

| # | ★ 자동 검사로 분리된 부분 | 리뷰에 남는 것 — 왜 자동화할 수 없나 | ★ 승격 조건 (관찰되면 자동화로 올린다) |
|---|---|---|---|
| **MO-7** | 공통 모듈에 **멱등 키 정의·유니크 제약 스키마 0건**(구조 검사 — 아키텍처 테스트 보조) | *"무엇이 중복인가"*(키 선택·커밋 경계의 적정성)는 도메인 지식이라 구조로 판정되지 않는다 — ★ **메타 규칙**(§7.4) | ADR-025 **R3**(공통이 정한 멱등 키로 수신측이 중복을 놓침)가 관측될 때 — 그때는 규칙이 아니라 **설계를 되돌린다** |
| **ER-5** | — | 값이 `[미확정]`이라 대조할 대상이 없다 | **대외 협의로 4N 값이 확정**될 때 → ER-4의 계약 케이스를 **placeholder 제외 없이** 값 단언으로 강화 |
| **TE-1** | 주/보조 계층 칸 **빈칸 0건**(스키마 검사) | *"그 배정이 옳은가"* 는 판단이다 | **케이스 대장**(E2 §8 TS-3)이 생길 때 — 빈칸 검사는 **즉시** 자동화되고 **배정 ↔ 케이스 집합 diff**가 열린다 |
| **TE-2** | 하한별 **테스트 source-set·태그** 일치(스키마 검사) | *"태그는 통합인데 실제로 경계를 태우나"* 는 실행 형태를 사람이 읽어야 안다 | TE-1과 동일(+ source-set·태그 규약 확정) |
| **TE-3** | — | 감도 증명(R-c)은 **의도적 규칙 반전 실행**이라 CI가 상시로 대신 못 한다 | ★ **변이 테스트 도입 시 ⓒ 감도 증명 부분이 부분 자동화**된다(§6.3 — 규칙 반전 후 red 확인은 변이 테스트의 정의와 같은 조작). ⚠️ 도입 조건은 E2 원문 그대로(D6) |
| **TE-4** | 워커 packet **필수 필드 존재**(task ID·기준 SHA·읽은 파일·실행 명령+결과·미완료·감도 증명 결과 — 스키마 검사) | *"읽었다고 적힌 것이 그 계층의 spec인가 · 이 사람이 독립 판정자인가"* 는 판단이다 | **packet 형식 확정 + 기계 검사** — 그때 필드 존재 검사는 즉시 자동화 |
| **TE-5** | **record-level 단언 존재** · **빈 기대값 골든 0건** · **종료 코드 마스킹 패턴 0건**(검사 스크립트 — `tools/` 선례) | *"이 단언이 **그 실패모드**를 실제로 반증하나"* 는 통과하는 테스트라 게이트가 못 잡는다 | 위 3종 패턴 검사는 **즉시 도입 대상**(D3). 남는 의미 판정은 위장 유형이 **패턴으로 반복 관측**될 때 추가 승격(관측 대장 §9) |
| **TE-6** | ⑵의 구조 부분 — domain에 **테스트 분기 0건** · 모의 구현이 **adapter 모듈에만**(아키텍처 테스트 보조) | ⑴ 주입 지점 고정 · ⑶ 세트 형식은 **설계 판단**이다 | 구체 세트가 `[구현 검증]`에서 확정되면 그 부분은 코드가 강제 |
| **PR-2** | **경로 기준 혼합 커밋 거부**(커밋 훅 — `docs/**` + 구현 경로 동시 변경) | *"의미 단위"* 는 판정 규칙이 없다 | ★ **혼합 커밋 검사는 관측을 기다리지 않고 즉시 도입**한다(D3 — *"2회 관측 후"* 는 자동화 가능한 것을 사람 몫으로 유예하는 것) |
| **PR-4** | — | ★ **메타 규칙**(§7.4) — 「리뷰라는 수단을 어떻게 쓰나」라 자동 강제가 **정의상 성립하지 않는다** | — (승격 대상 아님. ★ 다만 **표기·색인 지적을 리뷰에서 빼는 것**이 이 규칙의 실행이며, 그 부분은 이미 검산 도구가 소유) |
| **TL-3** | 도구 추가 PR에 **근거 첨부 여부**·후보 ID·관측 대장 행 참조 존재(스키마 검사) | ★ **메타 규칙**(§7.4) — 근거의 **충분성** 판정은 판단이다 | — (승격 대상 아님 — 첨부 여부 검사는 도구 추가 PR 템플릿이 생기면 즉시) |

### 7.3 ★ 잠정 리뷰 — 도구가 확정되면 자동으로 올라가는 규칙 (양식 D10)

| # | 지금 실제로 무엇이 막나 | 무엇이 확정되면 올라가나 | 올라가는 칸 |
|---|---|---|---|
| **KO-1** | 리뷰 (사람이 스타일을 지적한다) | 포매터·린트 도구 + 설정값 — **DV-4** | 린트·자동 검사 |
| **KO-2** | 리뷰 (`List` 반환 타입만 컴파일 — **별칭 경로는 아무것도 안 막는다**) | 린트 도구(**DV-4**) + 변경 감지 케이스 | 린트·자동 검사 |
| **KO-3** | 리뷰 (non-null 시그니처를 지나는 **경계 하나**만 컴파일) | 정적 검사 도구(**DV-4**) — `!!`·nullable 위치 규칙 | 린트·자동 검사 |
| **KO-4** | 리뷰 (**이미 `Money`로 선언된 API**만 컴파일) | 정적 스캔 도구(**DV-4**) — BR-07 규칙(E2 §2.1) | 린트·자동 검사 |
| **KO-5** | 리뷰 | 정적 검사 도구(**DV-4**) — ★ **아키텍처 테스트가 아니다**(금지 심볼 0건 = 린트로 충분) | 린트·자동 검사 |
| **ER-2** | 리뷰 (생성 타입은 **전달 경로만** 좁힌다 — 리터럴·`valueOf` 경로가 열려 있다) | 대장의 **기계 판독 원천 + 생성기**(**DV-1**) → drift 검사 + 리터럴 0건 스캔 | 린트·자동 검사 |
| **ER-4** | 리뷰 (계약 케이스는 **값이 확정된 코드에 한해** 유일성만 단언 — ER-5) | 정적 스캔 도구(**DV-4**) — 물리 코드 리터럴 0건 | 린트·자동 검사 |
| **PR-1** | 리뷰 (저장소 설정이 없다) | **브랜치 보호 규칙 설정**(E4 · 설정값 `[구현 검증]`) | 린트·자동 검사 |
| **KO-6** | 리뷰 | ★ **아키텍처 테스트 도구** (§6.3 — 지목 소유 = 공통 라이브러리 첫 커밋) | 아키텍처 테스트 |
| **MO-1** | 리뷰 (빌드 스크립트 diff — **빌드 도구는 순환만 거부**) | ★ 아키텍처 테스트 도구 | 아키텍처 테스트 |
| **MO-3** | 리뷰 (빌드 스크립트 diff — **역방향 한 줄은 컴파일된다**) | ★ 아키텍처 테스트 도구 | 아키텍처 테스트 |
| **MO-4** | 리뷰 | ★ 아키텍처 테스트 도구 | 아키텍처 테스트 |
| **MO-5** | 리뷰 | ★ 아키텍처 테스트 도구 (+ 런타임 경계 케이스) | 아키텍처 테스트 |
| **MO-6 ⑴⑵·⑶ 구조 부분** | 리뷰 (⑷ 의존 방향만 컴파일 · ⑶ 의미 부분은 **영구 리뷰**) | ★ 아키텍처 테스트 도구 | 아키텍처 테스트 |

> ★ **잠정 리뷰 14건이 기다리는 도구는 3종뿐이다** — **아키텍처 테스트 6건**(KO-6·MO-1·MO-3·MO-4·MO-5·MO-6) · **정적 검사(린트) 7건**(KO-1~5·ER-2·ER-4 — 그중 ER-2만 DV-1의 생성기, 나머지 6건이 **DV-4 하나**) · **브랜치 보호 설정 1건**(PR-1).
> ⚠️ ★ **이전 판은 KO-5·ER-4를 아키텍처 테스트 대기로 적었다** — *"금지 심볼·리터럴이 특정 패키지에 0건"* 은 **린트 규칙으로 충분**하므로 잘못된 배정이었다. 정정하면 **한 도구를 기다리는 최대 묶음은 DV-4(린트) 6건**이고, 그래서 DV-4가 §8의 **조기 확정 대상**이다.
> ★ 아키텍처 테스트의 도입 조건은 **공통 라이브러리 첫 커밋**이므로 Phase 5 착수와 거의 동시에 열린다 — ⚠️ **조건 충족 판단은 별도 작업**이고(양식 D6) 이 문서가 앞당기지 않는다. 이 문서가 정하는 것은 **누가 지목하나**(그 첫 커밋 작업)까지다.

### 7.4 ★ 메타 규칙 — D1의 명시 예외 3건

> **D1**은 *"강제할 수단이 정말 없으면 그것은 규칙이 아니라 가이드"* 라고 적는다. 아래 3건은 **가이드가 아니지만 자동 강제가 정의상 성립하지 않는다** — 다른 규칙의 강제 수단이 아니라 ★ **「강제 수단을 어떻게 운용하나」 자체를 정하기 때문**이다. 그래서 D1의 예외로 **명시**하고, §7.2의 승격 조건 열에는 `—`가 남는다(조건 없는 리뷰 전용이 아니라 **조건이 성립할 수 없는** 리뷰 전용이다).

| # | 무엇에 대한 규칙인가 | 왜 자동 강제가 성립하지 않나 |
|---|---|---|
| **PR-4** | ★ **리뷰라는 수단 자체** — 무엇을 보고 무엇을 안 보나 | 리뷰의 렌즈를 기계가 강제하면 그 기계가 곧 그 렌즈다(자기 참조). 이 규칙이 실행되는 형태는 *"표기 지적을 리뷰에서 뺀다"* 이고, **그 몫은 이미 린트·검산 도구로 내려가 있다** |
| **TL-3** | ★ **도구 확정 절차 자체**(C-08) | *"도입 조건이 관찰됐다는 근거가 충분한가"* 는 **판정 기준을 판정하는 일**이다. 첨부 **여부**는 스키마 검사로 내려가고, **충분성**만 남는다 |
| **MO-7** | ★ **소유의 경계**(공통 골격 ↔ 수신 컨텍스트) — 의미 부분 | *"무엇이 중복인가"* 는 도메인 지식이다. 구조 부분(공통에 키·제약 정의 0건)은 **아키텍처 테스트로 내려가고**, 남는 것은 *"이 키가 이 도메인에서 옳은가"* 뿐이다 |

> ⚠️ ★ **이 3건은 §7.1의 33건 계수에 그대로 포함된다** — 예외는 *"승격 조건을 적지 않아도 된다"* 이지 *"규칙이 아니다"* 가 아니다. **강제 수단 칸은 셋 다 채워져 있다**(전부 리뷰 — 빈칸 0건).

---

## 8. 열린 의문

| # | 의문 | 처리 |
|---|---|---|
| **DV-1** | ★ **`interfaces/**` 규격 표(오프셋·길이·타입)의 기계 판독 원천이 없다.** 계약 계층의 충족 판정 기준이 *"표를 테스트 데이터로 그대로 쓸 수 있나"* 인데, 원천이 없으면 케이스가 표를 **손으로 미러**하고 **표면 정본이 둘**이 된다(§6.1 ③) | ER-2(오류 모델 생성)와 **같은 형태의 문제**다. 원천 형식·생성 절차 = **E4(구성 관리) 또는 구현 착수 시** 판단. ⚠️ 지금 만들면 **규격 정본을 규약이 재구성**하는 것이라 금지 영역이다 |
| **DV-2** | **탐색 태그의 프로세스 제어 수단**(P-X3 `kill -9` 20지점 스윕 · P-X6·D-X8 `SIGSTOP`)이 테스트 실행기 밖에 있다 | `[구현 검증]`. ★ **새 도구를 넣지 않는다** — 실행 스크립트로 되는지를 먼저 본다(C-08) |
| **DV-3** | **통합 계층 격리 단위**(컨테이너 / 스키마 / 트랜잭션 롤백)와 **기동 비용** | `[구현 검증]` — 첫 측정이 E2 §5 *"테스트 실행 시간 예산"* 행을 채우고, 그 값이 TL-2의 **되돌리는 조건 R1** 판정 입력이 된다 |
| **DV-4** | ★ **포매터·정적 검사(린트) 도구와 설정값** — **KO-1·KO-2·KO-3·KO-4·KO-5·ER-4 6건**이 이 하나를 기다린다(§7.3) | `[구현 검증]`. ⚠️ 확정 전까지 스타일뿐 아니라 ★ **null 안전·값 타입·금지 심볼·금지 리터럴까지 전부 리뷰 몫**으로 남아 PR-4(구조 우선 렌즈)와 정면 충돌한다 — ★ **잠정 리뷰를 가장 많이 푸는 단일 항목이므로 조기 확정 대상**이다 |
| **DV-5** | ~~이 문서를 `README.md` 문서 지도·`HANDOFF.md`에 등재해야 하나~~ | ★ **닫음**(2026-08-06 리뷰 반영) — `README.md` **문서 지도**에 등재했다. `HANDOFF.md` 등재는 인계 시점 판단이며 이 문서의 열린 의문이 아니다 |

---

## 9. ★ 관측 대장 — "N회 누적" 조건의 정본

> ★ **이 문서에는 *"…가 2회 누적될 때"* 형태의 조건이 여럿 있다**(TL-1 R1·R2 · TL-2 R2 · MO-7 승격 · §6.3 후보 5종의 도입 조건 · §7.2의 승격 조건들). ⚠️ **그 사건을 어디에 적는지가 없으면 카운터가 없는 것이고, 카운터가 없는 조건은 영원히 발동하지 않는다** — *"지켜지지 않아도 아무 일도 안 일어나는 문장"*(D2)이 되돌리는 조건에서도 성립한다.
>
> ★ **그래서 이 절이 그 대장이다.** 아래 표가 **유일 창구**이며, 조건의 카운트는 **여기 등재된 행 수로만** 센다(기억·인상으로 세지 않는다 — 코드 대장 ER-1과 같은 규칙).

### 9.1 기록 규칙

| # | 규칙 |
|---|---|
| **OB-1** | ★ **누가 적나 = 그 사건을 만난 사람**(리뷰어·구현자 무관). **리뷰에서 발견되면 리뷰어가 그 PR에서 append**한다 — 나중에 몰아 적지 않는다(사후 재구성 금지) |
| **OB-2** | ★ **한 사건 = 한 행.** 같은 규칙이 같은 PR에서 두 번 걸려도 **원인이 같으면 1행**이다(같은 결함을 두 번 세면 조건이 거짓으로 발동한다) |
| **OB-3** | ★ **행은 지우지 않는다.** 조건이 발동해 도구가 들어오면 **처리 열에 결과를 적고 행은 남긴다**(발동 근거가 사라지면 되돌릴 때 판단할 것이 없다) |
| **OB-4** | ★ **조건 발동은 자동이 아니다** — 카운트가 찼다는 것은 *"이제 판정을 연다"* 는 뜻이고, **도입 판정 자체는 별도 작업**이다(D6). 이 대장은 **판정을 여는 트리거**이지 판정이 아니다 |
| **OB-5** | ⚠️ **카운트가 찼는데 열지 않았으면 그것도 한 행이다**(`규칙` 칸에 `OB-5`, `사건` 칸에 어느 조건이 찼는지) — 조용한 미발동을 남긴다 |

### 9.2 대장 (행: 일자 · 규칙 · 사건 · 처리)

| 일자 | 규칙 | 사건 | 처리 |
|---|---|---|---|
| — | — | *(아직 없음 — Phase 5 구현 착수 전)* | — |

> ★ **현재 0행이다.** 이 문서의 어떤 "N회 누적" 조건도 아직 카운트가 없다는 뜻이며, 그것이 정직한 상태다. ⚠️ **행이 0인데 조건이 발동했다고 적는 문서가 나오면 그 문서가 틀린 것**이다.

---

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v1.0 | 2026-08-06 | 신설 — E3 산출물 ①(내용 정본). **규칙 32건**(KO 5 · MO 7 · ER 5 · TE 6 · PR 6 · TL 3)에 ★ **강제 수단 칸 전수 부여**(빈칸 0 — 컴파일 6 · 테스트 10 · 린트 4 · 리뷰 12) · **§1 Kotlin**(공식 스타일 채택 + 고유 4종: 불변 우선 · null 안전 · ★ **값 타입 의무(`Money`·`BusinessDate` — 원시 타입 금지, BR-07 · ADR-025 CL-2)** · 시간 명시 주입) · **§2 모듈**(ADR-002 M-1~M-7 · ADR-025 CL-1·CL-3~CL-8 인용 — 컴파일이 막는 것과 아키텍처 테스트가 막는 것을 분리) · **§3 오류**(코드 대장 **유일 창구** · **생성된 오류 모델만**(수기 문자열 금지) · 판정 순서 K-1/X-10 · **채널 변환은 어댑터** · 4N 값 미확정) · **§4 테스트 작성**(E2 인용 — 주+보조 의무 · 하한 L1~L3 · **red-first R-a~R-d는 높음 stakes 한정** · 소유 3축 · 데이터 특칙 · **픽스처 골격 = TS-4 판정 착지**) · **§5 PR·리뷰**(브랜치·커밋 단위 · **머지 게이트는 E2 §3.0 인용**(재정의 금지) · **구조·불변식 우선 렌즈** · AI attribution 금지 · push·발행 확인) · ★ **§6 도구 확정** — E2 §4.1 판정 기준 대조로 **JUnit5(TL-1)·Testcontainers MySQL(TL-2) 확정**(C-08 형식: 요구·계약·도입 조건·되돌리는 조건), ★ **계약 계층은 부분 충족**(규격 표의 기계 판독 원천 부재 → DV-1), ④ 인수·탐색 프로세스 제어는 미지목, 후보 7종은 **E2 도입 조건 인용으로 보류 유지** · **§7 분포 집계 + 리뷰 전용 12건의 승격 조건 + 잠정 리뷰 8건**(그중 5건이 아키텍처 테스트 도구 하나를 기다린다) · 수치·설정값 확정 **0건**(전부 `[구현 검증]`) |
| v1.1 | 2026-08-06 | **E3 듀얼 리뷰 반영** — ★ **컴파일 과대평가 5건 재배정**(KO-3·KO-4·MO-1·MO-3·ER-2: *"nullable 선언·`!!`·새 `Long` 금액·역방향 의존 한 줄·오류 코드 리터럴은 **전부 컴파일된다**"* → 주 강제 수단을 정적 검사·의존 그래프 단언·리뷰로 내리고 컴파일은 보조로 표기) · **KO-2 별칭 정직화**(`List` 반환 ≠ 불변 → 방어 복사·영속 컬렉션 + 변경 감지) · ★ **KO-6 신설**(값 타입 **import 경로 유일 창구** — ADR-025 CL-2 ⑴ 인용. **규칙 32 → 33건**) · **MO-3에 ADR-002 §5 트리 예외 반영**(M-6 읽기 · **M-7 `core:ops(port)`** · `core:auth → banking/payment(port)`) · **MO-5 허용 outbound 닫힌 목록 + 런타임 경계 테스트 병기** · **MO-6 ⑶ 구조/의미 분리** · **ER-3을 K-1 원문 정합으로**(사람 3단 — **멱등 단락 없음** · 시스템 4단 · 채널 인가는 ① 앞) + 멱등 소유(MO-7)와의 관계 1행 · **ER-4 공허 green 방지**(placeholder 제외·대상 건수 출력·잠정 표기) · **TE-1·TE-2·TE-4·TE-5·TE-6·TL-3의 스키마/구조 검사 부분 분리**(판단만 리뷰) · **TE-3 ↔ 변이 테스트 승격 경로** · **PR-2 혼합 커밋 즉시 자동화**(*"2회 관측 후"* 삭제 — D3) · ★ **PR-3을 E2 §3.0·§3.1 원문 인용으로 교체**(*"탐색 실행은 머지를 막지 않는다"* ↔ *"만료된 판정은 그 자산에 닿는 변경의 머지를 막는다"* 구분 복원) · ★ **TL-2 판정 강등 = 부분 충족/확정 보류**(⑵ 조건부·⑶ 미측정 — E2 *"테스트마다 격리된 인스턴스"* 를 하한으로 명시) + **R1을 「측정 전 상한 선언」으로 재구성**(예산 행이 `[미확정]`이라 초과 판정이 불가능했다) · **TL-1 R1 인과 정정**(원천 부재는 DV-1이지 실행기 결함이 아니다) · ★ **§7.1을 「현재 / 도구 확정 후」 2열로 재작성**(현재 리뷰 **25/33** — 잠정 14 + 리뷰 전용 11 · 아키텍처 테스트와 테스트 게이트 칸 분리 · 사다리 5칸) · **§7.2 ↔ §7.3 집합 분리**(PR-1 이중 등재 해소) · **§7.3 재산정**(KO-5·ER-4는 린트 대기 — 아키텍처 테스트 대기는 **6건**) · ★ **§7.4 메타 규칙 신설**(PR-4·TL-3·MO-7 = D1 명시 예외) · ★ **§9 관측 대장 신설**(*"N회 누적"* 조건의 유일 창구 — OB-1~5 · 현재 0행) · **DV-4 범위 확대**(6건이 대기 — 조기 확정 대상) · **DV-5 닫음**(README 문서 지도 등재) |
