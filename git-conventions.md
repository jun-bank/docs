# 깃 협업 규약 — 커밋·브랜치·이슈·PR·릴리스 (내용 정본)

- 작성일: 2026-08-07
- 상태: 검토대기
- 입력(근거): `reference/writing/evidence-10-git-conventions.md`(리서치 원자료 r5 — 8축 원문 fetch) · `dev-conventions.md` **PR-1~PR-8** · `study/project-workflow/writing-methodology.md`(서식) · `architecture/test-strategy.md`(머지 게이트)
- 적용 범위: jun-bank org의 **모든 리포**(`docs` + 서비스 리포 + 공통 라이브러리) — 리포별 `.github/` 파일이 이 문서의 실행 형태다.

> ★ **이 문서는 `dev-conventions.md`의 상세 정본이다.** PR-7(커밋 메시지)·PR-8(마일스톤 → 이슈 → 브랜치 → PR)이 **무엇을 하라고** 적었고, 이 문서가 **어떤 모양이어야 하는가**를 적는다. 두 문서가 갈리면 **규칙의 존재는 dev-conventions가, 규칙의 형식은 이 문서가** 이긴다. ⚠️ **PR-7·PR-8을 이 문서가 다시 쓰지 않는다** — 인용하고 확장할 뿐이다.
> ⚠️ **머지 게이트는 `architecture/test-strategy.md` §3.0이 정본**이고 dev-conventions **PR-3**이 인용한다. 이 문서는 그것을 **PR 본문의 칸으로 내리는 것**만 한다(재정의 금지).

## 근거 라벨

이 문서의 규칙마다 근거의 종류를 표시한다(`writing-methodology.md` 라벨 체계와 같다).

- **[기관]** — 명세 원문·공식 문서·정평 있는 스타일 가이드가 그렇게 규정한다.
- **[실증]** — 실험·측정 결과가 있다.
- **[결정]** — 이 프로젝트가 정했다. 외부 근거가 없거나, 근거가 갈려 우리가 하나를 골랐다.
- **[구현 검증]** — 문서만 읽어서는 틀렸는지 알 수 없어 구현 접촉으로 판정을 미룬 것. 중앙 대장 `implementation-verification.md`에 같은 커밋으로 등재한다.

## 강제 수단 표기

`dev-conventions.md` §0의 강제 사다리를 그대로 쓴다 — **컴파일·빌드 > 아키텍처 테스트 > 테스트 게이트 > 플랫폼 강제 > 린트·자동 검사 > 리뷰**. ★ **잠정 리뷰** = 원래 위 칸이 막아야 하는데 설정·도구가 아직 없어 지금은 사람이 보는 것(§9가 전량 나열한다).

---

## 1. 유형 어휘 — 한 벌만 쓴다

> ★ **리서치가 찾은 가장 큰 결함이 여기다**: 유형을 말하는 어휘가 **세 벌**이었다 — 커밋 type(`feat`·`fix`…) · 이슈 라벨(`feature`·`bug`…) · PR 템플릿의 「변경 유형」(`NEW FEATURE`·`FIX`…). 세 벌이면 **같은 변경이 세 이름으로 불리고**, 릴리스 노트가 어느 축을 목차로 삼을지 정할 수 없다.

| # | 규칙 | 근거 | 강제 수단 | 비고 |
|---|---|---|---|---|
| **GC-1** | ★ **type 어휘는 여덟 개 한 벌이다** — `feat` · `fix` · `perf` · `docs` · `refactor` · `test` · `chore` · `build`. 이 어휘가 **커밋 type이자 이슈·PR 라벨(`type:*`)이자 릴리스 노트의 목차 축**이다. 세 자리에서 **같은 단어**를 쓴다 | **[기관]** Conventional Commits 규칙 14(*"`feat`/`fix` 외 type 사용 가능 — 명세는 목록을 고정하지 않는다"*) · Angular 8종 · commitlint `config-conventional` 11종 **[결정]** 8종의 선택 | **린트·자동 검사**(commitlint `type-enum` 오버라이드 + 라벨 파일 GC-23) ⚠️ *현재 **잠정 리뷰*** | ★ **`perf` 추가 판정 = 추가한다.** 기존 이슈 템플릿에 「성능 개선」이 있는데 커밋 어휘에 대응이 없어 **축이 어긋나 있었다**(성능 개선 이슈의 커밋이 `chore`로 들어간다 — r5 축1.2). Angular 8종·commitlint 11종 **양쪽 다 `perf`를 갖는다.** ⚠️ **`ci`·`style`·`revert`는 넣지 않는다**: `ci`는 파이프라인 변경이므로 `build`에 흡수(어휘를 늘리면 판정이 늘어난다) · `style`은 포매터가 소유하므로 사람이 분류할 일이 없다(dev-conventions **KO-1** — *"사람이 논쟁하지 않는다"*) · `revert`는 type이 아니라 **형식**으로 처리한다(GC-9) |
| **GC-2** | ★ **축 매핑은 아래 표가 정본이다** — type 하나가 라벨·릴리스 노트 카테고리·SemVer 증가로 어떻게 이어지는지 | **[기관]** SemVer FAQ(`fix`→PATCH·`feat`→MINOR·BREAKING→MAJOR) · GitHub `release.yml`(라벨이 카테고리) · Keep a Changelog 6종 | **플랫폼 강제**(`.github/release.yml` — GitHub이 라벨로 노트를 만든다) ⚠️ *현재 **잠정 리뷰**(파일 미작성)* | ⚠️ ★ **Keep a Changelog 6종과 type은 일대일이 아니다** — `refactor`·`test`·`chore`·`build`는 **갈 곳이 없다**(r5 축7.4). 그래서 **릴리스 노트에서 제외**한다(사용자가 읽을 변경이 아니다). 제외를 적지 않으면 노트가 커밋 로그 덤프가 된다(Keep a Changelog가 지목한 나쁜 관행 1번) |
| **GC-3** | ★ **PR 본문에 「변경 유형」 칸을 두지 않는다.** 유형의 유일한 표현은 **라벨**이다 — 본문 칸은 라벨과 중복되면서 **세 번째 어휘를 만든다** | **[결정]**(r5 축4.4 — 어휘 3벌의 발생 지점이 이 칸이었다) | **리뷰**(PR 템플릿에 칸이 없는 것이 이 규칙의 실행) | 같은 이유로 **이슈 제목 접두**(`[Feature]`)도 쓰지 않는다(GC-17) |

### 1.1 축 매핑 표

| 커밋 type | 이슈·PR 라벨 | 릴리스 노트 카테고리 | SemVer |
|---|---|---|---|
| `feat` | `type:feat` | Added | MINOR |
| `fix` | `type:fix` | Fixed | PATCH |
| `perf` | `type:perf` | Performance | PATCH |
| `docs` | `type:docs` | 제외 | 없음 |
| `refactor` | `type:refactor` | 제외 | 없음 |
| `test` | `type:test` | 제외 | 없음 |
| `chore` | `type:chore` | 제외 | 없음 |
| `build` | `type:build` | 제외 | 없음 |
| (type 무관 + `!`/`BREAKING CHANGE`) | `breaking` | Breaking Changes (최상단) | MAJOR |

> ⚠️ **`revert`는 이 표에 없다** — 커밋 `type-enum`에만 존재하는 **어휘 1벌의 유일한 예외**다(GC-9). 되돌리기는 이슈 유형도, 릴리스 노트 항목도 아니다. 예외를 적지 않으면 `git revert`가 만든 커밋이 형식 검사에서 걸린다.

---

## 2. 커밋 메시지

> ★ **PR-7이 정본이고 이 절은 그 형식 세부다.** PR-7이 정한 것: Conventional Commits · `type(scope): 제목` + 빈 줄 + 본문 · 이모지 금지.

| # | 규칙 | 근거 | 강제 수단 | 비고 |
|---|---|---|---|---|
| **GC-4** | **형식 = `type(scope): 제목` + 빈 줄 + 본문 + (빈 줄 + footer).** scope는 **괄호로 감싼 명사** — 코드베이스의 한 구역(모듈·컨텍스트). 문서 전용 변경은 scope를 생략한다 | **[기관]** Conventional Commits 규칙 1·4·5·6·8 · Angular | **린트·자동 검사**(commitlint) ⚠️ *현재 **잠정 리뷰*** | scope 어휘는 **ADR-002 §5 모듈 트리**를 따른다(새 어휘를 만들지 않는다) |
| **GC-5** | ★ **제목: 소문자 시작 · 마침표 금지 · 명령형 · header 전체 100자 이내.** 명령형 시험 — *"If applied, this commit will ___"* 이 자연스럽게 읽혀야 한다 | **[기관]** commitlint `subject-case`·`subject-full-stop`·`header-max-length` 100 · Angular · cbeams 규칙 5(명령형) | **린트·자동 검사**(commitlint) ⚠️ *현재 **잠정 리뷰*** | ★ **충돌 판정 2건**(§10 참조): ⑴ **대소문자** — cbeams 규칙 3(첫 글자 대문자)과 commitlint `subject-case`(대문자 계열 금지)는 **정면 충돌**한다. **소문자를 택한다** — 기계 강제가 걸리는 쪽이고, 둘을 함께 인용하면 규약이 자기모순이 된다 ⑵ **길이** — Tim Pope/cbeams **50자** vs commitlint/Angular **100자**. **100자를 택한다** — `type(scope): ` 접두가 자리를 먼저 먹으므로 50자는 사실상 불가하고, 50은 도구 기본값이 아니라 별도 설정이다. ⚠️ **단 50/72의 근거는 살아 있다**(`--oneline`·`shortlog`·GitHub 목록에서 잘린다) — **앞 72자 안에 요지를 담는다**(상한이 아니라 리뷰 신호) |
| **GC-6** | ★ **본문은 `docs` 외 필수.** *무엇을·왜*를 적고 *어떻게*는 코드가 설명한다. 줄 길이 100자 이내 | **[기관]** Angular(*"`docs` type 외에는 필수, 최소 20자"*) · Linus/Subsurface(*"explain **why**, as opposed to **what** you're doing"*) · Google eng-practices · commitlint `body-max-line-length` 100 | **린트·자동 검사**(본문 존재·줄 길이) + **리뷰**(*왜*가 있나) ⚠️ *현재 **잠정 리뷰*** | ⚠️ *"본문에 **왜**가 있나"* 는 기계가 못 본다 — 존재·길이만 자동이고 **내용은 리뷰 잔류**다 |
| **GC-7** | **파괴적 변경은 `type(scope)!: 제목` 또는 footer `BREAKING CHANGE: <설명>`(대문자)으로 표시한다.** MAJOR를 올린다 | **[기관]** Conventional Commits 규칙 11~13·15 · SemVer 규칙 8 | **린트·자동 검사** ⚠️ *현재 **잠정 리뷰*** | `!`를 쓰면 footer의 `BREAKING CHANGE:`를 생략할 수 있고, 그때는 **제목이 파괴적 변경의 설명**이 된다 |
| **GC-8** | ★ **이모지·AI attribution 금지** — 커밋·이슈·PR·릴리스 노트 **모든 발행 본문**에. 검증 과정의 출처(리뷰어·교차 검증 도구)도 커밋·주석에 적지 않는다 | **[기관]** gitmoji는 **이모지가 첫 토큰**이라 Conventional Commits 규칙 1(type 접두 MUST)과 **형식이 양립하지 않고** 표준 파서(`type-enum`)를 통과하지 못한다 **[결정]** dev-conventions **PR-5**·**PR-7** | **린트·자동 검사**(커밋·발행 훅) | ★ **금지 근거를 취향이 아니라 「기계 파싱 표준과 비양립」으로 적는다** — 취향으로 적으면 다음 사람이 취향으로 되돌린다. ⚠️ gitmoji README에는 단점이 **한 줄도 없다**(채택 판단에 필요한 반대 근거를 스스로 제공하지 않는 홍보 문서다 — r5 축1.7). ⚠️ 훅이 못 보는 표면(파일 경유 본문·일부 발행 명령)은 **절차 준수**다 |
| **GC-9** | **되돌리기 = `revert: <원래 header>` + 본문에 `This reverts commit <SHA>` + 사유.** `revert`는 커밋 `type-enum`에만 등재하고 **라벨·릴리스 노트 축에는 두지 않는다** | **[기관]** Angular revert 규약 · commitlint `type-enum`에 `revert` 포함 | **린트·자동 검사** ⚠️ *현재 **잠정 리뷰*** | ⚠️ ★ **이 예외를 적지 않으면 `git revert`가 자동 생성한 메시지가 형식 검사에 걸린다** — 어휘 1벌 원칙(GC-1)의 명시 예외 1건이다 |
| **GC-9b** | ★ **산출물 언어 = 한국어.** 커밋 메시지·코드 주석·이슈·PR·문서를 한국어로 쓴다. ⚠️ **예외: 코드 식별자**(변수·함수·타입·패키지명)는 영어 — 언어 생태계·라이브러리와 섞이므로 관례를 따른다. type·scope 키워드(`feat`·`fix` 등)도 규약 어휘라 영어 | **[결정]**(2026-08-07 사용자 — 문서·이슈가 한국어인데 주석·커밋만 영어라 일관성 붕괴) | **리뷰**(언어는 기계가 판정 못 함 — 잠정) | ★ 이유: 이 프로젝트의 사고·근거가 한국어로 남는데 코드 층만 영어면 **같은 사람이 두 언어를 오간다**(맥락 전환 비용). 식별자를 제외하는 이유 = Go `error`·Kotlin `sealed` 같은 키워드와 한글이 한 줄에 섞이면 오히려 읽기 나쁘다 |

---

## 3. 브랜치 모델

> ★ **판정: trunk 근접 — 영구 브랜치 하나 + 이슈 단위 단명 브랜치.** git-flow를 채택하지 않는다.

| # | 규칙 | 근거 | 강제 수단 | 비고 |
|---|---|---|---|---|
| **GC-10** | ★ **영구 브랜치는 `main` 하나다.** `develop`·`release-*`·`hotfix-*` 상설 브랜치를 두지 않는다 | **[기관]** nvie 본인의 2020 주의문(*지속 배포 웹 앱에는 "a much simpler workflow (like GitHub flow)"*) · Trunk-Based Development(*"**resist any pressure to create other long-lived development branches**"*) · endofline(master/develop 이중화는 잉여 — master 커밋이 곧 태그된 릴리스라면 develop이 이미 모든 정보를 갖는다) | **플랫폼 강제**(브랜치 보호 — 미설정) ⚠️ *현재 **잠정 리뷰*** | ★ **nvie가 git-flow를 여전히 권하는 조건은 「명시적으로 버전이 매겨지고 여러 버전을 동시에 지원」**이다. 우리 서비스 리포는 그 조건이 아니다. ⚠️ **공통 라이브러리는 앞으로 그 조건에 가까워질 수 있다**(다중 버전 동시 지원) — 그때 **release 브랜치를 just-in-time으로 잘라 하드닝 후 삭제**한다(TBD 방식). 상설 브랜치를 세우는 것이 아니다 |
| **GC-11** | ★ **브랜치 이름 = `type/issue-N-slug`** — `type`은 GC-1 어휘, `N`은 이슈 번호, `slug`는 소문자·하이픈(예 `feat/issue-42-deposit-idempotency`). ★ **type과 같은 이름의 브랜치를 만들지 않는다**(`feat` 브랜치가 있으면 `feat/x`를 만들 수 없다) | **[기관]** `git check-ref-format`(★ **`foo`와 `foo/bar`는 공존 불가** · 공백·`~`·`^`·`:`·`?`·`*`·`[`·`..`·후행 `.`·`@{` 금지) **[결정]** 접두 스킴 자체(GitHub Flow 공식 문서는 접두를 요구하지 않는다) | **린트·자동 검사**(브랜치명 CI 검사) ⚠️ *현재 **잠정 리뷰*** | ★ **이슈 번호를 이름에 박는 이유** = PR-8(이슈가 PR의 근거)이 **브랜치 단계에서 이미 깨지지 않게** 하려는 것. 이름에 번호가 없으면 이슈 없는 브랜치가 조용히 생긴다 |
| **GC-12** | ★ **브랜치 수명 상한 = 2 영업일.** 초과하면 브랜치를 늘리지 말고 **슬라이스를 쪼갠다**. 브랜치당 작업자 1명. **머지 후 즉시 삭제** | **[기관]** Trunk-Based Development(*"a couple of days"* — 2일 초과 시 장수 브랜치가 될 위험 · 브랜치당 개발자 1명) · Fowler(*feature 브랜치 "less than a day"* · Integration Frequency가 핵심 변수) · GitHub Flow(머지 후 브랜치 삭제 = 완료 신호 + 재사용 사고 방지) **[결정]** 2일 채택 | **리뷰** | ★ **1일(Fowler)이 아니라 2일을 택한 이유**: Fowler의 1일은 *"everyone commits to the mainline every day"* 라는 팀 CI 표준에서 나온 값이고, 우리는 **1인 + AI**라 하루 단위 통합이 강제될 팀 압력이 없다. TBD의 2일은 **장수 브랜치로 넘어가는 경계값**으로 제시된 것이라 상한으로 더 맞다. ⚠️ **상한을 적지 않으면 이슈 단위 브랜치가 조용히 git-flow의 feature 브랜치로 미끄러진다**(r5 축2.7) |
| **GC-13** | ★ **머지 방식 = rebase and merge(기본).** squash는 **예외**(단계 구분이 없는 단발 수정·fixup만 있는 PR). merge commit은 쓰지 않는다 | **[기관]** GitHub 머지 3종 · endofline(**선형 히스토리 강제**) **[결정]** rebase 채택 | **플랫폼 강제**(리포 설정에서 허용 머지 방식 제한) ⚠️ *현재 **잠정 리뷰*** | ★ **squash를 기본으로 두면 dev-conventions PR-2(*"여러 단계 작업은 단계마다 커밋"*)가 머지 순간에 소멸한다** — 단계 커밋이 하나로 뭉개지면 롤백 단위도 하나가 된다. rebase는 **선형 히스토리와 단계 커밋을 동시에** 남긴다. ⚠️ **대가**: rebase 후 각 커밋이 green이라는 보장은 CI가 PR 단위로만 보므로 없다 — PR-2의 *"의미 단위"* 가 그 몫을 진다 |
| **GC-14** | **`main` 직접 push 금지** — dev-conventions **PR-1** 인용(재정의 금지) | **[결정]** PR-1 | **플랫폼 강제**(브랜치 보호) ⚠️ *현재 **잠정 리뷰*** | 설정값은 **IV-21**(`operations/cicd.md` §3.2 — 생성 직후 ⑴만 필수 → CI 착지 후 ⑵ → CDV-8 판정 후 ⑶) |

---

## 4. 이슈·PR

| # | 규칙 | 근거 | 강제 수단 | 비고 |
|---|---|---|---|---|
| **GC-15** | ★ **빈 이슈를 허용하지 않는다** — `.github/ISSUE_TEMPLATE/config.yml`에 **`blank_issues_enabled: false`** | **[기관]** GitHub 공식(빈 이슈 차단) · VS Code 선례(빈 이슈 차단 + contact_links로 질문 우회) | **플랫폼 강제**(config.yml — GitHub이 실행) ⚠️ *현재 **잠정 리뷰**(현행 파일이 `true`)* | ⚠️ ★ **빈 이슈가 열려 있으면 템플릿 전체가 우회 가능하다** — 필수 칸도, **템플릿이 붙이는 라벨도** 함께 우회된다(라벨이 릴리스 노트 축이므로 GC-30까지 샌다). ★ 이것이 리서치 상위 개선안 중 **가장 값싼 한 줄**이다 |
| **GC-16** | ★ **이슈 필수 칸.** 공통 4칸 = **목표 · 정본 근거 · 완료 조건 · 검증 방법**. 버그는 추가로 **재현 절차 · 기대 vs 실제 · 환경/버전** | **[기관]** Kubernetes 버그 폼 필수 4칸(무슨 일이 / 무엇을 기대 / **최소·정확한 재현** / **버전**) · Rust(기대·실제·Meta) — 3사 공통축 = **재현 · 기대 vs 실제 · 환경/버전** **[결정]** 「정본 근거」 칸(우리 고유 — 정본 ID와 한 구절) | **리뷰** | ★ **은행 도메인이라 「환경」이 특히 크다** — 재현 데이터·계좌 상태·영업일·마감 전후가 결정적인데 현행 버그 템플릿에 **환경 칸이 없다**. ⚠️ 다만 md 템플릿에서 *"필수"* 는 **강제되지 않는다**(GC-18) |
| **GC-17** | ★ **이슈 제목에 유형 접두(`[Feature]` 류)를 붙이지 않는다.** 유형은 라벨이 나타낸다 | **[기관]** GitHub 사례들은 제목 접두 대신 라벨·이슈 타입을 쓴다 **[결정]** | **리뷰** | 접두는 라벨과 **정보가 중복**되고, 중복된 두 자리는 언젠가 갈린다(제목만 고치고 라벨은 안 고치는 날이 온다) |
| **GC-18** | ★ **v1에서 필수 칸의 기계 강제는 성립하지 않는다 — 그것을 자백한다.** md 템플릿은 **사용자가 지울 수 있는 프리필 텍스트**이고, yml form의 `required: true`는 ⚠️ **public 리포에서만 동작**한다. 그래서 GC-16은 **리뷰**가 막는다 | **[기관]** GitHub 공식(md = 프리필 / form = 필드 · `required`는 public 전용) | **리뷰** | ★ **[구현 검증] IV-27** — 우리 리포의 공개 설정과 `required` 실동작. ⚠️ **판정되기 전까지 yml form으로 전환하지 않는다** — private에서 form으로 바꾸면 *"필수라고 적혀 있는데 강제되지 않는" 상태*가 되어 **지금(md)보다 나쁘다**(강제되는 줄 알게 된다). 전환 조건 = IV-27이 `required` 동작을 확인할 때 |
| **GC-19** | ★ **PR 본문 5칸** = ⑴ **결론**(한 문장) ⑵ **관련 이슈**(`Closes #N` — 독립 칸) ⑶ **무엇을·왜** ⑷ **검증**(머지 게이트 ①~⑥ 체크) ⑸ **리뷰 포인트** | **[기관]** GitHub(이슈 링크 키워드 9종 — `close(s|d)`·`fix(es|ed)`·`resolve(s|d)`) · Kubernetes PR 템플릿(관련 이슈·릴리스 노트 블록) · Google eng-practices(첫 줄 = 명령형 완전 문장, CL을 안 봐도 이해되어야) **[결정]** 검증 칸에 안전선을 내리는 것 | **리뷰** | ★ **리서치가 지목한 결함 2건을 여기서 고친다**: ⑴ 이슈 링크가 **본문 예시에 묻혀 있어** 빠져도 티가 안 났다 → **독립 칸** ⑵ dev-conventions **PR-3의 안전선 ①~⑥이 규약 문서에만 있고 PR 본문에 없어 리뷰 시점에 보이지 않았다** → 체크 항목으로 내린다(⚠️ **재정의가 아니라 인용**이다 — 문면은 E2 §3.1이 소유). ⚠️ ★ **PR에는 issue form 같은 구조화 폼이 없다** — 필수 칸 강제는 CI(본문 검사)로만 가능하고 v1에는 없다. ⚠️ 키워드는 **PR이 기본 브랜치를 대상으로 할 때만** 해석된다 |
| **GC-20** | ★ **PR 크기: 400 LOC 또는 파일 20개를 넘으면 분할 신호**(상한이 아니라 신호 — 넘으면 슬라이스를 되돌아본다) | **[실증]** Cisco/SmartBear — 한 번에 **200~400 LOC 이하**, 세션 60분 이하, **500 LOC/시간 초과 시 결함 밀도 유의하게 하락**, 200~400 LOC를 60~90분에 보면 **결함 발견율 70~90%** · Google(~100줄 적정, ~1000줄 과대, ★ *"한 파일 200줄은 괜찮아도 **50개 파일에 흩어진 200줄은 과대**"*) **[결정]** 파일 20개 수치 | **리뷰** | ★ **줄 수만 세지 않고 파일 확산을 함께 보는 것이 Google의 요지**다. ⚠️ **1인 + AI라도 이 수치를 낮추지 않는다** — 실증의 근거는 팀 규모가 아니라 **리뷰어의 인지 한계**이고, 우리 리뷰어(사람 + 워커)도 같은 한계를 갖는다. 예외 2가지: **파일 삭제**(1줄로 계산) · 신뢰할 수 있는 도구의 **자동 리팩토링** |
| **GC-21** | ★ **어휘는 「PR」로 통일한다.** 우리 리포는 GitHub이므로 「MR」을 쓰지 않는다 | **[결정]**(r5 축4.5 — 파일은 GitHub에 있는데 규약 문서는 "MR"이라 부르고 있었다) | **리뷰**(검산 스크립트로 표기 검사 가능) ⚠️ *현재 **잠정 리뷰*** | ★ **dev-conventions PR-8·CONTRIBUTING이 「MR」을 쓴다 — 개정 제안 대상**(§10). ⚠️ GitLab로 옮기면 이 규칙 하나만 뒤집으면 된다(그래서 어휘를 한 곳에 모은다) |

---

## 5. 라벨 체계

| # | 규칙 | 근거 | 강제 수단 | 비고 |
|---|---|---|---|---|
| **GC-22** | ★ **3축을 유지한다** — ⑴ **type** 8종(GC-1) ⑵ **stakes** 3종(`stakes:high`·`stakes:mid`·`stakes:low`) ⑶ **특수** 4종(`iv`·`spec-gap`·`blocked`·`breaking`). ★ **priority 축은 두지 않는다** | **[기관]** 접두 3축 스킴(Status/Type/Priority) · ⚠️ 그 저자 본인이 priority에 회의적(*"도입하면 모든 이슈가 critical이 된다"*) · Kubernetes priority 5단계는 *"누가 무엇을 보장하는가"* 로 정의되어야 작동한다 **[결정]** stakes 축(우리 고유 — 검증 강도가 여기 비례) | **리뷰**(★ 축 구성의 타당성은 판단이다 — 라벨 **집합의 동기화**는 GC-23이 소유한다) | ★ **재판정 결과 = 3축 유지, 단 근거를 바꾼다.** ⑴ **priority 미도입**: 1인 프로젝트에서 우선순위를 표현하는 것은 **마일스톤**이다(GC-26) — 라벨로 중복하면 두 자리가 갈린다. Kubernetes식 priority가 작동하는 이유는 *"팀 리더가 진행을 보장할 책임"* 같은 **책임 정의**가 붙어서인데, 1인에게는 그 정의가 공허하다 ⑵ **status 축은 `blocked` 1종으로만** — 「진행 중」은 assignee와 PR 연결이 이미 표현한다. ⑶ ★ **stakes는 외부 근거가 없는 우리 축**이다(`architecture/test-strategy.md`가 검증 강도를 여기 건다) — [결정]으로 표시하고 기관 근거를 붙이지 않는다 |
| **GC-23** | ★ **라벨은 파일로 관리한다** — 리포마다 `.github/labels.yml`(이름·색·설명), 5개 리포가 **같은 집합**. 파일과 리포 라벨의 diff 검사를 CI가 본다 | **[기관]** GitHub 공식 — ⚠️ ★ **템플릿의 `labels:`는 리포에 이미 존재하지 않는 라벨을 자동으로 만들어 주지 않는다**(무음 실패) · 조직 기본 라벨 커스터마이즈는 **신규 리포 생성 시점의 초기값**이다 | **린트·자동 검사**(파일 ↔ 리포 라벨 diff) ⚠️ *현재 **잠정 리뷰**(파일 없음)* | ★ **현재 라벨은 파일로 관리되지 않는다 — 리포 5개 = 수동 5회 동기화**이고, 어긋난 것을 아무도 알지 못한다. ⚠️ ★ **무음 실패 경로가 실재한다**: 템플릿이 `type:fix`를 붙이려는데 그 라벨이 그 리포에 없으면 **이슈는 라벨 없이 열리고 에러도 나지 않는다** → 릴리스 노트(GC-30)에서 그 항목이 통째로 빠진다. 동기화 도구 지목 = **[구현 검증] IV-28** |
| **GC-24** | ★ **GitHub Issue Types(조직 1급 필드)를 지금 도입하지 않는다.** 도입 조건 = **labels.yml 동기화가 어긋난 사건이 2회 누적**될 때(관측 대장 = dev-conventions §9) | **[기관]** Issue Types는 조직 단위 단일 정의(기본 task/bug/feature · 조직당 25종)라 **리포 5개 동기화를 라벨 없이 해결**한다 **[결정]** 미도입 | **리뷰**(메타 — 도입 판정 절차) | ★ **도입하고 싶은 이유와 못 하는 이유가 둘 다 분명하다.** 이유: 동기화 문제를 근본에서 없앤다. ⚠️ **못 하는 이유**: 릴리스 노트가 **PR 라벨** 축에 걸려 있어(GC-30 · `release.yml`은 라벨만 읽는다) type 축을 라벨에서 빼면 **노트가 무너진다.** 즉 Issue Types는 라벨의 **대체가 아니라 추가**가 되고, 추가하면 어휘가 다시 두 벌이 된다(GC-1 위반). 조건이 차면 *"라벨 + Issue Types 병존이 어휘 2벌인가"* 부터 판정한다 |
| **GC-25** | ★ **리뷰 코멘트는 Conventional Comments 형식으로 쓴다** — `<label> [decorations]: <subject>`. 라벨 9종(`praise`·`nitpick`·`suggestion`·`issue`·`todo`·`question`·`thought`·`chore`·`note`) · 데코레이션 `(blocking)`/`(non-blocking)`/`(if-minor)`. ★ **`nitpick`·`note`·`thought`는 본질상 non-blocking**이다 | **[기관]** Conventional Comments | **리뷰**(★ 메타 규칙 — 리뷰라는 수단 자체의 형식) | ★ **이 규칙이 dev-conventions PR-4를 규칙에서 형식으로 내린다.** PR-4는 *"표기·포맷·문구 동기화는 리뷰가 보지 않는다"*(실측: 그 유형이 지적의 ~60%)인데, 지금은 **선언일 뿐 코멘트에 표시가 없다.** `nitpick`을 라벨로 붙이고 non-blocking을 강제하면 **그 60%가 머지를 막지 않는 것이 형식으로 보인다.** ⚠️ 이슈 라벨(GC-22)과는 **다른 층**이다(이슈에 붙는 것이 아니라 코멘트에 쓰는 접두) |

---

## 6. 마일스톤

| # | 규칙 | 근거 | 강제 수단 | 비고 |
|---|---|---|---|---|
| **GC-26** | ★ **마일스톤 = 리포별 · 슬라이스 단위**(`S0 기반`·`S1 UC-03` 형태) + **마감일 칸을 채운다** | **[기관]** GitHub 공식 — 마일스톤은 *"groups of issues or PRs **in a repository**"*(리포 범위) · 필드 = 제목·마감일·설명 · 완료 퍼센트 자동 **[결정]** 슬라이스 대응(dev-conventions **PR-8** 인용) | **리뷰** | ★ **우선순위를 라벨이 아니라 여기서 표현한다**(GC-22) — 마일스톤은 **드래그 우선순위 정렬**을 제공한다. 마감일을 비우면 완료 퍼센트만 남고 *"언제까지"* 가 사라진다 |
| **GC-27** | ★ **리포를 가로지르는 작업은 마일스톤으로 표현되지 않는다** — `common` 변경 → 서비스 3개 반영 같은 하나의 슬라이스는 **마일스톤 N개로 쪼개져 따로 닫힌다.** 조정 축이 필요하면 **조직 Project 1개**(iteration 필드)를 세운다. 도입 조건 = **2개 이상 리포에 걸친 슬라이스가 처음 열릴 때** | **[기관]** 마일스톤 = 리포 범위 / Projects = 조직·다중 리포·다축(상태·우선순위·이터레이션) · ⚠️ 공식 문서는 둘의 용도 구분을 명시하지 않는다(커스텀 필드가 *"내장 메타데이터(assignee, milestone, labels)를 넘어서"* 확장한다고 적어 **마일스톤을 Projects의 한 필드로 취급**함을 드러낼 뿐) **[결정]** 도입 조건 | **리뷰** | ★ **PR-8의 실제 비용이 여기 있다** — *"리포별 마일스톤으로 규격화"* 는 **리포 5개에 걸친 한 작업을 표현할 수 없다**는 뜻이다. ⚠️ **지금 Project를 만들지 않는 이유**: 아직 단일 리포 슬라이스만 있고, 빈 보드는 유지되지 않는다. ⚠️ 축 6의 마일스톤/Projects 구분표는 **공식 문서 대조에서 도출한 것이지 인용이 아니다**(r5 미확인 공백 3) — 그래서 이 규칙은 [결정]이다 |

---

## 7. 버전·태그·릴리스

> ★ **핵심 발견: 우리에게는 「커밋 → 버전」을 잇는 Gradle 다리가 없다.** PR-7이 Conventional Commits를 채택했지만 **거기서 버전을 도출하는 장치가 없어**, 형식만 지키고 이득(자동 버전·changelog)은 회수하지 못하는 상태다. 이 절은 그 상태를 **자백하고 조건으로 잠근다.**

| # | 규칙 | 근거 | 강제 수단 | 비고 |
|---|---|---|---|---|
| **GC-28** | **SemVer 2.0.0을 따른다.** ★ **공개 API를 선언하는 것이 전제**다(`common`의 공개 표면 = ADR-025의 경계). **1.0.0 전까지는 `0.y.z`** — 그동안은 *"anything MAY change at any time"* 이 규약이다 | **[기관]** SemVer 규칙 1·4·5·6·7·8 | **리뷰** | ⚠️ ★ **규칙 3: 릴리스된 버전의 내용은 변경 불가** — 태그를 옮겨 붙이지 않는다. 바꾸려면 **새 버전**이다 |
| **GC-29** | ★ **태그 = `vX.Y.Z` · annotated(`-a`) · push는 `--follow-tags`** | **[기관]** Pro Git — *"It's generally recommended that you create **annotated** tags"*(태거·날짜·메시지·GPG 검증 가능) · ⚠️ 태그는 `git push`로 **자동 전송되지 않는다** · `v` 접두는 **관례이지 강제가 아니다** **[결정]** `v` 접두 채택 | **린트·자동 검사**(태그 형식 CI 검사) ⚠️ *현재 **잠정 리뷰*** | ★ **리포당 아티팩트 1개를 가정한 이름이다.** 한 리포가 아티팩트를 둘 이상 발행하게 되면 `v1.2.0`으로는 구분되지 않으므로 **접두 스킴(`common-1.2.0` 류)과 manifest 모드를 재판정**한다(그때까지 미리 정하지 않는다) |
| **GC-30** | ★ **릴리스 노트는 `.github/release.yml`로 자동 생성한다** — 카테고리 = **GC-2 매핑 표**, `type:docs`·`type:refactor`·`type:test`·`type:chore`·`type:build`는 `exclude`, 마지막 카테고리에 catch-all `labels: ['*']` | **[기관]** GitHub 자동 릴리스 노트(`changelog.categories[*]`의 `title`·`labels` 필수 · `exclude.labels`/`exclude.authors` · `['*']` = 앞선 카테고리에 안 걸린 나머지 전부) | **플랫폼 강제**(GitHub이 실행) ⚠️ *현재 **잠정 리뷰**(파일 없음)* | ★ **여기서 라벨 체계(§5)와 릴리스(§7)가 물린다** — **PR 라벨이 릴리스 노트의 목차**다. 그래서 ⑴ GC-23(라벨 파일 관리)이 실패하면 노트가 샌다 ⑵ GC-24(Issue Types)가 막힌다 ⑶ catch-all을 두는 이유는 **라벨 없는 PR이 통째로 사라지는 것**을 막기 위해서다(무음 누락 방어) |
| **GC-31** | ★ **버전 자동화(semantic-release·release-please·axion)를 v1에 도입하지 않는다. v1 = 수동 annotated 태그 + `release.yml` 자동 노트.** 도입 조건 = ⑴ **수동 태그의 버전 증가가 실제 변경 유형과 어긋난 것이 2회 관측**되거나 ⑵ **`common` 소비 리포에서 버전 드리프트가 관측**될 때. 그때 후보 하나를 dev-conventions §6.2 형식(요구·계약·도입 조건·**되돌리는 조건**)으로 등재한다 | **[기관]** ⚠️ ★ **release-please에 Gradle 전용 전략이 없다** — `maven` 전략은 `pom.xml`을 갱신하지만 `java` 전략은 **파일을 스스로 갱신하지 않고** `extra-files`로 위치를 직접 지정해야 한다 · ⚠️ **axion-release는 Conventional Commits를 읽지 않는다**(태그 → 버전이지 커밋 → 버전이 아니다) · semantic-release는 Angular 규약 기반 **[결정]** 미도입 · 조건 | **리뷰**(메타 — 도입 판정 절차) | ★ **"둘을 잇는 정평 있는 관습이 존재하는지"가 이번 조사에서 확인되지 않았다**(r5 미확인 공백 5 — WebSearch 예산 소진). ⚠️ **근거 공백 위에 자동화를 세우면 되돌릴 때 비용이 크다.** 그래서 **조건으로 잠근다**: 관측 대장(dev-conventions §9)에 행이 쌓이기 전에는 열지 않는다. ★ **[구현 검증] IV-29** — `java` 전략 + `extra-files`로 Gradle 버전 파일 갱신이 성립하는가(도입 조건 발동 시 첫 판정 입력) |
| **GC-32** | ★ **`CHANGELOG.md` 파일을 두지 않는다** — 릴리스 노트가 정본이다. 단 **Keep a Changelog의 원칙은 노트 본문에 적용**한다: 사람을 위해 쓴다 · 유형별로 묶는다 · **ISO 8601(YYYY-MM-DD)** · 역시간순 · **커밋 로그 덤프 금지**. 도입 조건 = **리포 밖에서 변경 이력을 읽어야 하는 소비자가 생길 때**(`common` 외부 배포) | **[기관]** Keep a Changelog 1.1.0(원칙 7 · 카테고리 6종 · 나쁜 관행 4 — 커밋 로그 덤프·deprecation 미표시·모호한 날짜·일부만 기록) **[결정]** 파일 미도입 | **리뷰** | ★ **파일과 릴리스 노트를 둘 다 유지하면 이력 정본이 두 벌**이 되고, 한쪽만 갱신되는 날이 온다(GC-1과 같은 이유). ⚠️ **Deprecated는 자동 노트에 카테고리가 없다** — deprecation은 **커밋 footer `DEPRECATED:`**(Angular)로 남기고 노트에서는 Breaking Changes 옆에 수기로 적는다(*"deprecation을 표시하지 않는 것"* 이 Keep a Changelog가 지목한 나쁜 관행 2번이다) |

---

## 8. 멀티리포 의존 — `common` 소비

| # | 규칙 | 근거 | 강제 수단 | 비고 |
|---|---|---|---|---|
| **GC-33** | ★ **서비스 리포(앱)는 `common`을 정확 버전으로 고정한다.** 동적 버전(`1.+`·`[1.0,)`·`latest.release`)과 **SNAPSHOT 소비를 금지**한다 | **[기관]** Gradle 공식 경고 — *"Using dynamic versions and changing modules can lead to **unreproducible builds**"* · Renovate 고정 논거(**재현성** — 어느 버전이 깔렸는지 정확히 안다 / **가시성** — 결함 버전이 자동으로 들어오지 않고 **PR로 와서 changelog를 읽고 테스트한 뒤 받는다** / **롤백 용이**) · 권고표 *"앱 = 모든 의존성 고정"* | **린트·자동 검사**(동적 버전·SNAPSHOT 문자열 0건 스캔) ⚠️ *현재 **잠정 리뷰*** | ★ **은행 도메인에서 「가시성」이 결정적이다** — 자금 경로 모듈이 **아무도 모르게 갱신되는 경로**를 열지 않는다. ⚠️ 고정의 대가는 **업그레이드 PR 노이즈**이고, 그것은 Dependabot `groups`(여러 업데이트를 PR 하나로)로 완화한다. ⚠️ **간접 의존에는 무력**하다 — 그 몫은 GC-34가 진다 |
| **GC-34** | ★ **모든 리포에서 dependency locking을 켠다**(락 파일을 커밋한다). 업그레이드는 **Dependabot**(`package-ecosystem: gradle`, weekly, `groups`로 묶음) | **[기관]** Renovate — *"모든 프로젝트: 락 파일 사용"* · Gradle(동적 버전을 쓸 거면 locking을 함께) · Dependabot(`gradle`은 유효한 ecosystem · `groups`의 `patterns`/`update-types`/`dependency-type` · 새 릴리스에 **기본 3일 쿨다운**) | **빌드**(락 불일치 시 빌드 실패) ⚠️ *현재 **잠정 리뷰**(미구성)* | ★ **고정(GC-33)은 직접 의존만 잡고, 간접 의존은 락 파일만 잡는다** — 둘은 대체재가 아니라 **짝**이다. ★ **[구현 검증] IV-30**(Gradle lockfile 구성·5리포 적용) · **IV-31**(Dependabot `versioning-strategy` 값 목록 — ⚠️ r5에서 원문 회수 실패, 미확인 공백 4) |
| **GC-35** | ★ **버전 공유 수단 = 버전 카탈로그 발행.** `common` 리포가 `version-catalog` 플러그인으로 카탈로그를 **Maven 아티팩트로 발행**하고, 소비 리포는 `settings.gradle.kts`에서 `from("...")`로 임포트한다. **`common` 첫 발행과 동시에** 카탈로그도 발행한다 | **[기관]** Gradle 버전 카탈로그(`gradle/libs.versions.toml` 4절 · ★ 리포 간 공유 3방법 중 **⑴ 카탈로그 아티팩트 발행**이 표준 · 임포트 시 특정 버전 오버라이드 가능) | **빌드** ⚠️ *현재 **잠정 리뷰*** | ★ **이것이 없으면 리포 4개가 각자 버전을 적고, 버전 드리프트가 규약 없이 발생한다**(r5 축8.4). 카탈로그는 **리포 5개 구조에 정확히 맞는 수단인데 현재 계획에 없었다.** ⚠️ 카탈로그 자체도 버전이 있는 아티팩트이므로 **GC-33이 그대로 적용**된다(카탈로그를 동적 버전으로 당기지 않는다) |
| **GC-36** | ★ **발행처 = GitHub Packages.** ⚠️ **owner는 반드시 소문자**(대문자 미지원) · 인증은 Actions의 `GITHUB_TOKEN` · ★ **SNAPSHOT을 발행하지 않는다** | **[기관]** GitHub Packages + Gradle(`https://maven.pkg.github.com/OWNER/REPOSITORY` · owner 소문자 · PAT 또는 `GITHUB_TOKEN` · SNAPSHOT 지원됨) **[결정]** SNAPSHOT 미발행 | **빌드**(발행 실패로 드러난다) ⚠️ *현재 **잠정 리뷰*** | ★ **SNAPSHOT을 지원하지만 쓰지 않는 이유** = GC-33(SNAPSHOT 소비 금지)과 짝이다. 발행하지 않으면 **소비할 수도 없다** — 규칙을 두 곳에서 지키게 하지 말고 **원천에서 없앤다.** ⚠️ 그 대가: 통합 확인이 필요하면 **정식 pre-release 버전**(`1.1.0-alpha.1` — SemVer 규칙 9)을 낸다. pre-release는 정식보다 **낮은 우선순위**로 해석되므로 범위 해석에 끼어들지 않는다 |

---

## 9. 강제 수단 분포 — self-check

> ★ **`dev-conventions.md` §7.1과 같은 방식으로 2열로 센다** — 한 열로 세면 *"플랫폼 강제 6건"* 이라고 적어놓고 **그 설정이 오늘 하나도 없다**는 사실이 숨는다. 규칙당 **주 강제 수단 1개**로 센다(병기된 보조는 세지 않는다).

| 강제 수단 (사다리 순) | 현재 | 설정·도구 확정 후 | 확정 후 이동분 |
|---|---|---|---|
| **빌드** | 0 | **3** | GC-34 · GC-35 · GC-36 |
| **플랫폼 강제** | 0 | **6** | GC-2 · GC-10 · GC-13 · GC-14 · GC-15 · GC-30 |
| **린트·자동 검사** | **1** (GC-8) | **12** | + GC-1 · GC-4 · GC-5 · GC-6 · GC-7 · GC-9 · GC-11 · GC-21 · GC-23 · GC-29 · GC-33 |
| **리뷰** | **35** | **15** | GC-3 · GC-12 · GC-16 · GC-17 · GC-18 · GC-19 · GC-20 · GC-22 · GC-24 · GC-25 · GC-26 · GC-27 · GC-28 · GC-31 · GC-32 |
| **계** | **36** | **36** | 잠정 리뷰 **20건** |

검산: 현재 = 1 + 35 = 36 / 확정 후 = 3 + 6 + 12 + 15 = 36 / 이동분 = 36 − 15(리뷰 잔류) − 1(GC-8은 이미 린트) = **20 = 잠정 리뷰 건수**. 절별 규칙 수 = §1 **3** + §2 **6** + §3 **5** + §4 **7** + §5 **4** + §6 **2** + §7 **5** + §8 **4** = **36**. 세 값이 일치한다.

> ⚠️ ★ **오늘 36건 중 35건을 사람이 본다.** 그중 20건은 *"원래 기계가 막아야 하는데 파일·설정이 없어서"* 이고, 그 20건이 기다리는 것은 **네 가지뿐**이다:
> - **리포 `.github/` 파일 4종** — `config.yml`(GC-15) · `labels.yml`(GC-23) · `release.yml`(GC-2·GC-30) · PR/이슈 템플릿. ★ **가장 값싸고 가장 많이 푼다**(플랫폼 강제 4건이 파일 하나씩이다)
> - **저장소 설정 2종** — 브랜치 보호(GC-10·GC-14 — **IV-21**) · 허용 머지 방식(GC-13)
> - **커밋·태그·의존 검사 도구**(GC-1·4~7·9·11·29·33 — commitlint 계열 + CI 스캔 · **IV-28**)
> - **빌드 구성**(GC-34~36 — 락 파일·카탈로그 발행)
>
> ★ **리뷰 잔류 15건은 도구가 서도 사람 몫이다** — 대부분 *"이 내용이 옳은가"*(GC-6 본문의 *왜* · GC-16 이슈 칸의 내용 · GC-19 검증 칸의 실질 · GC-20 크기 판단)이거나 **도입 판정 절차 자체**(GC-24·GC-27·GC-31)다.

---

## 10. 판정한 충돌 — 근거가 갈렸을 때 무엇을 버렸나

> ★ **리서치 부록이 *"규약 작성 시 반드시 판정해야"* 로 남긴 7건의 처리를 한 곳에 모은다.** 판정을 흩어 놓으면 나중에 *"왜 이쪽인가"* 를 다시 논쟁하게 된다.

| 충돌 | A | B | 판정 | 근거 |
|---|---|---|---|---|
| 제목 대소문자 | cbeams 규칙 3(**대문자 시작**) | commitlint `subject-case`(**대문자 계열 금지**) | ★ **B — 소문자** (GC-5) | 기계 강제가 걸리는 쪽. 둘을 함께 인용하면 규약이 자기모순이 된다 |
| 제목 길이 | Tim Pope·cbeams **50자** | commitlint·Angular **100자** | ★ **B — 100자** (GC-5) | `type(scope): ` 접두가 자리를 먹어 50은 사실상 불가. ⚠️ 50/72의 근거(잘린 노출)는 *"앞 72자에 요지"* 로 살린다 |
| type 목록 | Angular 8종(**`chore` 없음**) | commitlint 11종(**`chore` 있음**) | ★ **우리 8종** — 기존 7종 + **`perf`** (GC-1) | 명세는 목록을 고정하지 않는다(CC 규칙 14). `perf`는 이슈 축과 어긋나 있어 추가하고, `ci`·`style`·`revert`는 흡수·위임·형식으로 처리 |
| 유형 어휘 | 커밋 type | 이슈 라벨 + PR 「변경 유형」 | ★ **한 벌로 통일** (GC-1·GC-2·GC-3) | 세 벌 중 **커밋 type을 축으로 삼고** 라벨을 `type:*`로 맞춘다. PR 본문 칸은 **삭제**한다 |
| 브랜치 수명 | git-flow(기능 완성까지) | Fowler **1일** · TBD **2일** | ★ **2일 상한** (GC-12) | Fowler의 1일은 팀 CI 표준값이고 TBD의 2일은 **장수 브랜치 경계값**이라 상한에 더 맞다 |
| 필수 칸 강제 | issue form `required: true` | **public 리포에서만 동작** | ★ **v1은 md 템플릿 + 리뷰**, form 전환은 IV-27 판정 후 (GC-18) | private에서 form을 쓰면 *"필수라 적혀 있는데 강제 안 되는"* 상태가 되어 지금보다 나쁘다 |
| 릴리스 노트 원천 | 커밋(semantic-release) | **PR 라벨**(`release.yml`) | ★ **PR 라벨** (GC-30) | ★ **Gradle 다리 부재**(GC-31) — 커밋에서 버전·노트를 뽑는 정평 있는 Gradle 경로가 확인되지 않았다. 라벨 축은 플랫폼이 **오늘 바로** 실행한다 |
| 머지 방식 (부록 밖 — 이 문서가 추가로 판정) | squash(선형·단순) | rebase(선형 + **커밋 보존**) | ★ **rebase** (GC-13) | squash는 dev-conventions **PR-2**(단계마다 커밋)를 머지 순간에 소멸시킨다 |

---

## 11. dev-conventions 개정 제안 — 별지 (★ 이 문서가 적용하지 않는다)

> ⚠️ ★ **아래는 제안이며 `dev-conventions.md`는 수정되지 않았다.** PR-7·PR-8은 여전히 현행 문면이 정본이고, 채택 여부는 사용자 판정이다. 채택 시 **집계(§7.1)·검산·변경 이력**을 함께 고쳐야 한다(규칙 수가 바뀌지 않는 개정이라도 문면이 바뀐다).

| # | 대상 | 제안 | 왜 |
|---|---|---|---|
| **개정-1** | **PR-7** | type 목록에 **`perf` 추가**(7종 → 8종) + 문면 끝에 포인터 *"형식 세부는 `git-conventions.md` §2가 정본"* | 이슈 템플릿의 「성능 개선」에 대응하는 커밋 type이 없어 **축이 어긋나 있다**(성능 커밋이 `chore`로 들어간다) |
| **개정-2** | **PR-8** | ★ **「MR」 → 「PR」 표기 교체**(3곳) + 포인터 *"브랜치·이슈·PR 형식은 `git-conventions.md` §3·§4가 정본"* | 우리 리포는 GitHub이고 MR은 GitLab 용어다 — 플랫폼과 어휘가 갈려 있다(GC-21) |
| **개정-3** | **PR-1** | 비고에 **브랜치 명명·수명 상한의 정본 포인터**(GC-11·GC-12) 1줄 | PR-1이 *"브랜치 base·이름은 작업 시작 전에 확인"* 이라 적는데 **무엇이 옳은 이름인지**가 어디에도 없었다 |
| **개정-4** | **§6.2 / §8** | 커밋 형식 검사 도구(commitlint 계열)를 **TL-4로 등재**(요구·계약·도입 조건·되돌리는 조건) — 또는 **DV-6**(도구·실행 환경 미확정)으로 §8에 등재 | PR-7이 *"도구 확정 전 잠정 리뷰"* 로 두었는데, ★ **규칙 집합은 이미 존재한다**(`config-conventional` + `type-enum` 오버라이드 1줄). ⚠️ 남은 미확정은 **규칙이 아니라 실행 환경**(commitlint는 Node 도구인데 우리 스택은 Kotlin/Gradle) — **IV-28** |
| **개정-5** | **§7.1 집계** | GC 규칙은 **dev-conventions 35건 계수에 포함하지 않는다**(별도 문서·별도 대장) — 그 사실을 §7.1 머리말에 1줄 | 두 문서가 서로를 세면 검산이 무너진다. ★ **PR-7·PR-8은 「규칙의 존재」로 1건씩 남고, GC-1~36은 그 형식**이다 |
| **개정-6** | **§9 관측 대장** | GC-24(라벨 동기화 2회)·GC-31(버전 증가 오판 2회)의 **카운터를 §9가 소유**한다고 1줄 | *"N회 누적"* 조건은 **대장이 없으면 영원히 발동하지 않는다**(§9 머리말과 같은 논리). 새 대장을 만들지 않고 기존 대장을 쓴다 |

---

## 12. 열린 의문

| # | 의문 | 처리 |
|---|---|---|
| **GV-1** | 리포가 **private인가** — 이슈 form `required`·조직 기능(Issue Types) 가용성이 여기 종속된다 | **[구현 검증] IV-27**. 확인 전까지 md 템플릿 유지(GC-18) |
| **GV-2** | 커밋 형식 검사 도구의 **실행 환경**(commitlint = Node 런타임 / JVM 대체 구현 / CI 전용 검사) | **[구현 검증] IV-28**. ★ **규칙 값(type-enum 8+1 · header 100 · subject-case)은 이 문서가 이미 정본**이므로 도구가 바뀌어도 값은 안 바뀐다 |
| **GV-3** | **Gradle에서 커밋 → 버전 자동화**가 성립하는가(release-please `java` 전략 + `extra-files`) | **[구현 검증] IV-29**. 도입 조건이 발동하기 전에는 판정하지 않는다(GC-31) |
| **GV-4** | **라벨 색 체계**에 정평 있는 근거가 있는가 | ⚠️ **미확인**(r5 미확인 공백 1 — 공식 문서는 색을 규정하지 않고, 3축 스킴 글도 색 근거를 설명하지 않는다). ★ **근거가 없으므로 규칙으로 적지 않는다** — `labels.yml`의 색은 [결정]이고 규약 대상이 아니다 |
| **GV-5** | rebase 머지(GC-13)에서 **중간 커밋 각각이 green인가**를 무엇이 보증하나 | 현재 없음 — CI는 PR 단위로만 본다. PR-2(*"의미 단위"*)가 절차로 진다. ⚠️ 커밋 단위 CI를 켜는 것은 **비용 대비 판정 미도래** — 되돌리기가 중간 커밋에서 실패한 사건이 관측되면 연다(관측 대장) |

---

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v1.6 | 2026-08-07 | GC-9b 신설 — 산출물 언어 한국어(식별자·규약 키워드 예외) |
| v1.0 | 2026-08-07 | 신설 — 리서치 8축(`reference/writing/evidence-10-git-conventions.md`) 기반 **GC-1~36**. ★ **유형 어휘 3벌 → 1벌 통일**(커밋 type = 라벨 = 릴리스 노트 축, **`perf` 추가**) · **충돌 8건 판정**(§10 — 소문자·100자·2일·PR 라벨 축·rebase 머지) · **빈 이슈 차단·이슈 필수 칸·PR 관련 이슈/검증 칸** · **라벨 3축 유지 재판정 + 파일 관리(labels.yml)** · **마일스톤 리포 범위의 한계와 Project 도입 조건** · ★ **버전 자동화 도입 조건 잠금**(Gradle 다리 부재 — v1은 수동 annotated 태그 + `release.yml` 자동 노트) · **멀티리포 = 앱 고정 + 락 + 카탈로그 발행** · 강제 수단 분포 2열(현재 리뷰 35/36 · 잠정 리뷰 20) · dev-conventions 개정 제안 6건(별지 — 미적용) · [구현 검증] **IV-27~31** 등재 |
