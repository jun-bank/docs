# 파일 규격 `CAPTURE-FILE` — 매입 파일 (매입 · 취소 레코드)

- 상태: **검토대기** (2026-08-06 신설 — `plans/2026-08-06/interface-specs/`)
- ★ **정본 = [UC-10 §7](../usecases/UC-10-capture-settlement-file.md)**(매입 레코드·파일 단위) **+ [UC-11 §7](../usecases/UC-11-refund-via-cancel-record.md)**(취소 레코드). 이 문서는 그 표면의 **물리 표현**이며, 충돌하면 **이 문서가 틀린 것이다**.
- 전제: **공통 규약 X-1~11**([README.md](README.md)) — 특히 **X-8 파일 공통**(고정길이 레코드 + H/D/T · 레코드 구분자 없음 · ★ **유형별 길이 상이 허용**)·**통화 3AN**(X-3 — 파일은 H가 1회 선언)·**PAN 취급**(X-11 — 이 파일은 PAN을 싣지 않는다). **여기서 재정의하지 않는다.**
- 채널: 매입사 → 발급사 · **배치 파일** · ★ **응답 상대가 없다**(§2) · 실시간 아님(예산 = *진척 없음* 판정 — 배치 SM §7)

> ★ **X-5 공통 헤더(50바이트)를 쓰지 않는다.** X-5는 *"전 **전문** 선두"* 의 규약이고 파일의 구조는 **X-8**이 따로 정한다(H/D/T · 파일 ID·영업일·건수 예고 · 트레일러 합계). 파일에는 `전문길이`·`상관 식별자` 헤더가 없으며, 그 자리를 **파일 헤더 레코드**와 **레코드 순번**이 대신한다(§4 멱등).
>
> ★ **한 파일에 매입 레코드와 취소 레코드가 섞여 온다**(BR-33) — 유형으로 갈리며 처리 경로가 다르다(매입 → UC-10 E2 / 취소 → UC-11 E4). 둘 다 `(fileId, recordId)` 멱등 대상이다(BR-23 · HS8).

---

## 1. 레코드 레이아웃

**레코드 길이 = 유형별 고정** — **H 64바이트 · D 61바이트 · T 67바이트**(2026-08-06 **R-12** — X-8 개정으로 **동일 길이 강제가 해제**됐다). 파서는 **선두 1AN 레코드 구분으로 유형을 판정**한 뒤 그 유형의 고정 길이만큼 읽는다 — 구분자 없는 파싱의 전제는 *"모두 같은 길이"* 가 아니라 *"유형마다 길이가 하나"* 다.

### 1.1 헤더 레코드 (H) — 파일당 1건, 선두 · **64바이트**

| # | 필드 | 오프셋 | 길이 | 타입 | 필수 | 의미 | 정본 연결 | ISO 8583 참조 |
|---|---|---|---|---|---|---|---|---|
| 1 | 레코드 구분 | 0 | 1 | AN | ✓ | `H` 고정 | X-8 | — |
| 2 | **통화** | 1 | 3 | AN | ✓ | `KRW` 고정 — ★ **본문 선두**(레코드 구분 다음 = 전문의 공통 헤더 다음과 같은 자리. X-3 개정 · R-01). ★ **파일 전체에 대해 H가 1회 선언**하며 D·T는 반복하지 않는다 | **BR-07** · X-3 | DE 49 (an3) |
| 3 | **파일 ID** | 4 | 20 | AN | ✓ | `fileId` — ★ **발신 기관 스코프에서 유일**(배치 INV-1 · R-06 — 필드 6과 짝). 재수신은 최초 배치를 반환한다(BF7) | **BR-23** · 배치 INV-1 · UC-10 §7 요청 | — |
| 4 | **영업일** | 24 | 8 | N | ✓ | `YYYYMMDD` — ★ **귀속 영업일 = 파일이 표기한 영업일**이며 **처리 시각이 아니다** | **BR-14**(Phase 3 M-9) · BR-25 · UC-10·11 §7 요청 | DE 15 (n4 MMDD — 길이 상이) |
| 5 | **데이터 레코드 건수** | 32 | 8 | N | ✓ | `totalRecords` — ★ **INV-3 건수 판정의 입력이자 정본**(H·T 제외, D 레코드 수. T의 같은 값은 **대조용 사본**이다 — §1.4). 못 읽으면 `RECEIVED`에서 **거부 + 재전송 요청**(B-8). ★ **결합 불변식 — 자리(8N)가 곧 상한이 아니다**: 파일당 실질 최대 건수 = **[미확정 — 파일 분할 규약과 함께 대외 협의]** 이며, **T 합계 15N이 담보하는 범위 이내여야 한다**(개별 금액 12N × 건수가 15N을 넘는 파일은 **규격 위반**이며 §1.4 수신 무결성 검사에서 **거부**된다 — 통과시키면 합계가 조용히 넘쳐 §1.4의 오버플로 차단이 무력화된다) | **배치 INV-3** · BF1 · UC-10 §4-P · §1.4(R-12 합계 자리) | — |
| 6 | 기관 코드 | 40 | 8 | AN | ✓ | 발신 매입사 식별 — ★ **파일 ID 유일성의 스코프 축**(R-06) | X-6 (**코드 체계 [미확정]**) | DE 32 |
| 7 | 파일 생성일시 | 48 | 14 | N | ✓ | `YYYYMMDDHHMMSS` — 발신측 시각(참고용). ★ **귀속 영업일은 필드 4다** | X-4 동형 · BR-14 | DE 7 (형식 상이) |
| 8 | 파일 버전 | 62 | 2 | N | ✓ | `01` — 의미 변경 = **새 파일 구분값**(X-5③·S-6a와 동형). 구조 필드이며 의미 필드가 아니다 | X-5③ 동형 · S-6a | — |

### 1.2 데이터 레코드 (D) — 공통 레이아웃 · **61바이트**

**매입 레코드와 취소 레코드의 레이아웃은 같다.** 갈리는 것은 **필드 3(유형)의 값**과 **필드 6(금액)의 의미**뿐이다(§1.3).

| # | 필드 | 오프셋 | 길이 | 타입 | 필수 | 의미 | 정본 연결 | ISO 8583 참조 |
|---|---|---|---|---|---|---|---|---|
| 1 | 레코드 구분 | 0 | 1 | AN | ✓ | `D` 고정 | X-8 | — |
| 2 | **레코드 순번** | 1 | 8 | N | ✓ | `recordId` — 파일 내 유일. ★ `(fileId, recordId)`가 멱등 키다(§4) | **BR-23** · UC-10·11 §7 | — |
| 3 | **레코드 유형** | 9 | 8 | AN | ✓ | **`CAPTURE`**(매입) / **`CANCEL`**(취소·환불). ★ 값은 **정본의 문자 상수**를 그대로 쓴다 — 물리 숫자 코드를 만들지 않는다(X-7의 취지) | **BR-33** · UC-10 §7 요청(*레코드 유형*) · UC-11 §7 요청 | DE 3 (Processing Code — 대응 아님) |
| 4 | **원거래 상관 식별자** | 17 | 20 | AN | ✓ | 원 승인 요청의 상관 식별자 — 지목 ① | **BR-17 ①** · BR-12 · UC-10·11 §7 | DE 90 (구성 상이) |
| 5 | **원거래 승인번호** | 37 | 12 | AN | ✓ | 발급사가 발번한 승인번호 — 지목 ②. ★ **매입·취소는 ①②를 모두 쓴다** | **BR-17 ②** · UC-10·11 §7 | DE 38 |
| 6 | **금액** | 49 | 12 | N | ✓ | 유형별 의미가 다르다 → §1.3. 통화 최소단위 정수 | BR-07 · X-3 · **BR-43 ①** | **DE 4** (n12) |

> ★ **더 싣지 않는다.** UC-10 §7의 레코드 단위 요청 칸은 *`recordId` · 레코드 유형 · 원거래 지목 · 매입액* 이 전부이고, UC-11 §7도 *(fileId, recordId) · 유형 · 원거래 지목 · 축소액* 이 전부다. 카드번호·가맹점·거래일시 등은 **계약에 없으므로 만들지 않는다**(필요하면 §5 F-2). ★ 특히 **카드번호(PAN)를 싣지 않는 것은 X-11의 적용 형태**다 — 파일은 **저장·재처리·감사에 오래 남는 표면**이라 평문 PAN이 들어오면 마스킹 경계 밖에 사본이 생긴다. 필요해져 F-2가 열리면 **X-11 마스킹이 먼저 적용**된다.
>
> ★ **통화를 반복하지 않는다** — 파일의 통화는 **H가 1회 선언**한다(X-3 · §1.1 필드 2). 레코드마다 실으면 같은 파일 안에서 값이 갈릴 수 있고, 그때 무엇이 정본인지 규격이 답할 수 없다.

### 1.3 데이터 레코드 유형별 의미

| 유형 값 | 무엇이 실리나 (필드 6) | 처리 경로 | 정본 |
|---|---|---|---|
| **`CAPTURE`** (매입) | **매입액** — 청산 요청 금액. 자동 반영 3조건: ① 매칭되는 선행 승인 ② 그 승인의 **첫 매입** ③ `매입액 ≤ 승인액 − 누적취소액` | **UC-10** · `Authorization.capture`(T8 / 만료 건 T9) · `Account.capture` · 부족분 `Receivable.incur` — **E2 레코드 단위 커밋** | **BR-16** · BR-18·20·24 |
| **`CAPTURE`** — ★ **부분 매입** | ★ **별도 유형이 아니다.** `매입액 < 승인액`인 매입 레코드일 뿐이며 유형·레이아웃이 같다. 반영 시 **홀딩은 전액 해제**되고(미매입 잔여분 포함 — BR-18) 당일 매입이면 미매입분만큼 두 층 한도가 복원된다 | 〃 | **BR-16 ③**(부분 매입 허용) · **BR-18** · BR-24 · DC-003 |
| **`CANCEL`** (취소·환불) | ★ **축소액** — *"거래를 얼마로 줄인다"* 이며 **매입액 기준**이다. ★ **반환액이 아니다** — 반환액·소멸액·회수액은 **우리가 BR-43 ① 산식으로 계산**한다(규격은 산식을 복사하지 않는다 — U-5) | **UC-11** · `Authorization.refund`(T10 전액 / T11 부분 / T16 `SETTLED`) · `Receivable.writeOff` · `Account.refund` — **E4 한 커밋** | **BR-33** · **BR-43 ①②** · INV-7 |
| **`CANCEL`** — 미매입 원거래 | 금지가 아니라 **`void`로 강등**된다(반환할 자금이 없다) — 강등은 **파일 경로 한정**이다 | 승인 SM §4.3 · UC-11 §4-C(F7) → UC-08의 조작 | **F7** · §4.3(v0.4) |
| **`CANCEL`** — `SETTLED` 원거래 | 원거래 **불변**(상태·`settledBusinessDate`) + **다음 영업일 순액의 차감분** | 승인 T16 · C4 정산 `refundTotal` | **F16** · **BR-43 ②** · BR-47 |

> ★ **실시간 취소 전문이 거절된 건이 이 유형으로 들어온다** — 매입된 원거래의 `CANCEL-REQ`는 `AUTH_ALREADY_CAPTURED`로 거절되고 매입사가 **파일 취소 레코드로 재접수**한다(B-3 — 매입 배치가 **E4 참여자**라 파일 없이 환불 커밋이 성립하지 않는다). → [cancel-message.md](cancel-message.md)

### 1.4 트레일러 레코드 (T) — 파일당 1건, 말미 · **67바이트**

| # | 필드 | 오프셋 | 길이 | 타입 | 필수 | 의미 | 정본 연결 | ISO 8583 참조 |
|---|---|---|---|---|---|---|---|---|
| 1 | 레코드 구분 | 0 | 1 | AN | ✓ | `T` 고정 | X-8 | — |
| 2 | **데이터 레코드 건수** | 1 | 8 | N | ✓ | 실제 D 레코드 수 — ★ **헤더 필드 5와 일치해야 한다**(정본은 **헤더**이고 이 값은 **대조용 사본**이다). 불일치 = 조용한 절단 신호 | **배치 INV-3** · BF1 · X-8 | — |
| 3 | **매입 금액 합계** | 9 | **15** | N | ✓ | `CAPTURE` 레코드 금액의 합 — ★ **원소(12N)보다 넓은 자리**(R-12) | X-8(금액 합계) · BR-41 | DE 4 계열 |
| 4 | **취소 금액 합계** | 24 | **15** | N | ✓ | `CANCEL` 레코드 금액의 합 — 〃 | 〃 | DE 4 계열 |
| 5 | 예비 (공백) | 39 | 28 | AN | — | **전부 공백**(X-2). 의미 없음 — 유형별 길이가 허용된 뒤로는 정렬 목적이 사라졌고 **자리만 존치**한다(제거 여부 = §5 **F-5** 대외 확정 시) | X-8 | — |

> ★ **합계를 유형별로 나누는 근거**: X-8은 *"금액 합계"* 라고만 정하는데 유형이 둘이므로 하나로 합치면 매입과 취소가 **상계**된다. *"합계로 대조하지 않는다 — **상계가 불일치를 은폐한다**"*(BR-41)가 그대로 적용되는 자리다.
>
> ★ **합계 자리가 15N인 근거**(2026-08-06 **R-12**): 레코드 금액은 12N인데 **합계를 원소와 같은 12N으로 두면** 건수가 늘었을 때 합계만 **조용히 넘친다**(넘친 값은 형식상 정상이라 파서가 잡지 못하고, 아래 무결성 검사가 *"불일치"* 로만 보여 원인이 절단인지 오버플로인지 갈리지 않는다). 15N은 12N 원소를 **1000건 규모까지 자리 걱정 없이** 담는다 — 합계 자리는 원소 자리보다 넓어야 한다.
>
> ★ **이 레코드의 소비자 = 수신 무결성 검사**(2026-08-06 **R-11** — B-8 확장): 수신측은 배치를 시작하기 전에 ⑴ **H 필드 5 = T 필드 2**(건수) ⑵ **D 레코드를 실제로 재계산한 유형별 합계 = T 필드 3·4** 를 대조한다. **하나라도 어긋나면 `RECEIVED`에서 거부하고 재전송을 요청**하며 **배치를 시작하지 않는다**(파일 손상 — §2). 통과한 뒤에는 **H의 `totalRecords`가 INV-3의 정본**이다.
>
> ⚠️ **BR-41과 충돌하지 않는다.** BR-41의 *"합계로 대조하지 않는다"* 는 **대사 층**의 규칙 — 우리 원장과 상대 데이터를 맞출 때 합계만 보면 상계가 불일치를 숨긴다는 뜻이다. 여기는 **전송 무결성 층**으로, *"방금 받은 이 파일이 보낸 그대로인가"* 를 파일 자신이 선언한 값과 대조할 뿐이며 **대사를 대체하지 않는다**. 그래서 합계를 **유형별로 분리**해 두 층 모두에서 상계가 생기지 않게 한다.

---

## 2. 응답 — ★ **없다**

**응답 전문이 없다.** 파일 채널에는 **거절할 상대가 접속해 있지 않다**(UC-10 §6 · 승인 SM §4 머리말). 성공도 실패도 **응답이 아니라 상태로 표현된다**:

| 결과 | 표현 | 정본 |
|---|---|---|
| 레코드 성공 | `processedRecords` 등재 (**E2 / E4 레코드 단위 커밋**) | B4 · UC-10 §7 응답 칸 |
| 레코드 실패 | ★ **격리(`isolatedRecords`) + 불일치 적재**(`Discrepancy.recordOrTouch`) | B5 · §3 |
| 파일 완료 | `COMPLETED` — ★ **건수 판정** `|처리| + |격리| = totalRecords` | **배치 INV-3** · B7 |
| 파일 중단 | `INTERRUPTED` — **진행분 보존**, 재개 시 두 집합에 있는 레코드는 건너뛴다 | B6·B3 · BF8 |
| 파일 손상(건수 미독 · ★ **H·T 대조 불일치**) | `RECEIVED`에서 **거부 + 재전송 요청** — 배치를 시작하지 않는다. ★ 대조 항목 = **건수(H 필드 5 = T 필드 2)** + **유형별 합계 재계산 = T 필드 3·4**(2026-08-06 **R-11** — B-8 확장 · §1.4) | **B-8**(UC10-2) · UC-10 §4-P |

> ★ **L7 해당 없음** — 격리 사유 구분은 **운영자 화면용**이며 매입사에게 응답으로 새지 않는다(UC-10 §6 · UC-11 §6).

---

## 3. 응답 코드 매핑 — ★ **없음 (격리 + 불일치가 대신한다)**

> **매입사에게 가는 응답 코드가 아니다**(UC-10·11 §7 오류 표 머리). 아래 두 표는 **내부 처리·운영자 화면·감사**의 표면이다.

### 3.1 레코드 금지 → 격리 + 불일치 유형 (정본 = 승인 SM §4.2 · `capture-batch.md` §5)

| 사유 | 유형 | 불일치 | 정본 |
|---|---|---|---|
| 선행 승인 없음 | `CAPTURE` | **M2** 미승인 매입 | BR-27 · UC-10 §4-F |
| 금액 초과 (`매입액 > 승인액 − 누적취소액`) | `CAPTURE` | **M4** 금액 초과 매입 — **상태는 정상이고 금액이 금지를 만든다** | **F15** · 승인 INV-3 · BR-16 ③ |
| 이미 매입된 승인 (후속 매입) | `CAPTURE` | **M7** — ★ **금액과 무관**하며 부분 매입 후 잔여분도 같다 | **F10·F11** · BR-32 |
| 무효 승인 (`DECLINED`·`VOIDED`·`REFUNDED`) | `CAPTURE` | **M8** — ★ **무효가 된 경로는 구분하지 않는다** | **F12·F13·F14** · BR-51 |
| **기한 초과** (승인일 + **10일**, CNP) | `CAPTURE` | **M19** — 격리 **즉시 적재**(대사 탐지 아님). M8과 다르다: 승인이 무효인 게 아니라 **매입 자격의 기한**이 지났다 | **BR-19**(Visa AI13522) · UC10-1 |
| 상한 초과 (`refundedTotal + 요청액 > capturedAmount`) | `CANCEL` | **M5** 취소 불일치 | **F18** · INV-7 · BR-43 ① |
| 원거래 매칭 실패 | `CANCEL` | **M5** — ★ **M5가 흡수한다**(B-13 · UC11-3. 매입 레코드의 M2·M8과 대칭이 아니다 — 조치가 갈리지 않는다) | BR-12 · UC-11 §4-M |
| ★ **무효 원거래**(`DECLINED`·`VOIDED`·`REFUNDED`)를 지목한 취소 레코드 | `CANCEL` | **M5** — ★ **M5가 흡수한다**(2026-08-06 **R-13** · **B-13 선례**: 취소 레코드 쪽은 사유가 갈려도 **조치가 갈리지 않는다** — 매입사에 재접수를 요청하는 한 갈래다. 매입 레코드였다면 **M8**로 갈렸을 상황이지만 그 대칭을 만들지 않는다) | 승인 SM **F22** · BR-51 · UC-11 §4-D |
| **원승인 보류 중**(`frozen = true`) | `CAPTURE` | ★ **없음 — 불일치를 만들지 않는다**(우리가 의도한 지연). 대사에서 **M1로도 잡지 않는다.** 보류 해제 후 **재처리 배치**가 반영·승격한다 | **F17** · BR-50 · BR-28 |

> ★ **보류 격리 중 원승인이 종료되면 사유가 M8로 자동 재분류**된다(`reclassifyIsolated` — B9 · UC14-2). 이후 승격은 BF12에 막힌다.

### 3.2 배치 조작 거절 코드 (정본 = 배치 SM §4 처리 열 — 운영자 화면·감사에 남는다)

| 코드 | 상황 | 정본 |
|---|---|---|
| `BATCH_DUPLICATE_FILE` | 같은 `fileId` 재수신 — **최초 배치를 반환**한다 | **BF7** · 배치 INV-1 · UC-10 §4-A |
| `BATCH_INCOMPLETE` | 건수 불일치 상태의 `complete` — ★ **조용한 절단 차단**(*파서가 뒤쪽 1000건을 버려도 "에러 없이 돌았다"가 된다*) | **BF1** · INV-3 · UC-10 §4-E |
| `BATCH_NOT_STARTED` | `RECEIVED` 상태에서 `markProcessed`·`isolate` | **BF5** |
| `BATCH_NOT_PROCESSING` | 처리 중이 아닌데 레코드 조작·중단·완료 | **BF4·BF6·BF9** |
| `BATCH_COMPLETED` | 완료된 배치에 레코드 **추가**·`start` (승격은 **이동**이라 해당 없다) | **BF2·BF3** · INV-4 · UC-10 §4-Q |
| `BATCH_NOT_COMPLETED` | 완료 전 배치에서 `promoteIsolated` | **BF11** |
| `PROMOTE_NOT_ALLOWED` | **보류 외** 사유 격리의 승격 — ★ *반영 안 된 레코드가 "처리됨"으로 표시되고 건수 판정만 통과하는* **조용한 실패의 새 창구** | **BF12** · INV-5 · UC-10 §4-R (승격 경로 = UC-14) |
| — (오류 아님) | **이미 처리·격리된 레코드** → ★ **조용히 건너뛴다**(재개할 때마다 일어나는 정상 경로) | **BF8** · UC-10 §4-B |

---

## 4. 멱등

| 항목 | 내용 |
|---|---|
| **파일** | ★ **`(기관, fileId)` 유일** — 헤더 레코드의 필드 6 + 필드 3. 배치 **INV-1**(2026-08-06 **R-06** 개정 — 매입사마다 자기 파일 채번을 쓰므로 전역 유일을 가정하면 **다른 매입사의 같은 `fileId`가 중복으로 판정되어 파일 하나가 통째로 반영되지 않는다**). 재수신 = **최초 배치를 반환**(BF7 · K-1 시스템 채널 규약) |
| **레코드** | ★ **`(기관, fileId, recordId)`** — 파일 키 + 데이터 레코드 필드 2. **BR-23**이 정본이며 **취소 레코드에도 적용**된다(BR-33 HS8 — 적용하지 않으면 재처리 시 **환불이 두 번 반영되어 고객에게 두 배**가 간다) |
| **정본 소유자** | **`CaptureBatch`**(`processedRecords` · `isolatedRecords`) — 두 집합은 **상호배타**(INV-2)이며 **어느 쪽에든 있으면 건너뛴다**(BF8) |
| **의미** | ★ **격리에 있다 = 미반영 · 처리에 있다 = 반영됨.** 승격(`promoteIsolated`)이 그 경계를 **원자적으로** 넘기므로 두 번 지시해도, 파일이 재전송돼도 걸러진다 |
| **재처리 배치** | 보류 격리의 재처리는 ★ **원 `(기관, fileId, recordId)`를 승계**한다(BR-50 · B8) — **새 키를 발급하면 이중 출금**이다 |
| **커밋 경계** | `markProcessed`가 자금 반영과 **같은 커밋**(매입 = **E2** / 환불 = **E4**). 갈리면 반영만 남고 배치는 "미반영"이라 **재시도가 같은 레코드를 또 출금**한다 |
| **건수 판정** | 멱등의 짝이 **INV-3**이다 — 격리 쪽 중복 방지가 없으면 `|처리| + |격리|`를 인위적으로 채워 **절단된 파일이 완료로 통과**한다 |

---

## 5. 열린 항목

| # | 항목 | 근거·소관 |
|---|---|---|
| **F-1** | ★ **파일 포맷·전송 프로토콜·도착 통지 채널** — **[미확정 — 외부 규격 필요 · 백로그]**. 매입사 규격이 입력이라 단독으로 정할 수 없다. ★ 본 문서는 **레코드 레이아웃**(X-8의 물리 표현)만 정하며 **전송·통지 계층은 비어 있다** | **B-7 · UC10-4** |
| F-2 | **레코드 부가 필드**(카드번호·가맹점·거래일시 등)의 필요 여부 — 계약에 없어 싣지 않았다. 매입사 규격이 요구하면 그때 **정본에 먼저 올린다** | UC-10·11 §7 요청 칸 |
| F-3 | **레코드 유형 값**(`CAPTURE`/`CANCEL`) 및 **파일 ID·레코드 순번 형식** — 문자 상수는 정본에서 왔으나 **대외 합의 대상** | BR-33 · BR-23 · F-1에 의존 |
| F-4 | **기관 코드 체계**(8AN 값 집합) | X-6 — 외부 규격 의존 |
| F-5 | **레코드 길이 H 64 · D 61 · T 67바이트와 필드 길이의 대외 확정** — ★ 유형별 상이(X-8 개정 · R-12)를 매입사 파서가 수용하는지, **T의 예비 28바이트를 존치할지**(정렬 목적이 사라졌다 — §1.4)를 함께 확정한다 | F-1·F-2·F-3에 의존 |
| F-6 | **진척 없음 판정 시간 · 재개 시도 상한** — 파일 채널의 예산 상한 | **BFS3 · BFS2** · 배치 SM §7 |
| F-7 | **환불 배치 주기** — 매입 파일과 같은 주기인지 | **UC11-1** · BR-43 |
| F-8 | **`SETTLED` 환불 차감 거래의 소유·감사 대상 식별자** — **이월**(Phase 3 **AU7** 설계 판정 선행) | **B-14 · UC11-2** |
| F-9 | **`processedRecords` 대용량 저장 방식** — 집합을 통째로 들면 직렬화 부담 | **CB1**(Phase 3 설계) |

---

## 6. 자기검증

| 레코드 | 필드 시작 오프셋 (연속) | 길이 합 | 선언 길이 | 일치 |
|---|---|---|---|---|
| **H** | 0 → 1 → 4 → 24 → 32 → 40 → 48 → 62 → **64** | 1+3+20+8+8+8+14+2 = **64** | 64 | ✓ |
| **D** | 0 → 1 → 9 → 17 → 37 → 49 → **61** | 1+8+8+20+12+12 = **61** | 61 | ✓ |
| **T** | 0 → 1 → 9 → 24 → 39 → **67** | 1+8+15+15+28 = **67** | 67 | ✓ |

- 오프셋 불연속·중첩: **없음** (각 행의 시작 = 직전 행의 시작 + 길이)
- ★ **레코드 길이가 유형별로 다르다 — H 64 · D 61 · T 67**(X-8 개정 · R-12). 파싱 전제 = **선두 1AN 레코드 구분으로 유형 판정 → 그 유형의 고정 길이만큼 읽기** ✓ (구 *"전부 61바이트 동일"* 을 대체한다)
- 통화: **H 1회 선언**(오프셋 1, 3AN `KRW`) · D·T에 통화 필드 **없음** — R-01 ✓
- 파일 총 길이 = `64 + (totalRecords × 61) + 67` 바이트 (H 1 + D `totalRecords` + T 1) — 파일 크기로도 건수를 검산할 수 있다(INV-3의 보조 검증. 유형별 길이가 달라도 H·T가 각 1건이라 역산이 성립한다)
- 수신 무결성 검사(R-11): **H 필드 5 = T 필드 2**(건수) · **D 재계산 유형별 합계 = T 필드 3·4** — 어긋나면 `RECEIVED` 거부 + 재전송 ✓
- 응답 코드: **없음**(§3 — 격리 사유 **9행**(불일치 M2·M4·M5×3·M7·M8·M19 + 보류=불일치 없음) + 배치 거절 코드 **7종** + 무음 건너뜀 1이 그 자리를 대신한다)

## 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| v0.3 | 2026-08-06 | **재점검 정정 — 항목 5**: H 건수(8N) 필드에 ★ **결합 불변식** 명시 — 파일당 실질 최대 건수는 **[미확정 — 파일 분할 규약과 함께 대외 협의]** 이며 **T 합계 15N이 담보하는 범위 이내**여야 한다(12N × 건수가 15N을 넘는 파일 = 규격 위반, 수신 무결성 검사에서 거부). 자리(8N)를 상한으로 읽으면 R-12의 오버플로 차단이 무력화된다 |
| v0.2 | 2026-08-06 | **듀얼 리뷰 반영 — R-01·06·08·11·12·13**: H에 통화 3AN 신설(레코드 구분 다음 — 파일당 1회 선언) · ★ **레코드 길이 유형별 상이**(H **64** · D 61 · T **67** — X-8 개정) · **T 합계 15N×2로 확장**(원소 12N보다 넓은 자리 — 조용한 오버플로 차단) · ★ **트레일러 소비자 명시**(수신 무결성 검사 — H·T 건수 + 유형별 합계 재계산 불일치 = `RECEIVED` 거부·재전송. BR-41 대사 층과 다른 층임을 명시) · **F22 무효 원거래 취소 레코드 = M5 흡수** 행 추가 · 멱등 키 **기관 축**(`(기관, fileId)`·`(기관, fileId, recordId)`) · PAN 비적재를 X-11 형태로 명시 |
| v0.1 | 2026-08-06 | 신설 — UC-10·11 §7의 물리 표현(고정길이 H/D/T **61바이트 동일**). X-5 헤더 비적용 판정 · 유형 = `CAPTURE`/`CANCEL`(부분 매입은 별도 유형이 아님 — BR-16 ③) · 트레일러 합계 **유형별 분리**(BR-41 상계 은폐 방지) · 응답 없음(격리 + 불일치) · 멱등 = `(fileId, recordId)` · 열린 항목 9건(★ F-1 = UC10-4 백로그 잔존) |
